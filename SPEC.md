# Anajak ERP — ข้อกำหนดระบบ

ไฟล์นี้กำหนดพฤติกรรมที่ต้องรักษาและเกณฑ์ตรวจรับ ไม่ใช่คำรับรองว่าทุกข้อผ่านบน Production แล้ว ผลตรวจ งานที่ยังขาด และเงื่อนไขปล่อยอยู่ใน [ROADMAP](ROADMAP.md); ภาพรวมและวิธีรันอยู่ใน [README](README.md)

## เงินและเอกสาร

- ช่องเงินเก็บ `Decimal(12,2)` คำนวณ Decimal และปัดสองตำแหน่งแบบ half-up ผ่าน `money.ts`; แปลง number เฉพาะขอบระบบรวมถึง aggregate
- ยอดตาม `src/server/services/pricing.ts`: ราคาเสื้อ/พิมพ์/ส่วนเสริม หักส่วนลดก่อนภาษี และ `totalAmount = max(0, subtotalBeforeTax + taxAmount)`; `platformFee` ไม่เข้ายอดหรือฐาน VAT ส่วนลดรวมติดลบ/เกินยอดต้องถูกปฏิเสธ Preview ฝั่ง client ต้องตรง server; รักษาค่าเริ่มต้นภาษีและข้อยกเว้นช่องทาง marketplace ตามสูตรเดิม
- เลข `PREFIX-YYMM-NNNN` อิงเดือน Asia/Bangkok ผ่าน `nextDocumentNumber()` ใน transaction ที่สร้างเอกสาร การนำเข้าเอกสารเก่าต้องรักษาลำดับเลข ไม่สุ่มเลขใหม่
- การออกบิลและแก้ยอดออเดอร์ล็อกแถวก่อนอ่านและคำนวณใน transaction เดียว รักษาเพดาน `billedFloor` ไม่เรียกเก็บเกินยอดและไม่ลดออเดอร์ให้ต่ำกว่าบิลที่ออกแล้ว **ข้อมูลเก่าที่เพี้ยนอยู่ยังเพิ่มยอดเข้าหา floor ได้** ตาม `assertOrderTotalCoversBilled`; ไม่สร้างด่านที่ปิดทางซ่อมนี้
- ใบเสร็จผูกงวดรับเงินด้วย `forPaymentId` แบบไม่ซ้ำ ยอดเคลียร์งวดรวมเงินสดกับ WHT และวันที่เงินเข้าจริง รักษาการออกเอกสารทุกงวดรวมมัดจำ บิล/ใบเสร็จต้องไม่ถูกนับซ้ำในการปิดงาน; เครดิตเทอมปิดงานได้เมื่อวางบิลครบโดยไม่บังคับเก็บเงินครบก่อน
- ใบกำกับ/ใบวางบิลยกเลิกพร้อมเหตุผลแล้วออกใหม่ ไม่ลบประวัติหรือ void ซ้ำ CN/DN อ้างใบเดิมและเหตุผล กระทบยอดค้างจริง; FK เอกสารสำคัญ Restrict การลบ
- คู่สัญญาบนเอกสารเป็น snapshot ณ วันออกผ่าน `document-party.ts` ยกเว้นใบเสนอราคาร่าง ห้ามเปลี่ยนที่อยู่/ชื่อบนเอกสารเก่าตามข้อมูลลูกค้าปัจจุบัน
- เงินสดและ `whtAmount` เก็บแยกและเข้าทะเบียน 50ทวิ REC ใช้รับเงินขายสดที่ไม่มีใบเรียกเก็บ; CN รับเงินไม่ได้ รักษาอัตรา/ฐานหักตามกฎเดิมจนมีงานเปลี่ยนที่อนุมัติ
- เอกสารพิมพ์ A4, light-only และอ่านขาวดำได้ คงต้นฉบับ/สำเนา ลายน้ำ void ข้อความที่กำหนดสำหรับเอกสารภาษี ยอด และลำดับหน้า; รายงานภาษีขาย export CSV/รูปแบบส่งต่อ PEAK ได้ การตรวจ template กับนักบัญชีเป็น gate แยก ไม่ใช้ unit tests รับรองแทน

**เกณฑ์ทดสอบ:** ครึ่งสตางค์ปัดถูก ราคา client/server เท่ากัน เลขข้ามเดือนไทยไม่ผิด งวด/บิล void ไม่ถูกบวกซ้ำ CN ไม่เปิดช่องรับเงินเกิน และการซ่อมยอดเก่าข้างต้นยังทำได้ ดู `money.test.ts`, `pricing.test.ts`, `document-number.test.ts`, `payment-plan.test.ts` ใน `src/server/services/` และ `src/lib/pricing.test.ts`

## สิทธิ์ ไฟล์ และข้อมูล

- Session ต้องมีทั้ง Supabase auth และ `User` ที่ active; auth ล่ม/หา user ไม่พบต้องปฏิเสธ ไม่ตกกลับเป็น OWNER รวม `/api/files` และ route ใหม่
- สิทธิ์จาก `src/lib/permissions.ts` ใช้ role เป็นค่าเริ่มต้นและรับ override รายคนตามรายการที่อนุญาต มีผลที่ server, UI, print และ MCP; `manage_users` ให้เฉพาะ OWNER และลด/ปิด OWNER active คนสุดท้ายไม่ได้
- งานผลิตใช้สิทธิ์ตามคำสั่ง: `manage_production` สำหรับจัดการผลิต, การตัดสิน QC ร้านนอกต้อง `supervise_operations` ร่วมด้วย, ยืนยันพร้อมส่งใช้ `update_order_status_production`, ใบส่ง/tracking ใช้ `ship_orders`; server ต้องตรวจแม้ UI ซ่อนปุ่มแล้ว
- หน้าทั่วไปตัดเงินตาม `see_order_money`/`see_finance` และสิทธิ์จริง ไม่เหมารวมจาก role; DTO หน้างาน/สถานี/TV ไม่มีช่องเงินโดยโครงสร้างแม้ผู้เปิดเป็น OWNER ไม่ขยายขั้นผลิต/ร้านนอกให้คิดต้นทุนต่องาน
- `src/proxy.ts` refresh session และใช้ทะเบียน `src/lib/public-routes.ts` สำหรับทาง public; `/api/*` ไม่กลายเป็นหน้า redirect login แทน response ของ API
- Bucket `designs` เป็น private อ่านผ่าน `/api/files` ที่ตรวจ path/bucket และออก signed URL อายุจำกัด; upload ไม่ทับไฟล์ (`upsert: false`) พนักงานต้อง active และผู้ถือ token เปิดเฉพาะไฟล์ใน allowlist ของ token ที่ยังใช้ได้ รวมไฟล์ทุกภาพที่หน้าอนุมัติ/ร้านนอกแสดง
- Service role ไม่ถึง client; token/ข้อมูลลับไม่ลง logs หรือ public repository; MCP เก็บ key แบบ sha256 และทุก tool เคารพสิทธิ์ผู้ถือ ขอบเขต tool จริงอยู่ `src/lib/mcp/tools/`
- Cron ที่ประกาศใน `vercel.json` ต้องตรวจ `CRON_SECRET` แบบ fail-closed; security headers อยู่ `next.config.ts`; backup JSON จำกัด OWNER และไม่อ้างว่ารวม Storage files

**เกณฑ์ทดสอบ:** ไม่มี session, inactive User, สิทธิ์ไม่พอ, token หมดอายุ/ไฟล์ข้ามใบ/encoded path traversal ต้องไม่ผ่าน; OWNER คนสุดท้ายและสิทธิ์ห้าม override ต้องได้รับการป้องกัน จอหน้างานไม่รั่วเงิน ดู `src/proxy.test.ts`, `src/lib/permissions.test.ts`, `outsource-share.test.ts`, `factory-board.test.ts`; การมี test เหล่านี้ไม่แทนการตรวจ console/route ที่ยังค้างใน ROADMAP §D

## ขาย แบบ และลูกหนี้

- ออเดอร์มี READY_MADE/CUSTOM และแหล่งเสื้อ FROM_STOCK/CUSTOM_MADE/CUSTOMER_PROVIDED ใช้ฟอร์มหลักเดียวสร้าง/แก้ แยกผู้ติดต่อกับที่อยู่ส่ง อ้างงานด้วยเลขที่และลูกค้า; รายละเอียดงานอยู่ `OrderItem.description` ไม่คืนช่อง `orders.title`/`quotations.title`
- ยืนยันออเดอร์ตรวจวงเงินลูกค้า และจอง Stock เมื่อมีสินค้าจากคลัง จองไม่สำเร็จต้องแสดงเหตุและ retry ได้; ระหว่าง pending/error ห้ามถือว่าพร้อมผลิต
- ใบเสนอราคาที่ ACCEPTED และยังไม่หมดอายุแปลงเป็นออเดอร์ได้แบบกันซ้ำ; ลูกค้ายอมรับผ่าน token ของใบตนเอง
- หน้าอนุมัติแบบ สถานะ อัปโหลด และใบงานร้านนอกไม่ต้องมีบัญชีพนักงาน แต่ token มีขอบเขต/วันหมดอายุ หน้า status ไม่เผยราคา ต้นทุน หรือสถานะภายใน; path อัปโหลดมาจาก server ไม่เชื่อตำแหน่งที่ client ส่งเอง
- แบบหนึ่งเวอร์ชันมีหลายภาพและลูกค้าอนุมัติทั้งชุด แก้แบบที่แท็บม็อกอัพและไฟล์ของออเดอร์ที่เดียว จออื่นอ่านผ่าน component กลาง; เวอร์ชันเก่าที่ `files` ว่างยังอ่าน `fileUrl` ได้ ไฟล์ที่แสดงตรงไม่ได้ต้องมีภาพตัวอย่าง และรูปที่ต้องใช้ token ต้องพก token ครบ
- การรับเงินเชื่อมบิล/ทะเบียนหัก ณ ที่จ่าย ใบวางบิลรวม ลูกหนี้ aging และติดตามเกินกำหนด; CRM เก็บลูกค้า/การติดต่อ/การค้นหา สินค้าใช้ soft-delete เพื่อรักษาประวัติ

## การผลิตแบบเดิม

### ความพร้อมและการควบคุมงาน

- `transitionOrder()` เป็นทางเปลี่ยน `internalStatus` หลังสร้าง: ตรวจเส้นทางสถานะ ใช้ optimistic lock และบันทึก `OrderRevision`; retry ที่สถานะเดิมไม่สร้างเหตุการณ์ซ้ำ
- Readiness คำนวณที่ server จากเงินตามเทอม แบบ และวัสดุ ไม่ใช่บังคับทุกงานจ่ายครบ: เครดิต/COD/ไม่ตั้งเทอมหรือยอดศูนย์มีข้อยกเว้นตาม `production-readiness.ts`; งานไม่มีพิมพ์ไม่รอแบบ งานพ้นเฟสแบบไม่ถูกย้อนกั้น และ CUSTOM_MADE ไม่รอรับเสื้อก่อนเปิดเพราะโรงเย็บเป็นขั้นงาน
- สินค้า FROM_STOCK ต้องจองสำเร็จ; เสื้อลูกค้าต้องตรวจรับ ความพร้อมแต่ละขั้นใช้ของดีที่ใช้ได้ ไม่รวมตำหนิเป็นยอดพร้อม
- หัวหน้าคุมจาก `/production` และใบผลิต ช่างลงมือที่ `/production/floor`; ทางสถานีเก่าคง redirect; `/factory` อ่านอย่างเดียว ตารางผลิตต่อเนื่อง ไม่แบ่งหัวตามสถานะ และจำตัวกรองใน URL
- จำนวนภาพรวมมาจากทั้งกระดานที่เกี่ยว ไม่ใช่แถวหน้าปัจจุบัน ปัญหามาจาก failed/on-hold/rejected/not-ready พร้อมเหตุจริง ไม่ตีความทุกงานเกินกำหนดเป็นติดปัญหา และไม่เพิ่มสรุปเงินบนจอผลิต
- เลือกขั้นเพื่อดูรายละเอียดไม่เท่ากับเริ่มงาน หัวใบแสดงขั้นถัดไปตาม server; ขั้นคู่มาจาก snapshot `pairWithPrevious` ไม่เดาจากลำดับหน้าจอ

### หลักฐานและจำนวน

- ขั้นที่ลงมือเองบันทึก checklist ที่ server ด้วย `ProductionStepCheck` และ key/ข้อความของข้อกำหนด; ผู้มีสิทธิ์ทำงานของตนหรือที่ยังไม่มีคนรับตามกฎหน้างาน ขั้นรับเสื้อ/พิมพ์/ร้านนอกต้องอาศัยหลักฐานจาก flow เจ้าของ ไม่ใช้ checkbox ยืนยันแทนเอกสาร
- จำนวนดี/เสียของแต่ละสินค้า/สี/ไซซ์อยู่ `OperationQuantity` ผู้ใช้ต้องกรอกจริง ไม่เติมดีอัตโนมัติ ก่อนปิดขั้น checklist ต้องครบและยอดดี+เสียต้องครบทุกแถวตามแผน; งานแก้ใช้เฉพาะยอดที่ต้องแก้ ไม่ยกทั้งออเดอร์มานับใหม่
- ย้อนขั้นต้องมีสิทธิ์หัวหน้าและออเดอร์ยัง PRODUCING; ย้อนเฉพาะขั้นจบที่ไม่มี flow เจ้าของ ไม่มีหลักฐานใบรับ/ร้านนอก/รอบพิมพ์ผูก และขั้นถัดไปยังไม่เริ่ม พร้อม audit
- ส่งเข้า QC ได้เมื่อทุกขั้นจบแล้วเท่านั้น ปุ่มส่ง QC ไม่ปิดขั้นค้างหรือแต่งยอดให้ แม้ใช้กระดาษร่วมด้วย; ใบที่ V2 เป็นเจ้าของต้องใช้คำสั่ง Manufacturing
- ใบรับเสื้อลูกค้าครบจึงปิดขั้น รับบางส่วนไม่ปิด; แก้ยอดต้องมีสิทธิ์ เหตุผล หลักฐานส่วนต่าง และ retry ไม่ซ้ำ รักษาความหมายการคืนของตำหนิ/รับทดแทน; ใบที่ V2 เป็นเจ้าของต้องปฏิเสธ writer เดิม
- การเบิก/คืนในฐานทดลองแก้ยอดสินค้า วัตถุดิบ และ audit ใน local transaction; การเรียก Stock จริงเป็น HTTP แยกระบบ ต้องรักษา idempotency และหยุดให้ตรวจเมื่อ Stock สำเร็จแต่หลักฐาน ERP ไม่ครบ ไม่อ้าง atomic transaction ข้ามระบบ รักษายอดจองของออเดอร์อื่นและไม่คืนของดี/ตำหนิสลับความหมายกัน

### DTF ร้านนอก และรีดร้อน

- รวมหลายใบในรอบพิมพ์ได้ นับเฉพาะฟิล์มดี ฟิล์มเสียต้องมีรอบพิมพ์เพิ่มก่อนปลดขั้นต่อไป
- สร้างใบส่งร้าน ส่งจริง รับหลายรอบ และตัดสิน QC เป็นคนละเหตุการณ์ ของกลับบางส่วน/เสียห้ามนับครบ; ยืนยันเฉพาะดีและเปิดส่งแก้เฉพาะคงเหลือ การตรวจรับร้านอยู่ก่อน QC สุดท้าย
- ยอดที่ส่งร้านไม่ถูกลงยอดหรือพิมพ์ซ้ำฝั่งทำเอง หัวหน้าส่ง DTF/รีด/CUSTOM ให้ร้านแทนได้เฉพาะยอดค้าง หลังปิด/ยกเลิกรอบพิมพ์ค้าง ขั้นเบิก/รับเสื้อใช้เอกสารวัตถุดิบและส่งร้านแทนไม่ได้
- รีดร้อนแบบเดิมเริ่มเมื่อฟิล์มพร้อม เตรียมเสื้อจบ และงานร้านนอกทุกสายจบตาม `evaluateHeatPressGate`; V2 มีสัญญา RETURN_QC แยกด้านล่าง

### QC แพ็ก และส่ง

- ลำดับคือ ผลิต → QC → แพ็ก → พร้อมส่ง QC กับแพ็กเป็นคนละด่าน ไม่สร้าง `PACKAGING` เป็น ProductionStep ใหม่ แต่ยังอ่านข้อมูลเก่าได้
- นับแยกสินค้า/สี/ไซซ์ ไม่ชดเชยกันข้ามสินค้า Good บางส่วนไม่ผ่าน; ของดีครบแม้มีเสียจากเสื้อเผื่อไม่สร้างงานแก้ที่ไม่มีวันจบ ผลนับและรอบคืนอยู่ใน QC ledger/`OrderRevision`
- งานแก้หนึ่งชุดต่อรอบตามที่เสีย ต้องตรวจซ้ำก่อนแพ็ก จำนวนใบส่งและการแพ็กต้องครบตามเกณฑ์ก่อนพร้อมส่ง แบ่งกล่อง/ใบส่งได้แต่ยอดไม่ซ้ำหรือเกิน
- รับคืนผูกใบส่งและจำนวนจริง เปิด QC เฉพาะที่คืน ใบทดแทนมีเพดานและรักษาประวัติเดิม; ข้อมูลเก่าที่แยก variant ไม่ได้ให้ผู้ดูแลแก้ ห้ามเดา และผล QC เก่าที่มีแต่ยอดรวมต้องนับใหม่

### หน้างานและกระดาษ

- แจ้งปัญหาส่งขั้นและเหตุผลให้ `reportStationProblem` หา work center ที่ server; ปลดผ่าน `resolveStationProblem` ช่างเห็นงานตน/ยังไม่มีผู้รับ หัวหน้าที่มี `supervise_operations` เห็นข้ามคน
- Scan/QR เปิดบริบทเท่านั้น ไม่รับงาน/เริ่ม/จบเอง ม็อกอัพเป็นข้อมูลรองพร้อมคำเตือนห้ามใช้วางตำแหน่งพิมพ์; คิว refresh ตามรอบ และ TV ต้องแสดงข้อมูลค้าง/refresh ล้มเหลวแทนทำเหมือนเป็นปัจจุบัน
- ใบพิมพ์ใช้แบบอนุมัติและ QR พก `?mockup=n`; ใบเก่ากว่าแบบล่าสุดต้องเตือนพร้อมทางพิมพ์ใหม่ รักษาตัวอ่าน marker ประวัติ `[ถือว่าผ่าน]` แม้เลิกเขียน marker ใหม่
- ยอดร่างที่ยังไม่บันทึกต้องไม่หายเงียบเมื่อ refresh ปิดแท็บ นำทางในแอป หรือ Back/Forward; ผลตรวจแต่ละเส้นทางอยู่ ROADMAP ไม่ถือว่ามี handler บางตัวแล้วครบทุกกรณี

**กรณีตรวจรับ A16 บนฐานทดลอง:** รับ 45+15 จนครบ 60, รับตำหนิ→คืนเฉพาะตำหนิ→รับทดแทน และซ่อมยอดแบบกดซ้ำ; DTF รวมสองใบ 120 ฟิล์มเสีย 3 แล้วพิมพ์เพิ่ม 3; รีดดี 58/เสีย 2 → QC ระบุ L 2 → แก้เฉพาะ L 2 → ตรวจซ้ำ; ร้านนอกส่ง 10 กลับดี 8/เสีย 2 → ส่งแก้ 2; DTF ทำดี 4/10 แล้วเครื่องเสีย → ยกเลิกรอบค้าง → ส่งร้านเฉพาะ 6; แบ่งส่ง 50/10 แล้วคืน L 2 จากใบ 10 → ตรวจและส่งทดแทน 2 โดยอีก 58 ยังอยู่กับลูกค้า

หลักฐานอัตโนมัติที่เกี่ยวอยู่ `production-readiness.test.ts`, `work-order-form.test.ts`, `qc-count.test.ts`, `qc.lifecycle.test.ts`, `packing-readiness.test.ts` ใน `src/server/services/` และ `production.send-to-qc.test.ts`, `outsource.fallback.test.ts` ใน `src/server/routers/` ต้องเปิดจอและลองบทบาทจริงประกอบเมื่อแก้ flow

## Production V2 — สัญญาก่อนเปิดใช้

การเปิด Manufacturing V2 ต้องผ่านด่านใน ROADMAP §B; ข้อกำหนดการปกป้องข้อมูลตาม ownership ต้องทำงานก่อน cutover และแม้ปิด flag หัวข้อนี้ไม่ใช่สถานะการเปิดใช้ของเว็บจริง

- `Production` เป็น Manufacturing Order, `ProductionStep` เป็น Operation Job โดยคง ID/FK เดิม RoutingVersion ที่ RELEASED เปลี่ยนไม่ได้; release เก็บ snapshot ตรวจ cycle ของ dependency และมี Final Pack ปลายทางเดียว Quantity แยกสินค้า/สี/ไซซ์/จุดพิมพ์ และ `OperationEvent` append-only
- คำสั่งใช้ `commandId` + request hash + `expectedRevision` และลำดับ lock เดียว Retry เดิมคืนผลเดิม; key เดิมแต่ payload ต่างหรือ revision เก่าต้อง reject; `availableCommands`/`blockedReason` จาก server เฉพาะของดีปลดขั้น Reject ต้องมี disposition และ rework ต้องตรวจซ้ำ
- สูตรเป็นข้อมูล seed แบบ idempotent จัดการที่ `/settings/routings`; สูตรที่ใช้แล้วคัดลอกเป็นร่างใหม่ `executionMode` เลือกรายขั้นเพื่อส่งร้านได้ ขั้นวัตถุดิบยังรักษาเจ้าของหลักฐาน ของกลับร้านผ่าน RETURN_QC ก่อนขั้นที่พึ่งมัน จึงเดินขนานกับ DTF ได้
- หลังเปิด MO นิยามสินค้า/สี/ไซซ์/จุดพิมพ์และหลักฐานรับเสื้อบนออเดอร์เป็น read-only; writer เดิมต้องตรวจ ownership หลัง lock ไม่ขึ้นกับ flag อย่างเดียว
- ออเดอร์ที่มี `work_order_number`/`completion_owner_step_id` เปลี่ยนจากหน้าออเดอร์ได้เฉพาะ COMPLETED แม้ปิด flag; เมื่อ flag เปิด สถานะใน `PRODUCTION_V2_FLOW_STATUS_TARGETS` ต้องมาจาก Manufacturing/Delivery ของทุกออเดอร์
- Final Pack owner หายหรือไม่ตรงต้อง fail-closed; READY_TO_SHIP มาจาก Final Pack เมื่อทุก operation ที่เปิดใช้จบแล้ว Capacity ที่ไม่มี standard time แสดงว่ายังไม่ประเมิน ไม่ใช่ศูนย์
- ก่อนเปิดฐานจริงต้องซ้อมสูตร→สถานี→ร้านนอก→QC เสีย/งานแก้→แพ็กแยกไซซ์ และตรวจ migration บนฐานทิ้งได้ ผ่านอนุมัติ target/backup และแผนย้อนกลับ; unit tests V2 มีข้อยกเว้น flag ใน test จึงใช้ยืนยันการเปิดเว็บจริงไม่ได้

## เกณฑ์ตรวจหน้าจอ

- ส่วนที่เปลี่ยนต้องช่วยให้ผู้ใช้รู้บริบท/สถานะ ทางไปต่อ ผลของปุ่ม เหตุที่ติดขัดและทางแก้ ผลสำเร็จและขั้นถัดไป และเรียนรู้จากคำ/รูปแบบที่สม่ำเสมอ; คำช่วยหรือคำเตือนที่จำเป็นอยู่ตรงจุดใช้ ตัวอย่างไม่ปะปนกับข้อมูลที่บันทึกแล้ว รายละเอียดเสริมเปิดเพิ่มได้ด้วยคีย์บอร์ด/จอทัช ไม่พึ่ง hover อย่างเดียว
- ข้อมูล/ปุ่มใช้ service และสิทธิ์จริง Loading, error พร้อม retry, ว่าง และไม่มีสิทธิ์แยกกัน ข้อมูลยังไม่มาไม่แสดงเป็นศูนย์ และไม่ซ้อน interactive controls
- ตรวจหน้าที่เปลี่ยนบน 390/1440 px และธีมที่รองรับ ตารางเลื่อนในกรอบเอง ไม่มี overflow ทั้งหน้า; เป้ากดอย่างน้อย 44px บนมือถือ/ทัชและ 36px บน desktop มี contrast ระดับ WCAG AA และเคารพ reduced-motion; public/print ใช้ light-only
- แท็บ/ตัวกรองเก็บใน URL และนำทางกลับได้; การเปลี่ยนฟอร์มรายการสินค้าต้องตรวจหน้าดูคู่กัน; ทิศหน้าตาและ token อยู่ DESIGN ส่วนด่าน `verify:ui` ต้องยังผ่าน
- ตรวจจากภารกิจจริงและทางติดขัดที่การแก้กระทบตาม ui-guidance; งานเล็กตรวจเฉพาะผลกระทบ งานเปลี่ยน flow หลักให้ผู้ตรวจอีกคน/agent ลองโดยไม่บอกตำแหน่งปุ่ม บันทึกบทบาท สิ่งที่ลอง ผลและส่วนที่ยังไม่ตรวจใน ROADMAP; ผลจาก AI ไม่รับรองว่าผู้ใช้ใหม่เรียนรู้ได้จริง
- การรับระบบประกอบผลตรวจโค้ด การเดินงานตามบทบาท และเอกสารพิมพ์จริงตาม ROADMAP §D ห้ามนำผล build/test ไปแทนผล Production

## นอกขอบเขต

ไม่ขยายเป็น GL/งบการเงิน, job costing, DTF auto-nesting แทน RIP, online designer เต็มรูป, แชตในแอป, ใบกำกับอย่างย่อ, time-clock/payroll, WMS/PR-PO-GRN แทน Stock, mockup generator, CMMS, anomaly detection, capacity planning หรือ BOM เต็มรูป และรายงาน ม.87(3) การต่อ e-Tax/PEAK/courier และเว็บสั่งซื้อเป็นแผนอนาคตที่ต้องตกลงขอบเขตใหม่ ไม่ถือว่ารวมในงานปัจจุบัน
