# DESIGN — Anajak ERP

ทิศหน้าตาที่เลือกใช้; พฤติกรรมอยู่ [SPEC](SPEC.md) งานเปลี่ยนทิศอยู่ [ROADMAP](ROADMAP.md) ไม่ใช้ไฟล์นี้เป็นรายการงานหรือสำเนาค่า theme

## ทิศหน้าตา

สว่าง โปร่ง เส้นบาง ผืนงาน near-white กับการ์ดขาว น้ำเงิน Anajak ใช้กับ primary/focus/selected ส่วน TV โรงงานใช้พื้นเข้ม อ้างอิงความเรียบและการจัดวางจาก Notion/Vercel; ใช้ Lucide สำหรับไอคอน

## ตอนนี้ใช้อะไร

- ลำดับความเด่น 3 ชั้น: จุดลงมือหลักหนึ่งจุด → ข้อเท็จจริง → ข้อมูลประกอบ ใช้ `ActionZone`, `Fact`, `Metric`, `InfoChip`, `DueTag` ตามหน้าที่
- ข้อความ 3 ระดับ: ไม่จำเป็นตัดออก / ข้อมูลเสริมเปิดผ่าน ⓘ / warning และเหตุที่ทำต่อไม่ได้เห็นตลอด; `Alert` ใช้แถบสีข้าง
- ตารางอยู่ในการ์ด เมนูที่เปิดใช้พื้นเทากับขีดแบรนด์ ไม่ใช้กล่องสีใหญ่แข่งกัน
- ใบผลิตเป็นฟอร์มคล้ายออเดอร์ ปุ่มลงมือ/ตรวจรับอยู่ในขั้น; หัวใบบอกขั้นถัดไป การเลือกดูขั้นไม่ทำให้เริ่มงาน
- หน้าดูรายการสินค้าใช้โครงเดียวกับฟอร์มเพิ่ม/แก้สินค้า ส่วนการพิมพ์ใช้ A4 และคงข้อมูลเอกสารครบ

## แหล่งจริงของ token และ component

- สี ระยะ มุม ตัวอักษร: [globals.css](src/app/globals.css); ฟอนต์: [layout.tsx](src/app/layout.tsx); class กลาง: [tokens.ts](src/components/ui/tokens.ts)
- โครงหน้า: [page-shell.tsx](src/components/page-shell.tsx), `src/components/ui/`; รูปเสื้อและแบบ: `src/components/mockup/`
- ด่าน: `npm run verify:ui`; ลำดับความเด่นตรวจโดย `scripts/ui-hierarchy-ratchet.ts` และ baseline ของมัน ไม่คัดค่าสี/ระยะมาเก็บที่นี่

## บทเรียนเทคนิค

- เลือกคู่ hover/pressed ให้ตรงพื้นผิว ไม่เช่นนั้นชี้แล้วไม่เห็นการตอบสนอง
- แถวตารางสูงสอดคล้องกับ `page-skeleton.tsx` เพื่อไม่ให้หน้ากระโดด
- Input มือถืออย่างน้อย 16px กันซูม เป้ากดจอทัชอย่างน้อย 44px; จอแคบเลื่อนเฉพาะตาราง ไม่ดันทั้งหน้า
- ตัวอักษรไทยต้องไม่ถูกตัดด้วย line-height แน่น; `leading-tight/snug/none` ใช้ได้เฉพาะตัวเลขล้วนที่ตรวจแล้ว
