# Audit วงจรออเดอร์ทั้งเส้น — 2026-06-11

> วิธีตรวจ: E2E จริงกับ DB (verify:e2e 19/19 — เส้นแกนเดินได้ครบ) + workflow 28 agents (6 auditor ต่อช่วงชีวิตออเดอร์ + ฝ่ายค้าน adversarial ทุกข้อ) — confirmed 31 · refuted 0
> ทุกข้อผ่านการพยายามหักล้างด้วยหลักฐานโค้ดแล้ว · แก้ทันทีไปแล้ว 2 ข้อ: [แบ่งส่งเด้งทั้งใบ] + [mutation เงียบ — default onError ใน hook กลาง]

## 1. [BLOCKER] Dialog แก้ไขรายการ: ทุกการปฏิเสธจาก server ล้มเหลวเงียบสนิท — ไม่มีข้อความ ไม่มี toast ผู้ใช้ติดตันและข้อมูลที่พิมพ์หาย
- **ช่วง**: เปิดงาน/รับออเดอร์ — เติมรายการหลังเปิดงานเบา (OrderEditDialog)
- **หลักฐาน**: src/components/orders/order-edit-dialog.tsx:132-140 (useMutationWithInvalidation ไม่ส่ง onError และทั้งไฟล์ไม่มี .isError/toast เลย) + src/hooks/use-mutation-with-invalidation.ts:42-51 (ไม่มี default onError) + src/components/providers.tsx:35-42 (QueryClient ไม่มี MutationCache/onError กลาง) — ตัวจุดชนวนที่มีจริง: จำนวน 0 (dialog ยอม min="0" บรรทัด 299 แต่ server ปฏิเสธ z.number().min(1) ที่ src/server/routers/order.ts:40) · ส่วนลดเกินยอด (src/server/services/pricing.ts:96-98 badRequest) · แท็บค้างแก้ตอนเริ่มผลิตแล้ว (order.ts:821-822)
- **ผลกระทบ**: นี่คือ funnel หลักของฟอร์มเปิดงานโหมดเดียว — การ์ดขั้นถัดไป "ใส่รายการ" เปิด dialog นี้ตรงๆ (orders/[id]/page.tsx:326-328,398-404) แอดมินเปิดงานเบาจากแชท → มาเติมรายการ/ราคา → กดบันทึก → ไม่เกิดอะไรเลย (spinner หยุด dialog ค้าง) → ปิด dialog = รายการที่พิมพ์ทั้งหมดหาย และไม่มีทางรู้ว่าผิดตรงไหน เป็น bug class เดียวกับที่ PROGRESS บอกว่าแก้แล้วใน dialog สร้างบิล/เปลี่ยนสถานะ ("เดิม server ปฏิเสธแล้วเงียบ") แต่ dialog นี้ตกหล่น — ไม่ใช่หนี้ข้อใดใน (1)-(13)
- **แนวแก้**: เพิ่ม onError → toast.error(err.message) ใน updateItemsMutation/updateFeesMutation (pattern เดียวกับ dialog สร้างบิล) + กล่อง error ใน dialog · ระยะยาว: ใส่ default onError ที่ useMutationWithInvalidation จะปิด class นี้ทั้งระบบ

## 2. [BLOCKER] ยกระดับหนี้ข้อ 8: เส้นทางธรรมชาติของงานที่ต้องมีใบเสนอ ไม่ใช่แค่ 'ไม่สะดวก' แต่สร้างออเดอร์ซ้ำ 2 ใบ + สถิติลูกค้านับซ้ำ โดยไม่มีจุดไหนเตือน
- **ช่วง**: สะพาน INQUIRY→ใบเสนอ→ออเดอร์ (convertToOrder)
- **หลักฐาน**: D:/dev/anajak-suite/erp/src/server/routers/quotation.ts:254-312 (convertToOrder สร้าง order ใหม่เสมอ ไม่ผูก/ไม่ยืนยันออเดอร์ INQUIRY เดิม + :305-308 increment totalOrders ซ้ำกับ D:/dev/anajak-suite/erp/src/server/routers/order.ts:568-574 ที่นับไปแล้วตอนเปิด INQUIRY) · grep 'quotation' ใน src/app/(dashboard)/orders = 0 ไฟล์ (ไม่มีปุ่ม/ลิงก์ออกใบเสนอจากออเดอร์เลย) · D:/dev/anajak-suite/erp/src/app/(dashboard)/quotations/new/page.tsx ทั้งไฟล์ไม่มี useSearchParams/orderId (prefill จากออเดอร์ไม่ได้ ต้องพิมพ์ลูกค้า+รายการซ้ำทั้งหมด) · D:/dev/anajak-suite/erp/src/server/routers/task.ts:88-96 (คิวติดตามลูกค้า = ออเดอร์ INQUIRY ทั้งหมด ไม่มีเงื่อนไขตัดใบที่ไปออกใบเสนอแล้ว)
- **ผลกระทบ**: ลูกค้า B2B เครดิตเทอมคือฐานรายได้ (ตาม CLAUDE.md) และงานพวกนี้ต้องมีใบเสนอก่อนแทบทุกงาน — เส้นทางจริงคือ: เปิดงาน INQUIRY จากแชท (กรอกรอบ 1) → ไป /quotations/new กรอกซ้ำรอบ 2 → ลูกค้าตกลง กด 'แปลงเป็นออเดอร์' → เกิดออเดอร์ CONFIRMED ใบที่สอง ส่วน INQUIRY เดิมค้างถาวรในคิว /my-tasks 'ติดตามลูกค้า' และหน้า orders จนกว่าจะจำได้ว่าต้องไปกดยกเลิกเอง · ถ้าทีมยืนยัน INQUIRY เดิมแทน (ทางที่เนียนกว่า) ใบเสนอจะค้าง ACCEPTED ตลอดพร้อมปุ่ม 'แปลงเป็นออเดอร์' ที่ยังกดได้ — ใครกดทีหลัง = ออเดอร์ผูกพันซ้ำเงียบๆ เข้าคิวผลิต+กินวงเงินเครดิต 2 เด้ง · totalOrders ลูกค้าเพี้ยน +1 ทุกงานแม้ทำถูกทุกขั้น ไม่มีทางทำถูกได้เลยในระบบปัจจุบัน
- **แนวแก้**: เพิ่ม quotation.sourceOrderId ผูกใบเสนอกับออเดอร์ INQUIRY · ปุ่ม 'ออกใบเสนอ' บนหน้าออเดอร์ INQUIRY prefill รายการจาก order items · convertToOrder เปลี่ยนเป็น 'ยืนยันออเดอร์เดิม' เมื่อมี sourceOrderId (สร้างใหม่เฉพาะใบเสนอลอย) — ตรงกับใบงาน 'สะพาน inquiry↔ใบเสนอ↔ออเดอร์' ที่วางไว้ แต่ควรขยับความเร่งด่วนขึ้นเพราะมันคือ flow หลักของฐานรายได้

## 3. [MAJOR] จำนวนสินค้า = 0 ผ่าน validation ฝั่งฟอร์มทั้งหมด แต่ server ปฏิเสธด้วย raw zod JSON ภาษาอังกฤษที่แอดมินอ่านไม่ออก
- **ช่วง**: เปิดงาน/รับออเดอร์ — ฟอร์ม orders/new
- **หลักฐาน**: src/types/order-form.ts:290-301 (validateOrderItemProduct เช็ค description/ราคา/ไซส์/แหล่งที่มา แต่ไม่เช็ค quantity เลย) + src/components/orders/new/product-table-row.tsx:195-198 (input min={0}, parseInt||0 — ลบเลขทิ้งได้ค่า 0) vs src/server/routers/order.ts:40 (variantSchema quantity z.number().min(1)) · ไม่มี errorFormatter ในระบบ (grep errorFormatter/ZodError ทั้ง src/server = 0 ผล) → กล่องแดงโชว์ createOrder.error.message ดิบที่ orders/new/page.tsx:598-602 เป็น JSON array ของ zod issues
- **ผลกระทบ**: เคสจริงง่ายมาก: แอดมินลบเลขจำนวนเพื่อพิมพ์ใหม่แล้วโดนแชท LINE ขัดจังหวะ → กรอกครบ กดยืนยันผ่าน dialog → server ปฏิเสธ โชว์ก้อน JSON ภาษาอังกฤษ ("too_small ... items.0.products.0.variants.0.quantity") — แอดมินไทย non-technical ตีความไม่ได้ว่าต้องแก้ช่องไหน = ติดตันหรือโทรหาเบส
- **แนวแก้**: เพิ่มเช็ค quantity >= 1 ใน validateOrderItemProduct (ที่เดียว ใช้ร่วมฟอร์ม+draft อยู่แล้ว) + เพิ่ม errorFormatter ที่ tRPC แปลง ZodError เป็นข้อความไทยอ่านได้

## 4. [MAJOR] ออเดอร์ที่กลายเป็น READY_MADE แล้ว ไม่มีทาง UI ไหนเพิ่มลายพิมพ์ได้อีก — ทั้งที่ server re-derive รองรับ ต้องยกเลิกออเดอร์เปิดใหม่
- **ช่วง**: เปิดงาน/รับออเดอร์ — เพิ่มลายให้ออเดอร์ที่ derive เป็น READY_MADE
- **หลักฐาน**: src/components/orders/order-edit-dialog.tsx:315,339 (section สกรีน/ส่วนเสริม render เฉพาะ orderType === "CUSTOM") + orders/[id]/page.tsx:402 (ส่ง order.orderType ปัจจุบัน) vs src/server/routers/order.ts:848-852 (updateItems จงใจ re-derive ชนิดออเดอร์จากเนื้อรายการช่วง DRAFT/INQUIRY — ฝั่ง server พร้อมรับ CUSTOM กลับ) และ orders/new เป็นที่เดียวที่สร้าง print ได้ (ฟอร์มสร้างเท่านั้น)
- **ผลกระทบ**: เส้นทางที่ P1 เพิ่งออกแบบเอง: เปิดงานเบา → เติมเสื้อเปล่าก่อน (ราคาเสื้อตกลงแล้ว) → ระบบ re-derive เป็น READY_MADE → ลูกค้าทักมาเพิ่มลาย DTF → dialog ไม่มีปุ่มเพิ่มสกรีนอีกแล้ว → ทางเดียวคือยกเลิกออเดอร์+เปิดใหม่กรอกซ้ำทั้งหมด (เปลืองเลขออเดอร์ + totalOrders ลูกค้านับเพี้ยน) — UI ปิดประตูที่ server เปิดไว้ให้ ไม่ใช่หนี้ 12b (อันนั้น cosmetic เรื่อง status bar)
- **แนวแก้**: ใน dialog เปลี่ยนเงื่อนไขเป็น "แก้ได้เมื่อ status ยังอยู่ช่วง re-derive (DRAFT/INQUIRY)" หรือโชว์ section สกรีนเสมอเมื่อยังแก้รายการได้ — server จะ derive ชนิดให้เองอยู่แล้ว

## 5. [MAJOR] อัปโหลดรูปอ้างอิงจากแชทล้มเหลวแล้วกลืนเงียบ — ไม่มี toast ไฟล์ที่เหลือในคิวถูกทิ้ง และไฟล์เกิน 5 ถูกตัดเงียบ
- **ช่วง**: เปิดงาน/รับออเดอร์ — แนบรูปจากแชท
- **หลักฐาน**: src/app/(dashboard)/orders/new/page.tsx:289-291 (catch {} พร้อม comment "Upload error - silently continue" — uploadFile throw จริงเมื่อ storage ล้มเหลว src/lib/supabase.ts:32-34 แล้ว loop ที่เหลือถูกตัดจบโดยไม่บอกอะไร) + page.tsx:262-263 (เลือกเกิน 5 ไฟล์ slice ทิ้งเงียบ — ต่างจากเคสไฟล์เกิน 10MB ที่มี toast.warning บรรทัด 269)
- **ผลกระทบ**: รูปจากแชทคือสเปกงานตัวจริงของโรงพิมพ์ — แอดมินบนมือถือกดแนบ 4 รูปจาก LINE, เน็ตสะดุด/bucket policy ผิด → spinner หยุดเฉยๆ ไม่มีข้อความ → ถ้าไม่สังเกตว่า thumbnail ขาด ก็เปิดงานไปโดยรูปหาย → ช่าง/ดีไซน์ทำงานไม่เห็นแบบ ต้องวิ่งกลับไปขอในแชทอีกรอบ (คนละประเด็นกับหนี้ข้อ 11 ที่ว่าไม่มีปุ่มอัปโหลดหลังสร้าง)
- **แนวแก้**: เปลี่ยน catch เป็น toast.error ระบุชื่อไฟล์ที่พลาด + เก็บ loop ต่อรายไฟล์ (try/catch ในลูป) + toast แจ้งเมื่อไฟล์ถูกตัดเพราะเกินเพดาน 5

## 6. [MAJOR] เกินที่หนี้ข้อ 8 จดไว้: convert แล้ว paymentTerms หาย → ด่าน 'เรียกมัดจำก่อนเริ่มงาน' ถูกข้ามเงียบ
- **ช่วง**: convertToOrder — เงื่อนไขชำระเงิน
- **หลักฐาน**: D:/dev/anajak-suite/erp/src/server/routers/quotation.ts:257-297 (order.create ใน convertToOrder ไม่มี field paymentTerms — Quotation มีแค่ terms free-text ที่ :87) · prisma/schema.prisma:298 (paymentTerms nullable → ออเดอร์เกิดมาเป็น null) · D:/dev/anajak-suite/erp/src/lib/order-next-step.ts:33,73 (DEPOSIT_TERMS.includes(null ?? "") = false → การ์ด 'เรียกมัดจำก่อนเริ่มงาน' ไม่ขึ้น ข้ามไป 'ส่งเข้าออกแบบ' ทันที)
- **ผลกระทบ**: งานที่ตกลงมัดจำ 30/50% ไว้ในใบเสนอ (พิมพ์อยู่ในช่องเงื่อนไขเป็นข้อความ) พอแปลงเป็นออเดอร์ ระบบพาเข้าออกแบบ/ผลิตเลยโดยไม่มีจุดไหนเตือนให้เก็บมัดจำ — กลไกมัดจำตามเทอมที่เพิ่งสร้างทั้งชุด (payment-plan/billing.suggest) ไม่ทำงานกับออเดอร์สาย convert เลย เงินมัดจำหลุดเงียบทั้งงาน ซึ่งหนี้ข้อ 8 จดแค่ 'items โครงเปล่า' ไม่ได้ครอบเรื่องนี้
- **แนวแก้**: เพิ่มช่อง paymentTerms (enum ชุดเดียวกับออเดอร์ใน src/lib/payment-terms.ts) ในฟอร์มใบเสนอ + copy ตอน convert · ระหว่างยังไม่ทำ อย่างน้อย default จาก customer.paymentTerms ตอน convert

## 7. [MAJOR] ยกระดับหนี้ข้อ 8 ครึ่ง 'items โครงเปล่า': ไม่มีจุดไหนใน flow บังคับ/เตือนให้เติมข้อมูลจริงก่อนผลิต — โครงเปล่าไหลเข้าผลิตได้สุดทาง
- **ช่วง**: convertToOrder — โครงสร้าง items
- **หลักฐาน**: D:/dev/anajak-suite/erp/src/server/routers/quotation.ts:282-293 (ทุก item ถูกบีบเป็น productType OTHER + variant เดียว size FREE ไม่มี prints/ไซซ์) · D:/dev/anajak-suite/erp/src/lib/order-next-step.ts:53,71-96 (itemCount≥1 → การ์ดขั้นถัดไปพาไปมัดจำ/ออกแบบทันที ไม่เคยรู้ว่า items เป็นโครง) · D:/dev/anajak-suite/erp/src/lib/production-steps.ts:59-61 (ไม่มี print → เดาชุด DTF ให้เฉยๆ) · กลไกรองรับที่มี: order.ts:817-823 updateItems ยังแก้ได้ถึงก่อน PRODUCING แต่เป็น delete-all-recreate (order.ts:837) = กรอกใหม่ทั้งใบรอบที่ 3
- **ผลกระทบ**: ออเดอร์สาย convert เดินผ่านทุกด่านได้โดยข้อมูลผลิตใช้ไม่ได้จริง: Job Ticket ตารางไซซ์ขึ้น FREE ทั้งใบ (ทีมแพ็ค/ผลิตไม่รู้ไซซ์อะไรกี่ตัว) · ฟีเจอร์ 'ช่างเห็นลาย' ไม่มีอะไรให้โชว์เพราะไม่มี print record · ขั้นตอนผลิตถูกเดาเป็น DTF แม้งานจริงเป็น DTG/สกรีน — ทางแก้เดียวคือพนักงานต้องนึกเองว่าต้องกรอกรายการใหม่ทั้งหมด (รอบที่ 3 หลังจากกรอกในใบเสนอแล้ว) ก่อนเข้าผลิต ซึ่งไม่มี UI ใดบอก
- **แนวแก้**: ระหว่างรอสะพานเต็มรูป: ใส่ banner/การ์ดเตือนบนออเดอร์ที่มาจาก convert (เช็คได้จาก quotation.orderId relation) ว่า 'รายการยังเป็นโครงจากใบเสนอ — เติมไซซ์/ลายก่อนเข้าผลิต' หรือบล็อก PRODUCTION_QUEUE เมื่อทุก item เป็น OTHER/FREE

## 8. [MAJOR] ยืนยันหนี้ (ใบงาน 'แก้ใบเสนอ' ที่วางไว้): ใบเสนอแก้ไม่ได้เลยแม้แต่สถานะ DRAFT — และไม่มี endpoint แก้ items ในระบบเลย
- **ช่วง**: แก้ใบเสนอหลังสร้าง/หลังส่ง
- **หลักฐาน**: D:/dev/anajak-suite/erp/src/app/(dashboard)/quotations/[id]/page.tsx:187 (comment: ปุ่มแก้ไขถูกถอด หน้า /edit ไม่มีจริง) · grep 'quotation.update' ใน src/app = 0 hits (mutation update เป็น API ตาย ไม่มี UI เรียก) · D:/dev/anajak-suite/erp/src/server/routers/quotation.ts:155-195 (update รับแค่ title/discount/tax/validUntil/terms/notes — แก้รายการสินค้า/ราคาต่อหน่วยไม่ได้แม้ผ่าน API)
- **ผลกระทบ**: ต่อรองราคา = เรื่องปกติของ B2B ไทย ทุกครั้งที่ลูกค้าขอแก้ → สร้างใบเสนอใหม่กรอกซ้ำทั้งหมด · ใบเดิมค้าง SENT ตลอด เพราะทางปิดเดียวใน UI คือ 'ลูกค้าปฏิเสธ' (ความหมายผิด — สถิติ rejected ปลอม) · เลขเอกสาร QT ไหม้ทิ้งทุกรอบเจรจา · list เต็มไปด้วยใบผี SENT ที่แยกไม่ออกว่าใบไหนเป็นเวอร์ชันล่าสุดของดีลเดียวกัน (ไม่มีการผูกใบแทนกัน) — หนักกว่าที่เข้าใจกันว่า 'แก้หลังส่งไม่ได้' เพราะจริงๆ คือพิมพ์ผิดตัวเดียวก่อนส่งก็ต้องสร้างใหม่
- **แนวแก้**: ใบงาน 'แก้ใบเสนอ' P1 ที่วางไว้ครอบอยู่แล้ว — ขอเพิ่ม scope: แก้ items ได้อย่างน้อยช่วง DRAFT/SENT + สถานะ 'ยกเลิก/แทนที่ด้วยใบใหม่' ที่ไม่ใช่ REJECTED

## 9. [MAJOR] ลูกค้าเปลี่ยนใจหลังอนุมัติแบบ = ไม่มีทางตรงกลับไปแก้แบบ — ทางลัดเดียวคือ ON_HOLD สองจังหวะที่ UI ไม่บอกและการ์ดขั้นถัดไปชี้ผิดทาง
- **ช่วง**: ออกแบบ/อนุมัติแบบ — แก้แบบหลังลูกค้าอนุมัติ
- **หลักฐาน**: src/lib/order-status.ts:192-237 — getNextStatuses จาก DESIGN_APPROVED ให้แค่ PRODUCTION_QUEUE/ON_HOLD/CANCELLED (ไม่มี DESIGNING; backward พิเศษมีแค่ QC→PRODUCING, PACKING→QC, PRODUCING→QUEUE) · src/server/routers/design.ts:18-19 UPLOADABLE/APPROVABLE = ["DESIGNING"] เท่านั้น + :109-114 upload ตอน DESIGN_APPROVED โดน CONFLICT · src/components/orders/order-design-section.tsx:72-74 ปุ่มอัปโหลด/ตรวจสอบหายเงียบที่ DESIGN_APPROVED (section ยังโชว์ตาม :103 แต่ไม่มี action ไม่มีคำอธิบาย) · ทางกลับมีจริงทางเดียว: order-status.ts:183-185 ON_HOLD→DESIGNING ผ่าน dropdown เปลี่ยนสถานะ แต่การ์ดแนะนำที่ ON_HOLD (src/lib/order-next-step.ts:176-186) แนะนำแค่ CONFIRMED/PRODUCTION_QUEUE ไม่เคยแนะ DESIGNING · grep "DESIGNING" ใน src/app = 0 จุด — ไม่มีปุ่ม "กลับไปแก้แบบ" ที่ไหนเลย
- **ผลกระทบ**: เหตุการณ์ปกติมากของโรงสกรีน (ลูกค้าอนุมัติแล้วทักมาขอเปลี่ยนสีโลโก้ชั่วโมงต่อมา) — กราฟิกเปิดออเดอร์เจอ section ออกแบบที่ไม่มีปุ่มอะไรเลย ไม่มี error ไม่มีคำใบ้ · คนหน้างานจะส่งแบบแก้กันทาง LINE แล้วผลิตจากไฟล์ในแชท → ประวัติแบบในระบบค้างที่เวอร์ชันเก่าที่อนุมัติแล้ว เสี่ยงพิมพ์ผิดเวอร์ชันและเถียงกับลูกค้าไม่ได้ว่าอนุมัติอันไหน
- **แนวแก้**: เปิด transition DESIGN_APPROVED→DESIGNING (และ PRODUCTION_QUEUE→DESIGNING ถ้ายังไม่เริ่มผลิต) ใน getNextStatuses พร้อมบังคับเหตุผล (revision changeType DESIGN) + ปุ่ม "ลูกค้าขอแก้แบบ" ใน order-design-section ตอนสถานะ DESIGN_APPROVED — หรืออย่างน้อยให้การ์ด ON_HOLD เสนอทาง "ปลดพัก → กลับไปแก้แบบ" เมื่อมีแบบอนุมัติแล้ว

## 10. [MAJOR] อัปโหลดแบบเป็น .ai/.psd แล้วส่งลิงก์ → ลูกค้าบนมือถือมองไม่เห็นแบบเลย ต้องตัดสินใจทั้งที่ดูไฟล์ไม่ได้ — ช่อง thumbnailUrl มีใน API แต่ UI ไม่เคยส่ง
- **ช่วง**: ออกแบบ/อนุมัติแบบ — ลิงก์อนุมัติบนมือถือ
- **หลักฐาน**: src/components/orders/order-design-section.tsx:247 dialog เชิญชวนเอง "อัปโหลดไฟล์แบบ (PNG, JPG, PDF, AI, PSD) เพื่อส่งให้ลูกค้าอนุมัติ" + :254 accept=".ai,.psd" · :76-83 handleUploadSubmit ส่งแค่ fileUrl+designerNotes — ไม่มีช่อง preview/thumbnail ทั้งที่ server รับ thumbnailUrl (src/server/routers/design.ts:98) · src/components/ui/file-upload.tsx:34-68 ไม่ generate thumbnail · หน้า approve (src/app/approve/design/[token]/page.tsx:178-196) fallback เป็นกล่อง "เปิดไฟล์แบบ" — กดแล้วมือถือดาวน์โหลด .ai/.psd ที่ไม่มีแอปเปิดได้
- **ผลกระทบ**: flow อนุมัติผ่าน token (จุดขายของระบบ) พังเงียบสำหรับไฟล์งานจริงของกราฟิก — ลูกค้ากดลิงก์เห็นแค่กล่อง "เปิดดูตัวอย่างไม่ได้" + ปุ่มอนุมัติ ต้องทักกลับ LINE ขอรูป แล้วการอนุมัติจริงเกิดนอกระบบ (ขายต้องมาบันทึกแทนทีหลังหรือไม่บันทึกเลย) · กล่อง fallback ที่ทำไว้กันรูปแตกแล้ว (จดใน PROGRESS) แต่ปัญหา "ลูกค้าตัดสินใจโดยไม่เห็นแบบ" ยังอยู่เต็มๆ และไม่อยู่ในหนี้ (1)-(13)
- **แนวแก้**: ตอนไฟล์หลักไม่ใช่รูป (isImageUrl=false) บังคับ/เตือนให้แนบรูป preview ใน dialog อัปโหลด แล้วส่งเป็น thumbnailUrl ที่ API รองรับอยู่แล้ว — หน้า approve โชว์ thumbnail เป็นภาพหลัก + ลิงก์ไฟล์งานไว้ดาวน์โหลด

## 11. [MAJOR] ไม่มี UI มอบหมาย/ย้ายเจ้าของงานเลย — staff claim เองอย่างเดียว แล้วล็อกคนอื่นถาวร
- **ช่วง**: ผลิต in-house — มอบหมายงาน
- **หลักฐาน**: src/server/routers/production.ts:118-141 (comment ยอมรับเอง 'ระบบยังไม่มี UI มอบหมายงาน' + staff แตะ step ของคนอื่น = FORBIDDEN 'งานนี้ถูกมอบหมายให้คนอื่นแล้ว') · production.ts:38-50 (create ไม่รับ assignedToId — วางแผนมอบงานตอนเปิดใบผลิตไม่ได้) · src/components/orders/order-production-section.tsx:571-647 (dialog อัปเดต step ไม่มีช่องผู้รับผิดชอบ) · src/app/(dashboard)/my-tasks/page.tsx:166-170 (badge 'ยังไม่มีคนรับ' โชว์อย่างเดียว ไม่มีปุ่มรับ/มอบ) · grep ทั้ง src ยืนยันไม่มี component ไหนส่ง assignedToId
- **ผลกระทบ**: หัวหน้าวางแผนว่าใครทำอะไรไม่ได้ — ใครกดก่อนได้งาน · ช่าง A กดอัปเดต step แล้วป่วย/ไม่อยู่ → ช่าง B โดน FORBIDDEN แตะต่อไม่ได้ และผู้จัดการก็ไม่มีหน้าจอย้ายงานให้ B (field มีใน API แต่ไม่มี UI เรียก) → งานค้างจนผู้จัดการต้องมากดสถานะแทนเองทุกครั้ง
- **แนวแก้**: เพิ่ม dropdown ผู้รับผิดชอบ (รายชื่อ user role ผลิต) ใน dialog อัปเดต step สำหรับ OWNER/MANAGER (server รองรับอยู่แล้ว) + ปุ่ม 'รับงานนี้' ใน /my-tasks สำหรับ step ที่ยังไม่มีเจ้าของ

## 12. [MAJOR] วนกลับแก้งานหลัง QC ไม่ผ่านแล้ว 'ใบผลิตหายจากบอร์ดผลิต' — Production ปิดแล้วไม่มีทางเปิดกลับ และ UI ไม่ให้เปิดใบใหม่
- **ช่วง**: ผลิต in-house — งานแก้หลัง QC ไม่ผ่าน
- **หลักฐาน**: src/server/services/order-status.ts:137-141 (ทุก step เสร็จ → production.status=COMPLETED — ไม่มีโค้ดไหน reset กลับ) · src/server/routers/production.ts:117-176 (updateStep เปิด step COMPLETED→IN_PROGRESS ได้ แต่ไม่แตะ production.status) · production.ts:179-180 (board กรอง status not COMPLETED → ใบผลิตที่กำลังแก้งานไม่ขึ้นบอร์ด) · src/components/orders/order-production-section.tsx:174-176 (canCreate ต้อง productions.length===0 + สถานะใน [PRODUCTION_QUEUE, DESIGN_APPROVED, CONFIRMED] → ออเดอร์ที่ถอยกลับ PRODUCING เปิดใบผลิตใบสองไม่ได้ ทั้งที่ server รองรับ — production.ts:54-55 + order-status.ts:44-46)
- **ผลกระทบ**: เคสจริงที่เกิดแน่ในโรงงานสกรีน: QC เจองานเสีย → ถอยออเดอร์ QUALITY_CHECK→PRODUCING → เปิด step กลับมาทำใหม่ → หน้า /production (บอร์ดรวมของทีม) มองไม่เห็นงานแก้นี้เลย เพราะใบผลิตยังนับเป็น COMPLETED — งานเร่งแก้กลายเป็นงานล่องหน ต้องจำเองว่าออเดอร์ไหนกำลังแก้ · เปิดใบผลิตรอบแก้แยกใบ (เช่น พิมพ์ซ่อม 20 ตัว) ก็ทำไม่ได้จาก UI
- **แนวแก้**: ใน updateStep: ถ้า step ถูกเปลี่ยนจาก COMPLETED เป็นสถานะอื่น และ production.status=COMPLETED → reset production เป็น IN_PROGRESS (ลบ endDate) · และ/หรือ เปิด canCreate ให้สร้างใบผลิตเพิ่มได้ตอนสถานะ PRODUCING

## 13. [MAJOR] step ที่กดว่า 'มีปัญหา' หายจากคิวงานของทุกคน และไม่แจ้งเตือนใคร — ออเดอร์ค้าง PRODUCING เงียบๆ
- **ช่วง**: ผลิต in-house — step มีปัญหา (FAILED/ON_HOLD)
- **หลักฐาน**: src/server/routers/task.ts:27-28 (myToday ดึงเฉพาะ step PENDING/IN_PROGRESS — FAILED/ON_HOLD หลุดจากคิว 'งานของฉัน') · src/server/routers/production.ts:100-176 (updateStep ไม่มี createNotification เลย — set FAILED ไม่กระดิ่งหาใคร) · src/server/services/order-status.ts:133 (finalize ต้องทุก step COMPLETED — มี FAILED ค้าง 1 ตัว ออเดอร์ติด PRODUCING ตลอด) · จุดเดียวที่เห็นคือบอร์ด /production badge แดง (production/page.tsx:31) ซึ่งไม่ sort/ไม่กรองงานมีปัญหาขึ้นก่อน
- **ผลกระทบ**: ความหมายของ FAILED คือ 'ต้องมีคนมาดูด่วน' แต่พฤติกรรมจริงตรงข้าม: หายจาก /my-tasks (คิวหลักของทีม mobile-first) ไม่มี notification → step ปัญหาถูกลืมง่าย ออเดอร์ค้างกำลังผลิตจนเลย deadline โดยไม่มีตัวชี้ (หนัก/เฉพาะเจาะจงกว่าหนี้ข้อ 12 ที่จดไว้ — ข้อ 12 คือ 'ค้างสถานะนานไม่มีตัวเตือน' เชิงเวลา แต่นี่คืองานปัญหาถูกคัดออกจากคิวทันทีที่กด)
- **แนวแก้**: task.myToday รวม FAILED/ON_HOLD (พร้อม badge แดงดันขึ้นบนสุด) + ยิง createNotification หา OWNER/MANAGER เมื่อ step ถูก set FAILED (pattern กระดิ่งมีแล้วจาก notifyDesignDecision/overdue)

## 14. [MAJOR] ต้นทุนที่ผู้ใช้กรอก (actualCost ต่อ step + ค่าจ้างร้านนอก) ไม่ไหลไปไหนเลย — กำไรขั้นต้นบนหน้าออเดอร์ไม่เคยเห็นเงินก้อนนี้ และช่อง 'ต้นทุนรวม' เป็น field ตาย
- **ช่วง**: ผลิต + outsource — ต้นทุนจริง
- **หลักฐาน**: prisma/schema.prisma:797 (Production.totalCost Decimal default 0) — grep ทั้ง src ไม่มีโค้ดเขียน field นี้แม้แต่จุดเดียว → src/components/orders/order-production-section.tsx:413-418 (กล่อง 'ต้นทุนรวม' เช็ค prod.totalCost > 0 = ไม่มีวันแสดง) · src/app/(dashboard)/orders/[id]/page.tsx:133-138,153-156 (ต้นทุน/กำไรออเดอร์ = ผลรวม CostEntry เท่านั้น) · CostEntry สร้างได้ที่เดียวคือกรอกมือ src/server/routers/cost.ts:59 — ไม่มีสะพานจาก ProductionStep.actualCost (production.ts:107) หรือ OutsourceOrder.totalCost (outsource.ts:182)
- **ผลกระทบ**: งาน silkscreen ทั้งสายคือ outsource — ค่าจ้างร้านนอกที่กรอกตอนเปิดใบถูกเก็บลง DB แล้วจบแค่นั้น กำไรขั้นต้นบนหน้าออเดอร์โชว์สูงเกินจริง เว้นแต่บัญชีไปคีย์เงินก้อนเดิมซ้ำใน CostEntry เอง (double entry ไม่มีอะไรบอกให้ทำ) — เกินขอบหนี้ที่จดไว้: หนี้เดิมพูดถึง platformFee→CostEntry กับ MaterialUsage อ่านกลับไม่ได้ (ข้อ 13 ท้าย) แต่ไม่ได้ครอบ actualCost/ค่าจ้าง outsource ที่ผู้ใช้กรอกผ่าน UI วันนี้แล้วหายเงียบ
- **แนวแก้**: ขั้นต่ำก่อนถึง P2 job costing: ตอน outsource QC_PASSED / ตอนกรอก actualCost → upsert CostEntry ผูกออเดอร์อัตโนมัติ (หรืออย่างน้อยรวมสองก้อนนี้เข้าเลข totalCost ที่หน้าออเดอร์ใช้) + ลบ/ซ่อน Production.totalCost ที่ตายแล้ว

## 15. [MAJOR] ยกระดับหนี้ข้อ 9 (ส่วนที่เหลือ): กด SHIPPED มือได้คลิกเดียว ไม่มี confirm ไม่ต้องมีใบส่ง/เลขพัสดุ — และถอยกลับไม่ได้
- **ช่วง**: ส่ง
- **หลักฐาน**: src/server/routers/order.ts:615-690 (updateStatus มีด่านเฉพาะ CONFIRMED กับ COMPLETED — ไป SHIPPED ไม่เช็คว่ามี delivery/tracking เลย) · src/app/(dashboard)/orders/[id]/page.tsx:181-183 (สถานะที่ไม่ใช่ CANCELLED mutate ทันทีไม่มี confirm) + :231-239 (ปุ่ม primary บนหัวหน้า READY_TO_SHIP คือ "จัดส่งแล้ว" คลิกเดียว) · src/lib/order-next-step.ts:145,155 (การ์ดขั้นถัดไปก็ยิง STATUS→SHIPPED ตรง — คำบรรยายบอกให้สร้างใบส่งก่อนแต่ปุ่มไม่บังคับ) · src/lib/order-status.ts:196-234 (จาก SHIPPED ไปได้แค่ COMPLETED/CANCELLED — ไม่มี backward, ไม่ holdable)
- **ผลกระทบ**: ยกระดับเพราะหนี้จดแค่ "กดได้โดยไม่มี tracking" แต่ของจริงเจ็บกว่า: (ก) ปุ่มที่ระบบชี้ให้กด (ทั้ง header primary และการ์ดเข็มทิศ) คือทางลัดข้ามใบส่งเอง (ข) กดพลาด/กดข้ามแล้วถอยไม่ได้เลย — READY_TO_SHIP ก็ไม่มีทางถอย → misclick เดียวออเดอร์ค้าง SHIPPED ถาวร แก้ได้แค่ DB ตรง (ค) พอเป็น SHIPPED แล้ว next-step เปลี่ยนเป็นเรื่องวางบิลทันที ไม่มีอะไรเตือนว่ายังไม่มีใบส่ง — ที่อยู่/เลขพัสดุไม่ถูกบันทึก ลูกค้าถามเลขพัสดุต้องไปขุด LINE (ที่อยู่ก็ไม่ไหลกลับโปรไฟล์เพราะไม่ได้ผ่าน delivery.create)
- **แนวแก้**: ด่านนุ่มฝั่ง server ใน updateStatus: ไป SHIPPED แล้วออเดอร์ไม่มี delivery record → ปฏิเสธพร้อมข้อความชี้ไปสร้างใบส่ง (หรืออย่างน้อย UI confirm dialog เตือน "ยังไม่มีใบส่ง/เลขพัสดุ") + เปิด transition ถอย SHIPPED→READY_TO_SHIP สำหรับ MANAGER ขึ้นไปไว้เป็นทางแก้ misclick

## 16. [MAJOR] ยกระดับหนี้ข้อ 13: ส่งกล่องแรกจากหลายกล่อง → ออเดอร์เด้งเป็น SHIPPED ทั้งใบทันที (auto-advance ใหม่ไม่เช็คใบส่งพี่น้อง) ✅ แก้แล้ว (2026-06-11)
- **ช่วง**: ส่ง (แบ่งส่งหลายรอบ)
- **หลักฐาน**: src/server/routers/delivery.ts:158-165 (delivery ใบใดใบหนึ่งเป็น SHIPPED/DELIVERED → advanceOrderForward เป็น SHIPPED ทันที ไม่นับว่ายังมี delivery อื่นของออเดอร์เดียวกันค้าง PENDING/PREPARING) — ตัดกับฝั่งผลิตที่กันเคสนี้ไว้ชัดเจน: src/server/services/order-status.ts:143-148 (นับ openProductions ก่อนดัน) · UI สร้างหลายใบส่งได้จริง: src/components/orders/order-delivery-section.tsx:167-171 (canCreate รวม SHIPPED)
- **ผลกระทบ**: หนี้ 13 จดแค่ "ไม่มี line items บอกไม่ได้กล่องไหนมีอะไร" แต่ auto-advance ที่เพิ่งเพิ่ม (commit ชุดสถานะเด้งเอง) ทำให้แบ่งส่งพังหนักขึ้น: ส่งของล็อตแรกล็อตเดียว สถานะออเดอร์+customerStatus (หน้าลูกค้า) กลายเป็น "จัดส่งแล้ว" ทั้งที่ของครึ่งหนึ่งยังอยู่โรงงาน · my-tasks ฝั่งการเงินขึ้น "ส่งแล้วรอวางบิล" + การ์ดขั้นถัดไปชวนปิดงาน → เสี่ยงวางบิล/ปิดงานก่อนของออกครบ
- **แนวแก้**: ก่อนดัน SHIPPED ใน delivery.updateStatus ให้นับ delivery ของออเดอร์ที่ status ยังไม่ถึง SHIPPED/DELIVERED (ไม่นับ RETURNED) — ดันเฉพาะเมื่อเหลือศูนย์ ตาม pattern เดียวกับ finalizeProductionIfComplete (line items จริงค่อยรอ P2 ใบแพ็คตามแผน)

## 17. [MAJOR] ลูกค้าเคลม/ของตีกลับ: mark RETURNED ได้แต่จบแค่นั้น — ไม่มีเส้นทางซ่อมงานต่อในระบบ ทุก workaround ทำข้อมูลเพี้ยน
- **ช่วง**: หลังส่ง — เคลม/ตีกลับ
- **หลักฐาน**: src/server/routers/delivery.ts:126,131,158 (RETURNED รับเข้า enum + set status เฉยๆ — ไม่มีผลต่อออเดอร์ ไม่มีกระดิ่ง มีแต่ SHIPPED/DELIVERED ที่ดันสถานะ) · src/lib/order-status.ts:196-234 (SHIPPED ไปได้แค่ COMPLETED/CANCELLED — ถอยกลับ QC/ผลิตไม่ได้ พัก ON_HOLD ก็ไม่ได้) · src/server/routers/production.ts:71-86 + src/server/services/order-status.ts:48-50 (เปิดใบผลิตซ่อมไม่ได้ — transitionOrder ไป PRODUCTION_QUEUE จาก SHIPPED โดน badRequest) · ทางหนี CANCELLED ก็เจ็บ: transitionOrder set แค่ cancelledAt (services/order-status.ts:63-66) ไม่ void บิล และ receivables นับใบแจ้งหนี้ไม่ void ทุกใบไม่สนสถานะออเดอร์ (src/server/services/receivables.ts:21-22,206) → ลูกหนี้ผีใน aging
- **ผลกระทบ**: โรงงานสกรีนเจอเคลม (พิมพ์เพี้ยน/ไซซ์ผิด/ลอก) เป็นเรื่องปกติ แต่พอของถูกตีกลับ: ออเดอร์ค้าง SHIPPED — ซ่อม/พิมพ์ใหม่ในใบเดิมไม่ได้ ต้องออกนอกระบบ (กระดาษ/LINE) หรือ duplicate ออเดอร์ราคาเต็ม (กินวงเงินเครดิตซ้ำ + โซ่บิลใหม่ทั้งที่ไม่เก็บเงิน) หรือยกเลิกทั้งใบแล้วบิลเดิมค้างเป็นหนี้ผีใน aging — next-step ระหว่างนั้นยังเชียร์ "ปิดงาน" อยู่เหมือนไม่มีอะไรเกิดขึ้น
- **แนวแก้**: ขั้นต่ำ: delivery เป็น RETURNED → แจ้งกระดิ่ง + เปิด transition SHIPPED→QUALITY_CHECK (หรือ ON_HOLD) สำหรับ MANAGER พร้อม reason เพื่อกลับเข้าวงจรซ่อมได้ · flow เคลมเต็ม (ซ่อม/ผลิตชดเชย/ลดหนี้ผูกใบจริง — โยงหนี้ข้อ 1) ควรเคาะกับเบสเป็นใบงานก่อน P2

## 18. [MAJOR] ปิดงาน (COMPLETED) คลิกเดียวไม่มี confirm และไม่มีทางเปิดกลับ — ปิดพลาด/เคลมหลังปิด = ติดตันถาวร
- **ช่วง**: ปิดงาน
- **หลักฐาน**: src/app/(dashboard)/orders/[id]/page.tsx:166-184 (confirm dialog มีเฉพาะ CANCELLED — COMPLETED mutate ทันที) + :231-239 (เป็นปุ่ม primary บนหัวตอน SHIPPED) · src/lib/order-next-step.ts:168-173 (การ์ดเข็มทิศก็เป็นปุ่ม "ปิดงาน" คลิกเดียว) · src/lib/order-status.ts:177-179 (COMPLETED → [] — ไม่มี transition ออก ไม่มี mutation reopen ที่ไหนใน order.ts) · หลังปิดแก้ได้แค่ notes/tracking — เงิน/รายการ/ค่าธรรมเนียมล็อกหมด (order.ts:751-762, 817-823, 908-910 — ตัวล็อกถูกต้องแล้ว แต่ยิ่งทำให้การปิดผิดใบแก้ไม่ได้เลย)
- **ผลกระทบ**: ด่านวางบิลครบช่วยกันได้เฉพาะใบที่ยังวางบิลไม่ครบ — งานขายสด/รับเงินครบแล้ว ปุ่ม "เสร็จสิ้น" คือ terminal คลิกเดียวไม่มีถามซ้ำ ปิดผิดใบ (หรือปิดแล้วลูกค้าเคลมวันรุ่งขึ้น) ไม่มีทางกลับนอกจากแก้ DB ตรง — เบสเป็น non-coder ทำเองไม่ได้ ระบบที่เหลือก็มองออเดอร์นี้จบแล้ว (หายจาก active ทุกคิว)
- **แนวแก้**: (1) confirm dialog ก่อน COMPLETED (มี useConfirm provider อยู่แล้ว — pattern เดียวกับยกเลิก) (2) เพิ่ม transition COMPLETED→SHIPPED จำกัด OWNER/MANAGER + บังคับ reason + audit — เปิดกลับเพื่อแก้แล้วปิดใหม่ได้โดยไม่ทะลุกติกาเงิน

## 19. [MAJOR] ระบบสั่งให้บัญชี "ปิดงาน" ใน my-tasks แต่ ACCOUNTANT ถูกตัดสิทธิ์ order.updateStatus ทั้งตัว — กดแล้วโดน FORBIDDEN เสมอ
- **ช่วง**: ปิดงาน (SHIPPED→COMPLETED) — role ACCOUNTANT
- **หลักฐาน**: src/server/routers/order.ts:22 `orderOps = requireRole("OWNER","MANAGER","SALES","PRODUCTION_STAFF")` (ไม่มี ACCOUNTANT) + order.ts:616 updateStatus `.use(orderOps)` · ขณะที่ src/server/routers/task.ts:9 `FINANCE_ROLES = [...,"ACCOUNTANT"]` + task.ts:107-128 ส่ง shippedOrders ให้บัญชี · src/app/(dashboard)/my-tasks/page.tsx:261 หัว section เขียนว่า "ส่งแล้ว — วางบิลให้ครบ/ปิดงาน" · src/lib/order-next-step.ts:168-173 การ์ดบนหน้าออเดอร์โชว์ปุ่ม "ปิดงาน" (STATUS→COMPLETED) โดยไม่เช็ค role
- **ผลกระทบ**: คนที่ระบบมอบหมายขั้น "ปิดงานหลังวางบิลครบ" (บัญชี — คนเดียวที่รู้ว่าบิลครบจริง) ทำขั้นนั้นไม่ได้เลย: my-tasks พามาที่ออเดอร์ → กดปิดงาน → toast FORBIDDEN ทุกครั้ง ต้องเดินไปขอ OWNER/MANAGER/SALES กดแทนทุกออเดอร์ — ฟีเจอร์ my-tasks กับ RBAC ขัดกันเอง (ทั้งคู่เพิ่งทำเสร็จคนละ commit)
- **แนวแก้**: เปิดทางแคบ: ใน updateStatus อนุญาต ACCOUNTANT เฉพาะ transition SHIPPED→COMPLETED (pattern เดียวกับข้อยกเว้น PRODUCTION_STAFF ที่ order.ts:633-647) หรือถ้าตั้งใจให้บัญชีไม่ปิดงาน ให้ตัด shippedOrders ออกจาก section ของ ACCOUNTANT ใน task.ts และแก้ copy

## 20. [MAJOR] ออเดอร์ที่เข้าคิวผลิตแต่ยังไม่มีใบผลิต หายไปจากทุกคิวของทุก role — และคนเดียวที่เปิดใบผลิตได้คือ OWNER/MANAGER แต่ปุ่มโชว์ให้ทุกคน (ยกระดับหนี้ข้อ 12: ไม่ใช่แค่ "ค้างนานไม่มีตัวเตือน" แต่ไม่โผล่ในคิวใครตั้งแต่วินาทีแรก)
- **ช่วง**: handoff ขาย→ผลิต (PRODUCTION_QUEUE ก่อนมีใบผลิต)
- **หลักฐาน**: src/server/routers/production.ts:9,33-34 `create .use(managerUp)` (OWNER/MANAGER เท่านั้น) · src/components/orders/order-production-section.tsx:174-176 `canCreate` เช็คแค่สถานะ ไม่เช็ค role → ปุ่ม "สร้างใบผลิต" (บรรทัด 263-272) โชว์ให้ SALES/PRODUCTION_STAFF กรอก dialog ครบแล้วโดนปฏิเสธ · src/server/routers/task.ts:24-35 คิวผลิตใน my-tasks query จาก productionStep เท่านั้น (ไม่มีใบผลิต = ไม่มี step = ไม่โผล่ แม้แต่ของ OWNER/MANAGER เอง) · src/app/(dashboard)/production/page.tsx:69 บอร์ดผลิตใช้ production.board ซึ่ง query ตาราง Production (production.ts:178-199) — ออเดอร์ PRODUCTION_QUEUE ที่ไม่มีใบผลิตไม่ขึ้นบอร์ด
- **ผลกระทบ**: เส้นทางปกติของงานสำเร็จรูป: SALES กด "เข้าคิวผลิต" → ออเดอร์ออกจากคิว INQUIRY ของขาย แต่ยังไม่เข้าคิวผลิตของใครเลย (my-tasks ทุก role + บอร์ดผลิต มองไม่เห็น) จนกว่า OWNER/MANAGER จะบังเอิญไปเปิดใบผลิตจากหน้าออเดอร์เอง — งานหล่นเงียบตรงรอยต่อที่ใช้บ่อยที่สุดของโรงงาน และ role อื่นที่พยายามช่วยจะโดน FORBIDDEN หลังกรอกขั้นตอนครบ
- **แนวแก้**: (1) เพิ่ม section "รอเปิดใบผลิต" ใน task.myToday (orders ที่ internalStatus=PRODUCTION_QUEUE/DESIGN_APPROVED และ productions=0) ให้ OWNER/MANAGER (2) ซ่อนปุ่มสร้างใบผลิตตาม role ด้วย pattern `me.role` ที่มีอยู่แล้วในไฟล์เดียวกัน (canOutsource บรรทัด 121) หรือพิจารณาเปิด production.create ให้ PRODUCTION_STAFF เพราะหน้างานจริงทีม 5 คน

## 21. [MAJOR] mutation ครึ่งหน้าออเดอร์พังแบบ "เงียบสนิท" — useMutationWithInvalidation ไม่มี default onError และ dialog ไม่ render error: อัปโหลดแบบ/อนุมัติแบบ/แก้ข้อมูลออเดอร์/สำเนาออเดอร์/ลบใบส่ง กดแล้วไม่เกิดอะไรโดยไม่มีคำอธิบาย (ยกระดับหนี้ข้อ 4: ที่จดไว้คือปุ่มการเงิน 2 ปุ่มที่ "server กันอยู่" — ของจริงคือทั้งหน้า และหลายจุดผู้ใช้ไม่เห็นแม้แต่ข้อความปฏิเสธ) ✅ แก้แล้ว (2026-06-11)
- **ช่วง**: ออกแบบ + ปุ่มทั้งหน้าออเดอร์ (ทุก role)
- **หลักฐาน**: src/hooks/use-mutation-with-invalidation.ts:42-51 ส่งต่อเฉพาะ onSuccess ไม่มี toast error กลาง · src/components/orders/order-design-section.tsx:55-70 uploadDesign/approveDesign ไม่มี onError + dialog อนุมัติ (300-351) ไม่ render `.error` เลย ขณะ server แยกสิทธิ์ src/server/routers/design.ts:92-93 upload=designerUp(:10), :183-184 approve=salesUp(:12) แต่ UI เช็คแค่สถานะ (order-design-section.tsx:72-74) → SALES กดอัปโหลดแบบ=เงียบ, DESIGNER กดอนุมัติแบบ=เงียบ, แม้ SALES ตัว role ถูกแต่เจอ CONFLICT (ลูกค้าเพิ่งตัดสินผ่านลิงก์) ก็เงียบ · src/components/orders/order-info-edit-dialog.tsx:113-116 ไม่มี onError (order.update=salesUp order.ts:716-717) · src/app/(dashboard)/orders/[id]/page.tsx:104-109 duplicate ไม่มี onError (salesUp order.ts:1006-1008) · src/components/orders/order-delivery-section.tsx:115-117 deleteDelivery ไม่มี onError + ลบทันทีไม่มี confirm (:312-321) ขณะ server จำกัด managerUp (delivery.ts:8,171-172) · ปุ่มเปลี่ยนสถานะ header (page.tsx:231-314) โชว์ทุก role โดยไม่เช็ค (DESIGNER/ACCOUNTANT โดนปฏิเสธทุกปุ่ม, PRODUCTION_STAFF โดนปุ่มฝั่งขาย/ยกเลิก ตาม order.ts:633-647) — อันนี้มี toast แต่ปุ่มก็ยังโกหก
- **ผลกระทบ**: ผู้ใช้กดบันทึกแล้ว dialog ค้าง/ปิดไม่ลง โดยไม่มีสัญญาณว่าทำไม → เข้าใจว่าระบบแฮงก์หรือ "บันทึกแล้วมั้ง" แล้วปิดหน้าทิ้ง = ผลอนุมัติแบบ/ข้อมูลที่แก้หายเงียบ ต้องไปถามกันนอกระบบ — ร้ายแรงกว่าที่หนี้ข้อ 4 จดไว้เพราะหลายจุดไม่มี feedback ใดๆ ไม่ใช่แค่ "ปุ่มโชว์เกินสิทธิ์แต่ server กัน"
- **แนวแก้**: จุดเดียวแก้ทั้งระบบ: ใส่ default `onError: (e) => toast.error(e.message)` ใน useMutationWithInvalidation (override ได้) — แล้วค่อยตามซ่อนปุ่มตาม role ด้วย trpc.user.me pattern ที่ใช้อยู่แล้วใน order-billing-section.tsx:105 / order-production-section.tsx:119

## 22. [MAJOR] shell ของแอปไม่มี mobile layout เลย — sidebar กว้าง 256px คงที่ทุกขนาดจอ ไม่มี drawer/hamburger: บนมือถือเหลือพื้นที่เนื้อหา ~120px ทั้งที่ flow ออกแบบให้ช่างสแกน QR เปิดออเดอร์บนมือถือ
- **ช่วง**: ทุกขั้นบนมือถือ (orders/[id] จาก QR Job Ticket, /my-tasks, /production)
- **หลักฐาน**: src/components/layout/sidebar.tsx:97-101 `<aside className="flex h-screen flex-col ... collapsed ? 'w-[68px]' : 'w-64'">` — ไม่มี breakpoint class (hidden/md:flex) ใดๆ ทั้งไฟล์ · src/app/(dashboard)/layout.tsx:18-22 render `<Sidebar />` เสมอใน `flex h-screen overflow-hidden` · topbar.tsx ไม่มีปุ่ม hamburger (grep md:/drawer/Sheet ทั้ง src/components/layout เจอแค่ kbd shortcut) · ซ้ำด้วย src/components/page-header.tsx:74 แถวปุ่ม action เป็น `flex shrink-0 items-center gap-2` ไม่มี flex-wrap ขณะ orders/[id]/page.tsx:206-314 ยัด 4 ชิ้น (ป้ายสถานะ+ใบสั่งงาน+ปุ่มสถานะ+dropdown) ลงแถวนั้น
- **ผลกระทบ**: หน้า ops ที่ลงทุนทำ mobile-first (/my-tasks เป้านิ้ว 56px, QR บน Job Ticket เปิด /orders/[id]) ถูก sidebar กิน 2 ใน 3 ของจอ 375px — เนื้อหาเบียดเหลือ ~119px ปุ่มสถานะใน header ล้น/เลื่อนหาไม่เจอ ผู้ใช้ต้องรู้เองว่ากดย่อ sidebar ได้ (และ state รีเซ็ตเป็นกางทุกครั้งที่ reload) — ในทางปฏิบัติช่างหน้างานใช้มือถือไม่ได้จริง ต้องกลับไปเดินหาคอม
- **แนวแก้**: ซ่อน aside บนจอเล็ก (`hidden md:flex`) + ปุ่ม hamburger ใน Topbar เปิดเป็น overlay drawer (มี Radix Dialog อยู่แล้วใช้ประกอบได้) · เพิ่ม flex-wrap ที่ page-header.tsx:74

## 23. [MINOR] กันลูกค้าซ้ำพังเมื่อเบอร์โทรถูกเก็บคนละ format — normalize อยู่แค่ใน picker แต่หน้า /customers เก็บเบอร์ดิบ และ server ไม่ normalize เลย
- **ช่วง**: เปิดงาน/รับออเดอร์ — เลือก/สร้างลูกค้า (CustomerPicker)
- **หลักฐาน**: src/components/customers/customer-picker.tsx:85,111 (ตัดขีด/ช่องว่างก่อนเช็คซ้ำ+ก่อนบันทึก) vs src/app/(dashboard)/customers/page.tsx:76 (ส่ง formData.phone ดิบมีขีดได้) + src/server/routers/customer.ts:47 (ค้นด้วย phone contains ตรงตัว ไม่ normalize) + customer.ts:124-129 (create ไม่มี dedup/normalize ฝั่ง server)
- **ผลกระทบ**: ลูกค้าที่ถูกสร้างจากหน้า /customers ด้วยเบอร์ "081-234-5678" จะไม่ถูกจับซ้ำเมื่อแอดมินอีกคน quick-create ด้วย "0812345678" (probe ใช้ contains เลขล้วน) → ลูกค้าซ้ำ 2 ราย ประวัติงาน/totalSpent/วงเงินเครดิตแตกเป็นสองก้อน ทำให้ด่านเครดิตที่เพิ่งสร้างใน P1 อ่อนลงโดยไม่รู้ตัว
- **แนวแก้**: normalize เบอร์ที่ชั้น server (customer.create/update ตัด [\s-] ก่อนเก็บ) + ค้นหาเทียบแบบ normalize ทั้งสองข้าง — แก้ที่เดียวครอบทุกทางเข้า

## 24. [MINOR] Draft localStorage เก็บเฉพาะ items — ลูกค้า/ชื่องาน/รายละเอียดจากแชท/รูป/ที่อยู่หายเมื่อ refresh/tab โดน discard และ banner ทำให้เข้าใจว่ากู้ได้ครบ ส่วนโหมดเปิดเบา (flow หลักของ P1) ไม่มี draft เลย
- **ช่วง**: เปิดงาน/รับออเดอร์ — draft กันข้อมูลหาย (orders/new)
- **หลักฐาน**: src/hooks/use-order-items-form.ts:11,32-45 (DRAFT_KEY = "order-draft-items" เก็บแค่ items และ save เฉพาะเมื่อ items.some(itemHasContent) — เปิดเบาไม่มีรายการ = ไม่ save อะไรเลย) + orders/new/page.tsx:430-444 (banner "พบข้อมูลร่างที่ยังไม่ได้บันทึก — กรอกต่อจากเดิม" แต่ field อื่นทั้งหมดเป็น useState ธรรมดา page.tsx:75-114)
- **ผลกระทบ**: เส้นทางจริงของแอดมิน: paste สรุปความต้องการลูกค้าลง "รายละเอียดจากแชท" → สลับแอปไป LINE เพื่อก๊อปที่อยู่ → mobile browser discard tab → กลับมาเหลือแต่รายการสินค้า (ถ้ามี) ข้อความที่ paste หายเงียบ ต้องไล่แชทใหม่ — โหมดเปิดเบาที่ทั้งงานอยู่ใน description ไม่มีตาข่ายเลยทั้งที่โหมดหนักมี
- **แนวแก้**: ขยาย draft ให้ครอบ field แกน (customerId/title/description/notes) ใน key เดียวกัน หรืออย่างน้อยแก้ข้อความ banner ไม่ให้เคลมเกินจริง

## 25. [MINOR] แถวสินค้าเปล่าที่เพิ่งกดเพิ่ม (จำนวน default = 1) ทำให้ฟอร์มสลับเป็นโหมดคิดเงินทันทีแบบมองไม่เห็น — submit ติด validation ของแถวเปล่า และเกิด draft ผีเด้งกลับทุกครั้งที่เปิดหน้า
- **ช่วง**: เปิดงาน/รับออเดอร์ — โหมดเปิดเบา vs แถวสินค้าเปล่า
- **หลักฐาน**: src/types/order-form.ts:82 (EMPTY_VARIANT quantity: 1) + types/order-form.ts:274 (itemHasContent นับ v.quantity > 0 → แถวเปล่าที่เพิ่งเพิ่ม = "มีเนื้อหา" ทันที) → orders/new/page.tsx:142,331-345 บังคับ validate เต็ม ("กรุณาระบุคำอธิบาย/ราคา/แหล่งที่มา") โดย error ไม่บอกทางออกว่าลบแถวทิ้งได้ + use-order-items-form.ts:37 เก็บเป็น draft ด้วยเกณฑ์เดียวกัน → แถวเปล่าค้างใน localStorage เด้ง banner ร่างทุกรอบ
- **ผลกระทบ**: แอดมินกดเพิ่มสินค้าเล่นๆ แล้วตัดสินใจเปิดเป็นการสอบถามแทน → เปิดงานไม่ผ่านโดยไม่เข้าใจว่าทำไม (ฟอร์มบอกว่า "เลือกลูกค้าอย่างเดียวก็เปิดได้") · ปิด tab ไปแล้วกลับมาเจอ "พบข้อมูลร่าง" ทั้งที่ไม่มีเนื้อหาจริง — เป็น friction ในเส้นทาง 15 วินาทีที่ P1 ตั้งใจทำให้เร็ว
- **แนวแก้**: ตัด v.quantity > 0 ออกจาก itemHasContent (จำนวนอย่างเดียวไม่ใช่เนื้อหา) หรือเปลี่ยน EMPTY_VARIANT quantity เป็น 0 — สองเกณฑ์ (ฟอร์ม+draft) จะหายพร้อมกันเพราะแชร์ฟังก์ชันเดียว

## 26. [MINOR] ยืนยันงานที่วางแผนไว้ (quote expiry): validUntil เป็นตัวหนังสือล้วน — EXPIRED ตั้งไม่ได้จาก UI, ไม่เช็คตอนอนุมัติ/แปลง, filter 'หมดอายุ' ว่างตลอดกาล
- **ช่วง**: อายุใบเสนอ (validUntil/EXPIRED)
- **หลักฐาน**: D:/dev/anajak-suite/erp/src/server/routers/quotation.ts:197-241 (updateStatus/convertToOrder ไม่มีการเทียบ validUntil แม้แต่บรรทัดเดียว) · src/app/(dashboard)/quotations/[id]/page.tsx:109-137 (ปุ่ม UI ส่งได้แค่ SENT/ACCEPTED/REJECTED — ไม่มีทางตั้ง EXPIRED) · ไม่มี sweep ใดแตะใบเสนอ (overdue.ts กวาดเฉพาะ invoice) · src/app/(dashboard)/quotations/page.tsx:26 (ตัวกรอง 'หมดอายุ' มีให้เลือกแต่ไม่มีข้อมูลเข้าได้) · src/app/(print)/print/quotation/[id]/page.tsx:52 (เอกสารพิมพ์สัญญา 'ยืนราคาถึง' ที่ระบบไม่ถือตาม)
- **ผลกระทบ**: ใบเสนอค้างหลายเดือนยังกด 'ลูกค้าอนุมัติ → แปลงเป็นออเดอร์' ได้ที่ราคาเดิม ทั้งที่ต้นทุน DTF/ผ้าเปลี่ยนแล้ว — ระบบไม่กั้นและไม่ติดธงอะไรเลย · เหตุที่ให้ MINOR: วันที่ยังโชว์ใน sidebar หน้า detail ([id]/page.tsx:499-505) คนพอเห็นเองได้ และทีมเล็ก 5 คน — เป็นช่องให้พลาด ไม่ใช่กำแพงกั้นงาน · งานนี้อยู่ในแผน next task แล้ว (PROGRESS.md:11) รายงานเพื่อยืนยันว่า scope ที่วางถูกต้อง: ต้องมีทั้ง sweep ตั้ง EXPIRED + ด่านกันแปลงใบเลยวันยืนราคา (หรือ confirm ก่อนแปลง)
- **แนวแก้**: เช็ค validUntil ใน updateStatus(ACCEPTED) + convertToOrder (เกินวัน = ต้อง confirm ยืนราคาใหม่) + sweep รายวันตั้ง EXPIRED (โครง cron จาก overdue.ts ใช้ซ้ำได้)

## 27. [MINOR] เช็คสถานะ ACCEPTED อยู่นอก transaction + updateStatus ไม่ validate ทางเดิน — แปลงซ้ำได้ออเดอร์คู่
- **ช่วง**: convertToOrder — กันกดซ้ำ/ย้อนสถานะ
- **หลักฐาน**: D:/dev/anajak-suite/erp/src/server/routers/quotation.ts:226-233 (อ่าน+เช็ค status ก่อนเข้า $transaction ที่ :254 — ใน tx ไม่ re-check ก่อน set CONVERTED ที่ :300-303 → 2 requests พร้อมกันผ่านด่านทั้งคู่ ได้ 2 ออเดอร์ และ orderId ถูกทับเหลือชี้ใบเดียว) · :197-220 (updateStatus ไม่เช็คสถานะปัจจุบัน — ใบ CONVERTED/REJECTED ถูกดันกลับ ACCEPTED ผ่าน API แล้วแปลงรอบสองได้)
- **ผลกระทบ**: เคสจริงต้องอาศัยสองจอกดพร้อมกันหรือยิง API ตรง (ปุ่มมี confirm dialog + disabled ตอน pending แล้ว) — โอกาสต่ำ แต่ถ้าเกิด = ออเดอร์ CONFIRMED ซ้ำเข้าคิวผลิต+กินวงเงิน โดยใบเสนอชี้กลับได้ใบเดียว อีกใบลอยไร้ที่มา · ต่างจาก transition map ฝั่ง server ที่เพิ่งทำให้ outsource — โมดูลใบเสนอยังไม่มีเกราะแบบเดียวกัน
- **แนวแก้**: ย้ายการเช็ค ACCEPTED เข้าไปใน $transaction (updateMany where {id, status:'ACCEPTED'} แล้วเช็ค count) + ใส่ transition map ให้ quotation.updateStatus ตาม pattern ที่ทำไว้ใน outsource แล้ว

## 28. [MINOR] DESIGNER กดส่งงานเข้าออกแบบเองไม่ได้ (CONFIRMED→DESIGNING) แต่ปุ่มโชว์ให้กดแล้วเด้ง FORBIDDEN — และออเดอร์ CONFIRMED ไม่โผล่ในคิว /my-tasks ของกราฟิก
- **ช่วง**: ออกแบบ/อนุมัติแบบ — handoff เข้าเฟสออกแบบ
- **หลักฐาน**: src/server/routers/order.ts:22 orderOps = OWNER/MANAGER/SALES/PRODUCTION_STAFF (ไม่มี DESIGNER) + :615-616 updateStatus ใช้ orderOps → DESIGNER โดน "คุณไม่มีสิทธิ์เข้าถึงฟีเจอร์นี้" (src/server/trpc.ts:66-70) · การ์ดขั้นถัดไปไม่เช็ค role (src/components/orders/detail/order-next-step.tsx:94-104) → DESIGNER เห็นปุ่ม "ส่งเข้าออกแบบ" กดได้ พังเป็น toast (orders/[id]/page.tsx:96-102) · ที่ CONFIRMED section งานออกแบบถูกซ่อนทั้งก้อน (order-design-section.tsx:103) จึงไม่มีปุ่มอัปโหลดให้ · คิวกราฟิกใน /my-tasks กรองเฉพาะ DESIGNING (src/server/routers/task.ts:64)
- **ผลกระทบ**: กราฟิกที่มีไฟล์พร้อมต้องรอ ขาย/ผู้จัดการ/เจ้าของ มากดปุ่มเดียวบนหน้าออเดอร์ก่อนถึงจะเริ่มงานได้ — ออเดอร์ที่รอเข้าออกแบบมองไม่เห็นจากคิวตัวเอง และปุ่มที่โชว์แล้วกดพังทำให้คิดว่าระบบเสีย (pattern เดียวกับหนี้ข้อ 4 ที่จดไว้สำหรับปุ่มการเงิน แต่จุดนี้ไม่ได้จด) · ทีม 5 คนพอตามตัวกันได้ จึงเป็นความฝืดไม่ใช่ทางตัน
- **แนวแก้**: ทางเลือก ก) เพิ่ม DESIGNER ใน updateStatus เฉพาะ transition CONFIRMED→DESIGNING (whitelist แบบเดียวกับ PRODUCTION_STAFF_STATUSES ที่ :633-646) · ข) อย่างน้อยซ่อนปุ่ม STATUS ในการ์ดขั้นถัดไปสำหรับ role ที่ server ไม่ให้สิทธิ์

## 29. [MINOR] ยกระดับหนี้ข้อ 13 (token regenerate ไม่ได้) เพราะของจริงกว้างกว่าที่จด: UI เสนอปุ่ม copy ลิงก์ที่ตายแล้วโดยไม่มีตัวบอก — รวมลิงก์ที่ถูกฆ่าทันทีทุกครั้งที่อัป v ใหม่ ไม่ใช่แค่หมด 30 วัน
- **ช่วง**: ออกแบบ/อนุมัติแบบ — อายุลิงก์อนุมัติ (ยกระดับหนี้ข้อ 13 บางส่วน)
- **หลักฐาน**: src/server/routers/design.ts:124-127 อัป version ใหม่ตั้ง tokenExpiresAt=now ให้ทุก version PENDING เก่า (ลิงก์ตายทันที ถูกต้องเชิง logic) แต่ src/components/orders/order-design-section.tsx:208-221 ปุ่ม copy ลิงก์ render ทุก version ที่มี approvalToken — component ไม่อ่าน tokenExpiresAt และไม่เช็ค approvalStatus เลย · token เกิดที่เดียวตอน upload (design.ts:136-137) ไม่มี mutation regenerate ในระบบ (grep approvalToken ทั้ง src ยืนยัน) · ลูกค้าที่ได้ลิงก์ตายเจอหน้า "ไม่พบแบบที่ต้องการ" (src/app/approve/design/[token]/page.tsx:46-62)
- **ผลกระทบ**: เคสที่เกิดบ่อยกว่า 30 วันมาก: มีแบบ 3 เวอร์ชันในลิสต์ ขายกด copy ผิดแถว (ทุกแถวหน้าตาเหมือนกัน) ส่งลิงก์ตายให้ลูกค้า → ลูกค้าตอบกลับว่าเปิดไม่ได้ เสียรอบสื่อสาร · ความเจ็บของ "regenerate ไม่ได้" เองอยู่ระดับกลาง เพราะมีทางหนีในระบบจริง 2 ทาง: ขายบันทึกผลแทนผ่าน design.approve ซึ่งไม่เช็ค token expiry (design.ts:183-208) และอัปไฟล์เดิมเป็น version ใหม่ตอนยัง DESIGNING ได้ token ใหม่ — ไม่ใช่ทางตันอย่างที่ความรู้สึกจากโน้ตอาจชวนให้คิด
- **แนวแก้**: ใน order-design-section: โชว์ปุ่ม copy เฉพาะ version ล่าสุดที่ PENDING และ tokenExpiresAt ยังไม่ผ่าน + ป้าย "ลิงก์หมดอายุ" สำหรับที่เหลือ — จุดเดียวจบ ไม่ต้องทำ regenerate ก็ลดอุบัติเหตุได้เกือบหมด

## 30. [MINOR] QC ไม่ผ่าน → ถอยออเดอร์กลับ PRODUCING ได้ แต่งานหายจากคิวผลิตทุกจอ (board + my-tasks)
- **ช่วง**: QC
- **หลักฐาน**: src/server/services/order-status.ts:137-141 (ผลิตครบ auto ปิดใบผลิตเป็น COMPLETED ก่อนเด้งเข้า QC) · พอ QC fail ถอยกลับ PRODUCING: src/server/routers/production.ts:178-180 (board กรอง production COMPLETED ออก) + src/server/routers/task.ts:28-30 (my-tasks เอาเฉพาะ step PENDING/IN_PROGRESS) — ใบผลิตปิดแล้ว step เสร็จหมด = งานชิ้นนี้ไม่โผล่ในคิวไหนเลยทั้งที่สถานะออเดอร์คือ "กำลังผลิต"
- **ผลกระทบ**: งานที่ QC ตีกลับเป็นงานเร่งที่สุดในโรงงาน แต่กลับมองไม่เห็นในคิวผลิต — ต้องพึ่งคนจำแล้วเข้าไปเปิด step ใหม่ในหน้าออเดอร์เอง (updateStep ไม่ล็อก ทำได้ ครบลูป) เสี่ยงงานเงียบหาย ลูกค้าทวงค่อยรู้
- **แนวแก้**: ตอนถอย QUALITY_CHECK→PRODUCING ให้ reopen ใบผลิต (status กลับ IN_PROGRESS) หรือสร้าง step ซ่อมอัตโนมัติ — งานจะกลับเข้า board/my-tasks เองโดยไม่ต้องแก้ query

## 31. [MINOR] ปลดพักออเดอร์ READY_MADE ไป DESIGNING ได้ทั้งที่เส้นทางสำเร็จรูปไม่มีขั้นออกแบบ — เข้าไปแล้วเดินหน้าต่อไม่ได้ (เหลือแค่พัก/ยกเลิก) และลูกค้ากดอนุมัติแบบผ่านลิงก์จะ error
- **ช่วง**: ON_HOLD ปลดพัก (งานสำเร็จรูป)
- **หลักฐาน**: src/lib/order-status.ts:183-185 `ON_HOLD → ["CONFIRMED","DESIGNING","PRODUCTION_QUEUE","CANCELLED"]` เป็น list ตายตัวไม่สน orderType · UI เสนอครบทุกตัวใน dropdown (orders/[id]/page.tsx:122-124,279-293) และ server ยอมเพราะ transitionOrder validate ด้วย list เดียวกัน (src/server/services/order-status.ts:48) · พอ READY_MADE อยู่ DESIGNING: order-status.ts:192-199 `flow.indexOf("DESIGNING")=-1` → ไม่มี forward, getNextStatuses เหลือ [ON_HOLD,CANCELLED] — ปุ่มหลักบน header กลายเป็น "พักงาน" · และ processDesignApproval (services/order-status.ts:177-182) จะ transition DESIGNING→DESIGN_APPROVED ซึ่ง invalid ใน READY_MADE → ลูกค้ากดลิงก์อนุมัติแล้วเจอ error
- **ผลกระทบ**: ผู้ใช้เลือกตัวเลือกที่ UI เสนอเองแล้วงานเข้าซอยตัน — หลุดออกได้แต่ต้องอ้อม (พักอีกรอบ→CONFIRMED) ซึ่งไม่มีใครบอกทาง · โอกาสเกิดต่ำ (ต้องพักงานสำเร็จรูปแล้วเลือกผิด) จึงเป็น MINOR
- **แนวแก้**: กรองรายการปลดพักด้วยเส้นทางจริง: `getNextStatuses(ON_HOLD)` คืนเฉพาะสถานะที่อยู่ใน FLOW_BY_TYPE[orderType] (intersect) — แก้ที่ lib จุดเดียว ทั้ง UI และ server validate ตรงกันทันที

