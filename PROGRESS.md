# PROGRESS — สถานะสด

## ตอนนี้ (2026-09-10 · A15 · แก้ยอดตรวจรับที่นับผิดได้แล้ว · รออนุมัติขึ้น main)
- เบสถาม "ยืนยันแล้วย้อนไม่ได้หรอ" → ไล่โค้ดพบว่าตันจริง: ย้อนขั้นจากใบผลิตไม่ได้ (ขั้นปิดด้วยหลักฐานใบตรวจรับ) และใบคืนเสื้อก็ถูกกันอีกชั้นเมื่อเปิดใบผลิตแล้ว = นับผิดแล้วแก้ไม่ได้เลยจากหน้าจอ
- เบสเคาะ: **ย้อนได้ แต่ต้องแก้ยอดพร้อมกัน** — ทำเสร็จแล้วใน branch `ui/step-commands-proto` **ยังไม่ขึ้น main**
- A13 (เลิกปิดขั้นตรวจรับให้เองตอนเปิดใบผลิต) ขึ้นเว็บจริงไปแล้ววันนี้ — `167c67a`

## สิ่งที่เปลี่ยนรอบนี้ (A15)
- **`correctCustomerGarmentReceipt`**: หัวหน้ากรอกยอดที่ถูกต้องต่อไซซ์ + เหตุผล → ระบบอ่านยอดรับสุทธิสด คำนวณส่วนต่าง แล้ว**ออกใบจริงเสมอ** (นับเกิน = ใบคืน · นับขาด = ใบรับเพิ่ม) ไม่แก้ใบเก่า → สถานะขั้นเดินตามยอด: ครบ = ปิดไว้ · ไม่ครบ = เปิดกลับ IN_PROGRESS → audit เก็บเหตุผล
- `CreateReceiptParams.correction` ให้ข้ามด่านกันคืนเฉพาะคำสั่งแก้ไขนี้ — ใบคืนปกติจากหน้าออเดอร์ยังถูกกันเหมือนเดิม
- router `goodsReceipt.correctCustomerGarment` — สิทธิ์ `supervise_operations` + เหตุผลอย่างน้อย 3 ตัวอักษร
- UI: เมนู ⋯ ของใบผลิตมีข้อ **"แก้ยอดตรวจรับเสื้อ"** (ต้องอยู่ในเมนูเพราะกล่องของขั้นไม่แสดงแล้วเมื่อขั้นปิดไป) เปิดกล่องกรอกยอดที่ถูกต้อง + เหตุผล · ถ้าขั้นตรวจรับยังเป็นขั้นปัจจุบันก็มีปุ่มในกล่องด้วย

## ผลตรวจ
- ด่านขั้นต่ำผ่านครบ: typecheck · lint 0 errors / 23 warnings เดิม · tests 172 files / **1,766 tests** ผ่าน (เพิ่ม 3 ข้อของคำสั่งใหม่) · verify:ui ผ่านครบ (work-order 34 / 0 ตก)
- **ทดสอบของจริงบนฐานทดลอง ORD-2609-0017**: แก้ S จาก 15 เหลือ 12 พร้อมเหตุผล → ระบบออกใบคืน 3 ตัวพร้อมหมายเหตุ "แก้ยอดตรวจรับ: นับซ้ำแล้วขาดจริง 3 ตัว" · ขั้นตรวจรับเปิดกลับเป็น IN_PROGRESS · หน้ากลับมาให้รับต่ออีก 3 ตัว
- **บั๊กที่เจอระหว่างทดสอบและแก้แล้ว**: ค่าเริ่มต้นของช่อง "ยอดที่ถูกต้อง" เคยคำนวณจาก (แผน − ยอดค้าง) ทำให้ไซซ์ที่เคย*รับเกิน*ถูกคืนโดยไม่ตั้งใจ → เปลี่ยนไปใช้ยอดรับสุทธิจริง
- ไม่แตะ schema/migration/dependency/env

## หนี้ที่เหลือของ A15
- test ของ service เอง (ส่วนต่างขึ้น/ลง · เปิดขั้นกลับ · กดซ้ำ) ยังไม่มี — ต้องใช้ harness DB แบบ `goods-receipt-concurrency.test.ts` · ตอนนี้มีแต่ test ผิว router
- ออเดอร์ที่ผูก Production V2 (`productionCompletionOwnerId`) ยังบันทึก/แก้จากใบผลิตแบบเดิมไม่ได้ ขึ้น "งานนี้ต้องบันทึกการรับของจากงานปัจจุบันในโหมดสถานี" — กติกาเดิม ไม่ใช่ของใหม่ (เจอที่ ORD-2609-0004)

## ฐานทดลองที่เปลี่ยนจากการทดสอบวันนี้
- ORD-2609-0017: มีใบตรวจรับหลายใบ + ใบคืน 1 ใบจากการทดสอบแก้ยอด · ขั้นตรวจรับเปิดค้างอยู่ 3 ตัว (รีเซ็ตด้วย `npm run db:seed:demo`)

## ค้างจากงานก่อนหน้า (ยังเปิดอยู่)
- A12 หน้าลอง `/proto/production-flow` โครงเดิม — รอเบสลองใช้แล้วสั่งปรับรายละเอียด
- A9 หนี้ 5 ข้อเดิม · Feature A2–A8/B/C/F ตาม ROADMAP ยังเปิด

## NEXT
1. เบสลองกดจริง: ในใบผลิตกด "เพิ่มเติม → แก้ยอดตรวจรับเสื้อ" กรอกยอดที่ถูกต้องแล้วดูว่าขั้นเปิดกลับให้รับต่อ
2. โอเคแล้วสั่ง "push main" เพื่อขึ้นเว็บจริง (รอบนี้มีของสะสม 3 ก้อน: ปุ่มอยู่ในกล่อง · ตรวจรับไม่ต้องมี popup · แก้ยอดที่นับผิด)

## สภาพแวดล้อม
- `npm run dev:demo` ที่ port 3000 ต่อฐานทดลอง local (Docker `anajak-postgres` · ฐาน `anajak_erp_demo`) — dev สองตัวพร้อมกันไม่ได้
- ดูหน้าจอผ่านแท็บ Chrome ที่ล็อกอินไว้ · ไม่ล้าง cookie/cache และไม่ bypass auth
