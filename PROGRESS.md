# PROGRESS — สถานะสด

## ตอนนี้ (2026-09-08)
- **เบสสั่งทำใบผลิตใหม่ให้เหมือนหน้าออเดอร์** (แถบสถานะบน · CTA บน · กดถึงไปขั้นถัดไป · ย้อนได้) → ทำหน้าลอง `/proto/work-order-form` ให้เคาะก่อน **ยังไม่แตะหน้าจริง** (ใบงาน ROADMAP §A9)
- โครงหน้าลอง: หัวใบ `PageHeader` + ราง 1 2 3 `OrderStatusBar` + `Tabs` ตัวจริงของหน้าออเดอร์ · แท็บ ขั้นงาน (ฟอร์มของช่องที่ยืนอยู่ + ตารางทุกขั้น) / ลายและเสื้อ / ข้อมูลใบ / ประวัติ · เขียนเองเฉพาะฟอร์มกับตาราง
- 4 ทาง: ปัจจุบัน (ลิงก์ไปหน้าจริง `/production/demo-production-outsource-overdue` — `X-Frame-Options: DENY` ใน next.config ทำให้ฝัง iframe ไม่ได้ จึงเปิดแท็บใหม่/หน้าต่าง 390 แทน) · **A** ทีละขั้นตามเลข · **B** ราง = ด่าน (ขั้นพร้อมกันรวมช่องเดียว ปุ่มบน = ปิดด่าน) · **C** ราง = เฉพาะจุดที่ระบบจด (ตาม A5 กระดาษเป็นหลัก)
- เครื่องยนต์จำลอง `_engine.ts` (pure): ต่างกันเฉพาะ `stagesFor` (รางนับอะไรเป็นช่อง) · ปุ่มหลักตามกติกาเดิม (เริ่ม/ปิดขั้น/ส่งร้าน/รับกลับ/เบิก/ตรวจรับ/ส่งเข้า QC/จัดการปัญหา) · ย้อนกลับ = เปิดขั้นล่าสุดที่ปิดใหม่ (เมนู ⋯ หัวหน้าเท่านั้น) · ข้อมูลปลอม 2 ใบ: ORD-2609-0009 (4 ขั้น ใบที่เบสเปิดดูจริง) และ ORD-2608-0061 (7 ขั้น DTF + ร้านนอก 2 ขั้น ขนาน + ขั้นกระดาษ)
- กู้โครง `/proto` กลับมาจาก git (`layout.tsx` noindex · `page.tsx` หน้ารวม · `_kit/use-proto-variant.ts`) · ทะเบียน `_registry.ts` เริ่มใหม่มีรายการเดียว
- ตรวจแล้ว: typecheck · lint 0 error · unit 164 ไฟล์ / 1,666 tests · verify:ui ผ่าน (แก้ 2 รอบ: `TABLE_HEAD_SURFACE` บนหัวตาราง + เลิกต่อข้อมูล ≥3 อย่างด้วยจุด → `InfoChipRow`) · เปิดดูใน Chrome ทั้ง A/B/C ใบ 4 และ 7 ขั้น · กด CTA เดินขั้นและย้อนกลับได้จริง · โหมดมืดผ่าน · จอ 375 ผ่าน (เรนเดอร์ HTML นิ่งชั่วคราวใน public/upload แล้วลบแล้ว — Chrome ย่อหน้าต่างไม่ได้)
- **เบสเคาะ A แล้ว (09-08 "เอา A")** + สั่ง "ทำช่องคู่ให้ดูในหน้าลองด้วย" → เพิ่มปุ่ม "ช่องคู่: เปิด/ปิด" บนแบบ A (`?pair=1` ค่าเริ่มต้นเปิด): ขั้นที่สูตรตั้ง `pairWithPrevious` รวมเข้าช่องก่อนหน้า (ใบ 7 ขั้น: ฟิล์ม DTF + ปักแขน = ช่อง 2) · ปุ่มบน = ขั้นที่กดได้ก่อน อีกขั้นปุ่มเล็กในฟอร์ม · ครบคู่รางเลื่อนเอง · เปิดดูใน Chrome แล้วทั้งเปิด/ปิดช่องคู่ · verify:ui/typecheck/lint ผ่าน
- **ข้อจำกัดที่ต้องบอกเบสตอนเคาะ:** "ย้อนได้" ของจริง server ไม่รับ (`production.updateStep` ปฏิเสธขั้น COMPLETED) ต้องเพิ่มคำสั่ง reopen + audit แยกต่างหาก ⚠️ ถามก่อน · ทาง A ทำให้ขั้นกระดาษ (A5) ต้องกดปิดในระบบทุกขั้น

## NEXT
1. **รอเบสดู "ช่องคู่"** ที่ `/proto/work-order-form?v=seq&big=1&pair=1` (สลับ pair=0 เทียบ) แล้วสั่งลงจริง — พร้อมยืนยัน 2 เรื่อง: ยอมให้ขั้นกระดาษมีปุ่มปิด (ถอยจาก A5 บางส่วน) · ช่องคู่ลงรอบแรกเลยหรือรอทีหลัง (ต้องเพิ่มช่องตั้งค่าในสูตร = schema)
2. หลังเคาะ → ลงของจริง `work-order-page.tsx` บน `work-order-controller.tsx` เดิม (ไม่แตะสิทธิ์/mutation) · เปิดใบงาน server "reopen ขั้นที่ปิดแล้ว" แยก ขอเบสก่อน
3. งานค้างเดิม: CI แดงจากบั๊กเขตเวลา `production-desk.test.ts` (TZ=UTC ตก 2) ยังไม่แก้ · §A2–A8/B/C/F ตาม ROADMAP

## บริบทที่ยังต้องรักษา
- กระดาษเป็นหลัก (A5): จดในระบบเฉพาะ เบิก/รับเสื้อ · ร้านนอก · ส่งเข้า QC · DTF ผ่านเองจากรอบพิมพ์ · ที่เหลือกระดาษ
- หนึ่งโมดูลสองสายตา (A4): หัวหน้าทำครบจากใบผลิต ช่างไป `/production/floor` · ห้ามถอดปุ่มลงมือจากใบผลิตอีก · Station/TV ไม่มีเงิน
- รางสถานะอ่านอย่างเดียว การเดินอยู่ที่ปุ่มบนหัวใบที่เดียว (กติกาหน้าออเดอร์ 08-11) · ห้าม truncate ป้ายไทย
- Production V2 บนเว็บจริงยัง legacy · ไม่เปลี่ยน flag

## สภาพแวดล้อม
- Port 3000 = `npm run dev:demo` (ฐาน `127.0.0.1:5433/anajak_erp_demo`) · Chrome (session เบส) เปิด `/proto/work-order-form` ได้เลย · in-app Browser ไม่มี session จึงเห็นแค่ /login
- branch `proto/work-order-form` — ยังไม่ merge main · ไฟล์ใหม่ทั้งหมดอยู่ `src/app/proto/`
