# PROGRESS — สถานะสด

## ตอนนี้ (2026-09-10 · A14.3 · ตรวจรับเสื้อทำในกล่องได้แล้ว · รออนุมัติขึ้น main)
- เบสสั่งรอบ 3: "ขอแบบไม่ต้องมี popup เวลากดบันทึกตรวจรับเสื้อลูกค้า รู้สึกมันจะไม่ต่อเนื่อง และมันควรจะบันทึกได้ด้วย เพราะบางทีลูกค้าส่งมาไม่ครบ · รวมทั้งใบพื้นหลังไม่ต้องสีเทา · คอลัมจำนวนให้เลขมันตรงกัน"
- ทำครบทั้ง 3 ข้อแล้วใน branch `ui/step-commands-proto` **ยังไม่ขึ้น main**
- A13 (เลิกปิดขั้นตรวจรับเสื้อให้เองตอนเปิดใบผลิต) ขึ้นเว็บจริงไปแล้ววันนี้ — `167c67a` · Vercel production READY

## สิ่งที่เปลี่ยนรอบนี้
- **ตรวจรับเสื้อลูกค้าทำในกล่องขั้นเลย** (`garment-receive-inline.tsx`): ตารางนับจริงต่อไซซ์ — ยังไม่ได้รับ · นับได้ · ตำหนิ — พร้อมปุ่มบันทึกในกล่อง และปุ่มพับ "เพิ่มรูป/หมายเหตุ" · ค่าเริ่มต้นของช่อง "นับได้" = ยอดที่ยังค้าง นับแล้วตรงก็กดบันทึกได้เลย
- ใช้บริการชุดเดิมทุกอย่าง (`goodsReceipt.context` / `create` / `confirmCustomerGarmentEvidence`) — ยอดต่อไซซ์ ตำหนิ รูป หมายเหตุ สิทธิ์ และกันกดซ้ำ เหมือน dialog เดิมทุกประการ · กล่องใบตรวจรับเดิมยังใช้จากหน้าออเดอร์และจอสถานีตามเดิม
- `StepPieceTable.replaceBody` สลับเนื้อกล่องได้โดยคงหัวการ์ดและแถบปุ่มมาตรฐาน · `actionFor` ไม่คืนปุ่มเปิด dialog ให้ขั้นนี้แล้ว
- แถว "รวมทั้งใบ" เอาพื้นเทาออก และเปลี่ยนตัวเลขเป็นข้อความล้วนไม่มีหน่วยต่อท้าย → ตรงคอลัมน์กับตัวเลขในตารางเป๊ะ (ทั้งตารางขั้นทั่วไปและตารางตรวจรับ)

## ผลตรวจ
- ด่านขั้นต่ำผ่านครบ: typecheck ผ่าน · lint 0 errors / 23 warnings เดิม · tests 172 files / 1,763 tests ผ่าน · verify:ui ผ่านครบ (work-order 34 ผ่าน / 0 ตก)
- ด่าน UI จับได้ 1 ข้อระหว่างทาง (หัวตารางใหม่ไม่ได้ใช้ `TABLE_HEAD_SURFACE` กลาง) → แก้แล้ว
- **ทดสอบบันทึกจริงบนฐานทดลอง** ที่ ORD-2609-0017: กรอก S นับได้ 10 จาก 15 แล้วกดบันทึก → ใบตรวจรับถูกสร้าง (S 10/15 · M 24/24 · L 21/21) · ยอดค้างเหลือ 5 · ขั้นเป็น IN_PROGRESS ไม่ปิดให้ = "รับไม่ครบก็บันทึกได้" ทำงานจริง
- **ข้อมูลทดลองเปลี่ยนไป 1 ใบ**: ORD-2609-0017 มีใบตรวจรับ 55 ตัวจากการทดสอบนี้ (รีเซ็ตด้วย `npm run db:seed:demo` ได้)
- ไม่แตะ schema/migration/dependency/env · ไม่แตะ service ฝั่ง server

## ข้อจำกัดที่เจอ (กติกาเดิมของ server ไม่ใช่ของใหม่)
- ออเดอร์ที่ตั้ง `productionCompletionOwnerId` (ผูกกับ Production V2) บันทึกใบตรวจรับจากใบผลิตแบบเดิมไม่ได้ ขึ้นข้อความ "งานนี้ต้องบันทึกการรับของจากงานปัจจุบันในโหมดสถานี" — เหมือนกับตอนใช้ popup เดิมทุกประการ (เจอที่ ORD-2609-0004 ในฐานทดลอง) · ถ้าจะให้ใบแบบนี้บันทึกได้ต้องทำงานฝั่ง V2 แยก ⚠️ ยังไม่ทำ

## ค้างจากงานก่อนหน้า (ยังเปิดอยู่)
- A12 หน้าลอง `/proto/production-flow` โครงเดิม — รอเบสลองใช้แล้วสั่งปรับรายละเอียด
- A9 หนี้ 5 ข้อเดิม (ฐานทดลอง V2 · ทางลัด `sendToQc` · ขั้นที่ปิดผ่าน flow อื่นไม่ผ่านด่านติ๊ก · ข้อกำหนดยังเป็นตารางนิ่ง · `work-order-route.tsx` ยังไม่ถูกใช้)
- Feature A2–A8/B/C/F ตาม ROADMAP ยังเปิด

## NEXT
1. เบสลองกดจริงในฐานทดลอง (ใบที่ลองได้: ORD-2609-0017 · ORD-2609-0022) — กรอกยอดแล้วกดบันทึกในกล่องได้เลย ไม่มี popup
2. โอเคแล้วสั่ง "push main" เพื่อขึ้นเว็บจริง
3. ถ้าอยากให้ใบที่ผูก Production V2 (เช่น ORD-2609-0004) บันทึกได้ด้วย บอกได้ — เป็นงานฝั่ง V2 แยกอีกใบ

## สภาพแวดล้อม
- `npm run dev:demo` ที่ port 3000 ต่อฐานทดลอง local (Docker `anajak-postgres` · ฐาน `anajak_erp_demo`) — dev สองตัวพร้อมกันไม่ได้
- ดูหน้าจอผ่านแท็บ Chrome ที่ล็อกอินไว้ (Claude in Chrome) · ไม่ล้าง cookie/cache และไม่ bypass auth
