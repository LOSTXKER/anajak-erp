# PROGRESS — สถานะสด

## ตอนนี้ (2026-09-10 · A14 · เมนูจัดใหม่ลงจริงแล้ว · หน้าลองรอเบสเคาะ)
- เบสส่งภาพเมนู ⋯ ในใบผลิตมาถาม "บางอันมันจำเป็นต้องใช้ แต่ก็ไปซ่อน หรือจะปรับ UI ยังไง"
- ไล่แล้วพบรากปัญหา: ใบผลิตเป็น**หน้าของหัวหน้า** (ช่างถูกส่งไป `/production/floor`) → "แจ้งปัญหา" กับ "มอบหมาย/แก้ให้" คืองานประจำของคนเปิดหน้านี้ แต่ถูกซ่อนหลัง ⋯ ทั้งคู่ ขัดเจตนา `MoreMenu` ที่ตั้งไว้ว่าเก็บ "คำสั่งที่นาน ๆ ใช้"
- เบสเคาะ: **จัดเมนูใหม่เลย + ทำหน้าลองทางที่ย้ายคำสั่งไปอยู่กับขั้นให้ดู** → ทำครบทั้งคู่แล้วใน branch `ui/step-commands-proto`
- ก่อนหน้านี้วันเดียวกัน: A13 (เลิกปิดขั้นตรวจรับเสื้อให้เองตอนเปิดใบผลิต) รวม main และขึ้นเว็บจริงแล้ว — commit `167c67a` · Vercel production READY

## สิ่งที่เปลี่ยนรอบนี้
- **เมนู ⋯ ในใบผลิต (ของจริง)**: เรียงตามความถี่ (แจ้งปัญหา → มอบหมาย/แก้ให้ → พักขั้น → ย้อนกลับ) · ขีดคั่นแยก "คำสั่งกับขั้นนี้" ออกจาก "ประวัติออเดอร์" · แดงสงวนให้ย้อนกลับอย่างเดียว (พักขั้นเลิกได้จากเมนูเดิม) · ตำแหน่งคงที่ทุกสถานะ ของที่กดไม่ได้ยัง disabled + บอกเหตุ ไม่ย้ายที่ ไม่ซ่อน
- `MoreMenuItem.separatorBefore` เพิ่มใหม่ (optional — ที่อื่นที่ใช้ MoreMenu ไม่กระทบ)
- **หน้าลอง `/proto/step-commands`** 3 ทาง: ปัจจุบัน (ทุกคำสั่งในเมนู ⋯) · A คำสั่งย้ายลงใต้การ์ดขั้นทั้งหมด · B แยกครึ่ง (แจ้งปัญหา+มอบหมาย อยู่กับขั้น) · ใช้ `WorkOrderView` ตัวจริง + fixtures ของ `/proto/work-order-states` · สลับ 8 สถานะ × หัวหน้า/ช่าง × ธีม × ปุ่ม 390 · แต่ละทางมีบรรทัด "ได้/แลกกับ" อยู่หัวจอ
- แตะไฟล์จริงแบบ additive เท่านั้น: `WorkOrderView` รับ `commands` (default `"header"` = ของเดิมเป๊ะ) · `WorkOrderSteps` รับ `stepCommands` (ของจริงไม่ส่ง) — เคาะแล้วเปลี่ยน default ได้เลย ไม่ต้องเขียนใหม่

## ข้อแลกที่ต้องให้เบสเห็นก่อนเคาะ
- ทาง A/B วางได้เฉพาะคำสั่งที่**กดได้ตอนนั้น** ตามกติกา `docs/DESIGN.md` "ห้ามวางปุ่มที่กดไม่ได้" → คำสั่งที่ยังทำไม่ได้หายไปเลย ต่างจากเมนูที่โชว์ disabled พร้อมเหตุผล (เห็นชัดที่สถานะ "รอเริ่มขั้นแรก" ซึ่งย้อนกลับยังกดไม่ได้)
- ทาง A ทำให้การ์ดขั้นยาวขึ้น · ทาง B ทำให้คำสั่งของขั้นเดียวกันอยู่ 2 ที่

## ผลตรวจ
- ด่านขั้นต่ำผ่านครบ: typecheck ผ่าน · lint 0 errors / 23 warnings เดิม · tests 172 files / 1,763 tests ผ่าน · verify:ui ผ่าน (dots 32 จาก baseline 35 · muted 243 จาก 254 · work-order 34 ผ่าน / 0 ตก)
- เปิดดูเองบนฐานทดลอง: เมนูใหม่เรียงถูก มีเส้นคั่น พักขั้นไม่แดงแล้ว · หน้าลองครบ 3 ทาง (A มีปุ่มใต้การ์ด · B เหลือ 2 ปุ่ม · A เมนูเหลือแค่ประวัติออเดอร์) · ธีมมืดอ่านออก
- **ยังไม่ได้ตรวจจอ 390 ด้วยตัวเอง** — หน้าต่าง Chrome ที่ใช้ย่อไม่ลง (resize ไม่มีผลกับหน้าต่างเต็มจอ) · หน้าลองมีปุ่ม "390" เปิดหน้าต่างมือถือให้กดดูเองได้ · แถวคำสั่งใหม่ใช้ flex-wrap จึงตัดบรรทัดเองเมื่อจอแคบ
- ไม่แตะ schema/migration/dependency/env · หน้าลองใช้ข้อมูลจำลอง ไม่ยิงฐานข้อมูล

## ค้างจากงานก่อนหน้า (ยังเปิดอยู่)
- A12 หน้าลอง `/proto/production-flow` โครงเดิม — รอเบสลองใช้แล้วสั่งปรับรายละเอียด
- A9 หนี้ 5 ข้อเดิม (ฐานทดลอง V2 · ทางลัด `sendToQc` · ขั้นที่ปิดผ่าน flow อื่นไม่ผ่านด่านติ๊ก · ข้อกำหนดยังเป็นตารางนิ่ง · `work-order-route.tsx` ยังไม่ถูกใช้)
- Feature A2–A8/B/C/F ตาม ROADMAP ยังเปิด

## NEXT
1. เบสเปิด `/proto/step-commands` กดสลับ 3 ทาง (ลองที่สถานะ "รอเริ่มขั้นแรก" กับ "ปิดขั้นแรกแล้ว" จะเห็นความต่างชัดสุด) แล้วบอกว่าเอาทางไหน
2. เคาะแล้วเปลี่ยน default ของ `commands` + ลบทางที่ไม่เอา + อัปเดตทะเบียน proto เป็น "เคาะแล้ว"
3. เมนูที่จัดใหม่ยังอยู่ใน branch — เบสสั่งเมื่อไรถึงรวม main

## สภาพแวดล้อม
- `npm run dev:demo` ที่ port 3000 ต่อฐานทดลอง local (Docker `anajak-postgres` · ฐาน `anajak_erp_demo`) — dev สองตัวพร้อมกันไม่ได้
- ดูหน้าจอผ่านแท็บ Chrome ที่ล็อกอินไว้ (Claude in Chrome) · ไม่ล้าง cookie/cache และไม่ bypass auth · คืนธีมสว่างหลังตรวจธีมมืดแล้ว
