# PROGRESS — สถานะสด

## ตอนนี้ (2026-09-07)
- **ล่าสุด: เบสปฏิเสธรวมถึง wizard ว่ายังรก/ใช้งานยาก → รื้อใหม่ `/proto/work-order-reset` แบบ ERP.** A ภาพรวมใบผลิต (แนะนำ): หัวใบสั้น แท็บ ภาพรวม / ขั้นงาน / สินค้าและแบบ / ข้อมูลใบ; หน้าแรกเน้นงานที่ต้องจัดการและปุ่มหลักเดียว รายละเอียดเปิดเมื่อเลือก. B โต๊ะงาน: รายการขั้นซ้าย รายละเอียดขวา; มือถือกดกลับรายการเมื่อต้องสลับงาน.
- สินค้า/ไซซ์/ลายครบทุกชุดอยู่ในแท็บและกดภาพขยายได้; ข้อกำหนดเปิดจากรายละเอียดงาน; ข้อมูลใบและประวัติที่มีหลักฐานอยู่แท็บท้าย. เมนูใบมีตัวอย่างเอกสารพิมพ์. ไม่มีกล่องหุบพับ. ฟอร์มรับกลับ/ปัญหา/แก้ปัญหา/มอบหมาย/บันทึกผลงานทดลองกรอก ตรวจช่อง และแสดงผลจำลองได้โดยไม่เปลี่ยนสถานะจริง.
- ตรวจ Chrome ที่เข้าสู่ ERP: A/B 1280×800 / 390×844 สว่าง/มืด และ 768×1024 ไม่ล้น. ตรวจรับของต้องเลือกผลและอธิบายปัญหา; กลับมาสถานะเดิม; menu → dialog → Escape คืน focus; เปิดสินค้าแล้วกลับมาขั้นเดิม; รูปครบ; มุมช่าง/ว่างถูกต้อง. รีดร้อนยังทำได้ 180/240 โดยไม่แสดงยอดสดกระดาษ; QC ยังรอทั้งสามสาย. IAB session หมดอายุจึงใช้ Chrome เดิม ไม่แก้ auth/env.
- ด่านรอบ reset: typecheck/lint/verify:ui ผ่าน (0 errors, 24 warnings เดิม, work-order 33/33); unit 164 files / 1,666 ผ่านด้วย DATABASE_URL dummy; detector `[]`; diff check ผ่าน. แก้ Radix description warning แล้วตรวจใหม่ไม่มี error/warn ของแอป (มี warnings จาก extension นอกแอป). ผู้ตรวจอิสระตรวจโค้ด+ภาพแล้วไม่พบ blocker สำหรับส่งหน้าลอง.
- `/proto/work-order-desk` รอบก่อนถูกทำเครื่องหมาย **พับ** หลังเบสปฏิเสธ; A/B/wizard ที่บรรยายด้านล่างเป็นประวัติก่อนรอบ reset. Production/server/สิทธิ์/schema/main ไม่เปลี่ยน. ทิศใหม่อิง SAP Fiori Object Page + Odoo Shop Floor แต่การจดกระดาษเป็นกติกาเฉพาะของ Anajak.
- เบสเปิด `/production/demo-production-outsource-overdue` ขอรื้อ UX/UI เพราะยังเยอะ → หน้าลองใหม่ `/proto/work-order-desk` บน branch `codex/work-order-desk-2026-09-07` (ต่อจาก `proto/work-order-lean-2026-09-07`, base `b5a7814`).
- **เบสขอแนว wizard เพิ่ม → C ทีละขั้นเป็นค่าเริ่มต้น** ที่ `/proto/work-order-desk?v=wizard`: เปิดขั้นที่ต้องดูอัตโนมัติ แสดงรายละเอียดครั้งละขั้น เลือกขั้นหรือกดก่อน/ถัดไปได้โดยไม่เปลี่ยนสถานะ. คอมมีแถบขั้นงาน มือถือใช้ช่องเลือก; งานสายอื่นที่ค้างและปัญหายังมีทางลัดให้ดู.
- เบสอยู่นอกบ้าน → ตรวจ Preview ของ `cb5eb3d` เป็น READY ที่ `https://anajak-b9cjrai86-lostxkers-projects.vercel.app`; สร้างลิงก์แชร์เฉพาะ deployment อายุ 7 วัน (หมดอายุ 14 ก.ย.). Browser เข้าถึงหน้า login ERP และเก็บปลายทาง wizard ถูกต้อง; ยังไม่ได้ตรวจ interaction หลัง login บน Preview. ลิงก์แชร์ส่งในบทสนทนา ไม่เก็บ token ใน repo; หน้าจริงไม่เปลี่ยน.
- ยังเทียบ **ปัจจุบัน / A ใบงานต่อเนื่อง / B ตารางคุมงาน / C ทีละขั้น** ได้. A/B ไม่มีกล่องหุบพับ; A เรียงงานที่ต้องดู → ถัดไป → ผ่านแล้ว; B เรียงทุกขั้นในตารางเดียว. เสื้อ/ลาย/ไซซ์ครบทุกชุดอยู่ด้านข้างบนคอม ต่อท้ายบนมือถือ.
- ใช้ข้อมูลตัวอย่างจากหน้าลองเดิม ไม่ต่อฐานข้อมูล; ปุ่มและเมนูเปิด dialog จำลองพร้อมแจ้งว่าไม่บันทึก. ปุ่มรับของกลับยังเป็นข้อเสนอ ต้องเชื่อม flow เดิมก่อนใช้จริง. C คงการทำงานกระดาษโดยไม่มีปุ่มจบขั้นหรือเวลาติดตามสด; รีดร้อนยังทำ 180 ตัวที่พร้อมได้แม้เสื้อขาด 60 ตัว.
- **หน้าจริง `work-order-page.tsx`, server, สิทธิ์, schema ไม่เปลี่ยน. ยังไม่ได้เลือกแบบใหม่หรือ merge main.** หน้าลองรอบก่อน `/proto/work-order-lean` ยังอยู่ครบ; รอบนี้รื้อโครงมากกว่าการตัดชิปหรือย้ายกล่อง.
- ตรวจ browser จริง: คอม 1280×720 / มือถือ 390×844, C สว่างใบร้านนอกเลยนัด + มืดใบ 7 ขั้น 4 สาย; ไม่มีแนวนอนล้น. เลือกขั้นบนมือถือและก่อน/ถัดไปทำงาน, กลับมาขั้นร้านนอกยังเลยนัดเดิม, QC ยังแสดงงานที่ต้องรอ, ขั้นผ่านแล้วไม่มีคำสั่งทำงาน, มุมช่างไม่มีคำสั่งหัวหน้า, สถานะไม่มีขั้นงานถูกต้อง. A/B และ dialog Escape/focus ผ่านในรอบก่อน.
- ด่าน: `typecheck` ผ่าน, lint **0 errors / 24 warnings เดิม**, unit **164 files / 1,666 tests ผ่าน** (ใช้ DATABASE_URL dummy เหมือน CI เพราะ shell ไม่มีค่านี้; ไม่ต่อฐานจริง), `verify:ui` ผ่านรวม work-order 33 ข้อ, Impeccable detector `[]`, `git diff --check` ผ่าน.
- Impeccable detector C `[]`; ผู้ตรวจอิสระดูโค้ด ภาพ 4 แบบ และหลักฐาน browser: **ไม่พบ blocker สำหรับส่งหน้าลอง C ให้เลือก**. กติกางานสายขนานและงานกระดาษผ่านตรวจแยก; A/B ผ่าน finish review รอบก่อน. ไม่ได้รัน build/deploy เพราะรอบนี้ส่งเฉพาะ branch หน้าลอง.

## NEXT
1. **ส่ง Preview ใหม่ `/proto/work-order-reset/view?v=record` → เบสลอง A ภาพรวมใบผลิต** ตามทิศล่าสุด; B โต๊ะงานให้เทียบ. สินค้าและประวัติเปิดตามแท็บ แลกกับการเปลี่ยนหมวดเมื่อต้องการรายละเอียด. หน้าลองยังไม่ใช่การอนุมัติ Production.
2. เคาะแล้ว → ปรับ SPEC ตามแบบที่เลือก → ลง presentation ใน `/production/[id]` โดยใช้ controller/dialog/readiness/record-mode/สิทธิ์เดิม. ตรวจ flow ร้านนอกและการรับกลับก่อนต่อปุ่มที่เสนอ; อย่าอ้างว่าปุ่มจำลองเป็นความสามารถที่ใช้งานได้แล้ว.
3. ตรวจจอจริงด้วยข้อมูลหลายชุด/สายขนานและมือถือ → ด่านครบ + build ก่อนขอ merge main. ไม่มีคำสั่งให้ขึ้น Production ในรอบนี้.
4. งานอื่นตาม ROADMAP §A2–A8/B/C/F: QC/แพ็กหน้างาน, ซ้อม Production V2 ต่อหลังจอพร้อม, หนี้ทางเข้ารอบพิมพ์/วัตถุดิบ/ประวัติละเอียด.

## บริบทที่ยังต้องรักษา
- ของจริงใบผลิตล่าสุดเป็นแบบ E ที่เคาะ 09-06: แผนที่สายขนาน + ตอนนี้ทำได้ + ข้อมูลประกอบพับท้าย. เบสไม่ชอบกล่องหุบพับ และล่าสุดขอ wizard แสดงทีละขั้น; จึงเพิ่ม C ในหน้าลองใหม่ ยังไม่เคาะลงจริง.
- แท็บรายการ/ฟอร์มสินค้า/ส่วนเสริม/การลดคำอธิบายทั้งเว็บ: session ก่อนหน้าบันทึกว่า merge main แล้ว 09-07; รอบนี้ไม่ตรวจ deployment ซ้ำ. สถานะ/ข้อแลกละเอียดอยู่ ROADMAP §A7–A8.
- กระดาษเป็นหลัก: จุดจด screen / paper / auto ตาม `lib/work-order-record-mode.ts`; ห้ามเปลี่ยนเป็นต้องติ๊กทุกขั้น. Checklist ปัจจุบันอ่านอย่างเดียว ไม่มีผลติ๊กในฐาน.
- หัวหน้าทำครบจากใบผลิต; ช่างมี `/production/floor`; ใช้เครื่องยนต์และสิทธิ์ชุดเดียวกัน. Station/TV ไม่มีเงิน.
- ทำเองเฉพาะ DTF; งานร้านนอกเดินขนานได้; สูตรที่ RELEASED ต้องคัดลอกเป็นร่างใหม่จึงแก้ได้.
- Production V2 บนเว็บจริงยัง legacy ตามบันทึกเดิม (`PRODUCTION_V2_ENABLED=0`); migration/seed/demo rehearsal ไม่ใช่หลักฐานว่าเปิดใช้แล้ว.
- เบสเคาะแล้ว: จด VAT, ร้านนอกสื่อสาร LINE, นักบัญชีใช้ PEAK. ใครถือ role ACCOUNTANT ยังไม่ชัด; อย่าปรับสิทธิ์เงียบ.

## สภาพแวดล้อม
- Port 3000 ยืนยันว่าเสิร์ฟ checkout `/Users/lostxker/dev/Git/anajak-erp` และเปิด demo route ได้ในรอบนี้. ไม่แก้ env, ไม่ restart server, ไม่ seed/reset.
- ฐานทดลองเดิม `127.0.0.1:5433/anajak_erp_demo`; `npm run dev:demo` / Docker `anajak-postgres`. ฐานจริง Supabase ห้าม reset หรือรัน integration ที่สร้างข้อมูลโดยไม่มี target/backup/คำสั่งชัด.
- เริ่ม session อ่าน ROADMAP/SPEC และ git สด. อ่าน ARCHITECTURE ก่อนเพิ่มโค้ด; docs/DESIGN เป็นกติกา UI. `/proto` มี noindex และทะเบียนกลาง.
