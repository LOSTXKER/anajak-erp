# PROGRESS — สถานะสด

## ตอนนี้ (2026-09-07)
- เบสเปิด `/production/demo-production-outsource-overdue` ขอรื้อ UX/UI เพราะยังเยอะ → หน้าลองใหม่ `/proto/work-order-desk` บน branch `codex/work-order-desk-2026-09-07` (ต่อจาก `proto/work-order-lean-2026-09-07`, base `b5a7814`).
- เทียบ **ปัจจุบัน / A ใบงานต่อเนื่อง / B ตารางคุมงาน**: แต่ละขั้นมีที่เดียว ไม่มีแผนที่หรือประวัติซ้ำ และ A/B ไม่มีกล่องหุบพับ. A เรียงงานที่ต้องดู → ถัดไป → ผ่านแล้ว; B เรียงทุกขั้นในตารางเดียว. เสื้อ/ลาย/ไซซ์ครบทุกชุดอยู่ด้านข้างบนคอม ต่อท้ายบนมือถือ.
- ใช้ข้อมูลตัวอย่างจากหน้าลองเดิม ไม่ต่อฐานข้อมูล; ปุ่มและเมนูของ A/B เปิด dialog จำลองพร้อมแจ้งว่าไม่บันทึก. ปุ่มรับของกลับยังเป็นข้อเสนอ ต้องเชื่อม flow เดิมก่อนใช้จริง. ส่วนตรวจรับเสื้อ/เบิกวัตถุดิบแสดงเป็นสรุปและเปิดเผยบนหน้าลอง.
- **หน้าจริง `work-order-page.tsx`, server, สิทธิ์, schema ไม่เปลี่ยน. ยังไม่ได้เลือกแบบใหม่หรือ merge main.** หน้าลองรอบก่อน `/proto/work-order-lean` ยังอยู่ครบ; รอบนี้รื้อโครงมากกว่าการตัดชิปหรือย้ายกล่อง.
- ตรวจ browser จริง: คอม 1280×720 / มือถือ 390×844, A/B สว่างใบร้านนอกเลยนัด + มืดใบ 7 ขั้น 4 สาย; ไม่มีแนวนอนล้น, รูป 3 ชุดโหลดครบ; สถานะไม่มีขั้นงาน/ไม่มีแบบอนุมัติถูกต้อง; มุมช่างไม่มีคำสั่งแก้ปัญหาของหัวหน้า; ตัวเลือกอยู่ใน URL; dialog Escape คืน focus ให้ปุ่มเปิด; console error/warn ไม่พบ.
- ด่าน: `typecheck` ผ่าน, lint **0 errors / 24 warnings เดิม**, unit **164 files / 1,666 tests ผ่าน** (ใช้ DATABASE_URL dummy เหมือน CI เพราะ shell ไม่มีค่านี้; ไม่ต่อฐานจริง), `verify:ui` ผ่านรวม work-order 33 ข้อ, Impeccable detector `[]`, `git diff --check` ผ่าน.
- Impeccable ผู้ตรวจอิสระดูโค้ดและภาพ viewport 8 ภาพ: **ship สำหรับส่งเทียบหน้าลอง**; ไม่ใช่อนุมัติ Production. ไม่ได้รัน build/deploy เพราะรอบนี้ส่งเฉพาะ branch หน้าลอง.

## NEXT
1. **เบสเปิด `/proto/work-order-desk` → เลือก A / B**. ฉันแนะนำ A สำหรับเปิดแล้วเห็นงานค้างทันที; B แลกกับความเป็นตารางตามลำดับและเลื่อนมากกว่า.
2. เคาะแล้ว → ปรับ SPEC ตามแบบที่เลือก → ลง presentation ใน `/production/[id]` โดยใช้ controller/dialog/readiness/record-mode/สิทธิ์เดิม. ตรวจ flow ร้านนอกและการรับกลับก่อนต่อปุ่มที่เสนอ; อย่าอ้างว่าปุ่มจำลองเป็นความสามารถที่ใช้งานได้แล้ว.
3. ตรวจจอจริงด้วยข้อมูลหลายชุด/สายขนานและมือถือ → ด่านครบ + build ก่อนขอ merge main. ไม่มีคำสั่งให้ขึ้น Production ในรอบนี้.
4. งานอื่นตาม ROADMAP §A2–A8/B/C/F: QC/แพ็กหน้างาน, ซ้อม Production V2 ต่อหลังจอพร้อม, หนี้ทางเข้ารอบพิมพ์/วัตถุดิบ/ประวัติละเอียด.

## บริบทที่ยังต้องรักษา
- ของจริงใบผลิตล่าสุดเป็นแบบ E ที่เคาะ 09-06: แผนที่สายขนาน + ตอนนี้ทำได้ + ข้อมูลประกอบพับท้าย. หน้าเบาลง A/B/C เดิมยังรอเลือก; เบสระบุล่าสุดว่าไม่ชอบการหุบพับ.
- แท็บรายการ/ฟอร์มสินค้า/ส่วนเสริม/การลดคำอธิบายทั้งเว็บ: session ก่อนหน้าบันทึกว่า merge main แล้ว 09-07; รอบนี้ไม่ตรวจ deployment ซ้ำ. สถานะ/ข้อแลกละเอียดอยู่ ROADMAP §A7–A8.
- กระดาษเป็นหลัก: จุดจด screen / paper / auto ตาม `lib/work-order-record-mode.ts`; ห้ามเปลี่ยนเป็นต้องติ๊กทุกขั้น. Checklist ปัจจุบันอ่านอย่างเดียว ไม่มีผลติ๊กในฐาน.
- หัวหน้าทำครบจากใบผลิต; ช่างมี `/production/floor`; ใช้เครื่องยนต์และสิทธิ์ชุดเดียวกัน. Station/TV ไม่มีเงิน.
- ทำเองเฉพาะ DTF; งานร้านนอกเดินขนานได้; สูตรที่ RELEASED ต้องคัดลอกเป็นร่างใหม่จึงแก้ได้.
- Production V2 บนเว็บจริงยัง legacy ตามบันทึกเดิม (`PRODUCTION_V2_ENABLED=0`); migration/seed/demo rehearsal ไม่ใช่หลักฐานว่าเปิดใช้แล้ว.
- เบสเคาะแล้ว: จด VAT, ร้านนอกสื่อสาร LINE, นักบัญชีใช้ PEAK. ใครถือ role ACCOUNTANT ยังไม่ชัด; อย่าปรับสิทธิ์เงียบ.

## สภาพแวดล้อม
- Port 3000 ยืนยันว่าเสิร์ฟ checkout `/Users/lostxker/dev/Git/anajak-erp` และเปิด demo route ได้ในรอบนี้. ไม่แก้ env, ไม่ restart server, ไม่ seed/reset.
- ฐานทดลองเดิม `127.0.0.1:5433/anajak_erp_demo`; `npm run dev:demo` / Docker `anajak-postgres`. ฐานจริง Supabase ห้าม reset หรือรัน integration ที่สร้างข้อมูลโดยไม่มี target/backup/คำสั่งชัด.
- เริ่ม session อ่าน ROADMAP/SPEC และ git สด. อ่าน ARCHITECTURE ก่อนเพิ่มโค้ด; docs/DESIGN เป็นกติกา UI. `/proto` มี noindex และทะเบียนกลาง.
