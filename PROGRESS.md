# PROGRESS — สถานะสด

## ตอนนี้ (2026-09-10 · A14.2 · รื้อตำแหน่งปุ่มในใบผลิตตามที่เบสสั่ง · รออนุมัติขึ้น main)
- เบสดูของจริงแล้วสั่งรอบ 2: "มอบหมายทำไมไม่ไปอยู่ในกล่องเช็คลิสต์ · CTA/ปุ่มดำเนินการของขั้นก็อยู่ในกล่องขั้นนั้นไปเลย · CTA ข้างบนเป็นปุ่มขั้นถัดไป แต่กดไม่ได้ และบอกด้วยว่าทำไมกดไม่ได้ · ปรับ UI กล่องใหม่ด้วย"
- ทำครบทุกข้อแล้วใน branch `ui/step-commands-proto` **ยังไม่ขึ้น main**
- A13 (เลิกปิดขั้นตรวจรับเสื้อให้เองตอนเปิดใบผลิต) ขึ้นเว็บจริงไปแล้ววันนี้ — `167c67a` · Vercel production READY

## โครงใหม่ของใบผลิต (ทุกปุ่มอยู่ในกล่องของเรื่องนั้น)
- **กล่องขั้น** = ตารางไซซ์ + แถบปุ่มท้ายกล่อง: ปุ่มดำเนินการของขั้นนั้น (ปิดขั้นนี้ / บันทึกตรวจรับเสื้อ / รับงานกลับ ฯลฯ) + "แจ้งปัญหาขั้นนี้" · ขั้นคู่ได้ปุ่มของตัวเองในกล่องตัวเอง · ขั้นเบิกเสื้อก็มีแถบเดียวกัน
- **กล่องเช็คลิสต์** = "ผู้ทำ" + ปุ่ม "มอบหมาย / เปลี่ยนคนทำ" บรรทัดเดียวกัน แล้วต่อด้วยข้อกำหนดที่ติ๊ก
- **หัวใบ** = ปุ่ม "ถัดไป: <ขั้นถัดไป>" แบบจาง กดไม่ได้ — **แต่ไม่ใช่ปุ่มตาย**: กดแล้วพาไปสิ่งที่ต้องทำก่อน (ติ๊กที่ยังว่าง → ช่องยอด → กล่องของขั้น) ตาม pattern เดิมของปุ่ม "ปิดขั้นนี้" จึงไม่ขัดกติกา DESIGN "ห้ามวางปุ่มที่กดไม่ได้"
- **ประโยคใต้รางบอกเหตุผลเสมอ**: พักไว้ · ติดปัญหา · ของอยู่ร้านไหน · รอขั้นก่อนหน้า · สิทธิ์ · ติ๊กอีกกี่ข้อ · ยอดขาดเท่าไร · ประโยคของ flow อื่น ("จัดการผ่านหน้ารอบพิมพ์ฟิล์ม DTF") · ไม่งั้น "กดปิดขั้น X ในกล่องด้านล่าง"
- **เมนู ⋯** เหลือของที่นาน ๆ ใช้: พักขั้น · ย้อนกลับ · ประวัติออเดอร์ · ปุ่ม "ส่งเข้า QC" ยังอยู่หัวใบและกดได้จริงเมื่อทุกขั้นปิดครบ
- ลบหน้าลอง `/proto/step-commands` — เคาะ B ไปแล้วและโครงเดินต่อจนตัวเลือก 3 ทางหมดความหมาย (ดูย้อนหลัง `git show cc1cd38 -- src/app/proto/step-commands`)

## งาน UI ที่เก็บพร้อมกันวันนี้
- การ์ด "เบิกเสื้อจากสต๊อค" มีผิวการ์ดเหมือนขั้นอื่นแล้ว และหัวการ์ดใช้โครงเดียวกับ `Section` (ชื่อ text-base → ยอด x/y ใต้ชื่อ → `InfoChip` ชิดขวา)
- แถว "รวมทั้งใบ" ท้ายตารางไซซ์ตรงคอลัมน์แล้ว (ต้นเหตุ: `Metric` เป็น block ธรรมดา `items-end` จึงไม่มีผล → `text-right`)
- เมนู ⋯ เรียงตามความถี่ + ขีดคั่นแยกคำสั่งของขั้นออกจากลิงก์ของทั้งใบ (`MoreMenuItem.separatorBefore` ใหม่)

## ผลตรวจ
- ด่านขั้นต่ำผ่านครบ: typecheck ผ่าน · lint 0 errors / 23 warnings เดิม · tests 172 files / 1,763 tests ผ่าน · verify:ui ผ่าน (dots 32 จาก baseline 35 · muted 243 จาก 254 · work-order 34 ผ่าน / 0 ตก)
- impeccable polish: detector 0 findings · แก้ที่พบเอง 2 จุด (ชื่อขั้นยาวดันหัวใบ → จำกัดความกว้าง+ตัดคำ · ประโยคใต้รางขึ้นต้นซ้ำกับหัวข้อ "ยังไปต่อไม่ได้ — ติด:")
- เปิดดูของจริงหลายสถานะ: ตรวจรับเสื้อ (ปุ่มในกล่อง + มอบหมายในเช็คลิสต์) · ติ๊กครบยอดครบ (ปุ่มปิดขั้นในกล่อง + ใต้รางบอก "กดปิดขั้น รีดร้อน ในกล่องด้านล่าง") · เส้นทางยาว/ร้านนอก (ชื่อขั้นยาวไม่ดันหัวใบ) · Light และ Dark
- **ยังไม่ได้ตรวจจอ 390 ด้วยตัวเอง** — หน้าต่าง Chrome ที่ใช้ย่อไม่ลง · หน้าลอง `/proto/work-order-states` มีปุ่ม "390" ให้กดดูเองได้ · แถบปุ่มใหม่ใช้ flex-wrap จึงตัดบรรทัดเมื่อจอแคบ
- ไม่แตะ schema/migration/dependency/env · จอสถานี `/production/floor` ไม่กระทบ (ยังส่ง embedded และไม่ส่ง footer)

## ค้างจากงานก่อนหน้า (ยังเปิดอยู่)
- A12 หน้าลอง `/proto/production-flow` โครงเดิม — รอเบสลองใช้แล้วสั่งปรับรายละเอียด
- A9 หนี้ 5 ข้อเดิม (ฐานทดลอง V2 · ทางลัด `sendToQc` · ขั้นที่ปิดผ่าน flow อื่นไม่ผ่านด่านติ๊ก · ข้อกำหนดยังเป็นตารางนิ่ง · `work-order-route.tsx` ยังไม่ถูกใช้)
- Feature A2–A8/B/C/F ตาม ROADMAP ยังเปิด

## NEXT
1. เบสเปิดใบผลิตในฐานทดลองลองกดจริง — ดูว่าปุ่มอยู่ตรงที่คิดไว้ไหม และประโยคใต้รางอ่านแล้วเข้าใจไหม
2. โอเคแล้วสั่ง "push main" เพื่อขึ้นเว็บจริง
3. ถ้ายังมีกล่องไหนหน้าตาไม่เข้าที่ ส่งภาพมาได้เลย

## สภาพแวดล้อม
- `npm run dev:demo` ที่ port 3000 ต่อฐานทดลอง local (Docker `anajak-postgres` · ฐาน `anajak_erp_demo`) — dev สองตัวพร้อมกันไม่ได้
- ดูหน้าจอผ่านแท็บ Chrome ที่ล็อกอินไว้ (Claude in Chrome) · คืนธีมสว่างหลังตรวจธีมมืดแล้ว · ไม่ล้าง cookie/cache และไม่ bypass auth
