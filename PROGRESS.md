# PROGRESS — สถานะสด

## ตอนนี้ (2026-09-10 · A13 เสร็จ · รอเบสลองของจริง)
- เบสเปิดใบผลิตของ ORD-2609-0003 แล้วใบไปยืนที่ "พิมพ์ฟิล์ม DTF" ทันที → ไล่แล้วพบว่า `production.create` ปิดขั้น "ตรวจรับเสื้อลูกค้า" ให้เอง เพราะออเดอร์มีใบตรวจรับครบ 80 ตัว (ลงวันที่ 4 ก.ย.) อยู่ก่อนแล้ว — เป็นกติกาที่ตั้งใจ ไม่ใช่บั๊ก แต่หน้าจอไม่บอก คนอ่านว่า "ระบบข้ามขั้น"
- เบสเลือกทางใหญ่: **เลิกปิดให้เองตอนเปิดใบผลิต** ต้องมีคนกดปิดในใบ · และเคาะขอบเขตว่า **ตอนช่างทำใบตรวจรับจริงที่หน้างานยังปิดขั้นให้เองเหมือนเดิม** (คนเพิ่งกดจริง ไม่ต้องกดซ้ำ)
- ลงของจริงแล้วใน branch `fix/garment-receive-manual-close` — ยังไม่ขึ้น main

## สิ่งที่เปลี่ยน
- `production.create` ตัดการปิดขั้น GARMENT_RECEIVE อัตโนมัติทั้งก้อน: ไม่อ่าน ledger ใบตรวจรับตอนเปิดใบ · ทุกขั้นเกิดเป็น "รอทำ" เสมอ · audit ไม่มีช่อง `autoCompletedGarmentReceiveSteps` · ตัด finalize ตอน create ที่กลายเป็นโค้ดตาย · import `netReceivedByVariant`/`receiptInspectionOfVariants` ออก
- `goods-receipt.ts` **ไม่แตะ** — ช่างนับครบที่หน้างานแล้วขั้นยังปิดเองเหมือนเดิม ตามที่เบสเคาะ
- ไม่ต้องทำ UI ใหม่: ใบผลิตมีปุ่ม "บันทึกตรวจรับเสื้อลูกค้า" อยู่แล้ว → กล่องใบตรวจรับ ซึ่งมีปุ่ม "ยืนยันหลักฐานเดิมและปิดขั้น" (`goodsReceipt.confirmCustomerGarmentEvidence`) สำหรับเคสรับครบไปแล้ว — มี lock/สิทธิ์/กดซ้ำได้ ครบอยู่ก่อนแล้ว
- ไม่บังคับติ๊กเช็คลิสต์ 3 ข้อของขั้นนี้เพิ่ม — ใบตรวจรับเป็นหลักฐานแทนการติ๊ก ตามกติกาเดิม (A9 หนี้ข้อ 3)
- เอกสาร: ใบงาน `ROADMAP.md` §A13 + กติกาใน `SPEC.md` (แก้ก่อนเขียนโค้ดตามกติกา repo)

## ผลตรวจ
- ด่านขั้นต่ำผ่านครบ: typecheck ผ่าน · lint 0 errors / 23 warnings เดิม · tests 172 files / 1,763 tests ผ่าน · verify:ui ผ่าน (dots 32 จาก baseline 35 · muted 243 จาก 254 · work-order 34 ผ่าน / 0 ตก)
- test ของ create 2 ตัวที่เคยยืนยันการปิดอัตโนมัติ เขียนใหม่เป็นยืนยันกติกาใหม่ (ขั้นเกิดเป็นรอทำ · ไม่อ่าน ledger · ใบที่มีแต่ขั้นตรวจรับไม่ปิดเองและไม่ดันออเดอร์เข้า QC) · test.each เดิมเปลี่ยนไปกันไม่ให้ auto-complete กลับมาแบบมีเงื่อนไข
- ตรวจของจริงบนฐานทดลอง โดยเปิดขั้นชั่วคราวแล้วคืนค่าเดิมเป๊ะ (จดค่าก่อนแก้ · ตรวจหลังคืน): ใบยืนที่ช่อง 1 "ตรวจรับเสื้อลูกค้า" · ปุ่มหัวใบ "บันทึกตรวจรับเสื้อลูกค้า" · กล่องขึ้น "มีหลักฐานรับครบแล้ว" + ปุ่ม "ยืนยันหลักฐานเดิมและปิดขั้น" กดได้เลยไม่ต้องกรอกยอดซ้ำ · ไม่ได้กดยืนยันจริง ไม่มีใบตรวจรับใหม่เกิดขึ้น
- ไม่แตะ schema/migration/dependency/env · ไม่แตะฐานจริง · ใบผลิตเก่าที่ปิดขั้นไปแล้วไม่ถูกย้อน

## ค้างจากงานก่อนหน้า (ยังเปิดอยู่)
- A12 หน้าลอง `/proto/production-flow` โครงเดิม — รอเบสลองใช้แล้วสั่งปรับรายละเอียด ยังไม่ต่อระบบจริง
- A9 หนี้ 5 ข้อเดิม (ฐานทดลอง V2 · ทางลัด `sendToQc` · ขั้นที่ปิดผ่าน flow อื่นไม่ผ่านด่านติ๊ก · ข้อกำหนดยังเป็นตารางนิ่ง · `work-order-route.tsx` ยังไม่ถูกใช้)
- Feature A2–A8/B/C/F ตาม ROADMAP ยังเปิด

## NEXT
1. เบสลองกดเปิดใบผลิตใหม่ในฐานทดลอง (ต้องรีเซ็ตฐานก่อนด้วย `npm run db:seed:demo` เพราะตอนนี้ไม่มีออเดอร์ที่รอเปิดใบเหลือแล้ว) แล้วดูว่าใบยืนที่ขั้นตรวจรับ ไม่ข้ามไปพิมพ์ฟิล์ม
2. เบสเคาะว่าจะรวมขึ้น main เมื่อไร — ยังไม่ push main (repo ต่อ Vercel = ขึ้นเว็บจริงทันที)
3. ใบผลิตใบเดิมของ ORD-2609-0003 ในฐานทดลองยังปิดขั้นไปแล้วตามกติกาเก่า — ถ้าเบสอยากเห็นสภาพใหม่กับใบนั้น ต้องรีเซ็ตฐานทดลอง

## สภาพแวดล้อม
- `npm run dev:demo` ที่ port 3000 ต่อฐานทดลอง local (Docker `anajak-postgres` · ฐาน `anajak_erp_demo`) — dev สองตัวพร้อมกันไม่ได้
- ดูหน้าจอผ่านแท็บ Chrome ที่ล็อกอินไว้แล้ว (Claude in Chrome) · ไม่ล้าง cookie/cache และไม่ bypass auth
