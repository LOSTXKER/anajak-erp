# PROGRESS — สถานะสด

## ตอนนี้ (2026-09-10 · A14 เคาะแล้ว ลงของจริงครบ · รออนุมัติขึ้น main)
- เบสเคาะ **B แยกครึ่ง** จากหน้าลอง `/proto/step-commands` — คำสั่งที่ใช้ทุกวันอยู่กับขั้น ที่เหลืออยู่เมนู ⋯
- พร้อมกันนั้นเบสทัก UI อีก 2 จุด: การ์ด "เบิกเสื้อจากสต๊อค" ไม่เข้าพวก (ไม่มีพื้นหลังรอง) และแถว "รวมทั้งใบ" ตัวเลขไม่ตรงคอลัมน์ — แก้ครบแล้วทั้งคู่ พร้อมไล่ตรวจจุดอื่นในหน้า
- ก่อนหน้านี้วันเดียวกัน: A13 (เลิกปิดขั้นตรวจรับเสื้อให้เองตอนเปิดใบผลิต) รวม main และขึ้นเว็บจริงแล้ว — `167c67a` · Vercel production READY
- งานรอบนี้อยู่ใน branch `ui/step-commands-proto` **ยังไม่ขึ้น main**

## สิ่งที่เปลี่ยน
- **คำสั่งของขั้น = แยกครึ่ง (ของจริง)**: "แจ้งปัญหาขั้นนี้" + "มอบหมาย/แก้ให้" เป็นปุ่มใต้การ์ดขั้นที่กำลังทำ (วางเฉพาะที่กดได้ตอนนั้น ตามกติกาห้ามวางปุ่มกดไม่ได้) · "พักขั้น" · "ย้อนกลับ" · "ประวัติออเดอร์" ยังอยู่ในเมนู ⋯ · `WorkOrderView.commands` default = `"split"` ค่าอื่นเหลือไว้ให้หน้าลองเทียบย้อนหลัง
- **เมนู ⋯ จัดใหม่**: เรียงตามความถี่ · ขีดคั่นแยกคำสั่งของขั้นออกจากลิงก์ของทั้งใบ (`MoreMenuItem.separatorBefore` ใหม่) · แดงสงวนให้ย้อนกลับอย่างเดียว · ตำแหน่งคงที่ทุกสถานะ ของที่กดไม่ได้ยัง disabled + บอกเหตุ
- **การ์ดเบิกเสื้อเข้าชุดกับขั้นอื่น**: ใบผลิตเลิกส่ง `embedded` → ได้ผิวการ์ดเหมือนขั้นอื่น (จอสถานียัง embedded เหมือนเดิม) · หัวการ์ดเป็นโครงเดียวกับ `Section`: ชื่อ text-base → ยอด `x / y ตัว` ใต้ชื่อ → `InfoChip` ชิดขวา แทนป้ายที่เขียนสีเอง
- **แถว "รวมทั้งใบ" ตรงคอลัมน์**: ต้นเหตุคือ `Metric` เป็น block ธรรมดา แต่โค้ดสั่งชิดขวาด้วย `items-end` ของ flex ซึ่งไม่มีผล → เปลี่ยนเป็น `text-right` (ตรงทั้งแบบ 2 คอลัมน์และ 4 คอลัมน์)
- หน้าลอง `/proto/step-commands` ทะเบียนเป็น "เคาะแล้ว" พร้อมบันทึกว่าเลือก B · กติกาใหม่เขียนใน `SPEC.md` · ใบงาน `ROADMAP.md` §A14 ปิดครบ

## ที่ไล่ตรวจตามที่เบสสั่ง "ตรวจส่วนอื่นๆด้วย"
- การ์ดอื่นในหน้าใช้ `Section` เข้าชุดอยู่แล้ว: เช็คลิสต์ · ข้อมูลออเดอร์ · สินค้า · การ์ดปัญหาใช้ `Alert` ตามมาตรฐานแจ้งเตือน (ตั้งใจให้ต่าง)
- ขั้นทุกชนิดนอกจากเบิกเสื้อใช้ `StepPieceTable` (Section) ชุดเดียวกันอยู่แล้ว — มีแค่ขั้นเบิกเสื้อที่หลุด แก้แล้ว
- `Metric` ที่สั่งจัดชิดด้วยคำสั่ง flex ผิดที่ มีจุดเดียวคือแถวรวมนี้ (ไล่ทั้ง repo แล้ว)

## ผลตรวจ
- ด่านขั้นต่ำผ่านครบ: typecheck ผ่าน · lint 0 errors / 23 warnings เดิม · tests 172 files / 1,763 tests ผ่าน · verify:ui ผ่าน (dots 32 จาก baseline 35 · muted 243 จาก 254 · work-order 34 ผ่าน / 0 ตก)
- เปิดดูของจริงบนฐานทดลอง: ใบ ORD-2609-0015 การ์ดเบิกเสื้อมีผิวการ์ดและหัวเข้าชุดกับเช็คลิสต์ · ปุ่มแจ้งปัญหา/มอบหมาย อยู่ใต้การ์ดขั้น · ใบ ORD-2609-0004 แถวรวมแบบ 2 คอลัมน์ตรง · หน้าลองสถานะ "กำลังทำ" แถวรวมแบบ 4 คอลัมน์ตรงทั้ง จำนวน/ทำแล้ว/เสีย
- **ยังไม่ได้ตรวจจอ 390 ด้วยตัวเอง** — หน้าต่าง Chrome ย่อไม่ลง · หน้าลองมีปุ่ม "390" ให้กดดูเองได้ · แถวปุ่มคำสั่งใช้ flex-wrap จึงตัดบรรทัดเมื่อจอแคบ
- ไม่แตะ schema/migration/dependency/env

## ค้างจากงานก่อนหน้า (ยังเปิดอยู่)
- A12 หน้าลอง `/proto/production-flow` โครงเดิม — รอเบสลองใช้แล้วสั่งปรับรายละเอียด
- A9 หนี้ 5 ข้อเดิม (ฐานทดลอง V2 · ทางลัด `sendToQc` · ขั้นที่ปิดผ่าน flow อื่นไม่ผ่านด่านติ๊ก · ข้อกำหนดยังเป็นตารางนิ่ง · `work-order-route.tsx` ยังไม่ถูกใช้)
- Feature A2–A8/B/C/F ตาม ROADMAP ยังเปิด

## NEXT
1. เบสเปิดใบผลิตจริงลองใช้ปุ่มใหม่ (แจ้งปัญหา/มอบหมาย อยู่ใต้การ์ดขั้นแล้ว) แล้วบอกว่าจะรวมขึ้น main เมื่อไร
2. ถ้ายังมีขั้นไหนหน้าตาไม่เข้าพวกอีก ส่งภาพมาได้เลย — ตอนนี้ทุกขั้นใช้โครงหัวการ์ดชุดเดียวกันหมดแล้ว

## สภาพแวดล้อม
- `npm run dev:demo` ที่ port 3000 ต่อฐานทดลอง local (Docker `anajak-postgres` · ฐาน `anajak_erp_demo`) — dev สองตัวพร้อมกันไม่ได้
- ดูหน้าจอผ่านแท็บ Chrome ที่ล็อกอินไว้ (Claude in Chrome) · ไม่ล้าง cookie/cache และไม่ bypass auth
