import { ProductionMetricsPage } from "@/components/production/production-metrics-page";

/** /production/metrics — ตัวชี้วัดการผลิต (MFG1–3 · เบสสั่งลงจริง 2026-09-16) อ่านอย่างเดียว ไม่มีเงิน */
export default function ProductionMetricsRoute() {
  return <ProductionMetricsPage />;
}
