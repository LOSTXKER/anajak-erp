import { StationScreen } from "@/components/station/station-screen";

/**
 * /station — จอสถานีสำหรับพนักงาน + แผงสถานีของหัวหน้า (แบบ A "หยิบงานเอง" · เบสเคาะ 2026-09-03)
 * ข้อมูลจาก factory.stationQueue (ไม่มีเงินโดยโครงสร้าง) · หน้าลงมือใช้เครื่องยนต์เดียวกับใบผลิต /production/[id]
 */
export default function StationPage() {
  return <StationScreen />;
}
