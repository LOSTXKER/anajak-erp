# Anajak ERP — งานและสถานะ

ข้อกำหนดอยู่ [SPEC.md](SPEC.md), วิธีใช้ [README.md](README.md), กติกา [AGENTS.md](AGENTS.md), ทิศทางหน้าจอ [DESIGN.md](DESIGN.md). งานที่จบและแบบที่ไม่เลือกดู Git; ไฟล์นี้เก็บเฉพาะงานที่ยังทำต่อ

## ตอนนี้ทำ

- [ ] **A16: ตรวจและปรับ UI ทุกส่วนตาม ui-guidance** — เกณฑ์เสร็จ: หน้าจอที่เลือกทำงานได้ตาม SPEC และผ่านภารกิจจริงหลายบทบาทบนฐานทดลอง
- ทำถึงไหน: ตรวจโครง 60 page routes รวม redirect/หน้าลอง และส่วนกลางครบกลุ่ม; รอบ 12–13 ก.ย. 2569 ลงหน้าจริงบน branch codex/ui-refactor-20260912 แล้วทั้ง shell/หัวหน้า/คิวผลิต/ใบผลิต/ออเดอร์/ใบเสนอ/สินค้า/ตั้งค่า พร้อมแก้งานหายจาก My Tasks และข้อความแจ้งเตือน. ยังไม่รื้อหรือซ้อมทุก flow ครบระบบ
- ตรวจแล้ว: typecheck, lint 0 errors/22 warnings, 188 files/1,870 tests และ verify:ui 255 + work-order 32 ผ่าน. CUA บน demo ตรวจ Light/Dark, 1440/390 และใบผลิต 1024; ค้นว่าง/ล้างกรอง, เข้ารายละเอียดเริ่มบนสุดและ Back คืน 2405px, แท็บคง draft, เปิดขั้นอนาคตอ่านอย่างเดียว/คืน keyboard focus, ช่องราคาว่าง/Esc/เตือนออกหน้า, dialog รับเงินมีคำอธิบาย/สร้างบิลไม่มี description ลอยและคืน focus. ยังไม่มีการเขียนธุรกรรมบน browser รอบนี้
- ติดอะไร: ขาดซ้อมบันทึกหลายบทบาท, token ใช้ได้/หมดอายุ และ PDF จริงครบ 5 แบบ; DTF/ร้านนอกยังมีช่องว่าง flow เดิม. ไม่มี release/CI/deployment ที่ยืนยันในรอบนี้
- ทำต่อ: รับ feedback หน้าจริงบน branch แล้วเดิน flow ที่ค้างตามตาราง; แยกงานบริการ/สิทธิ์/ข้อมูลที่พบออกจากการเปลี่ยนหน้าตา

### ขอบเขต A16

เบสสั่ง 2026-09-12 ให้ตรวจและรื้อ UI ทุกหน้า/ส่วนด้วย skill ใหม่. ใช้ ui-guidance ปัจจุบันใน bestos-brain และคง Prompt/น้ำเงิน Anajak. ถอดด่านที่ตรึงสี มุม เงา ความแน่น จำนวนกล่อง และชื่อคลาส; ข้อแนะนำหน้าตาเป็น warning ส่วนการอ่าน/เป้ากด/focus/สิทธิ์/สถานะ/ข้อมูล/public/print ยังตรวจ. ไม่เปลี่ยน schema สูตรเงิน status permission API หรือเปิด V2; branch นี้ยังไม่ใช่ทิศที่ปล่อย Production

การลงหน้าจริงใช้ component เดิมและข้อมูล demo: คิวผลิตเป็นตารางต่อเนื่องกับแถบค้น/กรอง; ใบผลิตแยกขั้นเปิดดูกับขั้นลงมือ คงงานคู่และ draft; ออเดอร์มีลูกค้า/กำหนดส่งทุกแท็บ; ใบเสนอจัดส่วนรายการกับสรุป; สินค้ารวมข้อมูลและตารางไซซ์ที่อ่านง่าย; ตั้งค่าเป็นกลุ่มลิงก์. หน้าลองเก่า /proto/ui-reset ยังอยู่เพื่ออ้างอิง แต่ไม่ใช่ผลส่งมอบใหม่

แก้พฤติกรรมที่พบ: My Tasks แยก key ตามชนิดงาน, สรุปบิลไม่ปลอมเลข 0 ระหว่างโหลด, รายละเอียดแจ้งเตือนไม่ตัดข้อความ, dialog เชื่อมคำอธิบายที่แสดงจริง, ราคา blank ไม่กลายเป็น 0/กัน blur ส่งซ้ำ/รักษา error draft, ใบผลิตล้างเฉพาะ draft ที่ server ยืนยันโดยไม่ทับค่าที่แก้ใหม่. ใบเสนอใช้ยอด stored และเตือนเมื่อผลรวมรายการไม่ตรง; ไม่ตั้งชื่อส่วนต่างเป็นค่าบริการ. Print แปลเงื่อนไขที่รู้จักเป็นไทย

### พื้นที่ที่ยังต้องเดินให้ครบ

บัญชีนี้แยกการตรวจโค้ดกับภารกิจจริง; ไม่ใช้จำนวน route หรือผล unit test อ้างว่าผู้ใช้ทุกบทบาททำงานครบแล้ว

| กลุ่มและทางเข้า | รอบนี้ทำ/ตรวจ | ยังต้องเดินจริง |
|---|---|---|
| shell/header/section/field/ปุ่ม/เมนู/ตาราง/dialog | ปรับส่วนกลางและด่าน; ค้น/กรอง/Back/แท็บ/keyboard/dialog บนจอจริง | error/refetch/no-permission ทุกบทบาท |
| /, /home, /my-tasks, /notifications, /analytics | ตรวจโครงครบ; My Tasks ไม่รวมงานต่างชนิดทับกัน; แจ้งเตือนอ่านครบ | คิว My Tasks ยังรวมงานอนาคตบางรายการ ต้องใช้ readiness จากแหล่งเดิม |
| /customers และรายละเอียด/เพิ่ม/แก้/เครดิต/ผู้ติดต่อ | ตรวจโค้ดและผลของ shell; ผลเปิดหน้ารอบก่อนอยู่ Git | บันทึกฟอร์ม/สิทธิ์และจัดโครงรายละเอียด |
| /orders, new, [id], edit และ 7 แท็บ | จัดรายการ/หัวใบ; เปิดรายการ/รายละเอียด/เงิน/ฟอร์มใหม่; ค้นว่างแล้วล้างได้ | บันทึกทุกแท็บหลายบทบาท; รายการแสดงเทียบหน้าแก้ |
| /quotations, new, [id] | จัดฟอร์ม/รายการ/รายละเอียด; สรุปยอด stored/แจ้งต่าง/มือถือ; print terms ไทย | ซ้อมสร้าง/ส่ง/อนุมัติ; from-order ยังไม่คัดค่าธรรมเนียม/ส่วนลด/ภาษี เป็นงาน payload แยก |
| /billing, notes, aging, tax, wht | ตรวจโครงครบ; จัดล้างกรอง/สถานะโหลด; ลอง dialog รับเงินและสร้างบิลโดยไม่ submit | ธุรกรรมจริงบน demo/สิทธิ์ |
| /production, [id], floor, /station, /factory | จัดคิว/ใบผลิต; ยอด draft ข้ามแท็บ/อ่านขั้นอนาคต/คืน focus/มือถือ/1024 | ช่าง/สถานี/QC/ร้านนอก → รับกลับ → QC; DTF รอบพิมพ์/คลังฟิล์มยังไม่มีทางใช้ครบ |
| /products, /products/[id] | รวมข้อมูล/ตาราง 5 คอลัมน์/สถานะมีข้อความ; ลอง blank/Esc/คำเตือน; component repro 14 กรณี | บันทึกจริง/stock refresh; API ส่ง costPrice และ basePrice fallback เป็นข้อจำกัดเดิม ต้อง audit แยก |
| /settings + company/users/stock/cost-rates/vendors/services/patterns/packaging/routings/backup/audit | ตรวจโครงครบ; จัดหน้ารวมกลุ่ม/ลิงก์ และเปิดผลจริง | ทุกฟอร์ม/สิทธิ์; ถ้อยคำ stock/audit |
| status/upload/quote/job/approve-design token | ตรวจโค้ด; รอบก่อนเปิด error 5 หน้า | token ใช้ได้/หมดอายุ/network/อนุมัติ/อัปโหลด |
| /print/quotation, invoice, billing-note, job-ticket, packing-list | ไม่เปลี่ยน print primitive; เปิด HTML ใบเสนอและยืนยันเงื่อนไข/ยอด | PDF จริง 5 แบบ; demo QT-2609-0001 lines 10,200 กับ stored subtotal 10,700 ไม่ตรง ยังไม่ซ่อมข้อมูล |
| /login; /v2, orders/new/[id]; /proto และหน้าลองเดิม | ตรวจทางเข้า/redirect/โค้ดครบ; ใช้บัญชีเจ้าของ demo | บทบาทอื่นและความเข้ากันได้เดิม; หน้าลองไม่ใช่ release |

### ขั้นต่อภายใน A16

1. ปรับจุดที่ได้รับ feedback บนหน้าจริง และต่อรายละเอียดลูกค้า/settings/ภาพรวมตามภารกิจ; ไม่สร้างตัวเลือกหรือ /proto เพิ่มโดยอัตโนมัติ
2. เดิน flow บันทึก demo หลายบทบาท ครอบคลุมเงิน/ลูกค้า/ตั้งค่า/ร้านนอก/QC; เปลี่ยนบริการหรือสิทธิ์เฉพาะใบงานที่อนุมัติ ไม่ใช้ UI ซ่อนข้อจำกัด
3. ลอง public ทุกสถานะและพิมพ์ PDF จริง 5 แบบ; ตรวจข้อมูลใบเสนอและค่าบริการก่อนอนุมัติซ่อมหรือเปลี่ยน payload

## คิว

- [ ] **A15:** DB harness ของ `correctCustomerGarmentReceipt`: ส่วนต่างขึ้น/ลง เปิดขั้นกลับ กดซ้ำ; ปัจจุบันมี router test และผลลองลด S 15 → 12 บน demo
- [ ] **A12:** production-flow โครงเดิมยังจำลอง รอเคาะรายละเอียดก่อนต่อ Manufacturing จริง; A/B เดิมถูกปฏิเสธ การปล่อยหน้าลองไม่ใช่เปิดระบบผลิตใหม่
- [ ] **A9/A5:** ฟอร์มยังขับ V2 ไม่ได้ (`lockProductionStepScope`); `sendToQc` ยังปิดขั้นกระดาษข้ามได้ ต้องมีใบงานบริการ/test. ผูกข้อกำหนดนิ่งใน `work-order-standards.ts` กับ routing; ตรวจผู้ใช้ `work-order-route.tsx` ก่อนพิจารณาลบ คงหลักฐานร้านนอก/QC/รอบพิมพ์/ใบตรวจรับแทน checkbox
- [ ] **A2/A4:** ต่อ QC/final-pack และทางกลับ floor → My Tasks/แจ้งเตือน ซ้อมหัวหน้า/ช่าง; PIN เครื่องร่วมต้องตกลงสิทธิ์ก่อนทำ สถานี settings/V2 `controlList/workOrder` รอ cutover
- [ ] **A5:** ครั้งพิมพ์/รุ่นกระดาษต้องอนุมัติ schema ก่อน, QR ไป QC, ตรวจงานพิมพ์จาก `scripts/verify-print.tsx` บน demo; เวลาที่อนุมานจากปิดงานกระดาษเดิมไม่ใช่เวลาทำจริง
- [ ] **A7/F:** หน้าแสดง/แก้สินค้าและชื่อ accessory enum ให้ตรงกัน; ตรวจ orphan `OrderInfoEditDialog`/`OrderItemsEditor` และ lint เมื่อแตะไฟล์
- [ ] **F:** แจ้งรับเงินใบที่อยู่ในใบวางบิล; ทบทวน dashboard width
- [ ] **F → A16:** factory alpha/hairline/แสงสะท้อนทัช, แถบยังไม่อ่าน/SegmentedControl และลำดับธีมมืดตามจอจริง
- [ ] **B1:** ก่อนซ้อม V2 ตรวจข้อขัดกันใน seed: `seed-demo-form-states.ts` สร้างใบไม่มีเลข MO แต่ `seed-demo.ts` ตรวจ V2 ทุกใบ (พบจากโค้ด 2026-09-12 ยังไม่รัน reset). จากนั้นซ้อมสูตร → สถานี → ร้านนอก → QC fail/rework → แพ็กแยกไซซ์บน demo ตาม README; ผลเดิมใช้ legacy/flag 0 ต้องตรวจค่าจริงก่อนเริ่ม
- [ ] **B2:** แก้ผลซ้อม V2 เป็นงานย่อย; ตรวจ mapping CUSTOM/งานขนานเมื่อจำเป็น ไม่คืนผังเก่าที่ถูกปฏิเสธ
- [ ] **B3:** ขออนุมัติ rollout ทีละขั้นตาม SPEC: target/backup → additive seed → flag → ตรวจ → ลบ legacy UI/writer ตาม rollout window. มีโค้ด/migration/seed/settings ไม่เท่ากับเปิดใช้จริง
- [ ] **B4:** หลัง cutover ต่อ QC rework target work center; DTF partial/waste/reprint event; owner/plan/SLA/audit actor read model; supervisor material recovery; WIP ownership ของ QC/final pack
- [ ] **MFG1–3:** ตัวชี้วัดผลิตแบบอ่านอย่างเดียว ไม่มีเงิน/ไม่เพิ่ม schema: งวดเดือนไทย ส่งตรงเวลา (`shippedAt` เทียบ `deadline`, ไม่มี deadline ไม่นับ), ทำถูกครั้งแรก `ΣqtyGood / (ΣqtyGood + ΣqtyDefect)`, ของเสียตาม `QcDefect.reason`/ไซซ์/สี/ลายและรูป. แยกสูตรจาก router; ทดสอบงวดว่าง/หารศูนย์/ยังไม่ส่ง/ยกเลิก/ไม่มี deadline, ตรวจ 1440/390 สองธีม แล้วเพิ่มเกณฑ์ใน SPEC
- [ ] **B6/B16:** นักบัญชีตรวจใบกำกับ/CN/DN และเลขรันจากเอกสารพิมพ์จริง; เดินงานกับทีมและนักบัญชีหนึ่งรอบก่อนเปลี่ยน flow การเงิน
- [ ] **สภาพแวดล้อมก่อนใช้จริง:** ยืนยัน public signup ปิด/Storage policies, Vercel rate limit/Bot Protection, env และผล cron สอง route, สำรองไฟล์/Auth พร้อมซ้อมกู้คืน ตาม README. เก็บผลตรวจ target/commit/เวลา แยกจาก CI/deployment; ยังไม่มีหลักฐานครบในรอบนี้

## พักไว้

### หลังมีข้อมูลใช้งานจริงประมาณหนึ่งเดือน

- C1 ราคาตามจำนวน × เทคนิค × ตำแหน่ง และราคาต่อลูกค้า เป็น service ใช้ต่อ P4 — รอข้อมูลราคา/งานจริงตามแผนเดิม
- C2 กวาดใบเสนอ SENT/แบบรอลูกค้า/INQUIRY/ร้านนอกเกินกำหนด → กระดิ่งผ่าน cron เดิม — รอเห็นอายุงานจริง
- C5 เก็บ UX หน้างานหลัง V2; C6 LINE OA notify — ระหว่างนี้ใช้ข้อความก๊อปส่ง

### P2–P4

- **P2 หลัง V2:** quantity line/event เพื่อติดตามชิ้น ของเสีย/พิมพ์ซ้ำ; จองสต๊อกลึกขึ้นกับ Anajak Stock และให้ Stock เลือก location เมื่อ ERP ไม่ระบุแทนค่าคงที่ใน `src/lib/stock-constants.ts`; ใบแพ็ก/แบ่งส่งจาก DeliveryLine. AP ร้านนอก + WHT ขาจ่าย 3%/50ทวิ/ภงด.53 ต้องทบทวนขอบเขตก่อนเริ่ม ไม่ใช่ job costing
- **P3 ลูกค้า:** portal สถานะ/ประวัติ/เอกสาร/อนุมัติ/สั่งซ้ำ; LINE แจ้ง/ทวง/รูป WIP; CRM follow-up/RFM; โควตาแก้แบบ/ค่าเกิน/ล็อกหลังอนุมัติ; ตัวอย่างจริง opt-in; ตรวจ DPI/พื้นโปร่ง; analytics ลึก — รอข้อมูลใช้จริงและคิวหลัง V2
- **P4 เว็บ/ระบบอื่น:** intake ของ `anajak-print-web` ใช้ order/design/payment pipeline เดียวกับหน้าร้านตาม SPEC; ราคาจาก C1, ขยาย MCP/ฟอร์มเก็บไซซ์องค์กร/e-Tax/PEAK/courier ตามปริมาณงาน — ยังไม่เริ่มเชื่อมเพิ่มในรอบนี้

ขอบเขตที่จงใจไม่ทำและกฎข้อมูลถาวรอยู่ SPEC; วิธีลงมือและข้อห้ามอยู่ AGENTS ไม่สร้างข้อกำหนดอีกชุดในแผน
