# Anajak ERP — Roadmap & ใบงาน build

> สร้าง 2026-06-10 โดย Nami (bestos) · แผนเต็ม+ที่มา: ใน repo bestos (sibling) → `records/projects/anajak-erp/` (plan.md + _survey-2026-06-10.md) · path เต็มต่างตามเครื่อง (Win `D:/dev/ai-agent2` · Mac `~/dev/Git/bestos`) — อย่า hardcode
> เบสเคาะแล้ว: ซ่อมต่อบนโครงเดิม · DB ว่างรื้อ schema ได้อิสระ · ผู้ใช้ = พนักงาน 5 คน + เบส + ลูกค้า · ภาษีเต็มรูป (ERP ออกใบกำกับเอง) + หัก ณ ที่จ่าย 2 ขา · เว็บ 5 นาที = ทีหลัง

## กติกา build (ผูกทุก phase — ห้ามข้าม)
1. เพดานจริง ~1-2 โมดูลเล็ก/เดือน — อย่ารับปากเกิน · ทุกไตรมาส prune backlog ที่ยังไม่เริ่มทิ้งครึ่ง
2. เปิด "gate บังคับ" ทีละตัว (รูปก่อนส่ง → ค่อยเพิ่มตัวถัดไป) — เปิดพร้อมกันพนักงาน bypass ข้อมูลกลายเป็นขยะ
3. manual field/CSV ก่อน API เสมอ (courier/บัญชี/stock-link)
4. ~~โซ่ต้นทุนห้ามสลับ: film log + outsource AP → job costing → ค่อย margin guard~~ **ยกเลิกทั้งโซ่ (เบสเคาะ 2026-06-12): ไม่คิดต้นทุนต่องานในระบบนี้** — ต้นทุนเหมา (หมึก 1 ขวดพิมพ์ได้หลายตัว) เบสคิดกำไรขาดทุนรายเดือนในระบบบัญชี · ห้ามเพิ่มช่องเงิน/ต้นทุนใน flow ผลิต-outsource อีก
5. **ไม่ build บัญชีแยกประเภท (GL)** — ERP ออกเอกสารขาย/ใบกำกับ/50ทวิ + export มาตรฐาน (CSV/Excel) ให้นักบัญชี · ยังไม่ผูก FlowAccount/PEAK (เบสเคาะ: ทำมาตรฐานก่อน)
6. surgical: แตะเฉพาะที่ใบงานสั่ง · โครงเดิมดี (order module/status machine/token approval) — ต่อยอด อย่ารื้อ
7. **refactor = targeted ไม่ big-bang** — ห้ามรื้อทั้ง codebase รอบเดียว (ไม่มี test คุ้มกัน) · ลำดับ: test แกนก่อน → refactor เฉพาะส่วนที่กำลังแตะ → boy-scout rule (แตะไฟล์ไหน เก็บกวาดไฟล์นั้น)
8. **UI ใหม่ผ่าน design system ไม่ใช่ไล่ทาสีทีละหน้า** — วางมาตรฐานกลางครั้งเดียว (P1.0) ทุกหน้าที่แตะหลังจากนั้นขึ้นมาตรฐานใหม่ทันที · ห้าม redesign หน้าที่ยังไม่มีงาน functional ไปแตะ (จะได้ไม่ทำซ้ำตอน P2-P3 เปลี่ยนหน้านั้นอยู่ดี)
9. **มาตรฐาน UI ที่เคาะแล้ว (2026-08-03 จาก audit — โค้ดใหม่ต้องตาม · ของเดิมเก็บตอนแตะไฟล์):**
   - โครงหน้า dashboard = `PageShell` (header + โหลด/พัง/ไม่มีสิทธิ์ + spacing/width) · หน้า public = `PublicPageShell` · state หน้า list = `useListPageState`
   - **validation ฟอร์ม**: กติกาต่อช่อง = validator function ใน `src/lib/` (มี test ได้ — แบบ `lib/customer-form.ts`) แสดงผ่าน `Field` prop `error` · ฟอร์มยาวเพิ่มกล่องสรุปหัวฟอร์ม (แบบ orders/new) · **toast สงวนไว้ server error หลัง submit และห้ามยิงคู่กับ Alert ในฟอร์ม** (เลือกที่เดียว — ในฟอร์มดีกว่า) · ห้ามพึ่ง HTML `required` อย่างเดียว (Select ของระบบไม่ block submit — ดู comment ใน ui/select.tsx)
   - **dialog**: เปิดแบบ conditional mount เท่านั้น (กติกาหัวไฟล์ `ui/dialog.tsx`) · ปุ่มท้าย = `DialogSubmitFooter`
   - ตาราง = `DataTable` (หัว sentence-case ห้าม UPPERCASE) · หน้า list = `ResponsiveList` (โหลด/พัง/ว่าง/สลับการ์ดมือถือ) · หัวตาราง custom = `TABLE_HEAD_SURFACE`
   - สีตัวหนังสือ = `text-strong` / `text-secondary` / `text-muted` (สลับธีมในตัว — ห้ามเขียนคู่ `dark:` เองสำหรับ 3 ระดับนี้ · ด่านใน `verify:ui`) · ช่องตัวเลข/เงิน = `NumberInput` / `MoneyInput` · ติ๊ก = `Checkbox`
   - สถานะ→สี/ป้าย ประกาศที่ `lib/status-config.ts` (+ `*_LABELS_CUSTOMER` สำหรับหน้า public) · วิธีส่ง `lib/shipping-methods.ts` · ช่องทางจ่าย `lib/payment-methods.ts` — ห้ามประกาศ map ซ้ำในหน้า

### 🎯 UI-2026 — หน้าตาทั้งเว็บให้ทันสมัย minimal (เบสเคาะจาก mockup รอบสอง 2026-08-25)
> ที่มา: เบสสั่ง "ปรับ UXUI ให้ทั้งเว็บทันสมัย minimal ตามเทรน 2026" → สำรวจโค้ดจริง 6 กลุ่มหน้า + benchmark Linear/Vercel/Stripe/Radix → เสนอ 3 ทิศทาง → เบสตีกลับรอบแรก ("ต่างไป · ขอพื้นที่หายใจ · order เด่นกว่า · ไม่เอาคำอธิบายใต้สถานะ") → รอบสองเคาะ "โอเคทำเลย"
> mockup ที่เคาะ: artifact `948ab738-2857-41a9-8770-dd6d5e263830`
> ขอบเขต: presentation เท่านั้น — ไม่แตะ query, mutation, permission, status transition, route, schema, dependency · แบ่ง 4 เฟส แต่ละเฟสจบในตัวและถอยกลับได้ · **ทุกเฟสต้องผ่าน `npm run verify:ui` โดยห้ามปิดด่าน** (แก้ค่าคาดหวังในด่านได้เฉพาะเมื่อบทบาทของ token เปลี่ยนจริง และต้องอยู่คอมมิตเดียวกัน)

**เฟส 1 — ฐาน (แก้ที่ token ครั้งเดียวเห็นทั้งเว็บ) ✅ 2026-08-26**
- [x] ความสูงบรรทัดที่ถูกกับฟอนต์ Prompt — Prompt มีกล่องบรรทัดธรรมชาติ 1.512em (Inter 1.21em) · `--text-sm` 14/20 → 14/22 · `--text-xs` 12/16 → 12/18 · `--text-2xs` lh 16 → 18 (คงขนาด 11px) · `--text-2xl` lh 1.2 → 1.3 · `--text-3xl` lh 1.15 → 1.25
- [x] เลิกบีบช่องไฟกับข้อความไทย — `letter-spacing` ทุกขั้นและที่ `body` เป็น 0 (สระบน/วรรณยุกต์ของ Prompt ห่างพยัญชนะอยู่แล้ว บีบแล้วชนกัน) · คงติดลบได้เฉพาะบล็อกตัวเลข/Latin ล้วน
- [x] ธีมมืดเลิกดำสนิท — ไล่ชั้นด้วยความสว่างจริง 5 ชั้น: chrome < bg < surface-muted < surface < elevated (เดิม bg `#000` กับ surface `#0a0a0a` ต่างกัน 4/255 และ chrome เท่ากับ bg เป๊ะ ซึ่งขัดคอมเมนต์ตัวเองที่ globals.css:219)
- [x] ธีมสว่างสลับทิศให้ตรงกับธีมมืด — กรอบเว็บจมลง เนื้อหาลอยขึ้น (`--color-chrome` ขาว → เทาอ่อน · `--color-bg` `#fafafa` → เกือบขาว · การ์ดขาวสุด) · ผลพลอยได้: ช่องค้นหาบนแถบบนมองเห็นเป็นครั้งแรก (เดิมพื้นช่อง = พื้นแถบเป๊ะ)
- [x] คืนสีเหลืองเตือน — **ย้าย `text-amber-500/600` (28 จุด) และ `text-yellow-600` ไป 700 ให้ครบก่อน แล้วค่อยเปลี่ยนค่า ramp** (สลับลำดับ = หน้าเว็บมีตัวหนังสือเหลืองอ่านไม่ออก) · 500 = สัญญาณ (จุด/แท่ง/ไอคอน) · 700 = ตัวหนังสือ · แก้ด่านให้เช็ก amber-500 แบบ non-text 3:1
- [x] บันไดเวลา + เส้นโค้งการเคลื่อนไหวเป็นครั้งแรก — `--duration-fast/base`, `--ease-out/in` ใน `@theme` · ห้าม `transition: all` · ห้าม stagger ในตาราง · reduced-motion สองชั้นเดิมต้องอยู่ครบ
- [x] ประกาศ `--color-ring` ที่หายไป (มี `ring-ring` ใช้อยู่ 5 จุดในจอผลิต/จอสถานี แต่ไม่มีตัวแปร → วงแหวนโฟกัสตกไปใช้สีข้อความ)
- [x] จอทัชเห็นแถบเลื่อน — `@media (pointer: coarse)` ให้ scrollbar thumb ปรากฏถาวร (เดิมโผล่เฉพาะตอน hover ซึ่งจอทัชไม่มี และเมนูซ้ายสิทธิ์เต็มยาวเกินจอแน่นอน)
- [x] `/orders` ตามที่เบสเคาะ: เลขออเดอร์แยกเป็นคอลัมน์ซ้ายสุด · ชื่องานเป็นตัวใหญ่สุดในแถว · ลูกค้าเป็นบรรทัดรอง · **ตัดคำอธิบายใต้สถานะออก** · ~~ตารางไม่มีกล่องครอบ~~ **ข้อนี้ถูกกลับในเฟส 6** — เบสเห็นของจริงบนจอกว้างแล้วตีกลับ
- [x] primitive ที่แตะต้องเลิกเขียนสีเทาดิบ — `CardTitle`/`CardDescription` ใช้ `text-strong`/`text-muted`
- [x] เก็บของที่ review รอบ adversarial ยืนยัน (33 ข้อเจอ · 5 ข้อถูกหักล้าง): hover ต้องอ่านออกบนผืนหน้าด้วยไม่ใช่แค่การ์ด (ตารางไม่มีกล่องครอบแล้ว) · ปุ่มบน chrome ใช้ชุด interaction ของ chrome · แถบล่างมือถือใช้ `INTERACTIVE_CHROME_PRESSED` แทนคลาสมือเปล่า (ตัวหนังสือตก 4.24:1 ตอนกดค้าง) · `flush` mode ให้ DataTable เสมอขอบและหัวตารางไม่มีแถบพื้น · `aria-label` การ์ดจอแคบอ่านชื่องาน · เพดานความกว้างสลับตามบทบาทบรรทัด · `.factory-board` คืน line-height เดิมให้จอ TV ที่สูงตายตัว · `.print-page` ล็อกช่องไฟกัน A4 ล้นหน้า · ไอคอนเหลืองบนพื้น amber ใช้ 700 · `FOCUS_INSET_ON_LIGHT` สำหรับกล่องขาวใต้ธีมมืดบังคับ · ลบ `transition: all` 3 จุด · ผูก overlay/การ์ด/ปุ่มกับ token จังหวะ
- [x] อัปเดตเอกสารที่กลายเป็นเท็จในคอมมิตเดียวกัน — `docs/DESIGN.md` ตารางสี+กฎ DataTable · `SPEC.md` ข้อสี/พื้นผิว · คอมเมนต์ใน `globals.css` และ `tokens.ts`
- [x] ขยายด่าน `verify:ui` ให้จับสิ่งที่เพิ่งหลุด — hover วัดเทียบ `bg` ด้วยไม่ใช่แค่ `surface` · เพิ่ม `chrome`/`chrome-hover` เข้าด่านสีข้อความ · ด่านห้ามใช้ amber/yellow-500 เป็นตัวหนังสือ · บันไดความลึกเช็กเป็น chrome < bg < surface แทนการล็อก hex ตายตัว
- [x] Verify: `verify:ui` ผ่านครบ · unit 1654/1654 · typecheck · lint 0 error (26 warning เดิมนอก scope) · production build 41 หน้า · เปิดจอจริงด้วย CSS ที่ build ออกมาแล้วทั้ง Light/Dark
- [x] ตรวจบนจอจริงหลังเบสสร้างบัญชีทดสอบ (2026-08-26) — `/orders` 1280 Light+Dark + 390 Dark · `/production` · `/factory` 1280×720 · overflow 0 · console error 0 · สถานะเหลือบรรทัดเดียวจริง
- [ ] **บล็อกก่อน deploy (ไม่ใช่งาน UI): ฐานข้อมูลคลาวด์ตามโค้ดไม่ทัน** — `orders.production_completion_owner_id` ไม่มีในคลาวด์ ทำให้ `order.list` พัง 500 · ต้องรัน migration กับคลาวด์ ต้องเบสเคาะเพราะเป็นของจริง


**เฟส 2 — โครงเว็บ + ตัวอักษร ✅ 2026-08-26**
- [x] แถบบน 64 → 48px · ตัดเส้นใต้โลโก้ที่ทำเป็นกากบาทมุมซ้ายบน · เสาไอคอนโลโก้กับเมนูตรงกัน · ~~โลโก้เป็น monochrome~~ **ข้อนี้คิดผิดและถูกกลับในเฟส 3.5** — ตราสัญลักษณ์ไม่อยู่ใต้กติกาสงวนสี
- [x] ชื่อหน้าเลิกใช้ขนาดนอกบันได `text-[1.65rem]` → `text-2xl font-semibold text-strong` (lh/tracking มาจาก token)
- [x] เลิก `uppercase` + `tracking-wide/wider/[0.1xem]` กับข้อความไทย — 19 จุดใน 15 ไฟล์ เหลือ 0
- [x] มุมโค้งของชิ้นเล็ก 8 → 6px (`RADIUS.item/field/inner`) · Button เลิก hardcode มาใช้ `RADIUS.field` · surface คง 8px
- [x] หัวคอลัมน์ตารางไม่ตัดกลางวลี ("กำหนดส่ง" เคยขึ้นสองบรรทัดกลางคำ)
- [x] หัวกลุ่มเมนู 6 → 4 โดยถอดหัวข้อของกลุ่มที่มี 2 รายการ — ไม่ย้ายรายการข้ามกลุ่ม (การจัดหมวดใหม่เป็นการตัดสินใจของเจ้าของ)
- [x] อัปเดตด่าน `verify:ui` + เทส navigation ให้ตรงกับเจตนาใหม่ในคอมมิตเดียวกัน

**เฟส 3 — สถานะ + สี + ความสม่ำเสมอทุกหน้า ✅ 2026-08-26**
- [x] `Badge` เปลี่ยนจากแคปซูลพื้นสีเป็นวงแหวน 1px + ตัวหนังสือสี มุม 6px — แก้ไฟล์เดียวเปลี่ยนพร้อมกัน 99 จุด · ใช้ `ring` ไม่ใช่ `border` เพื่อไม่ให้ขนาดป้ายขยับ
- [x] ยุบแคปซูลที่เขียนสูตรเองในหน้าผลิตให้มาใช้ `Badge` ตัวเดียวกัน
- [x] สีเทาเขียนมือ 343 จุดใน 69 ไฟล์ → `text-strong/secondary/muted` · คู่เส้นเขียนมือ 51 จุดใน 33 ไฟล์ → `border-border/divider` · เก็บอีก 6 จุดที่ฝั่งมืดเป็น alpha ขาว (หน้ากระดาษ A4 ยกเว้นโดยตั้งใจ)
- [x] ตารางลูกหนี้เหลือแดงเฉพาะช่วงเกิน 90 วัน (เดิมย้อม 4 ใน 5 คอลัมน์) · ป้ายกลุ่มลูกค้าเลิกยืมจานสีสถานะ · ยอดรวมใบเสนอราคาใช้ `DISPLAY_AMOUNT` · ตารางบิลเลิกมีน้ำเงินสองเฉดในแถวเดียว
- [x] แถวลูกค้า/ใบเสนอราคากดได้ทั้งแถว (เดิมไฮไลต์เต็มแถวตอนชี้แต่กดไม่ได้) เลขเอกสาร/ชื่อจึงเลิกเป็นลิงก์สีน้ำเงิน
- [x] ~~11 ตารางระดับบนสุด → `bordered={false} flush`~~ **ถูกกลับในเฟส 6** (คืนกล่องครอบทั้งหมด) · แถบแบ่งหน้ายังอยู่ที่ primitive ตัวเดียว
- [ ] `/factory` ยังเขียนสี alpha ดิบ 13 จุด — **ตรวจจอจริงแล้วอ่านถูกต้องทั้งจอ** จึงไม่ไล่แก้ในรอบนี้ (churn สูง ผลตาเห็นเป็นศูนย์) · ยกไปทำตอนแตะไฟล์นั้นตามกติกา boy-scout

**เฟส 3.5 — คืนสีแบรนด์ให้อยู่ถูกที่ ✅ 2026-08-26 (เบสทัก "อย่าลืมสีฟ้าที่เป็น asset เรา")**
> ตรวจด้วย workflow 6 agent ทุกจอ · ข้อเท็จจริง: จุดที่ใช้ blue-* ลดจาก 346 เหลือ 298 (14%) ซึ่งเกือบทั้งหมดคือการเก็บ "สีลิงก์" ออกจากตาราง = ถูกแล้ว · ปุ่มหลัก เมนูที่เลือก แท็บ แถบขั้นตอน แถบความคืบหน้า ยังเป็น Anajak Blue ครบ
> **แต่หายผิดที่ 3 จุด** เพราะกติกา "สงวนน้ำเงินให้ปุ่มหลัก/สิ่งที่เลือก/โฟกัส" ไม่มีช่องสำหรับคำว่า **ตัวตน** พอของชิ้นไหนไม่ใช่ปุ่มและไม่ใช่สถานะ กติกาจึงแปลว่า "ห้ามมีสี" โดยไม่มีใครตั้งใจ
- [x] **ตราสัญลักษณ์บนแถบบน** — เฟส 2 เปลี่ยนจากสี่เหลี่ยมน้ำเงินทึบเป็นกล่องขาวขอบเทา · คืนแล้ว + เพิ่มด่านล็อกใน `brand-selection.test.ts` (เดิมล็อกเมนู/แท็บ/ชิปไว้หมดแต่ไม่มีบรรทัดไหนล็อกโลโก้ นี่คือเหตุผลที่มันหลุดเงียบ ๆ)
- [x] **เอกสารพิมพ์ 5 ใบ เคยมีสีแบรนด์ 0 จุด** (ทั้งก่อนและหลังงานนี้ — ไม่เคยถูกตัดสินใจเลยสักครั้ง) · ใส่ตราน้ำเงินหัวใบ + เส้นคาดใต้หัวเป็นสีแบรนด์ · เพิ่ม `print-color-adjust: exact` ไม่งั้นเบราว์เซอร์ทิ้งพื้นสีตอนพิมพ์แล้วตราออกมาเป็นกล่องขาว
- [x] **หัวการ์ดทุกหน้าที่ลูกค้าเห็น** เป็นเทาโดยการออกแบบ · ใส่ช่องตราใน `PublicPageShell` ที่เดียวได้ครบ 5 หน้า · blind ship ปิดตราเองอัตโนมัติ (`hideBrandMark = hideFooter`) ไม่ต้องรอให้ caller จำ
- [x] คืนคู่สีให้ไทม์ไลน์หน้าติดตามสถานะ — วงกลมยังน้ำเงินแต่ตัวหนังสือกลายเป็นเทา คู่นี้ไม่พูดภาษาเดียวกัน
- [x] สร้าง `src/app/icon.svg` — ทั้ง repo ไม่เคยมีไฟล์โลโก้/favicon เลย แท็บเบราว์เซอร์จึงไม่มีแบรนด์
- [x] เก็บความไม่เท่ากันที่ตกสำรวจ — `/billing/aging`, `/billing/wht`, การ์ดจอแคบของ `/billing` และ `/quotations` ยังเป็นลิงก์น้ำเงินทั้งหน้า ทั้งที่ตารางเดสก์ท็อปของหน้าเดียวกันถอดไปแล้ว
- [x] `/billing` ไม่มีปุ่มหลักเลยบนเดสก์ท็อป = ทั้งหน้าไม่มีสีแบรนด์โดยไม่มีใครตั้งใจ · ยกปุ่มรับชำระขึ้นเป็นปุ่มหลักหนึ่งใบ
- [x] **หัวจอ `/factory` มีตรา Anajak แล้ว** (2026-08-26) — จอนี้ไม่มี sidebar/topbar ตราจึงไม่มีที่อยู่โดยอัตโนมัติ และก่อนหน้านี้ทั้งจอไม่มีคำว่า Anajak สักคำ ทั้งที่แขวนหน้าโรงงานทั้งวัน · ใส่ทั้งจอรุ่นเดิมและรุ่น v2 + ล็อกด้วยเทส

**เฟส 4 — จังหวะ + เก็บกวาด ✅ 2026-08-26**
- [x] `src/app/(dashboard)/loading.tsx` ไฟล์แรกของโปรเจกต์ — กดเมนูแล้วได้โครงร่างทันที ไม่ค้างหน้าเดิม 200-800ms (Next แนะนำ `loading.js` เป็นทางหลัก)
- [x] `ListPageSkeleton`/`ListSkeleton` เป็นรูปเดียวของระบบ — แทนแผ่นเทาก้อนเดียว `h-96` 15 ไฟล์ และแถบ `h-20` ของ `ResponsiveList` · ความสูงแถว 69px ตรงกับของจริงจึงไม่กระโดด
- [x] `useLinkStatus` ในเมนูซ้ายเป็นสัญญาณเสริมระหว่าง prefetch ยังวิ่ง (จุดเล็ก ไม่ทำให้แถวขยับ · `motion-reduce` ครบ)
- [x] Suspense fallback ของโครงเว็บเลิกเป็นกล่องเปล่า
- [x] แถบความคืบหน้ายุบเหลือสูตรเดียว + `transition-[width]` ผูก token จังหวะ (5 จุด เดิมไม่มี transition เลยทั้งที่ refetch ทุก 30 วิ)
- [ ] เส้น hairline เป็น alpha ค่าเดียว + ย้ายเส้นคั่นแถวเข้าเซลล์ — **เลื่อนต่อ** เพราะต้องรื้อ logic ด่านที่ parse hex ก่อน และตอนนี้เส้นทั้งระบบมาจาก token เดียวแล้ว (ผลตาเห็นน้อย)

**เฟส 5 — สีและดีไซน์ของแถบเมนู/แถบบน ✅ 2026-08-26 (เบสทัก "sidebar และ navbar ไม่ค่อยชอบการใช้สี และ design")**
> ไม่เดาว่าเบสติดตรงไหน — ทำ mockup กดสลับได้ 3 แบบ (artifact `a6f22bcc-2e7f-4564-bba8-af22d6336331`) เนื้อหาฝั่งขวาเหมือนกันหมด จะได้เทียบเฉพาะแถบเมนู
> เบสตอบ **"ข"** ก่อน แล้วเปลี่ยนใจเป็น **"ก"** — บันทึกไว้เพราะสองแบบนี้ต่างกันที่สีแถบด้วย ไม่ใช่แค่หน้าตาเมนู
- [x] **แบบ ก · เงียบ** — เมนูที่กำลังเปิดอยู่เลิกเป็นพิลฟ้าเต็มแถบ เหลือพื้นเทากลาง ๆ (`interactive-chrome-pressed` บน chrome · `interactive-pressed` ในลิ้นชักมือถือ) + ขีดสีแบรนด์ 2px ริมซ้ายของแถบ + ตัวหนังสือเข้มขึ้น · เหตุผล: พิลฟ้ากินพื้นที่และแย่งสายตากับปุ่มหลักที่ใช้น้ำเงินเหมือนกัน น้ำเงินในแถบเมนูจึงเหลือหน้าที่เดียวคือบอกว่า "อยู่ตรงนี้"
- [x] **แถบเมนูยังเป็นเทาอ่อน/เกือบดำเหมือนเดิม** — แบบ ก ไม่แตะบันไดความลึกของเฟส 1 (ต่างจากแบบ ข ที่จะคืนแถบเป็นสีขาวและย้อนทิศธีมสว่างที่เบสเคาะไปแล้ว)
- [x] แถบล่างมือถือใช้ภาษาเดียวกัน — พื้นเทากลาง + ขีดแบรนด์ด้านบน · เก็บปุ่ม "เพิ่มเติม" ที่ยังตกค้างสูตรเก่าและย่อหน้าที่เพี้ยน
- [x] แก้ด่านที่เข้ารหัสสัญญาเก่าไว้ในคอมมิตเดียวกัน — `verify:ui` (`navigationContractIsWired` เดิม **บังคับให้มี** พิลฟ้าและ **ห้ามมี** ขีดแบรนด์ ซึ่งกลับด้านกับแบบ ก ทั้งคู่) และ `brand-selection.test.ts` · ไม่ปิดด่านสักข้อ เปลี่ยนเฉพาะค่าคาดหวังที่บทบาทเปลี่ยนจริง
- [x] `interactive-selected` ยังเป็นน้ำเงินและไม่ถูกแตะ — เมนูโมดูลในหน้าผลิต แถบ segmented รอบพิมพ์ และจอสถานี ยังใช้ของเดิม (คนละระดับกับแถบเมนูหลัก จงใจให้ต่าง)
- [x] Verify: `verify:ui` ผ่านครบ · unit 1658/1658 · typecheck · lint 0 error · production build ผ่าน · จอจริง `/orders` Light+Dark วัดค่าสีที่คำนวณจริง (พื้น `#dcdce0`/`#222225` · ขีด `#3973b2`/`#6b9cca` · ขีดอยู่ที่ -12px = ชิดขอบแถบพอดี) และ `/factory` เห็นตรา Anajak

**เฟส 11 — Light workspace + Orders hierarchy + visible filters ✅ 2026-08-27 (เบสสั่ง)**
> เบสสั่งจากหน้า `/orders` จริง: “พื้นหลังสีเทาของธีมสว่างให้ขาวขึ้น” · “ชื่อลูกค้าให้เด่นกว่าชื่องาน” · “ตัวกรองไม่ต้องซ่อน เอาออกมาเลยและจัดสวย ๆ”
> ขอบเขต presentation เท่านั้น — คง query, URL state, permission, mutation, status transition, route, Dark theme, public และ print เดิม

- [x] เปลี่ยนผืนงาน Light จากเทาชัดเป็น near-white โดยยังให้ตาราง/การ์ดขาวอ่านเป็นผิวแยกผ่าน edge + เงากลางเดิม; ปรับ contract/d่านที่ล็อกความต่างสีเก่าให้ตรงบทบาทใหม่ ไม่ปิดด่าน
- [x] `/orders` ทั้ง desktop/mobile ใช้ **ชื่อลูกค้าเป็นบรรทัดหลัก** และชื่องานเป็นบรรทัดรอง พร้อม fallback ที่ไม่แสดงข้อมูลซ้ำ; หัวคอลัมน์เปลี่ยนเป็น “ลูกค้า / งาน”
- [x] ย้ายช่วงวันที่ · ช่องทาง · ประเภทออเดอร์ออกมาเห็นบน toolbar ตลอด โดยใช้ control กลางแบบ flat/compact; คืนปุ่ม “ล้างตัวกรอง” เมื่อมีค่าค้าง · มือถือ 320–390px wrap เป็นกริด 2×2 โดยไม่ล้นและรายการแรกอยู่ `y=481` (มีปุ่มล้าง `y=533`)
- [x] คง status rail, attention deep link, search, sort, pagination, CSV, empty/error/loading, URL state และ permission เดิมทั้งหมด
- [x] Verify: targeted 11/11 · `verify:ui` · unit 1659/1659 · typecheck · lint 0 error เมื่อไม่นับไฟล์ทดลอง untracked เดิม `scripts/_probe-sidebar.tsx` · production build · detector `[]` · browserจริง Light 1280/1024/390/320 + Dark 1280 รวมเลือก/ล้างตัวกรอง, overflow, console และ hydration

**เฟส 10 — Modern Minimal ทั้งเว็บ ✅ ลงแล้ว 2026-08-26 · เหลือ QA จอโรงงาน**
> เบสสั่งสามเรื่องพร้อมกัน: "ทุกหน้าไม่ต้องมีหัวข้อเล็กๆแบบนี้" (ส่งภาพ breadcrumb มาชี้) · "หุบเมนูไม่ต้องเด่น เอาไว้บนโลโก้ได้มั้ย" · "เว็บมันดูแข็งๆ ไม่ค่อยโมเดิร์น อยากได้ Modern Minimal โค้งมนหน่อย ทุกหน้าทุกส่วน"
> ตัวอย่างกดเล่นได้ (3 ระดับ × สองธีม): artifact `510dc177-7648-4c74-bcbb-127cc2ae952f`

- [x] **ถอดแถบเส้นทางออกทุกหน้า** — แก้ที่ `page-header.tsx` ที่เดียว · prop `breadcrumb` ยังอยู่และห้ามลบ เพราะเป็นที่มาของชื่อ/คำอธิบายปริยาย **และของปุ่มย้อนกลับ** · มี 5 หน้าที่เคยมีแต่ breadcrumb ไม่มี `back` ถ้าถอดเฉย ๆ จะไม่เหลือทางกลับบนจอ — ตอนนี้ปุ่มย้อนกลับสืบทอดจาก breadcrumb ให้อัตโนมัติ
- [x] **ปุ่มหุบเมนูย้ายไปข้างตรา แบบเงียบ** — ไอคอนล้วน ไม่มีป้ายชื่อ ใช้ทรงเดียวกับปุ่มกระดิ่งบนแถบบน · ตอนหุบไม่มีที่พอข้างตรา จึงลงไปอยู่บนสุดของเมนู
- [x] **เบสเคาะ "นุ่มเต็มที่"** — ชิ้นเล็ก 6 → **10px** · กล่องทั่วไป 8 → **12px** · การ์ด **16px** · การ์ดเลิกใช้เส้นขอบ เปลี่ยนเป็น **เงาชุดกลางชุดเดียว** · แถวตาราง `py-3 → py-4` เซลล์ `px-5 → px-6` · จังหวะระดับหน้า 24 → 28px
- [x] **แก้ที่ `@theme` ครั้งเดียวเห็นทั้งเว็บ** — นิยาม `--radius-md`/`--radius-lg` ใหม่ · `RADIUS.surface` → `rounded-2xl` · sed `card-surface` ที่คู่กับ `rounded-lg` เป็น `rounded-2xl` 69 บรรทัด
- [x] **กลับคำ 3 ข้อ + แก้ด่านตาม (ไม่ปิดด่านสักข้อ)**
  - ~~"กล่องห้ามมีเงาตกแต่ง"~~ → ด่านเปลี่ยนจาก "ห้ามมีเงา" เป็น **"ต้องมีเงาชุดกลางชุดเดียวและห้ามแรงเกินเพดาน"** (blur ≤ 20px · alpha ≤ 0.2) · ยังห้าม utility เงา (`shadow-sm/md/lg`) ทุกกรณี เพราะเงาต้องมาจาก `.card-surface` ที่เดียว
  - ~~"ชิ้นเล็กต้องโค้ง 6px"~~ → 10px · เหตุผลเดิม ("8px บนของสูง 28-36px อ่านเป็นเม็ดยา") ใช้ไม่ได้แล้วเพราะเฟสนี้ของสูงขึ้นทั้งชุด
  - ~~ความสูงแถว 69px~~ → **75px** วัดจากของจริงในเบราว์เซอร์ · เพิ่มด่านล็อกให้ `page-skeleton.tsx` กับ `data-table.tsx` พูดตรงกัน ทดสอบด้วยการเปลี่ยนกลับเป็น 69 แล้วด่านแดงตามคาด
- [x] **เก็บเมนูซ้ายตอนหุบ** (เบสทัก "sidebar ตอนหุบ UI ก็ไม่ดี" 2026-08-26) — วัดของจริงเจอสามอย่างพร้อมกัน:
  - **ปุ่มเมนูถูกบีบเหลือกว้าง 24px สูง 36px** = อ่านเป็นเม็ดยา · เพราะ `nav` มี `px-3` ซ้อนกับรางแถบเลื่อนสองข้าง เหลือเนื้อที่จริงแค่ 19px · แก้เป็น **ปุ่มสี่เหลี่ยมจัตุรัส 40px วางกลางราง** และถอด `px-3` ออกตอนหุบ
  - **แกนกลางสามเส้นไม่ตรงกัน** — ตรา 31.5 · ไอคอนเมนู 34 · ปุ่มหุบ 40 · ตอนนี้ทั้งสามอยู่ที่ 31.5 เท่ากัน (ปุ่มหุบขนาดเท่าปุ่มเมนู)
  - **ขีดสีแบรนด์ตอนหุบอยู่ริมรางจนอ่านเป็นคนละชิ้นกับไอคอน** — ตอนหุบเลิกใช้ขีด ให้พื้นเทาเต็มปุ่มบอก "อยู่ตรงนี้" แทน (ตอนกางยังใช้ขีดเหมือนเดิม)
  - ช่องว่างระหว่างกลุ่มย่อจาก 16 เหลือ 12px เพราะหัวกลุ่มหายไปตอนหุบ 16px จึงอ่านเป็นเว้นมั่ว
  - ด่านล็อกทั้งสามเรื่อง ทดสอบด้วยการทำให้ปุ่มกลับไปถูกบีบแล้วด่านแดงตามคาด
- [x] **แถบบนสูงขึ้น 48 → 56px** (เบสบอก "ดูต่ำไป" หลังทั้งเว็บโค้งมนขึ้น) — ของในหน้าสูงขึ้นทั้งชุดแล้ว แถบ 48px จึงกลายเป็นแถบที่แน่นกว่าเนื้อหาที่มันครอบอยู่ · ประวัติความสูง: 64 → 48 (เฟส 2) → **56px** (เฟส 10)
- [x] **ล็อกสามที่ให้สูงเท่ากันเสมอ** — แถวแรกของกริด `3.5rem` · แถบบน `h-14` · หัวเมนูซ้ายที่ถือตรา `h-14` · ถ้าไม่เท่ากัน เส้นล่างของตรากับของแถบบนจะไม่ต่อกันเป็นเส้นเดียวข้ามจอ · ด่านนับ `flex h-14` ต้องได้ 2 จุดพอดี ทดสอบด้วยการทำให้หัวเมนูเตี้ยลงแล้วด่านแดงตามคาด
- [x] Verify: `verify:ui` ผ่านครบ · unit 1659/1659 · typecheck · lint 0 error · build ผ่าน · วัดจอจริง (การ์ด 16px · เงาติด · แถว 74.5px)
- [ ] **ยังไม่ได้ตรวจ:** จอทัชโรงงานมีแสงสะท้อน เงาจะจางกว่าบนคอม — ต้องเปิดจอนั้นดูจริงก่อนขึ้นใช้งาน

**เฟส 9 — เก็บเมนูหุบตามที่เบสทักจากจอจริง ✅ 2026-08-26**
- [x] **ไอคอนตอนหุบไม่อยู่กลางคอลัมน์** — เมนูมี `overflow-y-auto` และแถบเลื่อนของโปรเจกต์กินที่จริง 10px พอเมนูยาวเกินจอ กล่องเนื้อหาเหลือ 53 จาก 63 ไอคอนจึงถูกดันไปกลางที่ 26.5 ขณะที่ตราซึ่งอยู่นอกเมนูยังอยู่ที่ 31.5 = เหลื่อม 5px · แก้ด้วย `scrollbar-gutter: stable both-edges` เฉพาะตอนหุบ (ตอนกางไม่ใช้ เพราะจะกินความกว้างชื่อเมนูไป 20px เปล่า ๆ) · วัดหลังแก้: ตรา ปุ่มหุบ และไอคอนทุกตัวอยู่กลางที่ 31.5 เท่ากันหมด
- [x] **ย้ายปุ่มหุบขึ้นไปบนสุดของเมนู** (เบสสั่ง) — เดิมอยู่ท้ายแถบ · ตอนนี้อยู่เหนือรายการเมนูทั้งสองสถานะ ตำแหน่งเดียวกันไม่ว่าจะหุบหรือกาง
- [x] ล็อกด้วยด่าน + ทดสอบด้วยการลบคำสั่งจองรางออกจริงแล้วด่านแดงตามคาด

**เฟส 8 — เก็บของที่พังตามหลังการรื้อโครง ✅ 2026-08-26 (จาก workflow ตรวจ 34 agent)**
> ส่งทีมไล่ตรวจสี่มุมหลังลงเฟส 6 พร้อม adversarial verify ทีละข้อ · ยืนยันของจริง 29 ข้อ (ซ้ำกันข้ามมุมบ้าง) เก็บรอบนี้ตามนี้

- [x] **ลำดับ Tab เดินขวาไปซ้าย** — `<header>` อยู่ก่อน `<aside>` ใน DOM แต่หลังย้ายโครง aside กินแถวที่ 1 ตราจึงนั่งมุมซ้ายบนสุด · คนกด Tab ได้ ค้นหา → กระดิ่ง → บัญชี (มุมขวาบน) แล้วเด้งข้ามจอกลับมาที่ตรา (WCAG 2.4.3) · ย้าย `<aside>` ขึ้นก่อนใน DOM
- [x] **ของบนแถบบนล้ำขอบขวาของเนื้อหา 16px** (บวกความกว้างแถบเลื่อนอีก 10px แบบไม่คงที่ระหว่างหน้าสั้น/ยาว) — แถบใช้ `px-3 lg:px-4` เนื้อหาใช้ `px-4 sm:px-6 lg:px-8` · ตอนแถบพาดเต็มจอไม่มีใครเห็น พอวางเหนือคอลัมน์เดียวกันพอดีจึงโผล่ · ให้แถบใช้กล่อง `mx-auto max-w-screen-2xl` ชุดเดียวกับเนื้อหา + `<main>` จองรางแถบเลื่อนด้วย `scrollbar-gutter: stable` และแถบเผื่อระยะเท่ากันผ่าน `--app-scrollbar-w`
- [x] **ตราในเมนูซ้ายไม่ตรงแนวไอคอนเมนู** — ตราอยู่ที่ 12px ไอคอนเมนูอยู่ที่ 24px (nav `px-3` + แถว `px-3`) · เปลี่ยนบล็อกตราเป็น `px-6` ให้เสาไอคอนต่อกันตลอดคอลัมน์
- [x] **ชื่อที่เครื่องอ่านไม่ตรงกับตัวหนังสือที่เห็น** — ลิงก์ตราตั้ง `aria-label="ภาพรวม"` ทั้งที่เห็นคำว่า "Anajak Print" (WCAG 2.5.3 — คนสั่งงานด้วยเสียงพูดชื่อที่เห็นแล้วไม่โดน) · ตอนกางเลิกตั้ง aria-label ปล่อยให้ชื่อมาจากตัวหนังสือ ตอนหุบค่อยตั้งเป็น "Anajak Print"
- [x] **หน้ารายการกระโดดทุกครั้งที่เปิด** — ตอนมีข้อมูล = ตารางในการ์ด แต่ตอนโหลด/ว่าง/พัง = เนื้อหาลอยบนผืนงาน กล่องจึงโผล่พร้อมข้อมูล · ทุกสถานะได้กรอบเดียวกันแล้ว และ `ResponsiveList` เลิกมีโครงร่างสูตรที่สอง หันไปใช้ `ListSkeleton` ตัวเดียวกับ `loading.tsx`
- [x] **วงกลมไอคอนของ EmptyState หายไปกับพื้น** — `bg-slate-100` (#f1f5f9) เกือบเท่าผืนงานใหม่ (#f1f2f4) · เปลี่ยนเป็น token กลาง
- [x] **ของกดได้ที่ยืนบนผืนงานชี้แล้วไม่ขยับ** (hover เหลือ 1.03 เท่า) — เพิ่มคู่ interaction ชุดที่สาม `interactive-page-hover/pressed` สำหรับ "ของที่ไม่ได้อยู่ในการ์ดและไม่ได้อยู่บนแถบ" · ใช้กับปุ่มแบ่งหน้าและปุ่มย้อนกลับบนหัวหน้า · **ตอนลงเฟส 6 ฉันถอดด่านที่วัด hover บนผืนงานทิ้งโดยไม่ได้หาที่มาแทน — ด่านใหม่นับจำนวนจุดที่ใช้จริง และทดสอบด้วยการลบออกทีละปุ่มแล้วด่านแดงตามคาด**
- [x] **ปุ่มในการ์ดขาวมีแถบเทาคาดรอบวงแหวนโฟกัส** — `ring-offset-bg` เคยแก้ปัญหาเดียวกันตอนผืนงานเกือบขาว พอผืนงานเป็นเทาจริงก็กลับมาเป็นอาการเดิมคนละสี · เปลี่ยนเป็น `ring-offset-transparent` ให้ช่องว่างโชว์พื้นที่อยู่ข้างหลังจริง
- [x] **เอกสารที่กลายเป็นเท็จ** — `SPEC.md` ข้อชุดสี + ข้อ "แถบบนพาดเต็มจอ" · `docs/DESIGN.md` ย่อหน้าบันไดที่ขัดกับย่อหน้าถัดไปเอง + ตารางสีทั้ง 5 แถว · `PROGRESS.md` ประวัติเฟส 1/3/4 ที่ถูกกลับคำไปแล้ว
- [x] **ด่านที่ขาด** — `user-menu.tsx` ไม่เคยมีเทสแตะเลยสักบรรทัด ทั้งที่อยู่บนทุกหน้า (บั๊กวงรีจึงอยู่มานาน) · และเทสล็อกตราใช้ `toContain` ซึ่งอ่อนลงเมื่อมีตราสองก้อน เปลี่ยนเป็นนับจำนวน
- [ ] **ยังไม่ได้เก็บ (บันทึกไว้ตั้งใจ):** แถบ "ยังไม่อ่าน" ในหน้าแจ้งเตือน · รางของ `SegmentedControl` · จุดขั้นที่ยังไม่ทำในรางเส้นทางการผลิต — ทั้งสามใช้ `surface-muted` ซึ่งตอนนี้สว่างกว่าผืนงาน จึงกลับทิศเมื่อชิ้นนั้นไปยืนบนผืนงานแทนที่จะอยู่ในการ์ด · ต้องดูทีละจุดว่าควรย้ายเข้าการ์ดหรือเปลี่ยน token ไม่ควรไล่แก้แบบเหมารวม

**เฟส 7 — แถบขาว · เมนูหุบได้ · เก็บจุดที่กวนใจ ✅ 2026-08-26 (เบสสั่งต่อหลังเห็นเฟส 6 บนจอจริง)**
> เบสสั่ง 3 อย่างติดกัน: "อยากได้ sidebar navbar สีขาว และเอาข้อความหัวข้อออกจาก navbar และปรับส่วนอื่นๆ ให้เข้ากัน" → "อยากปรับ UI sidebar หน่อย ให้มีโหมดหุบและกาง โดยหุบจะแสดงแต่ไอคอน และไม่ชอบเวลากดเลือกหัวข้อแล้วมีจุด"

- [x] **กรอบเว็บธีมสว่างเป็นขาว** — `--color-chrome` `#f0f1f3` → `#ffffff` · ผืนงานยังเป็นเทา `#f1f2f4` การ์ดขาว `#ffffff` · กรอบกับการ์ดเป็นขาวชุดเดียวกัน ผืนงานเทาคือพื้นจมชิ้นเดียวของธีมสว่าง
- [x] **ของที่ยืนบนแถบปรับตาม** — ช่องค้นหาเปลี่ยนจาก `bg-surface` (ขาวบนขาว เห็นแค่เส้น) เป็น `SUNK_PANEL` = ช่องจม · `interactive-chrome-hover/pressed` ธีมสว่างเท่ากับคู่ที่ใช้บนการ์ดแล้ว (ตั้งใจ เพราะพื้นเดียวกัน) ธีมมืดยังแยกอยู่
- [x] **เอาชื่อหมวดออกจากแถบบน** — ตำแหน่งที่อยู่บอกด้วยเมนูซ้ายที่ไฮไลต์อยู่แล้ว เขียนซ้ำบนแถบก็เป็นคำเดียวกันสองที่ · แถบเหลือหน้าที่เดียวคือของที่ใช้ได้ทุกหน้า (ค้นหา · แจ้งเตือน · บัญชี) จึงดันไปชิดขวาทั้งชุด
- [x] **เมนูซ้ายหุบ/กางได้** — กาง 15rem / หุบ 4rem เหลือแต่ไอคอน · ปุ่มสลับอยู่ท้ายแถบ (อยู่ที่เดิมทั้งสองสถานะ) · ความกว้างมาจาก `--app-sidebar-w` คอลัมน์เนื้อหาจึงขยับตามเอง · จำไว้ที่เครื่องผู้ใช้ด้วย `localStorage`
- [x] **ใช้ `useSyncExternalStore` ไม่ใช่ `useState` + `useEffect`** — กัน SSR กับ client เห็นค่าคนละอย่างตอน hydrate และไม่เพิ่ม warning `react-hooks/set-state-in-effect`
- [x] **หุบแล้วยังรู้ว่าปุ่มไหนคืออะไร** — คง `title` + `aria-label` ไว้ทุกเมนู · หัวกลุ่มหายไปตอนหุบ แต่ `aria-label` ของ `<ul>` ยังอยู่
- [x] **ถอดจุดกะพริบตอนกดเมนู** (`NavPendingMark` + `useLinkStatus`) — เบสไม่ชอบ · ไม่เสียสัญญาณ "ระบบรับรู้แล้ว" เพราะ `loading.tsx` เป็นทางหลักอยู่แล้ว จุดในเมนูเป็นแค่ตัวเสริมระหว่าง prefetch
- [x] **แก้ด่านให้ตรงกับสัญญาใหม่ พร้อม mutation test** — ด่านบันไดความลึกเลิกบังคับ `chrome < bg` ทั้งสองธีม เปลี่ยนเป็น "การ์ดลอยเหนือผืนงานทั้งสองธีม + Light chrome อยู่เหนือผืนงาน ≥1.1 เท่า + Dark chrome ยังจมใต้ผืนงาน" · ด่าน Sidebar เลิกล็อกความกว้างตายตัว มาล็อกตัวแปร/`useSyncExternalStore`/`title`/`aria-label` และห้ามคืนจุดกะพริบ · ทดสอบด้วยการทำโค้ดพังจริงแล้วด่านแดงตามคาด
- [x] Verify: `verify:ui` ผ่านครบ · unit 1658/1658 · typecheck · lint 0 error (จำนวน warning เท่าเดิมเป๊ะกับก่อนแก้) · build ผ่าน · ตรวจจอจริงด้วย CSS ที่ build ออกมา ทั้งสถานะกางและหุบ

**เฟส 6 — พื้นตาราง + แถบบน ✅ 2026-08-26 (เบสเลือก "การ์ดครอบ" + "ไม่มีแถบบนแต่ขอมี navbar")**
> เบสบอกสองเรื่อง: "การที่เอาตารางวางบนพื้นเลยดูแปลกๆ และไม่ชอบ" · "ไม่ชอบ Navbar แบบนี้ ปรับใหม่หน่อย"
> ข้อแรกคือการกลับคำตัดสินใจของเฟส 1/3 (ตารางไม่มีกล่องครอบ) — ตอนเคาะดูจากตัวอย่าง 4 แถวบนจอแคบ พอเป็นของจริงบนจอกว้างที่มีตัวกรอง+ช่องค้นหา+แบ่งหน้าครบ ผลคนละเรื่อง
> ตัวอย่างกดเล่นได้ (สองแกนอิสระ · ธีมสว่าง/มืด): artifact `fbecf315-2342-4be9-9ea4-5357e333501c`
> **ยังไม่แตะโค้ดจนกว่าเบสจะเลือก** (กติกา: งาน UX/UI ต้องให้เบสเคาะจากตัวอย่างก่อน)

- [x] **การ์ดครอบตาราง** — ถอด prop `flush` ออกจาก `data-table.tsx` ทั้งตัว · 12 จุดที่เคยเป็น `<DataTable.Root bordered={false} flush>` กลับไปใช้ `bordered` ปริยาย = `card-surface rounded-lg`
- [x] **ถ่างบันไดพื้นผิวธีมสว่าง** (ถ้าไม่ทำ กล่องที่ได้จะเป็นแค่เส้นบางเหมือนเดิม) — ผืนหน้า `#fbfbfc` → `#f1f2f4` · การ์ดต่างจากผืนหน้า 1.03 → **1.12 เท่า** · chrome `#f2f3f5` → `#f0f1f3` ยังจมใต้ผืนหน้าตามทิศเดิม · ธีมมืดไม่แตะ (เบสไม่ได้ทักฝั่งนั้น และบนพื้นมืดตาแยกชั้นได้ดีกว่า)
- [x] **chrome ถ่างได้แค่นี้** — ลองให้เข้มกว่านี้ (`#e6e7ec`) แล้ว hover ของมันต้องเข้มตาม ทำให้ `text-muted` บน `interactive-chrome-hover` ตกเหลือ 4.24:1 · สิ่งที่แยกเมนูซ้าย/แถบบนออกจากเนื้อหาจึงเป็น **เส้นขอบ** ไม่ใช่สีพื้น
- [x] **แถบบนย้ายไปอยู่เหนือเฉพาะคอลัมน์เนื้อหา** — `lg:col-span-1 lg:col-start-2` · เมนูซ้ายสูงเต็มจอ (`lg:row-span-2 lg:row-start-1`) และถือตราไว้ข้างบนในบล็อกสูง 3rem เท่าแถบบน เส้นล่างจึงต่อกันเป็นเส้นเดียว · จอแคบไม่มีเมนูซ้าย แถบจึงยังพาดเต็มจอและถือตราเหมือนเดิม
- [x] **แถบบนมีงานทำแล้ว** — ใส่ชื่อหมวดที่กำลังเปิดอยู่จาก `activeNavigationItem.label` (ทะเบียนเมนูตัวเดียวกับ sidebar ไม่ได้ดึง `<h1>` ของหน้ามา เพราะหัวข้อเป็นของหน้า ไม่ใช่ของกรอบเว็บ) · ช่องค้นหาจอกว้างหดจาก 512px เป็นชิป 240px
- [x] **ข้อบกพร่อง 3 ข้อที่ยืนยันแล้ว — แก้ครบ**
  - หัวคอลัมน์เยื้อง 20px — **หายไปพร้อมกับ `flush`** ที่เป็นต้นเหตุ ไม่ต้องเพิ่มโค้ดชดเชย
  - ตารางไม่มีเส้นปิดล่าง — **ขอบล่างของการ์ดปิดให้แล้ว** · `TablePagination` เลิกใช้ `border-t` (มันเคยเป็นเส้นปิดโดยบังเอิญ แล้วหายไปเองเมื่อมีหน้าเดียว) เปลี่ยนเป็น `pt-3` ยืนใต้การ์ด
  - รูปผู้ใช้เป็นวงรี 36×44 บนจอทัช — เพิ่ม `[@media(pointer:coarse)]:w-11` ให้ความกว้างเดินตามความสูง · พร้อมกันนั้นเลิกเป็นวงกลมน้ำเงินทึบ (เดิมมีน้ำเงินสองก้อนคนละมุมแถบเดียวกัน ตรากับรูปคนแย่งกันเป็นตัวแทนแบรนด์) และเลิก `hover:scale-105` ที่ผิดภาษากับทั้งเว็บ
- [x] **แก้ด่านที่เข้ารหัสสัญญาเก่าไว้ ในคอมมิตเดียวกัน (ไม่ปิดด่านสักข้อ)** — `stateContrastIsBalanced` เดิมบังคับ hover ต้องผ่านบนผืนหน้าด้วย ซึ่งมีเหตุผลเฉพาะตอนแถวไม่มีการ์ด · พอแถวกลับไปอยู่บนการ์ด เงื่อนไขนั้นบังคับให้ hover ต้องเข้มพอสำหรับพื้นเทาและอ่อนพอสำหรับพื้นขาวพร้อมกัน = เป็นไปไม่ได้ · เปลี่ยนไปวัดกับ `surface` อย่างเดียวพร้อมคอมเมนต์ว่าของที่ไม่อยู่ในการ์ดห้ามใช้คู่นี้
- [x] `orders-registry-polish.test.ts` เขียนสัญญาใหม่ + `--color-field-border` `#d4d4d8` → `#cfcfd4` (พอผืนหน้าเข้มขึ้น ขอบช่องกรอกตกจาก 1.43 เหลือ 1.32 ต่ำกว่ากรอบ 1.35–1.7 ที่ระบบตั้งไว้)
- [x] เอกสารที่กลายเป็นเท็จ แก้พร้อมกัน: `SPEC.md` · `docs/DESIGN.md` (สองที่ + ตารางสี) · เฟส 1/3 ในไฟล์นี้
- [x] Verify: `verify:ui` ผ่านครบ · unit 1658/1658 · typecheck · lint 0 error · production build ผ่าน · เปิดจอจริงด้วย CSS ที่ build ออกมาแล้วทั้ง 1440 Light/Dark และ 390 (จอแคบยังพาดเต็มจอและถือตรา)
- [ ] **ยกไปตั้งใจ:** ธีมมืดยังมีบันไดแคบกว่าธีมสว่าง (การ์ดต่างจากผืนหน้า 1.07 เท่า) — ตรวจจอจริงแล้วการ์ดยังอ่านออกเพราะมีเส้นขอบ และเบสไม่ได้ทักฝั่งนี้ · ถ้าจะถ่างต้องขยับ `surface`/`surface-elevated` พร้อมกันและไล่ contrast ตัวหนังสือใหม่ทั้งชุด

### 🎯 PRODUCTION-MOCKUP-ROUTE — เห็นแบบและตำแหน่งงานจากหัวใบผลิต (เบสสั่ง 2026-08-25)
> เก็บเฉพาะ first-view ของ `/production/[id]`: ใช้ข้อมูลม็อกอัพอนุมัติและขั้นผลิตจริงที่ query เดิมส่งมา · ไม่เปลี่ยน query, mutation, permission, status transition, polling, route หรือ no-money boundary

- [x] แสดงม็อกอัพที่อนุมัติล่าสุดบนหัวใบแบบอ่านอย่างเดียว พร้อมเวอร์ชัน/จำนวนภาพ และเปิด inspector เดิมเพื่อดูครบทุกด้าน; ถ้าไม่มีต้องบอกตรง ๆ ไม่สร้างภาพจำลอง
- [x] เพิ่มราง node ขั้นผลิตแบบหน้าออเดอร์จาก `productionWorkflowSteps` จริง โดยสีแยกเสร็จ/กำลังทำ/มีปัญหา/พัก/รอ และห้ามกดเปลี่ยนสถานะ
- [x] รางรองรับขั้นยาวและหลายเลนโดยไม่บีบคำไทยหรือทำหน้า overflow; ตารางเส้นทางเดิมยังเป็นหลักฐานละเอียดและ action เดิมไม่ย้าย
- [x] Verify targeted/full test, typecheck/lint/`verify:ui`/build, detectorรอบเดียว และ browser 1280/390 Light+Dark รวมเปิดม็อกอัพ→Escape/focus return, overflow และ console ✅ 2026-08-25

### 🎯 PRODUCTION-STEP-FLOW — เลือกดูและจัดการทีละขั้น (เบส follow-up 2026-08-25)
> เปลี่ยน node จากภาพรวมเป็นแผนที่บอกตำแหน่ง · ย้อนกลับ/ถัดไปเปลี่ยนเฉพาะขั้นที่กำลังดู ไม่เปลี่ยน workflow status และไม่ข้าม server gate

- [x] Node ไม่เป็นปุ่มและแยก current-view state ออกจากสี workflow; ค่าเริ่มต้นเปิดขั้นมีปัญหา/กำลังทำ/ขั้นแรกที่รอ ตามข้อมูลจริง
- [x] แสดงรายละเอียดเพียงขั้นที่เลือก: สถานะ ผลจริง ผู้รับผิดชอบ เงื่อนไข/หลักฐาน และยอดเสื้อเมื่อเป็นขั้นเตรียมเสื้อ โดยยุบตารางและการ์ดทุกขั้นที่ซ้ำออกจากหน้าหลัก
- [x] มี `ขั้นก่อนหน้า` และ CTA `ขั้นถัดไป` ที่เปลี่ยน view เท่านั้น; ปุ่มจัดการผู้รับผิดชอบ/ปัญหาและทางไป Station คงสิทธิ์/ปลายทางเดิม
- [x] รวมเลขออเดอร์ งาน ลูกค้า จำนวน กำหนดส่ง สถานะ และม็อกอัพไว้ในการ์ดข้อมูลงานเดียว; ตัด progress bar ซ้ำกับ node และย้าย CTA ก่อนหน้า/ถัดไปขึ้นหัวขั้นที่กำลังดู
- [x] รองรับ completed/failed/hold/loading/error/stale, ขั้นยาวและหลายเลนที่ 1280/390 Light+Dark; keyboard/focus/overflow และไม่มี mutation จากการนำทาง
- [x] Verify targeted/full test, typecheck/lint/`verify:ui`/build, detector และ browser flow จริง ✅ 2026-08-25

### 🎯 PRODUCTION-TWO-SURFACE-LAYOUT — คิวเลือกงาน + ใบปิดงานที่พาไปต่อ (เบส feedback 2026-08-25)
> ปรับ hierarchy ของ `/production` และใบผลิตที่จบครบทุกขั้นจากภาพจริง · คง card filter, semantic color, URL/search/sort, query/mutation/permission/status transition และ route เดิม

- [x] หน้าคิวย้าย freshness + search + sort เข้า workspace เดียวกับรายการ ลดแถวลอยและเส้นทางสายตาที่แตก โดย mobile ยังเรียง freshness → search → sort → งาน
- [x] ใบปิดงานยก “ผลิตเสร็จแล้ว” และ handoff เป็นผลลัพธ์/primary action ชัด แยกจากเลขออเดอร์และ metadata รอง
- [x] ยุบหลักฐานปิดงานที่ซ้ำเข้ากับเส้นทางงาน: ทุกขั้นเห็นผลจริง ผู้รับผิดชอบ และเวลาเสร็จ โดยไม่เคลม actor/source ที่ระบบยังไม่เก็บ
- [x] Active/blocked detail, card filter behavior, stale fail-closed, Back/focus และ destination เดิมไม่เปลี่ยน
- [x] Verify targeted/full test, typecheck/lint/`verify:ui`/build, detector รอบเดียว และ browser 1280/390 Light+Dark ไม่มี overflow/console error

### 🎯 PRODUCTION-LIVE-SIGNAL — ทำให้ผู้ใช้เข้าใจว่าเกิดอะไรขึ้น (เบส feedback 2026-08-25 “UI ไม่สื่อ”)
> เก็บ hierarchy/copy ของ freshness component กลางเท่านั้น · ไม่เปลี่ยนรอบ polling, query, mutation, permission หรือ workflow

- [x] Fresh state บอกชัดว่าระบบอัปเดตข้อมูลให้อัตโนมัติ พร้อมเวลาตรวจล่าสุดและรอบ 30 วินาที
- [x] Fetching บอกว่ากำลังตรวจข้อมูลล่าสุด; stale บอกว่าอัปเดตไม่สำเร็จและระบบจะลองใหม่ โดยไม่พึ่งสีหรือไอคอนอย่างเดียว
- [x] ใช้ไอคอนเส้นไม่มีพื้นหลัง, ไม่ทำเป็นปุ่ม/การ์ดปลอม และต้องอ่านได้ใน header แคบทั้ง desktop/mobile/Factory TV
- [x] Verify targeted/full test, typecheck/lint/`verify:ui`/build, detector รอบเดียว และ browser จริง Light/Dark + 390px

### 🎯 PRODUCTION-LIVE — ความสดที่ซื่อสัตย์ + ใบผลิตปิดงานแบบส่งต่อได้ (เบสสั่ง 2026-08-24 “ทำหมดเลย”)
> ขอบเขต presentation + query feedback ของ `/production`, `/production/[id]`, `/production/print-runs`, `/factory/station` และ `/factory` · คง polling, query, mutation, permission, status transition, no-money DTO และ route เดิมทั้งหมด · ไม่เพิ่ม schema/endpoint/dependency/config

- [x] มี freshness component กลางที่บอก `ซิงก์ล่าสุด`, `กำลังอัปเดต` และ `ข้อมูลค้าง` จาก query จริง โดยไม่ใช้ badge LIVE หรือ animation วนหลอก และประกาศ screen reader เฉพาะ state ที่มีความหมาย
- [x] `/production`, รอบพิมพ์ และ Station แสดง freshness ใกล้พื้นที่คิว; `/factory` ใช้ภาษา/semantics ชุดเดียวและยัง refresh เบื้องหลังทุก 30 วินาที
- [x] ใบผลิตที่จบทุกขั้นยกเวลาเสร็จจริง จำนวน และขั้นถัดไปขึ้น first viewport; ลด `เสร็จ/100%/ไม่มีปัญหา` ที่ซ้ำ และทำเส้นทางเต็มเป็นข้อมูลรองโดยไม่ซ่อน audit evidence
- [x] ใบผลิต active/blocked คง hierarchy และ action เดิม; stale data ปิด mutation แบบ fail-closed พร้อม retry ตามเดิม
- [x] Verify: targeted/full test + typecheck/lint/`verify:ui`/build + detector รอบเดียว · browser จริง 1280 ครบ 5 surface และใบปิดงาน 390, ตรวจ overflow/copy/hierarchy; Light/Dark contract ผ่าน token verifier และ unit test ครบ

### 🎯 ORDERS-POLISH — registry scan-first หลังรวม visual system (เบสสั่ง 2026-08-23)
> ขอบเขต targeted UI refinement ของ `/orders`: คง query, URL state, permission, status contract, route และข้อมูล server เดิม · ไม่แตะ mutation หรือ schema

- [x] Desktop แสดงชื่องานใต้ชื่อลูกค้า และรวมวันส่งจริง+countdown เป็นคอลัมน์เดียวเพื่อตัด horizontal scroll ที่จอ laptop
- [x] Status filter ปิดเป็น quick set 3–5 สถานะที่มีงาน/ต้องจัดการ และกาง `ทุกสถานะ` เมื่อจำเป็น โดย selected state ต้องมองเห็นและล้างได้เสมอ
- [x] Mobile card เป็น scan-first 2–3 ชั้น เห็นมากกว่าหนึ่งงานต่อ viewport พร้อมยอดเงิน 2 ตำแหน่ง, label ยอดรวม และ countdown ชุดเดียวกับ desktop
- [x] CSV ระบุขอบเขตว่าเป็นหน้าปัจจุบัน และย้ายออกจาก action หลักบน mobile โดยยังเข้าถึงได้
- [x] Verify: targeted test + full unit/typecheck/lint/`verify:ui`/build · browser 1440/1280/390/320 Light+Dark, search→open→Back, filter expand/select/clear, keyboard/focus, overflow, console

### 🎯 BRAND-SELECTION — คืน Anajak Blue ให้ current/selected ทั้งระบบ (เบสสั่ง 2026-08-23)
> แก้การตีความ Vercel ที่ลดสีแบรนด์มากเกินไป: canvas/panel/hover ยัง neutral แต่ current, selected, active underline/text/icon ต้องใช้ Anajak Blue `#3973b2` และมีคู่ Dark ที่อ่านผ่าน AA

- [x] Primitive กลาง `Tabs`, `FilterChip`, `FlowFilterBar`, toolbar active filter, segmented/menu selected และ navigation ใช้ selected role สีน้ำเงินชุดเดียว โดย hover/pressed ที่ยังไม่เลือกคง neutral
- [x] `/orders` คืน status filter แบบเดิม: desktop เห็น flow ครบใน panel และจอแคบใช้ quick set + กาง flow เพิ่ม โดย URL state/clear/focus/touch target เดิมยังทำงาน
- [x] กวาด source ทั้ง dashboard เพื่อไม่เหลือ selected underline/text สีดำหรือขาวตรง ๆ; ยกเว้น print grayscale และข้อความปกติที่ไม่ใช่ state
- [x] อัปเดต `docs/DESIGN.md`, UI contract และ regression test ให้ล็อก Anajak Blue เป็น active/selected/focus โดยไม่ทำทั้งเว็บกลายเป็นสีฟ้า
- [x] Verify: full unit/typecheck/lint/`verify:ui`/Prisma/build + browser ตัวแทน dashboard/orders/order detail/production/products/billing/settings ที่ desktop/mobile Light+Dark, keyboard/focus/overflow/error overlay

### 🎯 STATUS-FLOW-DISTILL — ลดเส้นซ้ำในแถบสถานะ Orders (เบสสั่ง 2026-08-23)
> ขอบเขต presentation ของ primitive กลาง `FlowFilterBar`: คงสถานะ, จำนวน, URL state, สี semantic, responsive behavior และการกดกรองเดิมทั้งหมด

- [x] Desktop เหลือกรอบ panel ชั้นเดียวและเส้นใต้ Anajak Blue เฉพาะสถานะที่เลือก โดยไม่มีเส้นบนล่างซ้ำ เส้นใต้หัวช่วง หรือเส้นตั้งคั่น “นอกเส้นทาง”
- [x] เปลี่ยน progress track ใต้ทุกสถานะเป็นจุดสี semantic ข้างชื่อ เพื่อคงการอ่านประเภทสถานะโดยไม่แข่งกับจำนวนงาน
- [x] ล็อก regression guard ว่าไม่มีเส้นซ้ำ/track กลับมา และ selected state ยังใช้ `ACTIVE_UNDERLINE`
- [x] ตรวจ `/orders` desktop/mobile Light/Dark, touch target/overflow/error overlay แล้วรัน final gates โดยไม่เปลี่ยน business flow

### 🎯 STATUS-FLOW-AFFORDANCE — ให้รู้ว่ากดกรองและกดซ้ำเพื่อล้างได้ (เบสสั่ง 2026-08-23)
> feedback หลังลดเส้น: โครงต้องยังอ่านเป็น control ไม่ใช่สถิติอ่านอย่างเดียว · เพิ่ม cue เท่าที่จำเป็นโดยไม่คืน progress track หรือเส้นคั่นทุกสถานะ

- [x] คืน hairline เฉพาะใต้หัวช่วงเพื่อช่วยจัดกลุ่ม โดยไม่เพิ่มเส้นบนล่าง/เส้นตั้ง/track ซ้ำ
- [x] เพิ่ม hover/pressed และ cursor บนพื้นที่กด โดยไม่แสดงข้อความแนะนำค้างบนหน้า
- [x] aria-label/title แยก action ของสถานะพักกับสถานะที่เลือก เพื่อให้ screen reader รู้ว่ากดซ้ำแล้วล้าง
- [x] ตรวจ interaction เลือก→กดซ้ำ→URL/จำนวนกลับทั้งหมด ทั้ง Light/Dark และ desktop/mobile ก่อนรัน final gates

## 🏭 PRODUCTION-V2 — หนึ่งข้อมูลจริง หนึ่งบ้านต่อหนึ่งงาน (เบสเคาะ 2026-08-22)

> เป้าหมาย: รื้อแกนการผลิตและ UX เป็นโครงการเดียวตามบทบาท ERP/MES — ERP ใช้วางแผน/ควบคุม/ตรวจย้อนหลัง, Station ใช้ลงมือ, Factory TV อ่านอย่างเดียว · ทำบน branch/worktree แยกจาก checkpoint `5972e65` · schema เพิ่มแบบ additive และห้าม apply/reset ฐาน shared หรือ remote ระหว่างสร้าง

- [x] **PV2.1 Core manufacturing** — เก็บ `Production` เป็น Manufacturing Order และยกระดับ `ProductionStep` เป็น Operation Job โดยคง ID/FK เดิม · เพิ่ม Work Center/Resource/สมาชิก/ปฏิทิน, routing แบบ versioned+immutable หลัง releaseและทุก lane รวมที่ Final Pack terminal เดียว, dependency ขนาน, quantity line แยกสินค้า/สี/ไซซ์/ตำแหน่งพิมพ์, append-only event, structured exception/rework และ reference snapshot
- [x] **PV2.2 Command contract** — เพิ่ม `manufacturing` query/command กลาง · ทุก command รับ `commandId` + `expectedRevision`, retry/concurrency ไม่เพิ่มจำนวน/stock/event ซ้ำ, readiness/dependency คำนวณบน server, `qtyGood` เท่านั้นเดินต่อ, reject ต้องมี disposition และ rework ต้องตรวจซ้ำ
- [x] **PV2.3 Permission and truth** — เพิ่ม `ship_orders` ให้ OWNER/MANAGER/SALES; Production Staff แพ็กได้แต่ส่งไม่ได้ · Station/Factory DTO ไม่มีราคา/ต้นทุนโดยโครงสร้างและใช้ `availableCommands` จาก server · Order status เปลี่ยนผ่าน transition service กลางเท่านั้น
- [x] **PV2.4 Canonical surfaces** — `/production` เป็นรายการทุกงานแบบ server-side pagination; `/production/[id]` เป็น Control Record ไม่มี action แทนพนักงาน; `/factory/station` เป็น current-job-first+same-order handoff; `/factory` เป็น TV read-only; Order/My Tasks เหลือ summary/deep link ตาม role
- [x] **PV2.5 Vertical flow** — Prep รับ/เบิก/คืน → DTF batch good/scrap/reprint → Heat Press partial → QC Hold/Rework/Scrap+ตรวจซ้ำ → Final Pack ตามสินค้า/สี/ไซซ์ → Office Delivery/ship; Outsource เป็น worklist ใน Production และ vendor public page เดิมยัง read-only
- [x] **PV2.6 Remove duplicate homes** — ย้ายทุก surface ให้ใช้ read/command contract ชุดเดียว · legacy writer ปฏิเสธ V2 record หลัง lock และ Order/Change Order แก้นิยามงานหรือหลักฐาน Prep ทับ snapshot ไม่ได้ · redirect `/production/print-runs`, `/production/films`, `/outsource` ไปบ้าน canonical · เก็บ old UI หลัง `PRODUCTION_V2_ENABLED` เพียง rollout window เดียว แล้วลบ old UI กับ legacy writers
- [x] **PV2.7 Isolated fixtures and verification** — seed เฉพาะฐาน V2 local ครอบ multi-variant/parallel lane/DTF หลายออเดอร์/defect-rework/outsource pass-fail/partial pack · ผ่าน domain, integration, permission, typecheck, lint, build และ browser จริงตาม viewport/สถานะที่ระบุใน acceptance plan ก่อนขอ cutover
- [ ] **PV2.8 Acceptance and release** — เบส walkthrough บทบาทหัวหน้าและพนักงานครบ flow จริงก่อนเปิดทั้ง module · ไม่มี backfill ประวัติปลอม · การเปลี่ยน/reset ฐานต้องระบุ target+backup แยก · ห้าม push main หรือ deploy ก่อนเบสตรวจรับ

---

## 🚦 แผนแนวทางใหม่ 2026-07-02 — เส้นทางสู่ go-live (จาก audit ใหญ่ 47 agents · adversarial verify 26/28 CONFIRMED)
> **ที่มา**: เบสสั่ง audit ว่า "ดีพอตามมาตรฐาน ERP โรงงานสกรีนหรือยัง" → รายงานเต็ม: bestos `records/projects/anajak-erp/audit-2026-07-02.md` (+ detail 10 ไฟล์ใน `_audit-2026-07-02/`)
> **คำตอบ**: ครึ่ง ops ดีพอ-เกินมาตรฐาน SaaS (74-85/100) · ครึ่งเงิน+ภาษียังไม่พร้อมใช้จริง (55-70) · โครงพื้นฐาน production (CI/backup/monitoring/rate-limit) = 0
> **ลำดับนี้ทับลำดับ P1-P4 เดิมชั่วคราวจนถึง go-live** · ทุกข้อ verify แล้วมี file:line ในรายงาน · ⚠️ 5 คำถามท้ายรายงานต้องให้เบสเคาะก่อนแตะข้อที่เกี่ยว

### Gate A — เงินห้ามผิด ✅ จบทั้งก้อน 2026-07-02 (+ adversarial review 16 findings แก้ครบ)
- [x] A1 `billing.recordPayment` จำกัดชนิดใบ — CN ห้ามรับเงิน · **REC รับได้เฉพาะขายสดตรง** (ออเดอร์ไม่มีใบเรียกเก็บ — review จับว่า block ทื่อๆ จะฆ่า flow ขายสด) + ซ่อนปุ่ม UI ตรงเงื่อนไข + REC/CN ไม่เก็บ dueDate + overdue sweep กรองเฉพาะใบเรียกเก็บ (กัน OVERDUE ปลอม)
- [x] A2 gate ต้นทุน/กำไรตาม role — `order.getById` ตัด costEntries/totalCost/profitMargin (FINANCE เท่านั้น) + payments (FINANCE+SALES) + **ทุน outsource/step ในใบผลิต** (review จับรั่วเพิ่ม) · `billing.listByOrder` gate role + การ์ดบิลซ่อนจากช่าง/กราฟิก · ปุ่มรับเงิน/void ตรง moneyRecorder · นิยาม role กลาง `lib/roles.ts`
- [x] A3 state machine ใบเสนอ — `lib/quotation-status.ts` (+test) · update/updateItems ล็อกแก้เฉพาะร่าง (conditional write + FOR UPDATE กัน race) · updateStatus validate transition + expectedStatus (กันจอค้างทับการยืนยันลูกค้า) + ดึงกลับร่างล้าง sentAt/acceptedAt + กันส่งใบหมดอายุ · ปุ่ม "ดึงกลับเป็นร่าง" (SENT/ACCEPTED มี confirm/REJECTED/EXPIRED — ปิด quick win #1 + ทางตัน ACCEPTED)
- [x] A4 ต้นทุนเข้า `$transaction` — service กลาง `services/order-cost.ts` (lock+recalc) ใช้ทั้ง cost router + **production step + outsource QC** (review จับ: 2 จุดหลังเขียน costEntry แต่ไม่เคย recalc → totalCost drift)

### Gate B — ก่อนใช้จริง (go-live gate · เรียงตามเจ็บ)
- [x] B1 **ใบลดหนี้/เพิ่มหนี้ครบองค์กฎหมาย** ✅ 2026-07-02 (+review 21 findings แก้ครบ): schema originalInvoiceId+adjustmentReason (migration `20260702150000` **⏳ เบสยัง apply ไม่ได้ — DB ต่อไม่ถึงจากเครื่อง**) · CN หักยอดค้างจริงทุกจุด (aging/ใบวางบิล/ทวง/MCP/เพดานรับเงิน/refund) · สถานะใบเดิมขยับเอง (ออก/void CN · คง OVERDUE · paidAt สมมาตร) · ใบพิมพ์ครบ ม.86/10 เป็น "มูลค่า" ฐานภาษี + หัก CN/DN ก่อนหน้า · CN สองความหมาย: อ้างใบเรียกเก็บ=ลดยอดค้าง · อ้างใบเสร็จ=คู่คืนเงิน (เพดานใบเสร็จรู้ linkage — ไม่ block ใบกำกับงวดถัดไป)
- [x] B2 **VAT default 7%** ✅ 2026-07-02 — ออเดอร์ default 7 (ฟอร์ม+server) · ช่องทาง marketplace ราคารวม VAT สลับ default เป็น 0 อัตโนมัติ (ไม่ทับค่าที่พิมพ์เอง) · ฟอร์มใบเสนอมีปุ่มลัด "VAT 7%"
- [x] B3 **tax point จ้างทำของ** ✅ 2026-07-02 (+review 18 findings: แก้ 15 · REFUTED 1 · จดหนี้ 2): `issueDate` (วันที่เอกสารตามกฎหมาย — print/จอ/portal ใช้แล้ว) + `forPaymentId @unique` ผูกใบเสร็จ↔งวดรับเงิน (1 งวด 1 ใบ · ยอดใบต้องเท่าเงินรับเป๊ะที่ server · void-ออกใหม่ได้+audit ปลดผูก · ใบผูกงวดข้ามเพดานกอง — CN หลังรับเงินไม่ block ใบกำกับ) · UI เตือนงวดค้างออกใบ + ปุ่มออกทันที prefill ฐาน/VAT จากสัดส่วนใบจริง + ช่องวันที่เอกสาร (บันทึกย้อน) + นับงวด WHT ล้วน + เตือนใบเสร็จไม่ผูกงวด
- [x] B4 **ปิดทาง bypass QC** ✅ 2026-07-02 (+review 10 findings แก้ 9 · จดหนี้ 1): server guard ใน `$transaction` อิงสถานะจริงจาก transitionOrder — QUALITY_CHECK→PACKING ต้องมี QcRecord ≥1 (เคสกดมือจริง "ลูกค้ารับของไม่ครบ/ของตีกลับ" มี record เสมอ = ผ่านได้) + promote ลาย/reopen งานแก้ key สถานะสดตัวเดียวกัน (ปิด TOCTOU) · outsource RECEIVED_BACK ต้องมีใบตรวจรับ OUTSOURCE_RETURN + `createGoodsReceipt` validate ใบ outsource เป็นของออเดอร์เดียวกัน/ชนิดถูก (กัน receipt ข้ามออเดอร์ผ่านด่าน) · UI: คอลัมน์ตรวจ /production เหลือปุ่มเดียว "ตรวจนับ QC" (เปิด QcCountDialog ตัวเดียวกับหน้าออเดอร์ · role ตรง server) · ปุ่ม "รับของกลับ" บอร์ดเลน+/outsource เปิดใบตรวจนับก่อน — เคยนับแล้ว (flip พลาด) flip ตรงไม่บังคับนับซ้ำ · แถบขั้นต่อไป QUALITY_CHECK ชี้ไปการ์ดตรวจนับ (ANCHOR "qc") แทนปุ่มเดินสถานะ · **หนี้รอเบส**: ship ของจริงผ่านใบส่งขณะ QUALITY_CHECK ยังได้ทาง API ตรง (UI ทำไม่ได้ · verify-ops 6.3 codify พฤติกรรมนี้อยู่ — จะปิดต้องเคาะ semantics ใบส่งใหม่)
- [x] B5 **รายงานภาษีขายรายเดือน export CSV** ✅ 2026-07-02 (+review 13 findings แก้ 10 · จดถามเบส 3): หน้า `/billing/tax` เลือกงวด (เดือนไทยเสมอ — ขอบงวด UTC+7) + สรุป + ตาราง + ดาวน์โหลด 2 ไฟล์ — ① CSV ฟอร์มรายงานภาษีขายสรรพากร (พ.ศ. ทั้งไฟล์ · CN ติดลบ · ใบ void คงแถวยอด 0 เลขไม่โดด · แถวรวมท้าย) ② CSV คอลัมน์ตาม field template import PEAK (CN ยอดบวก+ประเภทกำกับ · มียอด VAT จริง · ไม่รวมใบ void — **นักบัญชีเทียบ template จริงตอน B6 แล้วล็อกคอลัมน์**) · ขอบเขต = เอกสารกำกับภาษีจริง: REC + CN/DN ที่อ้าง REC เท่านั้น (**CN/DN อ้างใบแจ้งหนี้ = ปรับยอดค้าง ไม่เข้ารายงาน — review จับ BLOCKER หัก VAT ซ้ำ ภ.พ.30 ขาด**) · งวด = issueDate (B3) ?? createdAt · เลขภาษีห่อ `="..."` กัน Excel ตัด 0 · ใบแก้ tax มือไม่ตรง 7% → อัตรา "อื่นๆ" + เตือนคีย์ยอดมือ · pin เขตเวลาไทยที่ใบพิมพ์ด้วย (formatDocDate — กระดาษ/รายงานไม่เหลื่อมวัน) · **จดถามเบส**: index invoices ใหม่ 2 ตัว + snapshot ข้อมูลลูกค้าลงใบ (schema) + ล็อก/เตือนงวดที่ยื่นแล้ว (คุยกับนักบัญชีตอน B6)
- [ ] B6 **นักบัญชีรีวิว template ใบกำกับ/CN/DN + เลขรัน** ก่อนออกใบจริง (open decision ค้าง — พิมพ์ตัวอย่างจริงให้ดู)
- [x] B7 **CRM ใช้ได้จริง** ✅ 2026-07-03 (+review 9 findings CONFIRMED ทั้งหมด → แก้ครบ 9): ฟอร์มแก้ลูกค้า (dialog ครบ field + segment · ต่อ `customer.update` ที่ตายอยู่) + ปุ่มบันทึกการคุย (`addCommunicationLog` + timeline label ไทย) + pagination /customers (เกิน 50 เปิดหน้าถัดไปได้ · debounce ค้นหา · placeholderData กันตารางยุบ) · จาก review: สลับนิติบุคคล→บุคคลแล้วข้อมูลภาษี/วงเงินค้าง **ไม่ซ่อนอีกแล้ว** (โชว์ section + คำเตือนทั้ง dialog/หน้า detail — เดิมค่าค้างไหลเข้าใบกำกับแบบมองไม่เห็น/ล้างไม่ได้) · taxId required เมื่อนิติบุคคล (ตรงฟอร์มสร้าง) · แก้วงเงินแล้ว invalidate `creditStatus` (บรรทัด "ใช้ได้อีก" ไม่ค้างฐานเก่า) · ฟอร์มสร้างได้ toast error + ช่องวงเงิน disabled สำหรับ SALES
- [x] B8 **ถอดฟอร์มปลอมหน้า /settings** ✅ 2026-07-03: 4 section ปลอม (ข้อมูลโรงงาน/การผลิต/ความปลอดภัย/เชื่อมต่อภายนอก — input ไม่ผูกอะไร ปุ่มบันทึกแค่ปิดแถบ) + แถบ save ปลอม ถอดทิ้งหมด (รวมช่อง LINE token ปลอม — ปิด quick win C6 ส่วนนั้น) → หน้า settings = hub การ์ดลิงก์ของจริง 7 ใบ (เพิ่มการ์ด "เชื่อมต่อ Anajak Stock" ที่เดิมจมใน section ปลอม) · ตั้งค่าจริง (% มัดจำ/เพดานส่วนลด/ฟรีแก้แบบ) ค่อยทำเป็นชิ้นๆ เมื่อมีท่อ logic จริง — ห้ามขึ้นฟอร์มก่อนมีของ · เทสจริง: การ์ดครบ + spot-check company/stock เปิดได้
- [x] B9 **เพดานสองขา** ✅ 2026-07-03 (+review workflow 21 agents → 14 CONFIRMED แก้ 12 จดถามเบส 2 · skeptic รอบสอง 2 findings แก้ครบ): `billedFloor` (dual ของ remainingBillable กองต่อกอง: max(Σ D+F, Σ REC − DN + CN ที่ไม่อ้างใบเสร็จ)) + `assertOrderTotalCoversBilled` ล็อกแถวออเดอร์แล้วเช็คใต้ lock — เสียบครบทุก writer ที่ลดยอดได้: update/updateItems/updateFees + **addRevisionFee + quotation.convertToOrder** (สองช่อง bypass ที่ review จับ — sync ค่าแก้แบบลง/ใบเสนอต่อรองลงบนออเดอร์เปิดเบา) · block เมื่อยอดใหม่ < floor **และ** < ยอดเดิม (ออเดอร์ legacy บิลเกินยอดขยับเข้าหา floor ได้ ไม่ติดตาย) · **ปิด TOCTOU ทั้งแถบ**: mutation เงินทุกตัวคำนวณยอดใต้ lock (เดิม snapshot นอก lock — สองตัวชนกันยอดเพี้ยนสูง เพดานขาแรกพอง) รวม applyChangeOrder ซึ่งคง "เตือน ไม่ block" โดยเจตนา (เคสลดงานหลังรับมัดจำ+ออกใบกำกับ: เงินรับถูกกฎหมาย void ไม่ได้ CN อ้างใบเสร็จก็ไม่ลด floor → block = ทางตัน · codify ใน verify:co 7.1/7.2) · updateItems รับ `fees` optional — ฟอร์มแก้รายการยิง tx เดียว atomic (เดิมสองขา บันทึกค้างครึ่งเมื่อขาหลังโดน block) · voidInvoice ขา DN มีด่าน (void แล้วใบเสร็จจะเกินยอดออเดอร์ → block) + แก้ลำดับ lock billing.create (validate whitelist ก่อน lock — ตัดวง deadlock) · getById คืน `billedFloor` → UI เตือน amber ก่อนบันทึก (editor แยกข้อความโหมด CO · dialog แก้ข้อมูล · เงื่อนไข mirror server สองขา) · ข้อความ error ชี้ทางที่ผ่านจริง ("ยกเลิกบิลเดิม" — CN ไม่มีทางลด floor)
- [x] B10 **ถอด ON_HOLD จาก EDITABLE_STATUSES** ✅ 2026-07-03 (+review 4 มิติ/10 agents → 5 CONFIRMED แก้ครบ 3 + skeptic): ON_HOLD เข้าได้จากสถานะที่ล็อกแล้ว (DESIGN_APPROVED/PRODUCING/QC/PACKING) — เดิมอยู่ใน editable = ช่อง "พัก→แก้ยอดตรง→ปลดพัก" เลี่ยงใบแก้ไข ไม่มี CO/เหตุผล/ประวัติ · แก้: ลบ ON_HOLD จาก `ORDER_EDITABLE_STATUSES` → แก้ยอด/รายการต้อง "ปลดพักก่อน" (next-step card ชี้ทางนี้อยู่) · รวมข้อความ lock 3 จุดเป็น `orderEditLockedReason` helper (กัน drift + ครอบ ON_HOLD) · ปิดกิ่ง bypass ที่ review จับ: **addRevisionFee** (คิดค่าแก้แบบดันยอด — เดิม gate แค่ COMPLETED/CANCELLED) block ON_HOLD ด้วย · **แก้บั๊กเดิม (review จับ MAJOR)**: `OrderInfoEditDialog` แนบ discount+taxRate เสมอ → touchesMoney=true ทุกครั้ง → แก้ที่อยู่/หมายเหตุบนออเดอร์ล็อก (ON_HOLD + สถานะล็อกอื่น) โดนเด้งทั้งใบ → dialog รับ internalStatus, ปิดเฉพาะช่องเงินตอนล็อก + ไม่แนบ money fields (field ที่ไม่ใช่เงินบันทึกผ่าน) · UI: canEditItems ซ่อนปุ่มแก้รายการบน ON_HOLD
- [x] B11 **issueMaterials atomic + list MaterialUsage + product soft-delete** ✅ 2026-07-03 (+review 4 มิติ/13 agents → 4 CONFIRMED แก้ครบ + skeptic รอบสอง 1 edge แก้): ① **soft-delete** `Product.deletedAt` (migration `20260703021953`) — delete → ตั้ง deletedAt แทนลบแถว เก็บ MaterialUsage (FK Restrict — เดิม deleteMany ทิ้งประวัติเบิกถาวร) + order items + variants · กรอง `deletedAt:null` ที่ list/searchForOrder/getById/order pricing lookup/stats · sync upsert เติม `deletedAt:null` → resurrect เมื่อสินค้ากลับมาใน Stock · order.create ปฏิเสธสินค้าที่ลบ (เดิม hard-delete ล้ม FK · soft-delete รับเงียบ) ② **issueMaterials atomic** — ย้าย local writes (materialUsage + ตัด mirror) เข้า `$transaction` · **idempotent**: findFirst(stockMovementRef) skip replay + client ส่ง idempotencyKey คงที่ต่อ batch (เดิม UUID ใหม่ทุกคลิก → retry ตัดสต๊อคจริงซ้ำ) + server ปฏิเสธเมื่อ Stock duplicated แต่ ERP ไม่มีบันทึก (กันเขียนประวัติผิดจาก materials ที่ถูกแก้ระหว่าง retry) ③ **list endpoint** `stockSync.listMaterials` (กรอง itemType RAW_MATERIAL/CONSUMABLE — **ไม่ปนแถวเบิก/คืนเสื้อจาก garment-pick**) + component โหลดประวัติจริงจาก DB (เดิม local state reload หาย) · **หนี้จด**: (a) order.update/duplicate ไม่ guard สินค้าลบ (pre-existing · soft-delete ทำ FK ยังผ่าน) (b) reconcile sweep material mirror↔Stock (orphan เมื่อ Stock ตัดสำเร็จแต่ ERP tx ล้ม — ต้องกระทบมือ)
- [x] B12 **sidebar/ปุ่มกรอง role + print gate** ✅ 2026-07-03 (+review 3 มิติ/11 agents → gate ทุกตัว verify ตรง server + 1 MAJOR แก้ + 1 MAJOR จดถามเบส): `lib/roles.ts` +`SALES_DOC_ROLES`(=salesUp)/`canCreateSalesDocs`/`roleAllows` (source เดียว) · sidebar กลุ่มการเงิน 6 items→FINANCE_ROLES + ซ่อนทั้งกลุ่มเมื่อว่าง (mobile reuse ครอบ) · ⌘K money items→FINANCE_ROLES, create actions→SALES_DOC_ROLES · **ปุ่มสร้างจริง** (topbar "เปิดงานใหม่" + orders/quotations list header+empty state)→SALES_DOC_ROLES (review จับ: ⌘K gate แล้วแต่ปุ่มเด่นยังไม่ · ตรง server salesUp) · print gate `requirePrintRole` (RSC · lookup supabaseId+isActive · fail-closed try/catch): invoice/quotation=ORDER_MONEY_ROLES · billing-note=FINANCE_ROLES · job-ticket/packing-list ไม่แตะ (ช่างต้องเห็น · ไม่มีราคา) · **จดถามเบส (review จับ MAJOR · pre-existing A2-gap นอก scope menu/print)**: หน้า list orders/quotations/customers โชว์ totalAmount/totalSpent ให้ช่าง/กราฟิก + server reads (order/quotation/customer.list) เป็น protectedProcedure ไม่กรอง money → ถ้าจะปิดต้องเคาะนโยบาย (ช่างเห็นมูลค่าออเดอร์ไหม) + server strip field + UI ซ่อนคอลัมน์
- [x] B13 **delivery: trackingNumber ทุกสถานะ + state machine ใบส่ง** ✅ 2026-07-03 (+review 3 มิติ/10 agents → 2 MINOR แก้ + skeptic): fix เลขพัสดุกรอกตอน PREPARING หายเงียบ (เดิมเขียน delivery.trackingNumber เฉพาะ block SHIPPED · order.trackingNumber เขียนอยู่แล้ว = mismatch) → เขียนทุกสถานะ · **state machine** `lib/delivery-status.ts` (เลียน outsource): `isValidDeliveryTransition`(self=true)/`nextDeliveryStatuses` + DELIVERY_TRANSITIONS (เดินหน้า/ส่งตรง/รับเอง/ตีกลับทุกจุด/ถอยหนึ่งก้าว · บล็อกถอยไกลข้ามขั้น) · updateStatus: validate + conditional write updateMany where{id,status} กัน race + gate side effect (ดันออเดอร์/เตือนตีกลับ) + timestamp (shippedAt/deliveredAt) ด้วย **statusChanged** (self แก้เลขพัสดุไม่ยิงซ้ำ/ไม่ทับวันส่ง — review จับ) · UI dropdown กรองเหลือที่เดินได้ + ปุ่มอัปเดตโชว์บน DELIVERED ด้วย (ตีกลับหลังส่งถึง — เดิมซ่อน transition ตาย) · labels dedup จาก status-config
- [x] B14 ใบส่งของร้านนอกแบบ **LINE-friendly** ✅ 2026-07-03 (+review 3 มิติ/21 agents → 9 CONFIRMED แก้ครบ 9 · skeptic รอบสอง 1 residual แก้): หน้า public `/job/<token>` (เปิดไม่ต้อง login · เลียน pattern `/status` — token crypto.randomBytes(32) + expiry 90 วัน + fail-closed) โชว์ งาน/จำนวน/ตารางไซซ์/ลาย(แบบอนุมัติ+สเปคพิมพ์+ไฟล์แนบ)/กำหนดรับ · ปุ่ม "แชร์ให้ร้าน" ในหน้า outsource (canHandleGoods) เปิด dialog แนบไฟล์ลาย (FileUpload → attachment entityType `OUTSOURCE_ORDER` category PRINT_FILE) + สร้าง/หมุนลิงก์ + คัดลอก(try/catch+fallback execCommand) + แชร์เข้า LINE (`line.me/R/share`) · generateMetadata (OG unfurl) · **กันรั่ว**: server sanitize ตัด ค่าจ้าง/ราคาขาย/ชื่อลูกค้า/qcNotes ทิ้ง (ร้านไม่รู้ลูกค้าปลายทาง) · `/api/files?os=` branch + allowlist ที่ service เดียวกับหน้า · **BLOCKER ที่ review จับ (ไฟล์แนบไม่กรอง → ยัด proxy URL ไฟล์เงินให้ร้านเปิดได้)** ปิดสามชั้น: write-lock (`isOwnOutsourceFile` บังคับไฟล์อยู่โฟลเดอร์ `outsource/<jobId>/` + role productionUp) · read post-filter (กัน encoded `..%2f` ที่ startsWith เลี่ยงได้ — normalizeFileUrl ยุบแค่ `..` ธรรมดา) · proxy guard reject encoded separator · migration `20260703050000` (additive · applied)
- [x] B15 **โครงพื้นฐาน production** ✅ 2026-07-03 (code+config จบ · เหลือ console/deploy = checklist ให้เบส): ① CI `.github/workflows/ci.yml` (lint+typecheck+vitest · ไม่ต้อง DB/secret) + script `typecheck` ② ลบ `pnpm-lock.yaml` (โปรเจกต์ใช้ npm) ③ security headers ทุก route (HSTS/X-Frame-Options DENY/nosniff/Referrer-Policy/Permissions-Policy · **CSP จด follow-up** — ต้องไล่เทส inline+signed URL ก่อน) ④ env validate ตอน boot (`src/lib/env.ts` zod + `src/instrumentation.ts` · fail-fast DATABASE_URL/SUPABASE trio · +4 test) ⑤ Supabase audit: `npm run verify:supabase` (bucket private + anon อ่านไม่ได้ 3/3) + `docs/b15-supabase-audit.md` console checklist (**ปิด signup + เปิด PITR/backup เอกสารภาษี 5 ปี = เบสทำใน Supabase console**) ⑥ **rate-limit = เบสเคาะใช้ Vercel platform** (DDoS auto ฟรีอยู่แล้ว · WAF rate-limit rules ตอน deploy — `docs/b15-vercel-firewall.md` มีคำสั่งพร้อมรัน) · verify: tsc 0 · lint 0 · unit 308/308 · build ผ่าน · curl ยืนยัน headers
- [ ] B16 **walkthrough ของจริงกับทีม + พิมพ์เอกสารเงินให้นักบัญชีดู** (audit รอบนี้รีวิวจากโค้ด ไม่ได้เปิดจอจริง — UX คะแนนถือเป็นสมมติฐานจนกว่าจะลองจริง)

### Gate C — หลังใช้จริง ~1 เดือน (calibrate จากข้อมูลจริงก่อน build)
- [ ] C1 pricing engine ใบเสนอ (qty break × เทคนิค × ตำแหน่ง + ราคาต่อลูกค้า — ระหว่างรอ: mount useMarginEstimate ในฟอร์มใบเสนอ = quick win)
- [ ] C2 stale sweep (ใบเสนอ SENT/แบบรอลูกค้า/INQUIRY/outsource เลยกำหนด ค้างเกิน N วัน → กระดิ่ง — โครง cron+notification พร้อมแล้ว)
- [ ] C3 global search ⌘K ค้นข้อมูลจริง (order.list มี search ฝั่ง server แล้ว เหลือต่อท่อ palette)
- [ ] C4 Owner Pulse drill-down (/orders รับ URL param filter) + คอลัมน์กำหนดส่ง/sort deadline ใน orders list + mobile card
- [ ] C5 UX ops: หน้าใบผลิตโชว์ภาพลาย+ตารางไซซ์ (ช่างไม่ต้องเปิด order detail) · ปิดขั้นตอน 1-2 แตะ (เลิก dialog 5 แตะ) · จอ print-runs ลิงก์ไฟล์ลาย
- [ ] C6 LINE OA notify (P3 เดิม — ระหว่างนี้ template ก๊อปส่ง + ถอดช่อง LINE token ปลอมออกจาก settings)

### 🔑 PERM — ระบบสิทธิ์รายคน (เบสเคาะ 2026-07-06: ทาง B · ทำทีเดียวทั้ง 20 สิทธิ์ · ใบงานเต็ม+เหตุผล: `docs/spec-user-permissions.md` · ~6-7 วันทำงาน)
- [x] PERM1 **โครง** ✅ 2026-07-07: catalog **19 สิทธิ์** ใน `lib/permissions.ts` (ยุบจากชุด role จริง 24 แบบ · defaultRoles ถอดจาก requireRole ทุก router ณ วันนี้ · จุดชุดไม่ตรง catalog — product.delete/cost.delete/attachment inline/วงเงิน SALES — คงเช็คเดิม จดให้ PERM3) + `permissionOverrides` JSON บน users (migration `20260707053836` applied) + `hasPermission`/`effectivePermissions`/`parsePermissionOverrides` (ข้อมูลเสีย fail กลับ default · manage_users ไม่รับ override กันล็อคตัวเอง) + เทส matrix 6×19 pin ตารางแยกอิสระ (unit 416)
- [x] PERM2 **UI** ✅ 2026-07-07: /settings/users ปุ่ม "สิทธิ์ (n)" ต่อคน (ซ่อนแถวตัวเอง) → dialog checklist 4 กลุ่ม (ป้าย "ปรับเอง" เมื่อต่างจาก default ของ role · manage_users ล็อก "เจ้าของเท่านั้น" · ปุ่มรีเซ็ตเป็นค่าเริ่มต้น) + `user.setPermissions` (ownerOnly · กันแก้ตัวเอง · normalize เก็บเฉพาะ diff จริง · audit log) + user.list คืน overrides · **ทดสอบจอจริงครบวงจรบน browser** (ติ๊กตัด→ป้าย (1)→เปิดใหม่จำค่า→รีเซ็ต→ป้ายหาย · ทดสอบบน user [TEST-NAMI] — override ยังไม่มีผลจริงจนกว่า PERM3 wire)
- [x] PERM3 **migrate server** ✅ 2026-07-07 (จบพร้อม PERM6 ส่วน MCP): requireRole 111 จุด + canSee*/strip 16 + เทียบ role ตรง 12 + ชุดสถานะต่อ role → วิ่งผ่าน hasPermission (ทีละ router · commit ก้อนเล็ก · ctx โหลด overrides มากับ user query เดิม) · MCP/agent key ผ่าน helper เดียวกัน (→ PERM6)
  - **รอบแรก โซนเงิน ✅ 2026-07-07**: trpc ctx (`permissionOverrides` optional — script/MCP เดิมไม่ต้องแตะ) + `requirePermission` (ข้อความ FORBIDDEN เดิม) · migrate **order/quotation/customer/billing/billing-note** ครบ (gate+strip ทุกชั้น) · updateStatus whitelist ต่อ role → สิทธิ์ 4 กลุ่ม (PRODUCTION_QUEUE เฉพาะจาก PRODUCING คงเดิม · ข้อความ 3 role คลาสสิกคงเดิมเป๊ะ) · rollback→supervise_operations · **พิสูจน์**: verify 10 ชุด (moneygate/e2e/ops/p02/bn/confirm/co/revision/terms/wht) **ผ่านโดยไม่แก้ expectation แม้ตัวเดียว** + moneygate 8.1-8.4 ใหม่พิสูจน์ override จริงกับ DB (ติ๊กเพิ่มทะลุ strip+gate · ติ๊กตัด FORBIDDEN · ค่าเสีย fail-closed) — 33/33
  - **รอบสอง ✅ 2026-07-07 — server จบทั้งระบบ**: migrate อีก 20 router (production/delivery/qc/print-run/film-stock/stock-sync/goods-receipt/design/artwork/pattern/settings/user/analytics/product/task/packaging/service-catalog/outsource(+share)/customer-status/customer-upload/quotation-confirm) + inline: production.updateStep (มอบงาน/ต้นทุน/QC_FAILED override) + outsource QC ตัดสิน → supervise_operations · task.ts จอเช้า section ตามสิทธิ์จริง (ownWorkOnly=!supervise) · analytics dashboard → see_finance · **review equivalence audit ยืนยัน set-identical ทุกด่าน** (1 CONFIRMED doc-only: ป้าย supervise เขียนผิดว่าคุม "ยืนยันส่ง" — จริงๆ อยู่ manage_delivery ช่าง/ขายกดส่งได้ตาม flow เดิม → แก้ป้าย/คอมเมนต์แล้ว) · **จุดคงเช็คเดิมโดยเจตนา**: product.delete + service-catalog ownerOnly · cost.delete [O,A] · order orderOps (ครบ 6 role) · attachment inline ×3 · customer วงเงิน SALES ×2 · garment-pick userRole (auto-claim) · **พิสูจน์**: verify 13 ชุด 334 เช็คผ่านไม่แก้ expectation + tsc/lint/unit 416
- [x] PERM4 **migrate client** ✅ 2026-07-07: user.me คืน `permissions` (effective ± override) + `permAllows` helper (โหลดไม่เสร็จ = ซ่อน gated เหมือน roleAllows) → จอทั้งหมดอ่านสิทธิ์จริง: sidebar/⌘K/topbar (permission ต่อรายการ ตรงด่านปลายทาง) · order detail (canSeeMoney/isSalesUp/สถานะ/QC/ตรวจรับ/ผลิต) · `canPermsSetStatus` แทน canRoleSetStatus (mirror server 4 กลุ่ม+rollback) · หน้า print 3 หน้า → `requirePrintPermission` (RSC เช็ค override ด้วย) · quotations/billing×5/customers×2/production×2/outsource/analytics + component 7 ตัว (billing-section/delivery/design/artwork/step-dialog/customer-section) — ~55 จุด · **จงใจคง role**: MCP agent tools (เบสคุม key เอง — จดเป็นงานตาม) + canSetCredit หน้า customers (คู่ inline server) · verify: tsc 0 · lint 0 error · unit 416 · จอ OWNER สมบูรณ์บน browser
- [x] PERM5 **verify + ปิดช่อง** ✅ 2026-07-07: moneygate 8.1-8.7 (override ติ๊กเพิ่ม/ตัด/ค่าเสีย fail-closed ครอบเงิน+สถานะ+งานหัวหน้า กับ DB จริง 37/37) + **verify 14 ชุดผ่านไม่แก้ expectation** · **review รวบยอด 21 agents → 17 CONFIRMED แก้ครบ**: MAJOR 5 (cost-rates page ยัง role-based · garment-pick own-work drift → canSupervise · quotation สร้างได้แต่ดูไม่ได้ → gate see_order_money หน้า+⌘K · ปุ่มวงเงินเครดิต client/server ไม่ตรง → client ตาม role keep · ปุ่มใบส่ง/ยืนยันส่งไม่เช็คสิทธิ์ → canManageDelivery) · MINOR/NIT 12 (showCosts/showBlocked/⌘K memo dep/badge (n) helper/ข้อความ denial เป็น permission language/ป้าย 2 ตัว/role change reconcile overrides+audit/dead role field) · **จด follow-up**: MCP agent tools ยัง role-based (override ไม่มีผลบน MCP · เบสคุม key เอง) · ไม่มี invariant "OWNER เหลืออย่างน้อย 1"
- [x] PERM6 **follow-up จาก review PERM5** ✅ 2026-07-07: **MCP agent tools วิ่งผ่าน hasPermission** — verifyAgentToken แนบ permissionOverrides เข้า AgentContext · gate ต่อ tool เปลี่ยน allowedRoles → requiredPermission (receivables=see_finance · stock_check=any-of manage_production/create_sales_docs — union เท่า STOCK_ROLES เดิมเป๊ะ · order_status/today_queue ไม่ gate เหมือนเดิม) · strip เงิน → agentHasPermission — **override มีผลบน MCP key แล้ว** + **invariant "OWNER active ≥1"** (assertAnotherActiveOwner ใน user.update/setActive + เทส wiring ระดับ router `user.invariant.test.ts` — พิสูจน์ mutation จริง: ถอด guard เทสแดง) · review 4 มิติ+skeptic 10 agents: default-drift **0 findings** · CONFIRMED 2 (ฝั่งเทส) แก้ครบ · verify: unit 423 · **verify:mcp 59/59** (override ชั้น tool + e2e key จริง) · moneygate 39/39 · **จดค้าง**: gate aging MCP(see_finance) ↔ เว็บ billing-note(manage_billing_docs) ไม่ตรงข้าม surface — default เท่ากัน ต่างเฉพาะ override แยก · นโยบายรอเบสเคาะ
- **PERM สรุป**: ระบบสิทธิ์รายคนครบวงจร server+client+print+MCP · default = พฤติกรรมเดิม 100% (พิสูจน์ verify เดิมไม่แก้ expectation) · override มีผลจริงทุกชั้นรวม MCP agent key · tsc 0 · lint 0 error · unit 423 · **เหลือ UI browser test ฝั่ง non-OWNER = ผูก B16** (ต้อง login บัญชีทดสอบ — เบสทำเอง · classifier กันฉัน flip บัญชี)

### 🎨 UX — รื้อ UX/UI + จอโรงงาน (เบสสั่ง 2026-07-07: "UI รกใช้ยาก" + "โรงงานจะมีจอให้พนักงานตอนสกรีน" · ใบงานเต็ม: `docs/spec-ux-revamp.md` + audit 4 ฉบับใน `docs/_ux-audit-2026-07/` · เคาะแล้ว: ทีวี read-only + จอทัช/มือถือช่าง)
- [x] CC **ศูนย์บัญชาการผลิต** ✅ 2026-07-08 (เบสสั่งรื้อ "จอผลิตไม่มีจุดโฟกัส" + เคาะ "เห็นภาพรวมทั้งโรงงานก่อน"): `/production` → ภาพรวมหน้าเดียว (ต้องรีบ/สายการผลิต tile/คิวถัดไป/งานของฉัน) แตะ tile ลงลึกการ์ดเดิม · ตัด toggle แท็บ/บอร์ด · ใช้ kanban+me เดิมไม่มี endpoint ใหม่ · **แกนใหม่ของทั้งแผน UX** (spec §0) — ไม่รื้อเครื่องยนต์ รื้อการนำเสนอ · tsc/lint/unit 495 · browser ผ่าน
- [x] UX1 **จอช่าง ณ จุดงาน** ✅ 2026-07-07: ใบผลิตเห็นลาย+ตารางไซส์ (การ์ด "แบบ + ไซส์" + แถบเตือนไม่มีแบบอนุมัติ) + ปิดขั้น 1-2 แตะ (ปุ่มเร็ว+sheet จำนวน — ยิง updateStep เดิมล้วน) + B8 guards ครบ (outsource ค้าง/QC_FAILED/งานคนอื่น/GARMENT_PICK/press gate mirror บอร์ด) · review 42 agents CONFIRMED 18 แก้ครบ · verify browser จริง (ส่วนซ่อน QC — เบสเคาะ**คงเดิม** ตัดออกจากขอบเขต)
- [ ] UX2 **จอพิมพ์เห็นลาย + ปิดปุ่มหลอก B8** บน print-runs/films 【S-M】
- [ ] UX3 **หน้าสร้างออเดอร์เบาลง** (progressive disclosure — ราคาค่อยเปิดเมื่อมีรายการ · จอแรก ~18-20 control → ~8-9) 【M】
- [ ] UX4 **ทีวีคิวรวม `/factory`** (endpoint factory.board ไม่มีเงินโดยโครงสร้าง · auth = บัญชี "จอโรงงาน" login ค้าง ไม่แตะ schema · โชว์ชื่อลูกค้า+ชื่อช่าง) 【M-L】 — เคาะครบแล้ว 2026-07-07
- [ ] UX5 **detail ชิ้นเล็ก**: ตัด CTA ซ้ำ + แท็บลง URL 【S】
- [ ] UX6 **detail แท็บ "เงิน/บิล"** + รีด sidebar (การ์ดบิล 1,336 บรรทัดย้ายบ้าน) 【L】
- [ ] UX8 **เก็บฝั่งผลิต**: จุดรีด/แพ็ค + เศษเล็ก 【M】
- [ ] UX7 **ตารางสินค้า→การ์ดมือถือ + matrix default** 【L】 — เสี่ยงสุด ไว้ท้าย
- **ลำดับแนะนำ**: UX1→UX2→UX3→UX4→UX5→UX6→UX8→UX7 · คำถาม §4 เบสเคาะแล้ว 4/5 (2026-07-07 — เหลือข้อ 5 ยุบตรวจรับเสื้อ ไม่บล็อกงวดไหน) · ทับ C5 เดิม (ขยายเป็น UX1/UX2/UX8)

### 🎯 UX Refactor UX0–UX3 — ทางเดินงานทั้งระบบ (เบสเคาะ 2026-07-11 · ใบงานเต็ม `docs/spec-ux-refactor-2026-07-11.md`)
> ต่อจาก UX Revamp เดิมโดย **คง Prompt/สีแบรนด์/การ์ดเดิม** และแก้รากปัญหาเรื่องทางเข้า ลำดับงาน สิทธิ์ mobile และ accessibility แบบ targeted · ไม่มี schema migration/dependency ใหม่ · public URL/ข้อมูลเดิมต้องยังใช้ได้

- [x] **UX0 ฐาน UI + ความน่าเชื่อถือ** ✅ 2026-07-11 — control มือถือ ≥44px/input 16px (desktop 36px/14px) · `Field`/viewport-safe dialog/`ResponsiveList`/query state/capability gate · navigation registry เดียว+longest match · skip link/reduced motion/public forced-light · ปุ่ม hover-only และ empty-on-error ต้องหมด · jsx-a11y ยกเป็น error แล้วและผ่าน 0 error
- [x] **UX1 ทางเข้าตามบทบาท + จุดโฟกัส** ✅ 2026-07-11 — `/home` ส่งผู้มี `supervise_operations` ไป Dashboard ที่เหลือไป `/my-tasks` · แยกต้องทำก่อน/งานของฉัน/คิวทีมและตัดซ้ำ · Production supervisor/operator เรียงเนื้อหาคนละแบบ · KPI drill-down ด้วย URL state · global entity search ตาม permission · เพิ่มทางเข้า `/factory`
- [x] **UX2 flow ประจำวัน** ✅ 2026-07-11 — intake เดียว `/orders/new` (`/quotations/new` → `?next=quote`) · จอแรกเหลือข้อมูลหลักราว 5 จุด · Order detail เหลือ 4 tab หลักและ URL เป็น source เดียว · Production action ตามชนิดขั้น · core lists ใช้ desktop table + mobile card · Customer quick-create/contact/open-work
- [x] **UX3 ปิดทางตัน + targeted refactor** ✅ 2026-07-11 — Billing row → order money + print/action · quotation `updateDraft` atomic + `prepareShare` · product price local draft · Sync dialog reducer · Outsource เหลือคิวปฏิบัติการและ helper เดียว · Analytics แยกรายงานออกจากประวัติระบบ · แยก mega-component เฉพาะ pure decision logic ที่มี test ก่อน
- [x] **preview gate ผ่าน** ✅ 2026-07-11 — เปิดข้อมูลจริง `/my-tasks`, `/orders/new`, `/orders/[id]`, `/production/[id]` ทั้ง mobile/desktop ก่อนขยาย pattern และตรวจ core list เพิ่มที่ 320/375/640/768/1024/1440px + landscape; ช่วง CSS viewport เทียบ zoom 200% อยู่ในชุด 640–768px
- **เกณฑ์ปิด**: 320/375/640/768/1024/1440 + landscape/zoom 200% · keyboard/Escape/focus-return/reduced-motion/dark/public-light · 6 role+override · core mobile ไม่มี horizontal scroll · action มือถือ ≥44px · a11y lint 0 error · UI permission ตรง server · error ไม่ปลอมเป็น empty · quotation ไม่มี partial save · console 0 error · unit เดิมห้ามถอย
- **หลักฐานปิดงาน 2026-07-11**: browser จริงไม่มี horizontal scroll/console error และ public forced-light ผ่าน · keyboard tab navigation ผ่าน · role/override ตรวจด้วย permission tests ครบ 6 role + moneygate 39/39 · `typecheck` ผ่าน · lint 0 error · unit 567/567 (ฐานเดิม 495 ไม่ถอย) · production build ผ่าน 38/38 routes · ไม่มี schema/dependency ใหม่

### 🎯 UX4 — จุดนำสายตา + ความเนี้ยบทั้งระบบ (เบสสั่ง 2026-07-16 "วิเคราะห์+ปรับ UX/UI ให้ใช้ง่าย สวย มีจุดนำสายตา" · จาก audit 6 กลุ่มหน้า 81 findings)
> หลักการ: แก้ที่ primitive/config กลางก่อน (จุดเดียวส่งผลทุกหน้า) แล้วค่อยเก็บหน้า high-traffic · คงธีม/สี/โครงเดิมทั้งหมด · ไม่มี schema/dependency ใหม่

- [x] **UX4.1 สเกลปุ่มจริง + เป้านิ้วหน้างาน** — `button.tsx`: sm เล็กจริงบน desktop (มือถือคง ≥44px) · secondary/subtle ยุบเข้า outline · ถอน override `h-9` ในบอร์ดผลิต/command center · ปุ่มเบิกเสื้อ h-8 → เต็มมาตรฐาน · QC ผ่าน (ร้านนอก) ต้องผ่าน useConfirm ก่อน
- [x] **UX4.2 ภาษาสีสถานะชุดเดียว** — ลบ paymentStatusConfig ซ้ำใน /billing → ใช้ `lib/status-config.ts` ที่เดียว (เคาะ UNPAID=warning) · OrderStatusBadge dot ใหญ่ขึ้น + สีสถานะปลายทาง (ยกเลิก/เสร็จ)
- [x] **UX4.3 เลขเสี่ยงต้องเด่น** — StatCard รับ `tone`/`href` · aging/billing "เกินกำหนด" แดง+กดได้ · WHT เลิกการ์ดทำมือ · Dashboard ตัด metric ซ้ำ (บิลเลยกำหนด=บิลค้างชำระ) + แถวสองเป็นภาพรวมสะสมย่อ + PulseCard ยุบเข้า StatCard + การ์ดกดได้ต้องกดได้ทั้งใบ
- [x] **UX4.4 โทน neutral เดียว** — override slate ramp ใน `@theme` ให้เข้าตระกูล Apple gray เดียวกับ semantic token + green ramp anchor success · `.card-surface` ใช้ var · แก้ hex `#f5f5f7` หลุด 4 จุดใน layout
- [x] **UX4.5 ตัวหนังสือ ≥12px หน้างาน** — กวาด text-[9..11px] ใน orders/new + production/print-runs เป็น text-xs ขั้นต่ำ (ตาม DESIGN.md metadata ≥12px)
- [x] **UX4.6 หัวเรื่องมาตรฐานเดียว** — SettingsPageHeader → 26px semibold ให้ตรง PageHeader · label เมนูไทยล้วน (Dashboard→แดชบอร์ด, Outsource→จ้างร้านนอก · คำอังกฤษเก็บใน aliases) · DESIGN.md อัปเดตให้ตรงโค้ดจริง
- [x] **UX4.7 ทางไปต่อเมื่อ list ว่าง** — ResponsiveList รับ `emptyAction` · orders ติดตัวกรอง CTA = ล้างตัวกรอง · เลนผลิตว่าง/หลังผลิตเป็น EmptyState มีลิงก์ · dashboard ออเดอร์ล่าสุดว่างไม่หายทั้ง section
- [x] **UX4.8 ความเร่งด่วนบนแถว** — orders list: คอลัมน์กำหนดส่ง+สีเตือน (แดง=เลย, amber=ใน 48 ชม.) + sort ตามกำหนดส่ง + attention filter ยกเป็นแถว chip · my-tasks: badge ประเภทงานโชว์บนมือถือ + สีวันครบกำหนดตาม attention · production board มีวันส่งบนการ์ด
- [x] **UX4.9 เสียงเดียวบน order detail** — ตัด "→ ถัดไป" ออกจาก status bar ให้การ์ดขั้นต่อไปเป็นจุดนำเดียว · แท็บมือถือ grid ตาม role จริง (ช่าง/กราฟิก 3 แท็บไม่มีช่องตาย)
- [x] **UX4.10 ใบผลิตชี้ขั้นถัดไป** — steps list เน้น primary เฉพาะขั้นแรกที่ยังไม่เสร็จของเลน ขั้นหลังเป็น "รอขั้นก่อนหน้า" (ลอก pattern heatPressWaiting)
- [x] **UX4.11 ลูกค้าเครดิตเทอมเล่าเรื่อง** — customer detail: ค้างชำระโชว์เสมอเมื่อมีสิทธิ์เงิน (แดงเมื่อ >0) · header เหลือ แก้ข้อมูล+เปิดงาน (ช่องทางติดต่อมีการ์ดอยู่แล้ว)
- **ค้างไว้ก่อน (ใบงานถัดไป ไม่บล็อกงวดนี้)**: ~~outsource dialog · customer create form · print-table-row local Field~~ ✅ รอบเก็บตก 2026-07-17 → เหลือ: users table ดิบ (DataTable+mobile card) · add-product-popover → Radix 【M】 · typographic scale ประกาศใน @theme
- **เกณฑ์ปิด**: typecheck/lint 0 error · unit เดิมไม่ถอย · browser จริง desktop+mobile หน้า dashboard/my-tasks/orders/order detail/production/billing ไม่มี regression · dark mode + public light คงเดิม
- **ปิดงาน 2026-07-17 (Nami)**: ครบทั้ง 11 ข้อ · verify: tsc 0 · lint 0 error · test 576/576 (ฐาน 572 ไม่ถอย +4 เทสใหม่ firstPendingStepIdsByLane/navigation) · browser จริง light+dark: dashboard/orders/production board/billing/my-tasks mobile · ข้อยกเว้นตั้งใจ <12px เหลือ 2 จุด (ป้ายเวอร์ชันบน thumbnail + จุดนับบน badge กลม) · ~~UX4.11 แถว "งานยังไม่ปิด"~~ ✅ รอบเก็บตก 2026-07-17 (เพิ่ม openOrders ใน customer.creditStatus) · รอบเก็บตกเพิ่ม: ค้างชำระในแท็บเงินออเดอร์ (totalOutstanding+เทส) · settings hub จัด 3 หมวด · PulseCard กดได้ทั้งใบ · ฟอร์ม outsource/สร้างลูกค้า/print-table-row เข้า Field กลาง — verify รอบสอง: tsc 0 · lint 0 · test 577/577 · browser จริง settings/customer detail

### 📓 UX5 — รายการออเดอร์เป็นตารางเดียว (ทำจริงแล้ว **เบสเคาะไม่เอา ย้อนกลับ** 2026-07-17)
> ทำครบทั้งฟอร์ม (ยุบ ลาย/สินค้า/เสริม เป็นตาราง 6 คอลัมน์ ราคา×จำนวน=รวม ทุกแถว) และหน้ารายละเอียด (ตารางเดียวต่อรายการ) — เปิดงานจริงบน browser ยอดตรงสูตร แต่เบสดูแล้ว **ชอบแบบเดิม (แยกกลุ่ม ลาย/สินค้า/เสริม) มากกว่า** → revert ทั้งหมดก่อน commit · **อย่าหยิบไอเดีย "ตารางรวมชุดเดียว" มาทำซ้ำโดยไม่ถามเบสก่อน** · ถ้าจะปรับหน้านี้อีก ให้ปรับบนโครงแยกกลุ่มเดิม

### 🎯 UX follow-up — `/orders/new` สองคอลัมน์ (เบสสั่ง 2026-07-18)
- [x] จอ desktop แบ่งข้อมูลรับงาน (ลูกค้า/รายละเอียด/ไฟล์) ฝั่งซ้าย 1/3 + รายการ/ราคาฝั่งขวา 2/3 โดยจัดส่งและปุ่มเปิดงานคงเต็มแถว · จอเล็กเรียงคอลัมน์เดียวตามลำดับงานเดิม · คง progressive disclosure และโครงรายการแยก ลาย/สินค้า/เสริมตามมติ UX5 ✅ 2026-07-18
- **เกณฑ์ปิดผ่าน:** browser จริง 320/375/768/1024/1280/1440px ไม่มี horizontal scroll · light/dark + app console 0 error · `/orders/new` และ `?next=quote` โหลดถูก · ไม่แตะ logic/state/mutation · typecheck/lint 0 error · unit 577/577

### 🎯 UX follow-up — `/orders/new` คอลัมน์เดียว + รายการเป็นตาราง (เบสสั่ง 2026-08-02)
- [x] ยกเลิกโครง desktop สองฝั่ง ให้ลูกค้า/งาน → ไฟล์ → สินค้า/ราคา → จัดส่ง เรียงลงมาเต็มความกว้าง ✅ 2026-08-02
- [x] คงลาย/สินค้า/ส่วนเสริมเป็นคนละกลุ่มตามมติ UX5 · จอกว้างให้แต่ละกลุ่มเป็นตารางแถวเดียวพร้อมหัวคอลัมน์ · จอแคบเปลี่ยนทั้ง 3 กลุ่มเป็นการ์ด และใช้ container breakpoint ตามพื้นที่จริงเพื่อไม่บีบ/ตัดตารางในหน้าแก้รายการ ✅ 2026-08-02
- **เกณฑ์ปิดผ่าน 2026-08-02:** ไม่แตะ state/สูตรราคา/mutation · browser จริง `/orders/new` และ `?next=quote` ที่ 320/375/768/1024/1280/1440px + dark 1440px ไม่มี horizontal scroll/console error · typecheck ผ่าน · lint 0 error (38 warning เดิม) · unit 599/599 · `verify:ui` ผ่าน

### 🎯 UX follow-up — `/orders/new` รื้อโครงรับงานให้คลีนและใช้ง่าย (เบสสั่ง 2026-08-02)
> ขอบเขต presentation-only: คงหน้าเดียวคอลัมน์เดียว ข้อมูลกางครบ สูตรราคา validation draft permission และ mutation เดิม · ไม่กลับไป wizard/พับซ่อน/สองฝั่ง · ไม่รวมลาย/สินค้า/ส่วนเสริมเป็นตารางเดียวตามมติ UX5

- [x] จัด flow ใหม่เป็น 4 ตอนที่ชื่อและน้ำหนักสม่ำเสมอ: รับเรื่อง → รายการงาน → ราคาและเงื่อนไข → จัดส่ง โดยหนึ่ง surface ต่อหนึ่งตอนและไม่มี `Section`/การ์ดซ้อนกัน ✅ 2026-08-02
- [x] รวมไฟล์อ้างอิงไว้กับจุดรับเรื่อง · จัด field รองให้กระชับด้วย grid เฉพาะช่องที่สัมพันธ์กัน · ใช้ `Field`/`Alert` มาตรฐานแทน raw label/error surface ที่แตะ ✅ 2026-08-02
- [x] ในแต่ละชุดงานให้สินค้าอยู่ก่อนลายตามลำดับคำนวณจริง · คงตารางแยก 3 กลุ่มพร้อม mobile card · กลุ่มว่างมี CTA เดียวและถ้อยคำแยกระดับออเดอร์/ระดับชุดงานชัดเจน ✅ 2026-08-02
- [x] แยกราคา/ค่าใช้จ่าย/เงื่อนไขออกจากการ์ดรายการ และคง sticky bar ให้เหลือยอดหลัก + ยกเลิก + เปิดงาน ✅ 2026-08-02
- **ปิดงาน 2026-08-02:** browser จริง `/orders/new` และ `?next=quote` ที่ 320/375/768/1024/1280/1440px + light/dark ไม่มี horizontal scroll หรือ error overlay · ทดลองเพิ่มสินค้า ลาย ส่วนเสริม ค่าใช้จ่าย เปิด/ปิดที่อยู่ และกดส่งเพื่อดูสรุปช่องที่ขาด · `/orders/[id]` editor ยังใช้หน้าตาเดิมเมื่อไม่ส่ง presentation prop · typecheck ผ่าน · lint 0 error (38 warning เดิม) · unit 599/599 · `verify:ui` ผ่าน · ไม่รัน build ขณะ dev server ทำงาน

### 🎯 UX follow-up — รายการหน้าเปิดงาน/แก้ไขใช้ UI เดียว + ลายอยู่เหนือสินค้า (เบสสั่ง 2026-08-14)
> มติล่าสุดทับข้อ 131 ที่เคยให้สินค้าอยู่ก่อนลาย · ขอบเขต presentation-only: คง state, validation, ราคา, permission และ mutation เดิมทั้งหมด · shell/แท็บและปุ่มบันทึกยังต่างตามบริบทได้

- [x] ยุบ presentation branch ของ `OrderItemCard` ให้เหลือ contract เดียวทั้ง create/edit: ชื่อชุดงาน → ลายและงานพิมพ์ → สินค้าในชุดงาน → ส่วนเสริมในชุดงาน → หมายเหตุการผลิตชุดนี้ · ใช้หัวข้อ/ความกว้าง field/breakpoint ชุดเดียวกัน ✅ 2026-08-14
- [x] พื้นที่แก้รายการใช้ความกว้างอ่านง่ายเดียวกับหน้าเปิดงาน โดยไม่เปลี่ยนส่วนอื่นของ order detail ✅ 2026-08-14
- [x] verify: source/render guard กัน UI แตกแขนงกลับ · typecheck · lint · unit · `verify:ui` · browser จริง create/edit ที่ desktop+mobile พร้อม Light/Dark, ไม่มี horizontal overflow/console error และไม่บันทึกข้อมูลธุรกิจ ✅ 2026-08-14
- **ปิดงาน 2026-08-14:** ลบ prop/branch `appearance` ออกจาก `OrderItemCard` แล้วให้สอง caller ต่างกันเฉพาะกฎธุรกิจ · editor ใช้ `max-w-5xl` เท่าหน้าเปิดงาน · `verify:ui` ล็อกลำดับลายก่อนสินค้าและกัน presentation branch กลับมา · browser จริง 1440×900/390×844 Light+Dark วัด card 1024/348px และ CTA เท่ากันทั้งสองหน้า, populated edit สลับ table→card ถูก, target มือถือไม่ต่ำกว่า 44px, horizontal overflow/console error = 0 · typecheck ผ่าน · lint 0 error (29 warningเดิม) · unit 742/742 · Impeccable detector `[]` · ไม่รัน build ระหว่าง dev server ทำงาน

### 🐛 UX regression — หน้าแก้ยังไม่เหมือนหน้าสร้างทั้งฟอร์ม + ถูกบีบความกว้างผิดบริบท (เบสส่งภาพ 2026-08-14)
> รอบก่อนตรวจและรวมเฉพาะ `OrderItemCard` แล้วบังคับ `max-w-5xl` ให้ editor ทั้งก้อน จึงทำให้ส่วนลาย/สินค้าเหมือนกันแต่ค่าใช้จ่าย ส่วนลด สรุปยอด และ action bar ยังเป็น UI เก่า อีกทั้ง inline editor แคบกว่าหน้ารายละเอียดที่เป็น host · มติรอบนี้: **ความเหมือนวัดที่ component/composition ไม่ใช่บังคับ container คนละบริบทให้กว้างเท่ากัน**

- [x] ล็อกมาตรฐานความกว้างเชิงบทบาทไว้ที่ `PageShell`/`DESIGN.md`: `full` สำหรับ list/detail/inline edit · `wide` สำหรับ standalone document form · `content` สำหรับเนื้อหาอ่านโฟกัส · `form` สำหรับฟอร์มตั้งค่าสั้น; component ลูกห้ามจำลอง width ของ page shell ซ้ำ ✅ 2026-08-14
- [x] คืน `OrderItemsEditor` ให้เต็มพื้นที่แท็บของ order detail และใช้ `OrderFeeSection` + `OrderPriceSummary` ชุดเดียวกับหน้าเปิดงาน รวม fee catalog, MoneyInput, empty/mobile/table states และถ้อยคำมาตรฐาน โดยไม่แตะสูตรราคา/validation/permission/mutation ✅ 2026-08-14
- [x] ทำ action bar ของ editor เป็น flow ปกติชุดเดียวกับหน้าเปิดงาน ไม่ sticky/blur ทับเนื้อหา และเพิ่ม guard กัน max-width ซ้อน, UI ราคาแตกแขนง และ sticky regression กลับมา ✅ 2026-08-14
- [x] audit width roles + UI pattern ที่เกี่ยวทั้งระบบ แล้วแก้เฉพาะ regression ในขอบเขตนี้; หนี้หน้าอื่นบันทึกแยก ไม่ big-bang restyle · verify browser จริง create/edit empty+populated ที่ desktop/mobile Light/Dark, no overflow/console error พร้อม typecheck/lint/unit/`verify:ui`/detector ✅ 2026-08-14
- **ปิดงาน 2026-08-14:** create/edit ใช้ component กลางครบตั้งแต่รายการ → catalog error/retry → ค่าใช้จ่าย → ส่วนลด → สรุปยอด → action bar; editor รับความกว้าง `full` จาก order detail ส่วน create คง `wide` 1024px · browser จริง empty+populated 1440×900/390×844 Light+Dark: editor 1126/348px, create 1024/348px, mobile fee name 292px, overlap/overflow/console error = 0 และไม่บันทึกข้อมูลธุรกิจ · ด่านโค้ดดูที่ `PROGRESS.md`
- **ถูกทับด้วยมติล่าสุดด้านล่าง:** เลิก inline edit แล้วใช้ dedicated edit page ดังนั้น create/edit เป็น standalone document form ทั้งคู่และกว้าง `wide` 1024px เท่ากัน; order detail โหมดอ่านยังเป็น `full`

#### หนี้ width roles จาก audit ทั้ง repo — แยกแก้ ไม่ปน regression ออเดอร์
- [x] P2: ย้าย production detail จาก `max-w-4xl` ที่เขียนซ้ำมาใช้ `PageShell width="content"` ให้ loading/error/not-found/normal กว้างตรงกัน ✅ 2026-08-15
- [ ] P2: เคาะบทบาท dashboard home ที่ซ่อน `max-w-6xl` ใน `className` ว่าจะเป็น `full` หรือเพิ่ม named role กลาง; ห้ามถอดเพดานโดยเดาแล้วทำ dashboard ยืดทั้งจอ

#### หนี้ UI pattern จาก audit ทั้ง repo — คนละ workflow ต้องมีใบงาน/acceptance แยก
- [ ] P2: หน้าเปิดใบเสนอราคายังเป็น Card/form row รุ่นเก่า; ค่อย migrate เป็น `Section`/`Field`/price composition มาตรฐานโดยคง state และ conversion logic เดิม
- [ ] P2: settings company/cost-rates และฟอร์มแก้ใน patterns/services/users ยังมี raw label/Card ซ้อน; ทยอยย้ายทีละหน้า และห้ามกวาด editable grid ที่ใช้ `FIELD_LABEL` ถูกบทบาทอยู่แล้ว

### 🎯 UX follow-up — แก้ออเดอร์เป็นหน้าเต็มและใช้ฟอร์มเดียวกับสร้างจริง (เบสสั่ง 2026-08-14)
> คำสั่งล่าสุดทับ flow inline/dialog เดิม: จากหน้ารายละเอียด กดแก้ข้อมูลออเดอร์ ที่อยู่ หรือรายการงาน ต้องไป `/orders/[id]/edit` และเปิดแท็บที่เกี่ยวทันที · งานปฏิบัติการเฉพาะทาง เช่น ตรวจรับ อนุมัติแบบ ผลิต QC จัดส่ง บิล และรับเงิน คง flow/permission ของตัวเอง

- [x] เพิ่ม dedicated edit route แบบ `PageShell width="wide"` พร้อม loading/error/not-found/denied ที่ตรงกับหน้าสร้าง และ deep link `?tab=`/Back/Cancel กลับรายละเอียดเดิม ✅ 2026-08-14
- [x] ยกหน้า create เป็น form runtime เดียวที่รับ create/edit seed โดยใช้ component, hooks, validation, catalog, responsive state และ action bar ชุดเดียวจริง · edit ไม่อ่าน/เขียน draft localStorage และไม่รัน effect default ของการสร้างทับข้อมูลเดิม ✅ 2026-08-14
- [x] prefill ลูกค้า ข้อมูลรับเรื่อง รายการ/ลาย/สินค้า/ส่วนเสริม ค่าใช้จ่าย/ส่วนลด เงื่อนไข ที่อยู่ และไฟล์อ้างอิงจาก record จริง · ลูกค้า/ช่องทางที่เปลี่ยนไม่ได้ยังมองเห็นพร้อมเหตุผล ไม่ซ่อน ✅ 2026-08-14
- [x] บันทึกเฉพาะก้อนที่เปลี่ยนผ่าน `order.saveForm` ก้อนเดียวใน transaction: header/ที่อยู่ + รายการ/ค่าใช้จ่าย/ส่วนลด + ไฟล์อ้างอิง commit/rollback พร้อมกัน; `DESIGN_APPROVED`/`PRODUCTION_QUEUE` ออกใบแก้ไขพร้อมเหตุผล; สถานะหลังจากนั้นแสดงรายการ/ราคาอ่านอย่างเดียวโดยไม่ซ่อนแท็บ ✅ 2026-08-14
- [x] เปลี่ยน edit entrypoint ของ order detail ทั้ง header menu, next-step, overview info/address และ items card ให้ไป route กลางตามแท็บ · คง action งานผลิต/ตรวจรับ/แบบ/QC/บิลไว้ที่เดิม และ fail-closed เมื่อขาด `create_sales_docs` หรือ `see_order_money` ✅ 2026-08-14
- [x] เพิ่ม pure/source guard กัน form แตกแขนงและ inline/dialog กลับมา · verify browser empty+populated ที่ desktop/mobile Light/Dark, deep link/Back/Cancel/dirty state/read-only และ change-order ผ่าน unit/source guard (ฐานข้อมูลไม่มี fixture CO) พร้อม typecheck/lint/unit/`verify:ui`/Impeccable detector ✅ 2026-08-14
- **ปิดงาน 2026-08-14:** `/orders/new` และ `/orders/[id]/edit` render `OrderFormPage` ตัวเดียวกันภายใต้ `PageShell width="wide"` (1024px desktop / 348px ที่ viewport 390px) ส่วน detail คง `full` · การบันทึกใหม่เป็น transaction เดียวพร้อม optimistic fingerprints, receipt identity, credit Decimal delta และ stock version/CAS · dirty guard ครอบ Back/Cancel/sidebar/link/Command Palette/logout ก่อนทิ้งค่า · browser จริง Light/Dark empty+populated/deep-link/locked ที่ desktop+mobile ไม่มี overflow หรือ console errorและไม่กดบันทึกข้อมูลธุรกิจ · unit 824/824, typecheck, lint 0 error (28 warning เดิม), `verify:ui`, Impeccable detector `[]`, independent review **SHIP/CLEAR** · ไม่รัน build ระหว่าง dev server 3002 ทำงาน

#### Layout follow-up — แยกการจัดส่งออกจากการ์ดรับเรื่อง (เบสสั่ง 2026-08-14)
- [x] ย้าย `OrderShippingSection` เป็น sibling card ใต้ข้อมูลรับเรื่องในแท็บเดิมของ shared `OrderFormPage` เพื่อให้ create/edit เปลี่ยนพร้อมกัน · คง state/validation/draft/mutation/deep-link เดิม และ verify desktop/mobile ทั้งสวิตช์เปิด–ปิด ไม่มี overflow หรือ action overlap ✅ 2026-08-14 — browser จริง 1440×900/390×844/320×700 Light+Dark: สองการ์ดกว้างแนวเดียวกัน gap 16px, deep-link โฟกัสสวิตช์ถูก, ปิดแล้วเหลือ header card, เปิดใหม่ค่ากลับครบ, console error 0 · unit 824/824, typecheck, lint 0 error (28 warning เดิม), `verify:ui`, Impeccable layout detector `[]`

#### Layout follow-up — ภาพรวมออเดอร์ต้องอ่านสาระสำคัญได้ก่อนข้อมูลอ้างอิง (เบสสั่ง 2026-08-14)
> หลังย้ายการแก้ไขไปหน้าเต็ม แท็บภาพรวมมีหน้าที่สรุปเพื่ออ่าน ไม่ใช่จำลองฟอร์มแบบอ่านอย่างเดียว · ข้อมูลจริง สิทธิ์ และปลายทางแก้ไขเดิมต้องคงอยู่ แต่ช่อง optional ว่างห้ามกินพื้นที่ด้วยเครื่องหมาย `-`

- [x] วางสรุปออเดอร์เต็มแถวเป็นลำดับแรกทั้ง DOM/desktop/mobile โดยให้ลูกค้า กำหนดส่ง ความเร่งด่วน จำนวน และยอดตามสิทธิ์เห็นก่อนข้อมูลรอง ✅ 2026-08-15
- [x] แสดงเฉพาะข้อมูล optional ที่มีจริง · ค่าว่างที่มีผลกับงานใช้ข้อความตรง เช่น ยังไม่มีกำหนดส่ง/รายการ/ที่อยู่จัดส่ง และห้ามทำ `฿0` หรือ “ยังไม่จองสต๊อก” ให้ดูเป็นสถานะธุรกิจโดยไม่มีบริบท ✅ 2026-08-15
- [x] แยกเจ้าของข้อมูลให้ชัด: ข้อมูล/ที่อยู่ออกบิลอยู่การ์ดลูกค้า, ที่อยู่ส่งอยู่การ์ดการจัดส่ง, tracking พาไปแท็บจัดส่ง · คง `showMoney` gate ระดับ JSX และ edit route/deep-link เดิม ✅ 2026-08-15
- [x] verify browser จริง empty+populated ที่ 1440×900/390×844 Light+Dark: reading order, natural card height, no standalone dash/overflow/action overlap/console error, touch target และข้อมูลเงินไม่รั่ว พร้อม typecheck/lint/unit/`verify:ui`/Impeccable detector ✅ 2026-08-15
- **ปิดงาน 2026-08-15:** ภาพรวมเรียง `สรุปออเดอร์ → ลูกค้าและผู้ติดต่อ → การจัดส่ง` ทั้ง DOM และภาพจริง · สรุปมือถือเป็น 2×2 ทำให้ข้อมูลหลักครบก่อน bottom nav โดยเหลือ safe margin 90.7px ในเคสแคบสุด · การ์ดรองสูงตามเนื้อหาและแยกเจ้าของข้อมูลตรง flow แก้ไขใหม่ · optional ว่างไม่สร้างแถว `-`; empty state ใช้ข้อความตรงและไม่แสดง `฿0`/สถานะสต๊อกหลอก · browser empty+populated Light/Dark ไม่มี overflow/ชน/console error, target ≥44px และ deep-link เงิน/ข้อมูล/จัดส่งกลับถูก · unit 824/824, typecheck, lint 0 error (28 warning เดิม), `verify:ui`, Impeccable layout detector `[]`, independent review **CLEAR** · ไม่รัน build ระหว่าง dev server 3002 ทำงาน

#### CI hotfix — lockfile ต้องติดตั้งซ้ำได้ด้วย npm 11.17 (เบสอนุมัติ 2026-08-15)
> PR #1 merge แล้วแต่ GitHub CI หยุดที่ `npm ci` ก่อนเข้า test เพราะ lockfile ขาดรายการ dependency ย่อยแบบ exact ที่ npm 11.17 บังคับตรวจ · แก้เฉพาะ lockfile ห้ามเปลี่ยน dependency ที่ประกาศใน `package.json`

- [x] สร้าง `package-lock.json` ใหม่ด้วย npm 11.17 และยืนยัน diff ว่า `package.json`/dependency versions ไม่เปลี่ยน ✅ 2026-08-15
- [x] รัน `npm ci` ด้วย npm 11.17 จาก install สะอาด แล้วผ่าน test/typecheck/lint/`verify:ui` ก่อนส่ง hotfix PR เข้า `main` ✅ 2026-08-15
- **ปิดงาน 2026-08-15:** npm 11.17 เติม nested exact `@emnapi/core`/`runtime@1.10.0` ของ Rolldown และ WASI bundle metadata ที่ขาด พร้อม normalize `peer` flags โดยไม่แก้ `package.json` หรือ version/resolved/integrity/dependency เดิม · `npm ci` ผ่านจาก install สะอาด ตามด้วย unit 824/824, typecheck, lint 0 error (28 warning เดิม) และ `verify:ui` · PR #2 merge เข้า `main` ที่ `a92e953` และ GitHub main-push CI run `31832013951` ผ่านครบ · `npm audit` ยังรายงาน 13 รายการเดิม (2 low, 2 moderate, 9 high) ให้ประเมินเป็นงาน dependency audit แยก ห้ามใช้ `npm audit fix` อัตโนมัติ

### 🎯 UX follow-up — ช่องกรอกต้องเห็นว่าเป็นช่อง + คอลัมน์ตรงกัน (เบสเจอบนของจริง 2026-08-03)
> เบส: "บางอย่างที่มันต้อง fill ข้อมูล ไม่ต้องซ่อน ช่อง user จะได้รู้ว่าช่องไหนต้องใส่ · ช่องบางช่องถูกกลืนไปกับพื้นหลังสีเทา · แถวคอลัมขอให้มันตรงกันทั้งหมด"
> **บทเรียน:** รอบ "ลดเส้นทั้งเว็บ" ลดเลยจุดที่ควรลด — ขอบของ *กล่องครอบ* ถอดได้ แต่ **ขอบช่องกรอกถอดไม่ได้** เพราะมันคือสัญญาณเดียวที่บอกว่า "ตรงนี้กรอกได้"

- [x] `FIELD_SURFACE` กลับเป็นพื้นขาว + เส้นบาง (พื้นเทาจม slate-100 ต่างจากพื้นหน้า `#f3f4f6` แค่ 1 ขั้น · จากกล่องย่อย slate-50 แค่ 6 ขั้น = กลืนหาย) ✅ 2026-08-03
- [x] ถอดชุด `quiet` ทิ้งทั้งหมด (token + prop ของ Input/Select + 16 จุด) — ช่องในตารางเคยโปร่งสนิทจนอ่านเป็นตัวหนังสือธรรมดา · **ห้ามเอาแนวคิด "ช่องกรอกโปร่ง" กลับมาโดยไม่ถามเบสก่อน** ✅ 2026-08-03
- [x] `SUNK_PANEL` เข้มขึ้นเป็น slate-100 ได้ เพราะช่องเป็นพื้นขาวแล้ว ✅ 2026-08-03
- [x] `ItemTableCols` — โครงคอลัมน์ 8 ช่องใช้ร่วมกันทั้งตารางสินค้า/ลาย/ส่วนเสริม (ส่วนเสริมใช้ `colSpan` ให้ "ราคา"+ถังขยะลงตำแหน่งเดียวกัน) ✅ 2026-08-03
- [x] `verify:ui` บังคับ `border-slate-200`+`bg-surface` และห้าม `bg-slate-100`/`border-transparent` กลับมา ✅ 2026-08-03
- **verify:** typecheck 0 · lint 0 error (37 warning) · unit 599/599 · `verify:ui` ผ่าน · `next build` 38 หน้า · browser จริง light+dark · **วัดบนหน้าจริง: 3 ตารางได้ขอบซ้ายคอลัมน์ชุดเดียวกัน** `359·435·799·903·1019·1111·1187·1283`

### 🎯 UX follow-up — ปรับสัดส่วนทั้งเว็บ (เบสเคาะจาก mockup 2026-08-03)
> เบส: "ดีขึ้น แต่ดีได้กว่านี้ ต้องปรับองค์ประกอบให้สมส่วน สวยงามมากขึ้น" → วัดของจริงก่อน แล้วแก้ที่ token กลาง
> ขอบเขต: design-token only · ไม่แตะ logic/state/mutation

- [x] **มุมโค้งเป็นบันได** — เพิ่ม `RADIUS.field` 10px ให้ช่องกรอก/ช่องเลือก/กล่องข้อความ (เดิม 16px เท่าการ์ด · โค้งเกือบครึ่งความสูง 36px) → การ์ด 16 › กล่องย่อย 12 › ช่องกรอก 10 › ชิ้นเล็ก 8 › ปุ่มกลม ✅ 2026-08-03
- [x] **ความสูง control ชั้นเดียว** — `CONTROL_H_SM` ยุบมาเท่า `CONTROL_H` (จอเดียวเคยมี 32/36px ปนกัน 30/27 ชิ้น ขอบล่างไม่ตรง) · คงชื่อ token ไว้ไม่ต้องแก้ 26 จุด ✅ 2026-08-03
- [x] **`FIELD_MEASURE`** — ช่องบรรทัดเดียวที่ยืนลำพังกว้างเท่า 1 คอลัมน์ของกริด 2 ช่อง (เดิมยืดเต็ม 976px) ✅ 2026-08-03
- [x] **จังหวะ/น้ำหนัก** — `Section` ขอบการ์ด 24→28px · หัวข้อ→เนื้อหา 16→20px · ป้าย→ช่อง 6→8px · เลขตอน 24→20px · ยอดรวมท้ายฟอร์ม 16→20px ✅ 2026-08-03
- [x] `verify:ui` บังคับ `rounded-[10px]` และห้าม `sm:h-8` กลับมา ✅ 2026-08-03
- **verify:** typecheck 0 · lint 0 error (37 warning) · unit 599/599 · `verify:ui` ผ่าน · `next build` 38 หน้า · browser จริง + **วัดค่าซ้ำบนหน้าจริง**: มุมโค้งเหลือ 10px ชั้นเดียวบน control · ความสูง 36px ค่าเดียว 32 ชิ้น · ช่องบรรทัดเดียวกว้างสุด 480px

### 🎯 UX follow-up — ลดเส้น/ขอบทั้งเว็บ ระดับ 3 (เบสเคาะจาก mockup 2026-08-03)
> เบสส่งภาพ `/orders/new` มา: "มันดูยากอะ อยากลดการใช้เส้น ขอบทั้งเว็บเลย ให้มัน minimal หน่อย"
> ขอบเขต: **design-token only** — ไม่แตะ logic/state/mutation · เปลี่ยนที่ token กลางเป็นหลัก ให้ทุกหน้าได้ตามทันที ไม่ไล่แก้รายหน้า

- [x] `FIELD_SURFACE` เลิกใช้ขอบ → พื้นจม (`slate-100` / dark `slate-950`) · คง `border-transparent` ไว้ให้ `FOCUS_FIELD` ทำงานและความสูงไม่ขยับ ✅ 2026-08-03
- [x] `.card-surface` ถอดวงแหวน `0 0 0 1px` เหลือเงานุ่ม (ขยายเงาชดเชย) · `DataTable.Body` เลิกเส้นคั่นแถว · ซ่อนลูกศร `input[type=number]` ทั้งเว็บ ✅ 2026-08-03
- [x] token ใหม่: `FIELD_SURFACE_QUIET` (+ prop `quiet` ของ Input/Select — ช่องในตารางแก้ข้อมูล โปร่งสนิท) · `SUNK_PANEL` (กล่องย่อยในการ์ด จางกว่าผิวช่องกรอกหนึ่งขั้น) ✅ 2026-08-03
- [x] หน้าเปิดงาน: ถอดกรอบรอบตาราง 3 จุด · ถอดกรอบ "รายการที่ N" · ช่องในแถวตาราง 16 จุด→`quiet` · ปุ่ม "เพิ่ม…" →ghost · ถังขยะ 7 จุดเทา-แดงตอนชี้ ✅ 2026-08-03
- [x] หน้าอื่น: ถอดเส้นคั่นเฉพาะแถวที่กดได้ (มี hover แยกแถวอยู่แล้ว) · แถวข้อความนิ่งคงเส้นไว้ ✅ 2026-08-03
- [x] `verify:ui` บังคับกฎใหม่ + ใส่ `border-slate-200/70`,`bg-white` ในลิสต์ "ต้องไม่มี" กันขอบกลับมาเงียบ ✅ 2026-08-03
- **verify:** typecheck 0 · lint 0 error (37 warning) · unit 599/599 · `verify:ui` ผ่าน · `next build` ผ่านทุก route · browser จริง light+dark: `/orders/new` (ว่าง+มีสินค้า+สเปคตัดเย็บ) · `/orders` 77 แถว · แดชบอร์ด
- [ ] **รอเบสดูของจริงแล้วเคาะ 2 จุด:** ตาราง `/orders` ไม่มีเส้นคั่นแถวแล้ว (ตัวอย่างที่เคาะมี 3 แถว ของจริง 77 แถว — ถ้าหลุดแถว ใส่กลับที่ `data-table.tsx` จุดเดียว) · ปุ่ม outline ทั่วไป (ยกเลิก/ตัวกรอง/ช่วงวันที่) ยังมีขอบตาม mockup

### 🎯 UX follow-up — `/orders/new` แบบ B: การ์ดแบบแดชบอร์ด (เบสเคาะจาก mockup 2026-08-03)
> เบสสั่ง “ปรับ UXUI ให้เข้ากับหน้าอื่นๆ อย่างแดชบอร์ด และทำให้ดูใช้งานง่าย” → ส่ง mockup 3 แบบให้เคาะก่อน เบสเลือก **แบบ B** (แยกการ์ด + แถบขั้นตอน) และสั่งแก้บั๊กที่เจอไปด้วย
> ขอบเขต: ทุกช่องยังกางครบ · **ไม่มี wizard / ไม่พับซ่อน / ไม่สองฝั่ง** ตามมติเดิม · ไม่แตะสูตรราคา validation permission หรือ mutation (ยกเว้นบั๊ก 3 ตัวที่เบสสั่งให้แก้)

- [x] เลิก ledger ผิวเดียว → 4 การ์ด `Section` บนพื้นเทา + `PageShell width="wide"` (ได้ระยะ space-y-6 และชั้น loading/error/denied กลาง) ✅ 2026-08-03
- [x] หัวข้อตอน: เลขเป็นชิปกลม + คำอธิบายใต้หัวข้อ · `StepRail` sticky กดกระโดด + จุดบอกตอนที่มีข้อมูล + ป้าย “เก็บร่างอัตโนมัติแล้ว” ✅ 2026-08-03
- [x] แถบล่างเลิกโปร่ง+blur เป็น `card-surface` ทึบ + เผื่อ safe-area · ยอดรวมเรียกชื่อเดียวกับสรุปยอด ✅ 2026-08-03
- [x] กล่องสเปคตัดเย็บเลิกใช้ `<Alert variant="warning">` (role=alert + ช่องกรอกข้างใน = ผิดกติกา tokens.ts) → กล่องพื้นจมเทา · ป้าย “ลูกค้า” ใช้ `<Label htmlFor>` มาตรฐาน · หัวข้อ “เงื่อนไขการขาย” ใช้ `Section headingLevel={3}` · เส้นคั่นโหมดมืด 8 จุด slate-800 → white/10 ✅ 2026-08-03
- [x] **ของกลาง:** `Input`/`Select` `size="sm"` เลิกบังคับ 12px บนมือถือ (iOS ซูมทุกครั้งที่แตะ · ขัด DESIGN.md 16px) → `sm:text-xs` + แก้ assertion `verify:ui` ให้ตรงกฎจริง ✅ 2026-08-03
- [x] บั๊ก 3 ตัว: ภาษี 0% ถูกทับเป็น 7% ตอนเลือกนิติบุคคล · ร่างหัวฟอร์มกู้คืนเงียบไม่มีแบนเนอร์ · เลขที่ PO ค้างเมื่อสลับลูกค้า ✅ 2026-08-03
- [ ] **verify browser จริงยังทำไม่ได้** — dev server ไม่ hydrate (reproduce กับโค้ดเดิมด้วย = incident client state เดิม) · ต้องให้เบส Clear site data ก่อน แล้วตรวจ: ชิปแถบขั้นตอนกระโดดจริง · มือถือ 375px แตะช่องแล้วไม่ซูม · บั๊ก 3 ตัวหายจริง · 320/375/768/1024/1280/1440px light+dark ไม่มี horizontal scroll/console error
- **verify ที่ผ่านแล้ว:** typecheck 0 · lint 0 error (37 warning · ลดจาก 38) · unit 599/599 · `verify:ui` ผ่าน · `next build` ผ่านทุก route

### 🎯 UX follow-up — `/orders/new` composition pass 2 (เบส feedback 2026-08-03 ว่า “ยังรกและจัดองค์ประกอบไม่ดี”)
> ขอบเขต presentation-only ต่อจากรอบก่อน: ทุก field ยังมองเห็น หน้าเดียวยาว ไม่มี wizard/accordion/สองฝั่ง · คง product/print/add-on แยกกลุ่มและไม่แตะ state สูตรราคา validation permission หรือ mutation

- [x] เปลี่ยนการ์ดใหญ่ 4 ใบเป็นฟอร์มผิวเดียวแบบ ledger แบ่ง 4 ช่วงด้วยเส้นบางและลำดับเลข เพื่อให้สายตาเห็น flow แทนการเห็นกรอบ ✅ 2026-08-03
- [x] ลดเสียงรบกวนช่วงรับเรื่อง: customer picker กระชับบนจอกว้าง · ช่องทางเป็น select · ลด copy ซ้ำ · ไฟล์อ้างอิงเป็นแถว action สั้นโดยไม่ซ่อนข้อมูล ✅ 2026-08-03
- [x] ลด empty surface ในรายการ/ราคา: CTA แหล่งสินค้า/ลาย/ส่วนเสริมเป็น action สั้น · ค่าใช้จ่ายระดับออเดอร์เป็นแถวสั้น · summary และ action footer ไม่สร้าง card ซ้อน ✅ 2026-08-03
- [x] verify browser จริงแบบก่อน–หลังที่ 320/375/768/1024/1280/1440px · light ทุก breakpoint + dark 1280px · ไม่มี horizontal scroll/error overlay/console error · เพิ่มสินค้า ลาย ส่วนเสริม ค่าใช้จ่าย เปิดที่อยู่ และ shared `/orders/[id]` ไม่ถอย · typecheck/lint/unit/`verify:ui` ผ่าน ✅ 2026-08-03
- **ปิดงาน 2026-08-03:** ใช้ origin สะอาดหลบ client state เก่าของ Turbopack ที่ `localhost:3000` ตาม incident ที่บันทึกไว้ · ตารางสินค้าจอกว้างมีหัวครบ 8 คอลัมน์และพื้นที่แคบใช้ card · interaction ทั้ง 4 จุดผ่าน · lint 0 error (38 warning เดิม) · unit 599/599 · `verify:ui` ผ่าน · ไม่รัน build ขณะ dev server ทำงาน

### 🐛 UX regression — แถบปุ่มติดจอทับพื้นที่ทำงาน (เบสส่งภาพ 2026-08-14)
> ทำซ้ำได้ที่ viewport ปกติ 1280×720 โดยไม่ต้องซูม: `/orders/new` เปิดแท็บรับเรื่องแล้วแถบเปิดงานทับหัว “การจัดส่ง”; popup แก้ลูกค้าให้ footer ทับช่องหมายเหตุ · เป็น regression ของ sticky action ที่บันทึกไว้ในข้อ 196 และก้อน popup A ด้านล่าง

- [x] popup ยาวแยก body ที่เลื่อนได้ออกจาก header/footer แล้ว ปุ่มเห็นตลอดโดยไม่วางทับช่องกรอก และคงเพดานเดิม `90dvh` ✅ 2026-08-14
- [x] `/orders/new` ย้าย action bar กลับไปอยู่ท้าย flow ทุกขนาดจอ หลังจอจริงยืนยันว่า sticky รับ pointer แทน field ทั้ง desktop/mobile ✅ 2026-08-14
- [x] verify browser จริง Light/Dark ที่ 1280×720, 1512×827 (ขนาดภาพที่เบสส่ง) และ 390×844: overlap = 0, เลื่อนต้น–ท้ายครบ, ปุ่มรับ click ตรงตัว, ไม่มี horizontal overflow/console error · typecheck ผ่าน · lint 0 error (29 warning เดิม) · unit 742/742 · `verify:ui` ผ่าน · detector `[]` ✅ 2026-08-14
- **ปิดงาน 2026-08-14:** ต้นเหตุคือ sticky footer/action bar อยู่ใน scroll container เดียวกับ field โดยไม่แยกพื้นที่ จึงวางทับและรับ pointer แทนเนื้อหาเมื่อความสูงจอสั้น · แก้เชิงโครงที่ popup และเลิก sticky บนหน้าเปิดงาน; ไม่แตะข้อมูล validation permission ราคา หรือ mutation · ไม่รัน build ขณะ dev server 3002 ทำงาน

### 🐛 UX regression follow-up — popup ลูกค้ายังไม่พอดีกรอบ (เบสส่งภาพ 2026-08-14 21:00)
> หลังแยก scroll body แล้ว footer ไม่ทับ field แต่กรอบยังเว้นซ้าย 24px/ขวา 56px และ scrollbar ทำให้ขอบ field ขวาห่างถึง 70px; ที่ viewport จริง 1512×827 เพดาน `90dvh` บีบ body จนช่องหมายเหตุเห็นเพียง 46.8/96px ทั้งที่ safe frame มาตรฐานสูงพอ · ภาพล่าสุดเป็น crop DPR2 ขนาด 1616×1580; ที่ 320px ล้นแนวนอน 14px

- [x] แยก header/body/footer เป็น 3 พื้นที่จริงใน popup ลูกค้า: เผื่อปุ่มปิดเฉพาะ header, body เลื่อนเอง, footer เป็น static และแต่ละส่วนถือ padding ซ้าย–ขวาเท่ากัน ✅ 2026-08-14
- [x] ใช้ safe frame กลางแทน `90dvh`: viewport 1512×827 เห็นช่องหมายเหตุครบ 96/96px ตั้งแต่เปิด; จอสั้น/มือถือเลื่อนถึง field สุดท้ายเต็มโดยกรอบหัว/ท้ายไม่ขยับ ✅ 2026-08-14
- [x] verify browserจริง Light/Dark ที่ 1512×827 (viewport ของภาพล่าสุด), 390×844 และ 320×700: desktop field inset 24/24px, mobile padding 20/20px โดย scrollbar แยกอยู่นอกเนื้อหา, overlap/horizontal overflow = 0, Escape ปิดได้, ปุ่มมือถือ 44px และ hit-test ตรงตัว · typecheck ผ่าน · lint 0 error (29 warningเดิม) · unit 742/742 · `verify:ui` ผ่าน · detector `[]` ✅ 2026-08-14
- **ปิด follow-up 2026-08-14:** ต้นเหตุร่วมคือ `pr-14` กลางสำรองปุ่ม X ให้ทั้งการ์ด + local `90dvh` บีบความสูง; แก้เฉพาะ popup ลูกค้าเป็น row-owned padding และใช้ `overflow-clip` กัน browser เลื่อนกรอบนอกเมื่อโฟกัส field ล่าง · ไม่เปลี่ยนข้อมูล validation permission หรือ mutation · ไม่รัน build ขณะ dev server 3002 ทำงาน

### 🎯 UX follow-up — เปิดข้อมูลสร้าง/รายละเอียดออเดอร์ทั้งหมด (เบสสั่ง 2026-07-18)
- [x] `/orders/new`: กางข้อมูลงาน รายการ+ราคา ไฟล์ ที่อยู่ รายละเอียดลาย/สินค้า และสเปคตัดเย็บที่ UI มีอยู่เดิมตลอด · เลิกปุ่ม “เพิ่มเติม/ซ่อน” ที่ใช้เพื่อ declutter · ที่อยู่ยัง optional และเงื่อนไขตามชนิดลูกค้า/ช่องทาง/สินค้าเดิมต้องไม่เปลี่ยน — ✅ 2026-07-18
- [x] `/orders/[id]`: เลิกซ่อนเนื้อหาที่ UI มีอยู่เดิมหลังแท็บและเมนู “เพิ่มเติม” · แสดงรายการ งานผลิต จัดส่ง เงิน/บิล ไฟล์ และประวัติต่อกัน พร้อมปุ่มลัดแบบ scroll · กางเส้นทางสถานะและประวัติทั้งหมด โดยไม่เพิ่ม raw field จากฐานข้อมูลที่ยังไม่มี UI — ✅ 2026-07-18
- [x] คง permission/state gates ทุกชั้น: ผู้ไม่มี `see_order_money` ต้องไม่เห็นเงิน/ต้นทุน/กำไร/บิล · action แก้ไข/สถานะ/ผลิต/QC/จัดส่งยังตรงด่าน server · business-conditional และ action workflow ไม่ถูกบังคับให้ render ก่อนมีบริบท — ✅ 2026-07-18
- **เกณฑ์ปิดผ่าน 2026-07-18:** browser จริง `/orders/new`, `?next=quote`, `/orders/[id]` ที่ 320/375/768/1024/1280/1440px · light/dark · ไม่มี horizontal scroll/console error · ลิงก์เก่า `?tab=` แปลงเป็น anchor + focus ถูกจุด · `verify:moneygate` 43/43 (รวม description/reason/approval token) · typecheck/lint 0 error · unit 584/584

### 🎯 ใบงานถัดไป — แยก "ที่อยู่ผู้ติดต่อ" ออกจาก "ที่อยู่จัดส่ง" + เก็บบั๊ก UI (เบสสั่ง 2026-08-12)
> เบส: "เรื่องที่อยู่ผู้ติดต่อ ควรแยกกันมั้ย ที่อยู่ของผู้ติดต่อ กับที่อยู่จัดส่ง โดยที่อยู่จัดส่งสามารถเลือกที่อยู่ของผู้ติดต่อได้ ปรับหน้าผู้ติดต่อ และสร้างออเดอร์ รายละเอียดออเดอร์ และที่อื่นๆที่ฉันไม่รู้ · และ Ui หน้านี้มันไม่พอดีกรอบ popup และตรวจบั๊กอื่นๆด้วย"
> **เบสเคาะ 2026-08-12: ทาง (ก) — ไม่แตะ schema · เก็บบั๊กครบ 4 ข้อ + popup** · mockup ที่เคาะ: https://claude.ai/code/artifact/69e2f732-7c80-478e-8bfa-4324f9e6b51b

**ก้อนงานที่เคาะแล้ว — ✅ ปิดครบ 2026-08-12** (3 commit: `ef2ecdd` popup · `2ce73a9` เอกสารพิมพ์ · `b22c611` ที่อยู่)
- [x] **A · popup ปุ่มปักก้นกรอบ** — แก้ `ui/dialog.tsx` จุดเดียว: `DialogContent` เป็น `pb-0`, `DialogFooter` เป็น sticky bottom + พื้น surface · ครอบ 28/33 ไฟล์ที่ใช้ `DialogFooter` · ตามเก็บ 2 ตัวที่เขียนปุ่มเอง (`settings/users:447`, `billing/aging:447`) · **ห้ามใช้ negative margin** (ทดลองแล้วเกิดช่องโหว่ให้เนื้อหาลอดออกมา)
- [x] **B · ป้าย/ชื่อเรียกให้ตรงกันทั้งเว็บ** — `customer-form-fields.tsx:136` "ที่อยู่ (จัดส่ง)" → **"ที่อยู่ผู้ติดต่อ"** + hint ว่าใช้บนเอกสารเมื่อไม่มีที่อยู่ออกบิล · `create-delivery-dialog` "ที่อยู่หลัก" → "ที่อยู่ผู้ติดต่อ" · หน้าลูกค้าเพิ่มป้ายกำกับ (เดิมมีแต่ไอคอนหมุด) · ลำดับคำ "ตำบล/แขวง" ให้ตรงกันทั้งระบบ
- [x] **C · ปุ่ม "ใช้ที่อยู่ลูกค้า" 3 จุด + เลิก prefill เงียบ** — `order-shipping-section` (เปิดงาน) · `order-info-edit-dialog` (แก้ออเดอร์) · `create-delivery-dialog` (ใบส่ง — ดึงจาก `order.shipping*` ก่อน) · กดปุ่มแล้ว**เปิดสวิตช์ "จัดส่งตามที่อยู่" ให้เอง** (บั๊ก: วันนี้ prefill เงียบแต่สวิตช์ปิด → กดเปิดงานแล้วที่อยู่หายไม่มีคำเตือน)
- [x] **D · เอกสารพิมพ์ที่อยู่ครบ** — helper กลาง 1 ตัวประกอบ `billingAddress + subDistrict + district + province + postalCode` ให้ใบเสนอราคา/ใบแจ้งหนี้+ใบกำกับ/ใบวางบิลใช้ร่วม (วันนี้พิมพ์แค่บรรทัดแรก = ที่อยู่ผู้ซื้อไม่ครบตาม ม.86/4) · ใบเสนอราคาแปลง `branchNumber "00000"` → "สำนักงานใหญ่" ให้ตรงกับอีก 2 ใบ
- [x] **E · ที่อยู่ส่งของห้ามทับโปรไฟล์** — `delivery.create` + `saveAsCustomerAddress` เขียน `customer.address` ได้**เฉพาะตอนโปรไฟล์ยังว่าง** (กติกาเดียวกับ `phone` ที่ทำถูกอยู่แล้ว) · UI ปิด checkbox พร้อมบอกเหตุผลเมื่อลูกค้ามีที่อยู่แล้ว
- [x] **F · ลบที่อยู่ที่กรอกผิดได้** — `order.update` 7 ช่อง shipping เป็น `.nullable()` + ฟอร์มส่ง `null` แทน `undefined` (วันนี้ลบข้อความแล้วกดบันทึก ค่าเก่ายังอยู่เงียบๆ) · `customer.update.address` ให้ `nullable` เท่ากับ `billing*`
- [x] **verify** ✅ — typecheck ผ่าน · lint 0 error (37 warning เดิม) · unit **652/652** (เพิ่ม `address-fill` 10 + `customer-doc-address` 8) · `verify:ui` ผ่าน · ไม่มี console error

**ปิดงาน 2026-08-12 — ยืนยันจากจอจริง (ไม่ใช่แค่ typecheck):**
- popup: วัด DOM หลังแก้ — เนื้อใน 1284px ในกรอบ 744px ปุ่มอยู่ที่ขอบล่างพอดี (`footerBottom === dialogBottom`) · พื้น footer = พื้น dialog เป๊ะทั้ง light `#fff` และ dark `#2e2e31` · `padding-bottom: 0px` ทำงานผ่าน `has-[]` · เลื่อนอ่านครบไม่มีเนื้อหาลอดใต้ปุ่ม (probe `elementFromPoint` 20 จุด)
- flow เต็ม: กรอกที่อยู่ผู้ติดต่อ → เปิดงาน (กดปุ่มก๊อป → สวิตช์เปิดเอง) → **ที่อยู่ถูกบันทึกจริงบน ORD-2608-0001** (เดิมหายเงียบ) → ลบที่อยู่จาก dialog แก้ไข → หายจริง
- ใบส่งของ (ORD-2607-0058 · QC ผ่าน UI ไม่ข้าม `isValidTransition`): prefill ครบ 7 ช่องจากใบงาน · รอบแรกเติมโปรไฟล์ที่ยังว่างได้ · **รอบสอง checkbox ปิดตายพร้อมบอกเหตุผล** (ยืนยันจาก DB ว่า `customer.address` ไม่ถูกทับ)
- หน้าพิมพ์: ใบเสนอราคา QT-2607-0003 + ใบแจ้งหนี้ INV-D-2607-0006 พิมพ์ที่อยู่ 2 บรรทัดครบ + "สำนักงานใหญ่" แทน "สาขา 00000"
- ⚠️ กับดักเดิมเจอซ้ำ 3 ครั้ง: Turbopack HMR ค้างหลังแก้ไฟล์เยอะ → หน้าตายพร้อม "module factory is not available" · แก้ด้วย `Cmd+Shift+R` ไม่ใช่บั๊กโค้ด · **ป้ายเก่าจะโชว์ค้างจนกว่าจะ hard refresh — อย่าเพิ่งสรุปว่าโค้ดไม่ทำงาน**

**⚠️ เจอระหว่างสำรวจ ยังไม่แก้รอบนี้ (ต้องแตะ schema = ใบงานถัดไป):**
- **ใบกำกับภาษีไม่ snapshot ที่อยู่ผู้ซื้อ** — `Invoice`/`Quotation`/`BillingNote` ไม่มีฟิลด์ที่อยู่เลย ดึงสดจากลูกค้าตอนกดพิมพ์ → แก้ที่อยู่ลูกค้าวันนี้ พิมพ์ใบเก่าซ้ำได้ที่อยู่ใหม่ ไม่ตรงต้นฉบับที่ส่งลูกค้า/ยื่นสรรพากรไปแล้ว
- `order.duplicate` ไม่ก๊อป `shipping*` ทั้ง 7 ช่อง (งานซ้ำลูกค้าเดิมต้องกรอกที่อยู่ใหม่ทุกครั้ง — ไม่มีคอมเมนต์บอกว่าตั้งใจ)
- `delivery.update` ไม่มี audit log และไม่มีด่านสถานะ — แก้ที่อยู่ใบที่ส่งออกไปแล้วได้ ไม่มีร่องรอย
- server ไม่ validate/normalize ที่อยู่เลยสักบรรทัด (ไม่ trim ไม่จำกัดความยาว ไม่ตรวจไปรษณีย์ 5 หลัก) — กติกาทั้งหมดอยู่ฝั่งจอ ยิงผ่าน API ตรงข้ามได้หมด

**ของที่มีอยู่แล้ววันนี้ (สำรวจแล้ว ไม่ต้องไปหาใหม่):**
- `Customer.address` — ที่อยู่ของลูกค้า **แต่ป้ายบนฟอร์มเขียนผิดว่า "ที่อยู่ (จัดส่ง)"** (`customer-form` dialog) → ต้นตอความสับสนทั้งเรื่อง
- `Customer.billingAddress/billingSubDistrict/billingDistrict/billingProvince/billingPostalCode` — ที่อยู่ออกบิล 5 ช่อง (มีจริง แต่หน้าออเดอร์เพิ่งเริ่มโชว์รอบ 2026-08-11)
- `Order.shippingRecipientName/shippingPhone/shippingAddress/shippingSubDistrict/shippingDistrict/shippingProvince/shippingPostalCode` — ที่อยู่ส่งของ **แยกต่อใบอยู่แล้ว** (7 ช่อง)
- หน้าเปิดงานมี effect prefill ที่อยู่จากลูกค้าตอนเลือกลูกค้า (`orders/new/page.tsx` ~323-338) แต่ไม่มีปุ่มก๊อปย้อนหลัง
- แท็บภาพรวมของหน้าออเดอร์แสดง "ที่อยู่ลูกค้า (ก๊อปมาใช้ได้)" เป็นข้อความอ่านอย่างเดียวแล้ว — **ยังไม่มีปุ่มก๊อปจริง**

**ทางที่เบสไม่เลือก (บันทึกไว้เผื่ออนาคต):** (ข) ตาราง `CustomerAddress` หลายที่อยู่ต่อลูกค้า + เลือกตอนเปิดงาน — ถูกจริงระยะยาวเมื่อมีลูกค้าที่ส่งหลายจุดประจำ แต่กระทบ migration + หน้าลูกค้า + ใบกำกับภาษี

### 🎯 UX follow-up — ฟอร์มออเดอร์ตัวเดียวใช้ทั้งสร้างและแก้ + หน้าสร้างเป็นแท็บ (เบสสั่ง 2026-08-11 “เอาเลย”)
> เบส: “หน้าสร้างออเดอร์ ปรับเป็น tab ไปเลย ไม่ต้องเรียงลงมายาวๆ ละ · หน้ารายละเอียดกดแก้ไข ก็ใช้ฟอร์มเดียวกับสร้างออเดอร์เลย Ui จะได้เหมือนกัน”
> mockup ที่เคาะ: https://claude.ai/code/artifact/106f7ead-c498-4be2-a50a-3c23896212eb
> **ทักไว้แล้ว:** โครง 4 ช่วงของหน้าสร้างเบสเคาะเอง 2026-08-03 (ข้อ 186) — รอบนี้เปลี่ยน**แค่กลไกนำทาง** ไม่ย้ายของข้ามช่วง ถอยได้ด้วย revert commit เดียว

- [x] **ก้อน 1 · แท็บบนหน้าสร้าง** ✅ 2026-08-12 — `StepRail` → `Tabs` 4 ใบ (ช่วงเดิมเป๊ะ) · `src/lib/order-form-tabs.ts` (pure + เทส 13 ข้อ) · `validateForm()` คืน `{tab, message}[]` โดย**ติดป้ายที่บรรทัด push เดิม ไม่เพิ่มเงื่อนไขใหม่** · จุดเขียว (มีข้อมูล) / จุดแดง (มีที่ต้องแก้ — โผล่หลังกดบันทึกครั้งแรกเท่านั้น) · **แดงชนะเขียว** · กดบันทึกไม่ผ่าน = เด้งไปแท็บแรกที่ติด + ทุกข้อใน error box มีปุ่ม “ไปแก้ที่แท็บ…” · แท็บจำใน URL (`?tab=`) ด้วย history API ไม่ทับ `?next=quote`/`?customerId=`
- [x] **ก้อน 2 · ยกฟอร์มเป็นชิ้นกลาง** ✅ 2026-08-12 (`28396f5`) — `useOrderHeaderForm()` คุมหัวฟอร์ม 13 ช่องที่ 2 หน้าถือเหมือนกันแต่คนละรูป (หน้าเปิดงาน = useState กระจาย · กล่องแก้ = object ก้อนเดียว) · **ไม่ยุบรายการ/ค่าใช้จ่าย/ที่อยู่เข้ามาด้วย** — 3 ก้อนนั้นมี hook ของตัวเองและกติกาการบันทึกคนละแบบ · เจอ `isMarketplaceChannel` เขียนซ้ำ 3 ที่ ทั้งที่ `lib/order-status` มีอยู่แล้ว → รวมเป็นตัวเดียว
- [x] **ก้อน 3 · โหมดแก้ “ข้อมูล + ที่อยู่”** ✅ 2026-08-12 (`923e75e`) — กล่องแก้ใช้ `OrderDetailFields` + `OrderShippingSection` ตัวเดียวกับหน้าเปิดงาน (เดิมเขียนช่องเอง เรียงคนละลำดับ ป้ายคนละคำ) · ช่องทางเป็นช่องเทาพร้อมเหตุผล ห้ามซ่อน · เลขออเดอร์มาร์เก็ตเพลสย้ายเข้าช่องข้อมูลงาน บล็อก "Marketplace" ที่ซ้ำถูกยุบ · ที่อยู่โหมดแก้ไม่มีสวิตช์ (`alwaysOn`) — ลบที่อยู่ด้วยการล้างช่องได้แล้วตั้งแต่ input เป็น nullable · ช่องภาษี % มีอยู่แล้วในกล่องแก้ (ที่ถอดไปคือหน้าเปิดงาน — ตั้งใจ ไม่ต้องคืน)
  - ⚠️ **ยังเหลือจากใบงานเดิม:** ปิดร่าง localStorage + effect อัตโนมัติ — ยังไม่ต้องทำ เพราะกล่องแก้ยังถือ state ของตัวเอง (ไม่ได้ใช้ `useOrderItemsForm` ที่มีร่าง) · จะจำเป็นตอนก้อน 4 ดึงรายการเข้าฟอร์มร่วม
- [x] **ก้อน 4 · ดึงรายการ/ค่าใช้จ่าย/ส่วนลด เข้าฟอร์มร่วม** + บันทึกเฉพาะส่วนที่แก้จริง (`discount` เป็นของฝั่งรายการเท่านั้น ห้ามส่งไปกับ `order.update`) ✅ 2026-08-14 — ปิดด้วย shared form + atomic `order.saveForm`
- [x] **ก้อน 5 · ใบแก้ไขออเดอร์ + สถานะที่แก้ไม่ได้** — `DESIGN_APPROVED` ปุ่มเป็น “บันทึก + ออกใบแก้ไข” บังคับเหตุผล · เลยเข้าคิวผลิตแล้วรายการอ่านอย่างเดียว **ห้ามซ่อนแท็บ** (ซ่อนแล้วคนคิดว่าข้อมูลหาย) ✅ 2026-08-14
- [ ] **ก้อน 6 · เก็บกวาด** — caller/mount ถูกถอดและ guard ห้ามกลับมาแล้ว; ไฟล์ `OrderInfoEditDialog`/`OrderItemsEditor` เดิมยังเก็บไว้จนได้รับอนุญาตให้ลบไฟล์
- **สถานะหนี้ข้อมูลเดิม:** (1) shared `order.saveForm` รับ nullable รายช่องแล้ว จึงล้างข้อความ/ที่อยู่จากหน้าใหม่ได้โดยไม่ค้างค่าเก่า; public `order.update` เดิมยังเป็น compatibility contract แยก (2) หน้าใหม่รักษา `OrderItemProduct.id` เดิมและห้ามลบ/เปลี่ยน identity ที่มีใบตรวจรับแล้วโดยไม่แก้ schema; mutation legacy `updateItems`/`applyChangeOrder` ยังเป็นหนี้แยกจน caller เดิมหมดทั้งหมด

### 🎯 UX follow-up — ปิดบั๊ก responsive + สถานะข้อมูลจาก audit ทั้งเว็บ (เบสสั่ง 2026-08-02 “ทำเลย”)
> ขอบเขต surgical: แก้เฉพาะบั๊กใช้งานจริงที่ยืนยันจาก browser แล้ว · คงชุดสี/ความ minimal/โครงหน้าปัจจุบัน · ไม่มี schema หรือ dependency ใหม่

- [x] **UI-P0.1 แท็บเล็ตไม่บีบหัวข้อและตาราง** — `PageHeader` ต้องยอมตัด action ลงแถวใหม่เมื่อพื้นที่ไม่พอ · `/billing/tax`, `/billing/wht`, `/production/films` ใช้ mobile card ถึง breakpoint ที่มีพื้นที่พอจริง ✅ 2026-08-02
- [x] **UI-P0.2 Product Picker มือถือเลือกตัวแปรได้จริง** — รายการสินค้าเดิมคงไว้ · เมื่อขยายสี/ไซซ์บนจอเล็กต้องเป็นแถว/การ์ดอ่านง่าย ไม่มีหัวตารางหรือราคา/สต๊อก/จำนวนทับกัน · desktop คงตารางเดิม ✅ 2026-08-02
- [x] **UI-P0.3 ไม่ใช้ empty state กลบ loading/error** — notification dropdown, ไฟล์แนบออเดอร์ และสถานะเครดิตลูกค้า แยก “กำลังโหลด / โหลดไม่สำเร็จ / ไม่มีข้อมูล” ชัดเจน พร้อม retry ในจุดที่เหมาะสม ✅ 2026-08-02
- **ปิดงาน 2026-08-02:** typecheck ผ่าน · lint 0 error (39 warning เดิม) · unit 589/589 · verify:ui ผ่าน · browser จริง 320/390/800/1440px ไม่มี horizontal scroll/error overlay/console error · ปิด dev server จำลอง API ล้มเหลวแล้วยืนยัน notification แสดง error+retry และกู้ข้อมูลกลับได้

### 🎯 UX follow-up — ลำดับชั้นข้อมูลหน้าออเดอร์ (เบสเคาะจากภาพจริง 2026-08-02)
> ขอบเขต surgical: ปรับเฉพาะ `/orders` และ metadata สถานะกลาง · คงสี ระยะ และโครงตารางเดิม · ไม่แตะ query/business logic

- [x] แยก `พักงาน`/`ยกเลิก` ออกจากเส้นทางงานหลัก แต่คงปุ่มกรองและจำนวนครบ ✅ 2026-08-02
- [x] ลดเสียงรบกวนของสถานะเลขศูนย์ + ใช้ชื่อย่อที่อ่านครบในแถบ โดยชื่อเต็มยังอยู่ใน accessible label/tooltip ✅ 2026-08-02
- [x] เปลี่ยนคอลัมน์นับถอยหลังเป็น `เหลือเวลา` และซ่อนเมื่อหน้านั้นไม่มีวันส่งที่ต้องติดตาม (เว้นกรณีกำลังเรียงตามกำหนดส่ง) ✅ 2026-08-02
- [x] ภาษาไทยสม่ำเสมอ (`ส่งออก CSV`, `สั่งทำ`) + แสดงจำนวนผลลัพธ์ใกล้ตัวกรอง โดยแยก label UI ไม่เปลี่ยนสัญญา MCP ✅ 2026-08-02
- [x] กลุ่มเรียง/วันที่/ตัวกรองที่ 320px ต้องห่อบรรทัด ไม่ตัดปุ่มทิ้งแม้หน้าไม่มี horizontal scroll ✅ 2026-08-02
- [x] verify: typecheck · lint · unit/verify:ui · browser จริง desktop+mobile และกดกรองสถานะข้อยกเว้นได้ ✅ 2026-08-02
- **ปิดงาน 2026-08-02:** typecheck ผ่าน · lint 0 error (38 warning เดิมทั้ง repo) · unit 599/599 · `verify:ui` ผ่าน · browser จริง 320/1280px ไม่มี horizontal scroll/console error · กรองยกเลิก 21 งาน/พักงาน 0 งาน/ล้างตัวกรองกลับ 77 งานถูกต้อง · ไม่รัน build ขณะ dev server ทำงาน

### 🎯 UX follow-up — ธีมสว่างแบบ A บนพื้นขาว (เบสเคาะจากตัวอย่าง 2026-08-02)
> ขอบเขต design-token only: พื้นเนื้อหาและการ์ดขาวตามแบบ A · กรอบเว็บ/พื้นจมใช้เทากลางคนละระดับเพื่อแยกโซน · ธีมมืดและเอกสารพิมพ์ต้องไม่เปลี่ยน

- [x] แยกพื้นหน้า/การ์ด/กล่องย่อย/กรอบเว็บให้เป็น 4 ชั้น โดยพื้นหน้าและการ์ดคงขาวตามแบบ A แต่ใช้เทากลางกับกรอบเว็บ พื้นจม และเมนูที่เลือก ✅ 2026-08-02
- [x] คงสีแบรนด์และสีสถานะเดิม · ไม่ไล่ทาสีรายหน้า · overlay/dialog ยังต้องลอยและอ่านง่ายบนทั้งสองธีม ✅ 2026-08-02
- [x] verify: typecheck · lint · `verify:ui` · browser จริง light/dark บน dashboard/orders/orders-new/settings ที่ desktop+mobile โดยไม่มี console error หรือ horizontal scroll ✅ 2026-08-02
- **ปิดงาน 2026-08-02:** พื้นหน้า/การ์ด `#fff` · กรอบเว็บ `#f3f4f6` · พื้นจม `#eceff2` · เมนูที่เลือกเข้มขึ้นหนึ่งขั้น · dark token และกฎงานพิมพ์ไม่เปลี่ยน · typecheck ผ่าน · lint 0 error (38 warning เดิมทั้ง repo; layout ที่แก้ตรวจซ้ำ 0) · `verify:ui` ผ่าน · browser จริง 1280/390px ทั้ง light/dark บน 4 หน้าครบ ไม่มี horizontal scroll/console error · ไม่รัน build ขณะ dev server ทำงาน

### 🎯 UX follow-up — หัวตารางสีเดียวกับกรอบตาราง (เบสเคาะจากภาพจริง 2026-08-03)
> ขอบเขต surgical ที่ `DataTable` กลาง: โหมดสว่างให้หัวตารางใช้พื้น `surface` เดียวกับกรอบตาราง · คงเส้นคั่น สีตัวอักษร สีแถว และธีมมืดเดิม

- [x] เปลี่ยนหัวตารางมาตรฐานจากพื้นเทาเป็นพื้นเดียวกับ `card-surface` โดยไม่ไล่แก้รายหน้า ✅ 2026-08-03
- [x] verify: typecheck · lint · `verify:ui` · browser จริง `/orders` และตารางมาตรฐานหน้าอื่นใน light/dark โดยไม่มี console error หรือ horizontal scroll ✅ 2026-08-03
- **ปิดงาน 2026-08-03:** `DataTable.Head` ใช้ `bg-surface` ใน light ทำให้หัวกับกรอบเป็น `#fff` เท่ากันทั้ง `/orders`, `/customers`, `/billing` · คงเส้นคั่นและ `dark:bg-white/[0.03]` เดิม · typecheck/lint/`verify:ui` ผ่าน · browser จริง 1280px ทั้ง light/dark ไม่มี error overlay, console error หรือ horizontal scroll · ไม่แตะตาราง custom/เอกสารพิมพ์

### 🎯 UX follow-up — ขยายสีหัวตารางให้ครบทุกตารางบนเว็บ (เบสสั่งต่อ 2026-08-03)
> ขอบเขต: ทุกตารางที่แสดงเป็น UI ให้หัวใช้สีเดียวกับกล่องในโหมดสว่าง · คง dark override ของแต่ละบริบท · เอกสารพิมพ์คงเดิม

- [x] audit `<thead>` ทุกจุดและเก็บหัวตาราง custom ที่ยังมีพื้นเทา โดยไม่ทาสีซ้ำจุดที่โปร่งเห็นพื้นกล่องอยู่แล้ว ✅ 2026-08-03
- [x] verify: ไม่มีหัวตาราง UI เหลือ `bg-slate-50/100` ใน light · typecheck · lint · `verify:ui` · browser จริงหน้าตาราง custom ทั้ง light/dark ✅ 2026-08-03
- **ปิดงาน 2026-08-03:** ตรวจ `<thead>` 21 จุดครบ — UI 17 จุด + เอกสารพิมพ์ 4 จุด · แก้ 4 จุดที่หลุดจริง (ตารางสินค้า/ลาย/ส่วนเสริมใน `/orders/new` และแมปหมวดหมู่ `/settings/stock`) · รวมสูตรหัวบน surface เป็น `TABLE_HEAD_SURFACE` และเพิ่ม assertion ใน `verify:ui` · 12 หัว custom ที่โปร่งเห็นพื้นกล่องตรงอยู่แล้วคงไว้ · typecheck ผ่าน · lint 0 error (38 warning เดิม) · browser จริง light/dark ไม่มี overflow, error overlay หรือ console error

### 🎯 UX follow-up — พื้นหน้าเทา + Navbar/Sidebar ขาว (เบสสั่ง 2026-08-03)
> ขอบเขต design-token/layout only: สลับลำดับชั้นเฉพาะธีมสว่าง · การ์ด/ตารางคงขาว · ธีมมืดและเอกสารพิมพ์คงเดิม

- [x] พื้นหน้าสว่างใช้เทา `#f3f4f6` · Navbar/Sidebar/เมนูมือถือใช้ขาว `#fff` แบบทึบ · คง surface ขาวและ muted surface เดิม ✅ 2026-08-03
- [x] verify: typecheck · lint · `verify:ui` · browser จริง dashboard/orders/orders-new/settings ที่ desktop และเมนูมือถือ รวม light/dark โดยไม่มี console error หรือ horizontal overflow ✅ 2026-08-03
- **ปิดงาน 2026-08-03:** สลับ token กลางเฉพาะ light เป็นพื้นหน้า `#f3f4f6` และ chrome/surface `#fff` · Topbar/Sidebar desktop ขาวทึบ ส่วน dark คง `chrome/90` + blur เดิม · browser จริง light บน dashboard/orders/orders-new/settings และ drawer มือถือ 390px สีตรงทุกชั้น · dark dashboard คง token เดิม · ไม่มี horizontal overflow, runtime/tRPC error หรือ console warning/error · typecheck ผ่าน · lint 0 error (38 warning เดิม) · `verify:ui` ผ่าน · ไม่แตะกฎงานพิมพ์

### 🐛 Regression follow-up — Turbopack โหลด client/server คนละรุ่น (เบสพบ 2026-08-03)
> ขอบเขต surgical: ตามเส้นทาง `RootLayout → Providers → DashboardLayout → Sidebar/Topbar` และแยก source bug ออกจาก Fast Refresh/client state · ไม่แตะ query, auth หรือ business logic

- [x] ตาม render path และ bundle graph ครบ: `RootLayout → Providers → DashboardLayout → Sidebar` ถูกต้อง · provider ครอบทั้ง Suspense หลัก/fallback · React/tRPC/React Query มีอย่างละชุด · Provider/Sidebar import `src/lib/trpc.ts` module เดียวกัน ✅ 2026-08-03
- [x] ทำ differential ได้: origin เดิม `localhost:3000` hydrate client code เก่าคนละรุ่นกับ server (ข้อความ/class/breakpoint ก่อนหลาย commit) ขณะที่ origin ใหม่ `localhost:31873` จาก source และ `.next` ชุดเดียวกันผ่าน dashboard/orders/settings โดยไม่มี tRPC/runtime error ✅ 2026-08-03
- [x] ย้าย `.next` เก่าออกและ cold restart แล้ว · ไม่เติม provider ซ้อน/global singleton เพราะสมมติฐานนั้นถูกหักล้างและจะกลบต้นเหตุ ✅ 2026-08-03
- [x] reproduce hydration class mismatch หลังเปลี่ยนสี shell ได้บน origin 3000 · source + SSR/client chunk ใช้ class รุ่นใหม่ตรงกัน และ cold origin 31879 จาก source เดียวกัน hydrate ผ่านโดยไม่มี console error จึงยืนยันว่าไม่ใช่ class/theme logic ✅ 2026-08-03
- [ ] ปิดแท็บ `localhost:3000` ของผู้ใช้ทั้งหมดแล้วล้าง cache/site data ของ origin หนึ่งครั้ง จากนั้น verify หน้าเดิมอีกครั้ง · ถ้ายังเกิดค่อยขออนุญาตถอด `--turbopack` จาก dev script (config change)
- **สถานะ 2026-08-03:** source code ไม่พังและ production path ไม่ได้รับผล · ปัญหาอยู่ที่ client state/Fast Refresh เฉพาะ dev origin 3000 · server 3000 ถูกเปิดใหม่แล้ว แต่แท็บจริงต้องทิ้ง client chunks เก่าให้หมดก่อน

### 🎯 UX Standardize — refactor UI ให้มาตรฐานเดียวทั้งระบบ (เบสเคาะ "ทำเลยทั้งหมด" 2026-08-03 · จาก audit 13 ทีม 59 findings ผ่าน adversarial verify)
> ขอบเขต: ยกระดับ "มาตรฐานที่มีอยู่แล้วให้ไปถึงทั้งระบบ" — ไม่ redesign ภาพ ไม่แตะ business logic/query/permission · ทุกชิ้นเป็น commit เล็กแยกกัน ห้าม big-bang · รายละเอียดหลักฐานเต็ม: audit output 2026-08-03 (จะสรุปย่อยลงใบงานตอนทำแต่ละชิ้น)

- [x] **ชุด 0** — เพิ่มขนาด dense ใน `control-size.ts` (เป็น `size="dense"` ของ Input/Select) + กวาดสูตรก๊อป 15 จุด + Select ในแถวเดียวกันที่เตี้ยกว่า 4px เข้าแถว ✅ 2026-08-03
- [x] **ชุด 1 บั๊กจริง** — (a) `lib/shipping-methods.ts` + `*_LABELS_CUSTOMER` ใน status-config (บั๊กใบแนบกล่องพิมพ์ OTHER + สี/คำหน้า status ลูกค้า drift หายแล้ว · ฟอร์มใบส่งได้ตัวเลือก Shopee/Lazada ที่หายไป) (b) จอโกหก 4 component แยก โหลด/พัง/ว่าง ครบ (c) 5 หน้า public เข้า `(public)` + noindex ทุกหน้า + PublicPageShell/FullScreenLoading/InfoRow + providers อ่าน prefix จาก `lib/public-routes.ts` (d) formatter กลางปัก Asia/Bangkok + `formatDateShort/formatTime/formatBaht` แทนสูตรเขียนสด ✅ 2026-08-03
- [x] **ชุด 2 ชั้นที่หายไป** — (a) `PageShell` + `AccessDenied` migrate 23 หน้า (รวม home/my-tasks/analytics/print-runs/notifications) ทุก state มี header · description ที่ drift สองเวอร์ชันถูกยุบ (b) `useListPageState` + `usePageClamp` 10 หน้า — products ได้ debounce + filter ใน URL · films เลิก hack window.location (c) `DialogSubmitFooter` + กติกา conditional-mount ใน dialog.tsx + migrate footer 21 dialog (เหลือ 1 จุด 2-action โดยตั้งใจ) (d) `text-strong/secondary/muted` + กวาด 154 จุดใน 48 ไฟล์ + ด่านใหม่ใน `verify:ui` ✅ 2026-08-03
- [x] **ชุด 3 กวาดเข้ามาตรฐาน** — (a+b) ตารางดิบ 7 จุด → DataTable · tax/wht/films → ResponsiveList · thead custom ที่เหลือใช้ TABLE_HEAD_SURFACE (c) มาตรฐาน validation เขียนเป็นกติกา build ข้อ 9 + quotations/new + billing/notes เข้า Field (d) `order-billing-section` 1,212→446 บรรทัด (dialog เงิน 4 ตัวแยกไฟล์ conditional mount — ปิดคลาสบั๊ก reset มือค่าค้าง) + `order-delivery-section` 856→409 (3 dialog) ✅ 2026-08-03
- [x] **ชุด 4 เก็บตก** — Checkbox (แทนดิบครบ) · NumberInput/MoneyInput (primitive พร้อม — จุดใช้เดิมทยอยแทนตอนแตะไฟล์ตามกติกา 7) · ListCards/ListCardItem/MetaGrid + customers เป็นตัวอย่าง · CustomerFormFields (validation สร้าง=แก้ · server error แสดงที่เดียว) · notifications ใช้ TablePagination (products คงเลขหน้า custom — logic อยู่บน URL state แล้ว) · DESIGN.md ตรงของจริง ✅ 2026-08-03
- [x] verify: typecheck 0 error · lint 0 error · unit 599/599 · `verify:ui` ผ่านครบรวมด่านธีมมืดใหม่ · smoke ทุก route ไม่มี 500/error marker · `next build` production ผ่านทุก route ✅ 2026-08-03
- [x] ตรวจตาบน browser จริง (production build · origin 31902 · session จริงของเบส) — dashboard/orders/orders-new/orders-[id]/customers/settings-services/billing-tax/notifications ครบทั้ง dark และ light · **dialog สร้างบิลที่แตกใหม่เปิดจริง: prefill จาก billing.suggest มาครบ (ชนิดบิล/ยอด/ภาษี/ยอดรวม)** · มือถือ 390px ไม่มี horizontal overflow + ListCard แสดงถูก · หน้า /status token ผิด → PublicLinkError บนธีมสว่างบังคับ · console 0 error ตลอดรอบ ✅ 2026-08-03 — จุดหน้าตาเปลี่ยนโดยตั้งใจให้เบสดูผ่านตา: หัวตาราง settings เลิก UPPERCASE · AccessDenied เดียวทั้งระบบ · quotations/new เป็น max-w-5xl · สี "ชำระบางส่วน" หน้าลูกค้าเป็นฟ้า · spinner แทน text-swap ใน dialog

### Quick wins คั่นระหว่าง Gate (ต่อปุ่มให้ backend ที่มีอยู่ — ชิ้นละ ≤ ครึ่งวัน)
~~ปุ่ม "ดึงกลับเป็นร่าง" ใบเสนอ SENT (ทำใน A3)~~ · ~~ปุ่มร่างทวงหนี้บนหน้า aging~~ ✅ 2026-07-03 (tRPC billingNote.dunningDraft + dialog สลับโทน+คัดลอก) · ~~ปุ่ม UI recordRefund~~ ✅ 2026-07-03 (dialog บนการ์ดบิล) · ~~แก้เลข "ค้างชำระ" /billing ให้สูตรเดียวกับ aging~~ ✅ 2026-07-03 (Σ outstandingOf) · **เหลือ**: ตารางบิลกดได้+filter+pagination · เมนู "งานออกแบบ" เลิกชี้หน้า stub · จับ isError 17 หน้าที่เงียบ (ขัด DESIGN.md เอง)

### Refactor targeted (ทำตอนแตะไฟล์นั้นตามกติกา 7 — ห้าม big-bang)
~~ลบ dead export สูตรเงินเก่าไม่มี VAT (lib/pricing.ts)~~ ✅ 2026-07-03 · ~~FINANCE_ROLES ฝั่ง client (6 หน้า) → import lib/roles.ts~~ ✅ 2026-07-03 (+MONEY_RECORDER_ROLES) · ~~recordPayment/void ลง service+test~~ ✅ 2026-07-05 · ~~INVOICE_TYPE_LABELS~~ ✅ 2026-07-05 · ~~FINANCE_ROLES server~~ ✅ PERM3 · ~~test กลุ่ม stock/ผลิต~~ ✅ 2026-07-06 · ~~ด่านปิดงานวางบิลครบ + convertToOrder → services/quotation-convert.ts + payment-plan.ts~~ ✅ 2026-07-07 (เทส 25) · ~~production.updateStep → services/production-step-plan.ts~~ ✅ 2026-07-07 (เทส 46) · ~~schema index FK+hot paths 25 ตัว~~ ✅ 2026-07-07 (migration 20260707184420 applied แล้ว) — **กลุ่ม refactor targeted เคลียร์หมดแล้ว** · เพิ่มใหม่ 2026-07-07 (perf): task.myToday/revenueByMonth ขนาน + sweep หลุด critical path · จดค้าง: Supabase JWT keys ยัง HS256 — migrate เป็น asymmetric แล้วเปลี่ยน getServerSession → getClaims() ตัด 1 HTTP/request · staleTime 5s→30-60s รอวัดจริง

---

## 🔴 P0 — ฐานราก (ทำก่อนทุกอย่าง · ห้าม deploy/ใช้จริงจนจบ P0)

### P0.1 Auth จริง + RBAC
- [x] แก้ `src/server/trpc.ts:26-28` — lookup user ด้วย `supabaseId` (ตอนนี้ใช้ `id` = ไม่มีวัน match)
- [x] **ตัด dev-OWNER fallback ทิ้งทั้งหมด** (`getDevUserId`, fallback ใน `createContext`/`isAuthed`/`requireRole` — trpc.ts:13-20,38-39,53,58,68,75) → ไม่มี session = UNAUTHORIZED
- [x] login จริง (`src/app/(auth)/login/page.tsx` ตอนนี้เป็น TODO redirect) — Supabase signInWithPassword + signOut (user-menu logout ตอนนี้เป็นปุ่มหลอก)
- [x] สร้าง `src/proxy.ts` กัน route (dashboard)/(portal) + เช็ค session ใน layout
- [x] หน้า invite/จัดการ user (6 คน) ผูก supabaseId ↔ User + Role
- [x] ไล่ `requireRole` ให้ครบทุก mutation (✅ 97/97 — audit เต็ม 2026-07-15: requirePermission/requireRole 88 + inline role check + per-user scope + token flow · ตัวสุดท้าย attachment.create ปิดด่าน INVOICE แล้ว + test 5 เคส) ตามตาราง role ใน `Anajak-Print-Features.md` §7
- [x] public procedures (design.getByToken/approveByToken) — เพิ่ม token expiry + กันตัดสินซ้ำฝั่ง server

### P0.2 เงินถูกต้อง
- [x] Float → `Decimal` ทุก field เงินใน `prisma/schema.prisma` (54 จุด) + ปรับโค้ดคำนวณ (DB ว่าง — push ใหม่ได้เลย)
- [x] แก้บั๊ก platformFee: `src/server/routers/order.ts:394` (create ไม่รวม) vs `:728,:793` (update รวม) vs UI `orders/new/page.tsx:196` — เลือกสูตรเดียว ใช้ทุกที่
- [x] `billing.recordPayment` (billing.ts:118-179) — รวม 4 writes เป็น `$transaction` เดียว (รวม voidInvoice/recordRefund ด้วย)
- [x] **เลขเอกสารรันต่อเนื่อง** — สร้าง model `DocumentSequence` (ต่อชนิดเอกสาร/เดือน, กันชนใน transaction) แทนสุ่ม 4 หลักใน `src/lib/utils.ts:39-87` — บังคับตามกฎหมายใบกำกับภาษี
- [x] enforce status transition ที่ชั้น server กลาง — ปิดทาง `production.create` (production.ts:51-54) และ `design.upload/processDesignApproval` เขียน internalStatus ตรงข้าม `isValidTransition`
- [x] status guard ของ `order.updateFees`/`order.update` (ตอนนี้แก้เงินได้แม้ COMPLETED/CANCELLED)

### P0.3 วินัยฐานข้อมูล
- [ ] เริ่ม `prisma migrate` + baseline (ตอนนี้ migrations ว่าง ใช้ db push อย่างเดียว = ย้อนกลับไม่ได้)
- [ ] แก้ `prisma/seed.ts` ให้ตรง schema ปัจจุบัน (ตอนนี้พังจริง: Notification.channel/body/sentAt ไม่มีแล้ว · OrderItem fields ย้ายไป OrderItemProduct แล้ว) + แยก master data จริง (ServiceCatalog 26 รายการ) ออกจาก demo data
- [ ] ใส่ index ที่ hot paths: Order.customerId/internalStatus/createdAt, Invoice.paymentStatus, ProductionStep.assignedToId

### P0.4 ออกแบบเผื่ออนาคต (เบาๆ ไม่ over)
- [ ] tax-point rule ลง design: order line จำแนก ขายสินค้า/จ้างทำของ + ใบกำกับออก**ทุกงวดรับเงินรวมมัดจำ** (โครงรอ P1 build แต่ schema ต้องเผื่อตั้งแต่ตอนแตะ Decimal)
- [ ] แยก business logic แกน (pricing/status/เลขเอกสาร) เป็น function ใน `src/server/services/` ที่ tRPC เรียก — เผื่อ order-intake API + MCP (P4) เรียกซ้ำได้

### P0.5 จัดระเบียบโค้ด (เบสสั่งเพิ่ม 2026-06-10 — targeted refactor)
- [ ] **test แกนก่อน refactor** — vitest + test ครอบ: pricing ทุกเส้นทาง (รวม platformFee) · status transition · เลขเอกสาร · payment/void/refund (ตอนนี้ 0 tests — นี่คือเกราะของทุก refactor หลังจากนี้)
- [ ] ลบ dead code ที่ survey ชี้: `size-matrix.tsx` (กลับมาใช้จริงใน P1.12 — ถ้า rewrite ง่ายกว่าก็ลบ), `useOrderDraft` (hooks ไม่ถูกใช้), BrandProfile/rfmScore (คง schema แต่ mark รอ P3), `/quotations/[id]/edit` dead link, empty dirs `api/sync/*` + `products/new`, `(portal)` ว่าง (จะถูกใช้จริง P3)
- [ ] แก้ payment-method mismatch (`order-billing-section.tsx` ส่ง TRANSFER/PROMPTPAY แต่ labels ใช้ BANK_TRANSFER/QR_CODE) — รวม enum/labels ไว้ที่เดียว
- [ ] แตกไฟล์ยักษ์ที่กำลังจะแตะอยู่แล้ว: `orders/new/page.tsx` (875 บรรทัด) แยกเป็น component/hook ตามหน้าที่ — ทำตอน P1.12 แตะ multi-size อยู่แล้ว ไม่แตกล่วงหน้า
- [ ] ตั้ง lint/format ให้เข้ม + รันผ่านทั้ง repo (eslint config มีแล้ว — เพิ่ม rule กัน pattern ที่เจอ: silent catch, window.prompt/confirm)
- [ ] เอกสาร `docs/ARCHITECTURE.md` สั้นๆ: โครง router/service/lib ใครทำอะไร — กัน AI session ใหม่วางของผิดที่

**เกณฑ์จบ P0:** คนนอกเปิดเว็บแล้วเข้าไม่ได้ · ทีม 6 คน login จริงได้ตาม role · ยอดเงินทดสอบตรงทุกเส้นทาง (รวม platformFee) · มี migration history · seed รันผ่าน

---

## P1 — เอกสาร + การเงินไทย + งานรายวัน (สรุป — รายละเอียด: plan.md ใน bestos)
**P1.0 Design System + UI มาตรฐานใหม่ (เบสสั่ง 2026-06-10 — ทำเป็นงานแรกของ P1):** design tokens (สี/ฟอนต์/spacing/radius — ฐาน Tailwind 4 + shadcn ที่มีอยู่) · component มาตรฐาน (table/form/dialog/status badge/empty state) · เลิก `window.prompt/confirm` ทั้งระบบ → dialog จริง · **mobile-first สำหรับหน้า ops** (task queue/production — พนักงานใช้มือถือหน้างาน) · หน้าใหม่+หน้าที่แตะใน P1-P3 ใช้มาตรฐานนี้ทันที · ปิดท้าย P1 มีรอบเก็บตกหน้าเก่าที่เหลือ — เกณฑ์: ดูเป็นระบบเดียวกันทุกหน้า ใช้บนมือถือได้จริง ·
PDF ครบชุด (ใบเสนอ/แจ้งหนี้/เสร็จ+**ใบกำกับเต็มรูป ม.86/4** · ยกเลิก-ออกใหม่ห้ามลบ · ใบลดหนี้-เพิ่มหนี้) · **Job Ticket ใบสั่งงานหน้างาน+QR** · WHT ขารับ (ทะเบียน 50ทวิ + reconcile 97/3) · มัดจำตาม payment terms + overdue cron · **ใบวางบิล + ลูกหนี้ aging + เช็ค credit limit** · approval gate ส่วนลด/void · **ปฏิทินภาระงาน+เช็คทันไหม (เบา)** · **ราคาต่อลูกค้า + quote expiry + แก้ใบเสนอ** · **task queue "งานของฉันวันนี้"** · notification จริงจาก event · multi-size matrix · รายงานภาษีขาย export CSV/Excel
### P1.0-VISUAL — Visual Identity ทั้งระบบ (code-first · เบสอนุมัติ 2026-08-22)

- [x] เพิ่ม visual contract กลางให้ `PageHeader`/`PageShell` และ `Section` โดยหัวข้อหลักยังเป็นข้อความล้วนสำหรับ screen reader
- [x] เพิ่ม `EntityMark` (ภาพจริง → initials → icon) และ `ContextPanel` สำหรับบริบทคงที่ที่ไม่ใช่ alert
- [x] ใช้ object identity เฉพาะช่องตัวตนของออเดอร์ ลูกค้า เอกสาร สินค้า งานผลิต ร้านนอก และผู้ใช้; ไม่ใส่ icon ในตัวเลข วันที่ หรือ status ทุกช่อง
- [x] กระจาย visual hierarchy ครบ dashboard, Production/Station/Factory TV, public token 5 surface และ print A4 5 ชนิด โดยไม่เปลี่ยน query/mutation/permission/status/business flow
- [x] ลบศัพท์นักพัฒนาออกจาก UI ผู้ใช้ เช่นคำสั่ง seed และชื่อโหมดภายใน
- [x] เพิ่ม regression test/`verify:ui` และตรวจ real browser ตัวแทนทุก surface ตาม desktop/mobile/touch, public error, print A4 screen, overflow, hydration และ console; blind-ship/semantics/fallback ล็อกด้วย component test

### P1.0-COLOR-COPY — Industrial Fresh + Progressive Help (เบสอนุมัติ 2026-08-22)

- [x] คง Anajak Blue `#3973b2` เป็น primary/selected/focus และเพิ่ม module tone กลาง: Production teal, Product saffron, Finance violet, System graphite
- [x] เพิ่ม `VisualTone` ให้ visual primitives/navigation/public โดยสีสถานะและ print grayscale ไม่เปลี่ยนความหมาย
- [x] เพิ่ม `HelpTip` ที่ใช้ mouse/keyboard/touch ได้ และย้ายเฉพาะคำอธิบายคงที่ที่ไม่จำเป็นต่อการตัดสินใจเข้า progressive disclosure
- [x] ถอด `description` ออกจาก PageHeader/PageShell/Section; เก็บ factual metadata แบบสั้น และคง warning/error/validation/blocker/legal/action consequence ให้เห็นตรงหน้า
- [x] ตรวจ public token normal/success/expired/error/blind-ship บน demo local และ export PDF A4 จริงทั้ง 5 ชนิด
- [x] เพิ่ม component/`verify:ui` guard, full gates, browser Light/Dark/mobile และ Impeccable detector

### P1.0-DISTILL — ลดไอคอนหลังตรวจจอจริง (เบสสั่ง 2026-08-23)

- [x] หัวหน้ามาตรฐานและหัว custom ของ Station/Factory TV ใช้ไอคอนเส้นเรียบตามสีบริบท โดยไม่มีกรอบ พื้นสี หรือเงา
- [x] registry table/list ไม่วาด object icon หรือ initials ซ้ำกับข้อความ; action/status icon ที่มีความหมายยังคงได้
- [x] รายการออเดอร์ใช้รูปม็อกอัพจริงล่าสุดเท่านั้น; รอบแรกไม่มีรูปให้เว้นพื้นที่ล่องหน โดย feedback ล่าสุดแทนที่ด้วยช่องภาพว่างชุดเดียวกับคิวผลิตใน `P1.0-SIDEBAR-MOCKUP`
- [x] ล็อกด้วย component/`verify:ui`, ตรวจจอจริง desktop/mobile Light/Dark และรัน final gates โดยไม่เปลี่ยน business flow

### P1.0-PAGE-COPY — คืนคำอธิบายสั้นใต้หัวข้อ (เบสสั่ง 2026-08-23)

- [x] ทุก `PageHeader`/`PageShell` แสดงคำอธิบายสั้นหนึ่งประโยคใต้หัวข้อโดยอัตโนมัติ และเปิดให้หน้ารายละเอียด override ตามบริบทจริง
- [x] แยก `description` ออกจาก `meta`: description บอกว่าหน้านี้ใช้ทำอะไร ส่วน meta แสดงข้อเท็จจริงเฉพาะรายการ/สถานะที่เปลี่ยนได้
- [x] ย้าย page-level `HelpTip` ที่เป็นเพียงคำอธิบายหน้ามาแสดงตรง; คง tooltip เฉพาะสูตร กติกา หรือรายละเอียดเสริมที่ไม่ต้องเห็นตลอดเวลา
- [x] ล็อกความยาวและ fallback ด้วย unit/`verify:ui`, ตรวจ desktop/mobile Light/Dark และรัน final gates โดยไม่เปลี่ยน business flow

### P1.0-SIDEBAR-MOCKUP — รูปออเดอร์ตรงคิวผลิต + Sidebar ใหม่ (เบสสั่ง 2026-08-23)

- [x] รายการออเดอร์ใช้ `MockupThumbnail` ชุดเดียวกับคิวผลิตทั้งรูปจริงและช่องว่างขอบประ บน desktop/mobile โดยไม่สร้าง icon รูปแบบใหม่
- [x] feedback รอบถัดมาลด Sidebar เป็นโครงแบน 240px: ไม่มีพื้นครอบหมวด/marker สี เหลือชื่อหมวดเบา ๆ และ active สีน้ำเงินจุดเดียว โดยคงรายการ เมนู สิทธิ์ และ route เดิมทั้งหมด
- [x] คง mobile bottom navigation และ More sheet flow เดิม พร้อมตรวจ active route, keyboard/focus, overflow และ Light/Dark
- [x] เพิ่ม regression guard, อัปเดต SPEC/DESIGN/PROGRESS, รัน final gates + Impeccable detector และตรวจ browser จริงก่อน commit

### P1.0-FILTER-MINIMAL — ลดน้ำหนักปุ่มกรองทั้งระบบ (เบสสั่ง 2026-08-23)

- [x] feedback รอบถัดมาตัดหน้าตาปุ่มและสี selected ออกทั้งหมด: `FilterChip` เป็นข้อความ neutral + เส้นใต้ โดยคง icon, count, URL และพฤติกรรมเดิม
- [x] ใช้สถานะเลือกด้วยเส้นใต้/น้ำหนักข้อความร่วมกับ `aria-pressed`; ไม่มีพื้น กรอบกล่อง เงา radius หรือช่อง Check ล่องหน
- [x] ตรวจหน้าสินค้าเป็นตัวแทน desktop/mobile Light/Dark, รัน regression guard + Impeccable detector และไม่แตะ business flow

### P1.0-FILTER-POPOVER-POLISH — เก็บกล่องตัวกรองหลายเงื่อนไขทั้งระบบ (เบสสั่ง 2026-08-23)

> ปรับเฉพาะ presentation/interaction ของ `FilterPopover` กลางและ caller ที่ใช้จริง (`/orders`, `/production`) หลังจอจริงชี้ว่าตัวเลือกหลายรายการถูกวาดเหมือนแท็บจนสแกนยาก · คง query, URL, count, permission และ business flow เดิมทั้งหมด · แถบสถานะออเดอร์ที่เบสชอบไม่เปลี่ยน

- [x] **โครง overlay อ่านเป็นงานเดียว** — หัวกล่อง/ปุ่มปิด/กลุ่มฟิลด์/footer มี hierarchy และระยะเดียวกัน; เปิดแล้ว focus เข้า panel โดยไม่ทำวง focus ที่ปุ่มปิดเด่นขึ้นมาเอง
- [x] **ชนิด control ตรงกับจำนวนตัวเลือก** — ตัวเลือกเกิน 5 ใช้ `Select` ตาม contract กลาง; กลุ่มหลายเงื่อนไขมี label ชัดและไม่ใช้เส้นใต้แบบแท็บซ้อนใน popover
- [x] **สถานะกรองค้างและ action ชัด** — trigger แสดง active count ด้วย Anajak Blue, ล้างทั้งหมด disabled เมื่อไม่มีค่า และปุ่มดูรายการไม่แข่งน้ำหนักกัน
- [x] **ตรวจทั้งเว็บและจอจริง** — inventory filter surface ทั้งระบบ; เดินกรอง→เปิดซ้ำ→ล้าง, Escape/focus, overflow และ console บน `/orders` + `/production` ที่ desktop/mobile Light/Dark พร้อมรัน regression/final gates ก่อน commit

**เพิ่มเข้า P1 (เบสอนุมัติ 2026-06-11 จากผล audit flow ทั้งระบบ):** แพ็คเก็บตกหน้างาน — ขั้นตอนผลิต DTF/DTG จริง (เดิม enum เป็นชุดโรงเย็บ) + โชว์ลายอนุมัติบน order detail/Job Ticket + แจ้งกระดิ่งเมื่อลูกค้าตัดสินแบบ + ด่านปิดงานต้องวางบิลครบ · **Outsource UI ทั้งก้อนดึงจาก P2 มาทำเลย** (silkscreen ส่งร้านนอก 100% แต่ระบบใช้ไม่ได้จริง) — ส่วน AP vendor/WHT ขาจ่าย ยังอยู่ P2 ตามเดิม
## 🏭 FLOW-REDESIGN — รื้อทั้งระบบตามผังใหม่ (เบสเคาะครบ 2026-06-12 · แบบเต็ม: `docs/flow-redesign-2026-06-12.html`)
> เบสสั่ง: "มองมุมโรงงาน วาด flow ก่อน" → ศึกษา 5 มุม + ตอบคำถาม 10 ข้อ → เคาะครบ · **งานชุดนี้มาก่อน P1 ที่เหลือ** (WHT เลื่อนไปทำพร้อมก้อน 3 ขาเงิน)
> มติผูกพัน: outsource ส่งก่อนเสมอ · นับของ 2 จุด (ของเข้า/QC) · ห้ามเพิ่มงานกรอกหน้างาน · การสั่งซื้ออยู่ Stock ที่เดียว · ยอดจองโชว์ในแอป Stock · ต้นทุน = เรตตั้งครั้งเดียวคูณอัตโนมัติ (ไม่ใช่ตามจริงต่อชิ้น)

**ก้อน 1 — กระดูกสันหลัง: ด่านพร้อมผลิต + เตรียมของ + เชื่อมสต๊อคจริง (✅ จบทั้งก้อน 2026-06-12 — ทดสอบ flow จอง→เบิก→คืน ข้ามแอปจริงผ่านครบ ดู PROGRESS):**
- [x] **ฝั่ง Stock** (repo `../anajaktshirt-stock`): แก้ `POST /api/erp/movements` variant-aware + `$transaction` + กันติดลบ + `RETURN` + `orderRef` ราย line + idempotency key — service กลาง `erp-stock-service.ts` · SQL applied + verify 13/13 (2026-06-12)
- [x] **ฝั่ง Stock**: ตาราง `stock_reservations` + endpoint จอง-ปลดจอง + `GET /stock` คืน reservedQty/availableQty + โชว์ยอดจองหน้า /stock · products คืน `variant.lastCost` (2026-06-12)
- [x] **ฝั่ง ERP**: ยืนยันออเดอร์ FROM_STOCK → จองรายไซส์-สีอัตโนมัติ (ปลดจองตอนยกเลิก/ปิดงาน + จองใหม่ตอนแก้รายการ + ปุ่มจองใหม่) · ใบเบิก (ISSUE+orderRef ตัดยอดจอง) ผูกขั้น GARMENT_PICK · ใบคืนเศษ (RETURN) — เสื้อโรงเย็บ→PO/GRN ทำฝั่ง Stock ตามมติ (ERP มีใบตรวจรับรองรับขารับ · ปุ่มลัดจาก ERP = รอบเก็บตก)
- [x] **ฝั่ง ERP**: ด่านพร้อมผลิต (เงินตามเทอม ✓ + แบบอนุมัติ ✓ + ของครบ ✓) — คิว /production แยกกอง "ติดอะไร รอใคร" ช่างไม่เห็น · soft-gate หัวหน้าข้ามได้ (จงใจ)
- [x] **ฝั่ง ERP**: ใบตรวจรับของเข้า (นับจริงต่อไซส์ + รูป + ตำหนิ — mobile-first) เสื้อลูกค้า/เสื้อโรงเย็บ/รับกลับร้านนอก + ใบคืนของลูกค้า (กระทบยอดรับ-คืน) · ขาด/เกินกระดิ่งแอดมินทันที
- [x] **ฝั่ง ERP**: ProductionStep มี qtyDone/qtyTotal (บอก "บางส่วน" ได้) + ใบส่งร้านนอกแบ่งส่งหลายรอบ (ขั้นปิดเมื่อทุกใบตัดสิน+จำนวนครบ)

**ก้อน 2 — หน้าเครื่อง:** รอบพิมพ์ฟิล์ม (รวมหลายออเดอร์ กดเสร็จเป็นชุด) + จุดตัดแยกฟิล์ม + คลังฟิล์มพร้อมรีด (พิมพ์เผื่อ ติดป้ายลาย/ลูกค้า) + คิวรีด gate ฟิล์ม∧เสื้อ + จอเช้า 5 บทบาท + 5 ตัวเลขเจ้าของ + เรตต้นทุนกลาง (Settings ~5 ช่อง → กำไรขั้นต้นอัตโนมัติตอนตีราคา + เตือนเมื่อทุนซื้อเบี่ยง >10% — เบสเคาะ "เอาก็ได้ ถ้าทำได้จริงไม่งง" · การสั่งซื้ออยู่ Stock ที่เดียว ERP มีปุ่มลัด)
**ก้อน 3 — ขาออก + เงิน:** QC เชิงนับ (เสีย×ไซส์×ลาย×สาเหตุ×รูป) + เผื่อเสีย default 3% + แพ็คนับยืนยัน + รายการต่อกล่อง + blind ship + แบ่งส่ง + **WHT ขารับ 3% + ทะเบียน 50ทวิ** (ของเดิมใน P1) + UI แนบสลิปโอน (evidenceUrl มี API แล้ว)
**ก้อน 4 — ขอบลูกค้า (ภาค 2 `docs/flow-redesign-part2-2026-06-12.html`):** ไฟล์ 3 ชั้น (ดิบ/แบบอนุมัติ/ไฟล์พิมพ์ — ห้ามไฟล์ลอย) + ลิงก์อัปโหลดต่อออเดอร์ส่งใน LINE + ปุ่มแอดมินแนบแทนลูกค้า (attachment.create มี API ขาด UI) + **คลังลายต่อลูกค้า + สเปกติดลาย** (→ สั่งซ้ำ 1 คลิกเต็มรูป — duplicate มีแล้ว) + preflight DTF 3 เช็ค + นับรอบแก้แบบ (**เบสเคาะ: ฟรี 2 รอบ เกินคิด 100 บาท/รอบ** — เด้งเป็นรายการอัตโนมัติ) + ลิงก์ยืนยันใบเสนอ (ก๊อป token pattern จาก design) + **portal ขั้น 1: ลิงก์สถานะต่อออเดอร์/ต่อลูกค้า** (customerStatus พร้อมแล้ว · เบสเคาะ: โชว์เฉพาะส่วนของลูกค้า — สถานะ/กำหนดส่ง/แบบอนุมัติ/เอกสารของเขา/พัสดุ ห้ามมีข้อมูลภายใน) + size matrix (P1.12 เดิม) + Storage เปลี่ยน public URL → signed URL
**ก้อน 5 — MCP เฟสแรก (เบสเคาะ: ไม่รีบ — ทำตามลำดับ):** embed ใน Next.js (`app/api/mcp/[transport]` + vercel/mcp-handler เรียก services ตรง — P0.4 ปูไว้แล้ว) · AgentApiKey model (key ต่อ agent ผูก Role เดิม + audit ทุก call) · เครื่องมือ read-only 4 ตัว: สถานะออเดอร์ (dual view) / คิววันนี้+งานเสี่ยงสาย / ลูกหนี้+ร่างทวง (ร่างให้คนส่ง) / เช็คสต๊อค · **ห้ามมี**: เปลี่ยนสถานะ/เงิน/ลบ · เฟสสอง: draft order จากแชท (DRAFT/INQUIRY เท่านั้น คนยืนยันใน UI)
**ก้อน 6 — โฉมใหม่:** UI ทั้งระบบ (ทำภาพให้เบสเลือกก่อน) + ทางลัดงานชิ้นเดียว + ใบแก้ไขออเดอร์ (change order — อนุมัติแล้วล็อก แก้ผ่านใบแก้ไขเท่านั้น)

### 🎯 UI V2 แบบคู่ขนาน — หน้าเริ่มงานจริงที่ `/v2` (เบสสั่ง 2026-08-12)
> เบสขอข้าม mockup และให้ทำของจริงบน URL แยก เพื่อเทียบกับหน้าเดิมโดยไม่ทุบของที่ใช้อยู่ · รอบแรกแก้ปัญหา “ทุกอย่างดังเท่ากัน” ที่ dashboard/shell ก่อน แล้วค่อยตัดสินทิศทางทั้งระบบจาก render จริง
>
> ขอบเขต: UI + route ใหม่เท่านั้น · ใช้ query/permission/ข้อมูลจริงชุดเดิม · ลิงก์ทุก action ไป flow เดิมที่ใช้งานได้ · ไม่แตะ schema, business logic, dependency, config หรือ URL เดิม

- [x] **โครง V2 แยกจริง** — `/v2` มี auth ชั้นเดียวกับ dashboard เดิม · มีทางกลับเวอร์ชันเดิม · URL/หน้าเดิมไม่เปลี่ยน
- [x] **action-first hierarchy** — งานที่ควรเช็กก่อน + ทางลัดเปิดงาน/คิวงานอยู่เหนือสถิติสะสม · ไม่มี KPI ซ้ำชื่อ/ซ้ำความหมาย · ข้อมูลเงิน/เมนูเคารพ permission เดิม
- [x] **navigation ลดเสียงรบกวน** — desktop เหลือเมนูงานหลัก + ค้นหาทุกเมนูผ่าน command palette · mobile ใช้ bottom navigation เป้านิ้ว ≥44px และมีทางเข้าฟังก์ชันครบ
- [x] **minimal แต่ไม่จืด** — ใช้ token/สีแบรนด์เดิม เน้นสีเฉพาะ action/สถานะสำคัญ · มี micro-interaction ที่เคารพ reduced-motion · loading/error/empty ครบ
- [x] **verify ของจริงก่อนเคาะทิศ** — desktop 1440px + mobile 390px ไม่มี horizontal overflow/error overlay · keyboard focus เห็นชัด · เปิด flow จาก `/v2` ไปงานจริงได้อย่างน้อย 1 รายการ · typecheck/lint/test/build ผ่าน

#### ใบงาน V2 Orders — รวม / เปิดใหม่ / รายละเอียด (เบสสั่ง 2026-08-12)
> ต่อจาก dashboard V2 ด้วยเส้นทางขายที่ใช้ทุกวัน · แชร์ business logic กับหน้าเดิม ไม่ copy ฟอร์ม/mutation · เพิ่มเฉพาะ route + composition V2

- [x] **route + navigation ต่อเนื่อง** — เพิ่ม `/v2/orders`, `/v2/orders/new`, `/v2/orders/[id]` ใต้ auth layout เดิม · active nav ถูก route · command palette/recent order/new order พาเข้า V2 เฉพาะหน้าที่มีแล้ว
- [x] **รายการ action-first** — ยุบแถบ 14 สถานะบนมือถือให้รายการจริงขึ้นก่อน `y=600` แต่ยังกรองสถานะครบ · search/filter/sort/pagination/CSV/loading/error/empty ใช้ query เดิม
- [x] **เปิดออเดอร์ใช้ form เดิมร่วมกัน** — สูตรราคา/draft/validation/upload/`next=quote`/mutation ไม่แยกชุด · ผู้ไม่มี `create_sales_docs` เห็น forbidden ก่อนฟอร์ม
- [x] **รายละเอียดใช้ flow เดิมร่วมกัน** — status transition/next step/design/รับเข้า/QC/delivery/money/document/duplicate ใช้ component+server guard เดิม · path กลับ/duplicate สำเร็จอยู่ใน V2
- [x] **ไม่ทำของเดิมถอย + verify จริง** — `/orders*` พฤติกรรมเดิม · 390/1440 ไม่มี overflow · keyboard/focus/Thai/permission/loading/error/empty ผ่าน · typecheck/lint/test/verify:ui/build ผ่าน (build หลังหยุด dev)

#### ใบงาน V2 ลดคำอธิบาย + คืนแถบสถานะเดิม (เบสสั่งจากจอจริง 2026-08-12)
> ตัดข้อความที่หัวข้อ/ปุ่มสื่อความหมายได้อยู่แล้วออกจาก V2 ทั้ง dashboard, รายการ และหน้าเปิดงาน · คงข้อความผิดพลาด สิทธิ์ ผลกระทบ และข้อจำกัดที่ต้องรู้ก่อนกด · หน้ารวมออเดอร์จอกว้างกลับมาเห็นเส้นทางงานเต็มแบบเดิมตามภาพอ้างอิง ส่วนมือถือยังใช้ตัวกรองย่อเพื่อไม่ดันรายการพ้นจอแรก

- [x] **quiet copy เฉพาะ V2** — ลดคำแนะนำซ้ำโดยไม่เปลี่ยนข้อความ validation/error/permission/safety และไม่ทำให้หน้าเดิม `/orders*` เปลี่ยน
- [x] **status rail แบบเดิมบนจอกว้าง** — `/v2/orders` แสดงทุกช่วงงานในแถบเดียว กรอง/กดซ้ำเพื่อล้างได้เหมือนเดิม · จอแคบคงชิปย่อ + เปิดทุกสถานะได้
- [x] **verify จอจริง** — 390px รายการแรกยังอยู่ก่อน `y=600` · 1440px เห็น rail เต็ม · ไม่มี overflow/hydration/error · typecheck/lint/test/verify:ui ผ่าน

#### ใบงาน V2 แท็บ minimal แบบเส้นใต้ (เบสส่งภาพอ้างอิง 2026-08-12)
> เปลี่ยนเฉพาะ V2 จากถาด segmented + แผ่นขาวมีเงา เป็นข้อความบนพื้นหน้าและเส้นใต้แท็บที่เลือก · คง Radix keyboard/ARIA, sticky, mobile horizontal scroll และจุดแดง/เขียวที่มีความหมาย · หน้าเดิมใช้ segmented เหมือนเดิม

- [x] **appearance แยก V2/V1 ชัดเจน** — V2 หน้าเปิดงานและรายละเอียดใช้ underline · V1 ไม่เปลี่ยน · ไม่ผูก appearance กับ guidance หรือ bottom-nav
- [x] **minimal แต่ไม่เสียสถานะ** — ตัดเลข 01–04 ใน V2 · คง label, validation dot, filled dot, focus ring และ active underline
- [x] **verify ของจริง** — 390/1440 ไม่มี overflow · active tab เลื่อนเข้าจอ · Arrow/Home/End, URL/back/deep-link/validation ผ่าน · typecheck/lint/test/verify:ui/build ผ่าน

#### ใบงาน Promote V2 → UI หลัก + พื้นขาวนวล (เบสเคาะจากจอจริง 2026-08-12)
> ผลทดลอง V2 ผ่านแล้ว เบสให้แทน V1 บน URL เดิมและขอ refactor ไม่ให้มีสอง presentation ต่อกัน · ใช้ semantic token กลางปรับพื้นหน้าเป็น off-white/การ์ดขาว/พื้นจมอ่อน ลด halo ของเงา และคง brand/status/dark theme

- [x] **canonical shell + routes ทางเดียว** — `/` และ `/orders*` ใช้ UI ที่เคยอยู่ V2 · navigation อ่าน canonical registry โดยตรง · `/v2*` redirect พร้อม query · ไม่มีลิงก์ “เวอร์ชันเดิม” หรือ runtime prefix resolver
- [x] **ลด compatibility branch** — orders/create/detail/tabs/permission helper เหลือค่า production ชุดเดียว · ไม่ copy query/mutation/business logic · ไฟล์ shell V1 ที่ไม่มี caller ยังไม่ลบจนเบสอนุญาต
- [x] **เมนูและสิทธิ์ไม่ถอย** — desktop/mobile เข้าถึงทุกโมดูลตาม permission ได้ · CTA เปิดงานทั้งหมดใช้ gate สร้างเอกสาร+เห็นเงินชุดเดียว · unauthenticated ยังเข้าไม่ได้
- [x] **พื้นผิวสว่างขึ้นจาก token กลาง** — page bg `#f7f7f8` · surface/chrome ขาว · sunk `#f2f2f4` · semantic neutral ตรง ramp/ผ่าน AA · เงาการ์ด light เบาลง · dark/overlay/status color คงเดิม
- [x] **shell regression ปิดครบ** — bottom-nav offset กลางครอบเปิดงาน/แก้รายการ/รอบพิมพ์ · ตารางกว้างไม่ถูกบีบ · active nav/⌘K/More/focus return/notifications link ใช้ได้
- [x] **verify ของจริงก่อนจบ** — dashboard/orders/create/detail + อย่างน้อย production/billing/settings ที่ 390/1440 · redirect query/back-refresh · permission/loading/error/empty · ไม่มี overflow/hydration/console error · typecheck/lint/test/verify:ui/build ผ่าน

#### ใบงานเก็บภาพจริง — hover แยกชั้น + Navbar เต็มจอ (เบสชี้จากภาพจริง 2026-08-12)
> สี hover เทาเดิมชนกับ `sunk` จนชี้แล้วแทบไม่เปลี่ยน และ top chrome เริ่มหลัง sidebar ทำให้ navbar ดูขาดกลางจอ · แก้ที่ semantic interaction token + โครง AppShell กลาง ไม่ไล่ patch รายหน้า

- [x] **hover มี feedback ชัดแต่ไม่ดัง** — neutral interactive ทั้ง primitives/shell/dashboard ใช้สี hover ชุดเดียว · pressed/focus ชัดกว่า · selected คงสีแบรนด์ · light/dark ไม่กลืนกับพื้นเดิม
- [x] **navbar เต็ม viewport** — ย้าย brand block เข้า header ผืนเดียว แล้วให้ sidebar เริ่มใต้ header · คง scroll boundary, permission nav, command palette, mobile header/bottom nav และ safe-area เดิม
- [x] **verify ของจริง** — 390/768/1024/1440 ไม่มี overflow · header/chrome/border เต็มจอ · hover/active/focus แยกได้ · dashboard→ออเดอร์ flow ใช้งานจบ · typecheck/lint/test/verify:ui/build ผ่าน

#### ใบงานออกแบบระบบสีใหม่ทั้งเว็บ — Light + Dark (เบสชี้จากภาพจริง 2026-08-13)
> รุ่นก่อนแก้ hover จาก “กลืน” ไปไกลเกินจนกลายเป็นแถบเทาหนัก · รอบนี้แก้ที่ระบบสีทั้งหมด ไม่ patch เฉพาะ dashboard: ล็อกน้ำเงินแบรนด์ `#3973b2` ค่าเดียว แล้วออกแบบ neutral/status/surface/interaction ใหม่ให้ทุกหน้ารับพร้อมกันจาก token กลาง

- [x] **palette กลางชุดเดียว** — light/dark มี page, chrome, surface, sunk, border, field, overlay และข้อความ 3 ระดับที่สัมพันธ์กัน · neutral ramp ตรง semantic token · ไม่ใส่ hex ราย component
- [x] **interaction เบาแต่ครบ state** — hover ไม่เป็นแถบเข้ม · pressed ชัดกว่า hover · selected ใช้น้ำเงินแบรนด์ · focus/disabled/keyboard-highlight ไม่ถูกสีอื่นกลบและข้อความผ่าน AA
- [x] **สีสถานะเป็นระบบเดียว** — info/success/warning/error และ badge/tint อ่านได้ทั้งสองธีมโดยไม่แย่ง primary action · public/print คงพื้นขาวและเอกสารไม่ตามธีมหลังบ้าน
- [x] **verify ของจริงทั้งระบบ** — dashboard/orders/create/detail + production/billing/settings ที่ 390/768/1024/1440 · light/dark, hover/pressed/selected/focus, dialog/menu/table/form/status ไม่มี overflow/hydration/console error · typecheck/lint/test/verify:ui/build ผ่าน

#### ใบงานเก็บสีรอบสอง — Dark เทาเข้ม + Hover เทากลาง (เบสชี้จากจอจริง 2026-08-13)
> เบสเคาะให้คืนบรรยากาศโหมดมืดเป็นเทาเข้มแบบก่อน redesign และไม่ใช้ฟ้าเป็น hover ทั้งสองธีม · น้ำเงิน Anajak ยังสงวนให้ primary, selected และ focus เท่านั้น · แก้ที่ semantic token กลางเพื่อให้ทั้งเว็บเปลี่ยนพร้อมกัน ไม่ไล่ patch ราย component

- [x] **Dark กลับเป็น neutral gray ที่มี depth** — page/chrome/card/sunk/elevated/field เป็นบันไดเทาเข้มแบบเดิม โดย card ยังแยกจาก page และกล่องจม/กล่องลอยอ่านทิศถูก
- [x] **Hover/pressed ไม่อมฟ้า** — light/dark ใช้ neutral gray · hover เบากว่ารุ่นแถบเทาเดิม · pressed ชัดกว่า hover · selected/focus/primary คงน้ำเงินเดิม
- [x] **ด่านกันถอย + verify ของจริง** — token guard กัน hover กลับไปอมฟ้าหรือชนพื้น structural · text/control contrast ผ่าน · ตรวจ dashboard/orders/create/detail ทั้ง Light/Dark และ 390/1280 · typecheck/lint/test/verify:ui/build ผ่าน

#### ใบงานเก็บ Sidebar ให้เข้าระบบสีล่าสุด (เบสทักต่อ 2026-08-13)
> Sidebar ต้องใช้ semantic เดียวกับส่วนเนื้อหา ไม่ใช่แค่รับสีพื้นใหม่: inactive/hover/pressed/selected/focus ต้องอ่านลำดับเดียวกันทั้ง desktop, drawer และ bottom nav

- [x] **สูตรรายการเดียวกัน** — primary/secondary/drawer ใช้ active label+icon สีเดียวกัน · inactive icon ขยับตาม hover/pressed · focus/radius/weight สอดคล้องกัน
- [x] **mobile current state ไม่พึ่งสีอย่างเดียว** — active มีทั้งน้ำหนักและรูปทรง · “เพิ่มเติม” มี `aria-current` เมื่อ route ปัจจุบันอยู่ในกลุ่ม
- [x] **verify Sidebar ของจริง** — Light/Dark desktop + 390px ไม่มี overflow · active/hover/pressed/focus แยกได้ · permission/navigation เดิมไม่ถอย · typecheck/lint/test/verify:ui/build ผ่าน

#### ใบงานลด hover ของ navigation ให้เหลือ typography (เบสทักต่อ 2026-08-13)
> Minimal ของระบบไม่ควรวาดแถบเต็มเมื่อเพียงชี้เมนู: navigation ที่ยังไม่ถูกเลือกเปลี่ยนน้ำหนักด้วยสีข้อความ+ไอคอนเท่านั้น · current/selected ยังคงพื้นฟ้า · focus และ touch pressed ยังคง feedback ที่มองเห็น

- [x] **แยก semantic ตามชนิด interaction** — desktop sidebar, More drawer และขั้นสถานะบน rail ใช้ text/icon-only hover · clickable card ใช้เงา+ข้อความ/ไอคอน · ไม่แตะปุ่ม, icon control, filter/status choice บนมือถือ, menu highlight หรือตารางที่ต้องมี surface feedback
- [x] **คง state ที่จำเป็น** — selected/current มีพื้นฟ้าและ `aria-current` เดิม · keyboard focus ring เดิม · pressed บน touch ยังมีพื้นตอบสนอง · mobile bottom nav ไม่ถอย
- [x] **ล็อก regression + ตรวจจอจริง** — guard ห้าม navigation/status step/card วาดพื้นตอน hover แต่บังคับ text/elevation/pressed/selected/focus ตามชนิด · ตรวจ Light/Dark desktop + drawer/mobile และรัน typecheck/lint/unit/verify:ui/build

#### ใบงานคืน Surface Hover แบบสว่าง (เบสทักจากจอจริง 2026-08-13)
> มติล่าสุดทับใบงาน text-only ข้างบน: ปัญหาไม่ใช่แถบ hover แต่เป็นเทาเดิมใกล้ผืนหน้าจนดูเหมือนแถวถูกเจาะ · คืน feedback เต็มพื้นที่แบบเดิม แล้วทำเฉด Light ให้ขาวและเบาลง โดยไม่เปลี่ยน page/card hierarchy

- [x] **คืน full-surface feedback ตามพื้นที่กด** — sidebar/More drawer, ขั้นสถานะ desktop และ clickable card กลับมาเปลี่ยนพื้นทั้ง hit area · คง selected/current, focus, pressed และ mobile bottom nav เดิม
- [x] **ปรับสีที่ token กลาง ไม่ patch รายหน้า** — Light hover ต้องขาวกว่า `#eceef1` แต่ยังแยกจาก card ขาวและ pressed · Dark คง neutral hierarchy ที่ผ่านจอจริง · ไม่แตะ page, card, field หรือสีแบรนด์
- [x] **ล็อก regression + ตรวจจอจริง** — guard ตรวจ hover/pressed/selected/focus และลำดับสี · verify Light/Dark desktop + 390px บน orders/sidebar/status/card โดยไม่มี overflow, hydration, console error หรือ overlay · typecheck/lint/unit/verify:ui/build ผ่าน

#### ใบงาน Impeccable UX integrity refactor (เบสให้ audit + รื้อได้ทั้งระบบ 2026-08-13)
> Audit โค้ดและจอจริง desktop/mobile พบว่า visual world ล่าสุดมีลำดับและควรคงไว้ · หนี้ที่ควรรื้อคือความซื่อสัตย์ของ interaction, query ที่ยิงโดยไม่จำเป็น, คิวหลายคนที่ค้าง, draft ที่บอกว่ากู้ได้แต่เก็บไม่ครบ และชื่อ V2/dead shell ที่ทำให้คนแก้ผิดบ้าน

- [x] **R0 ยึด product truth ก่อนรื้อ** — เพิ่ม `PRODUCT.md` จาก SPEC/ROADMAP/flow และมติที่เบสเคยเคาะ · ล็อกผู้ใช้ งานหลัก ข้อห้าม และหลัก “จุดโฟกัสเดียว” ไว้ถาวร
- [x] **R1 canonical UI architecture** — ย้าย shell/dashboard/order helpers ตัวจริงออกจากชื่อ `v2-*` · update import/test · ลบ Sidebar/Topbar/MobileSidebar เก่าที่ `rg` ยืนยันว่าไม่มี caller · คง `/v2*` compatibility redirect ไว้
- [x] **R2 แท็บเบาและสิทธิ์นิ่ง** — `TabsContent` lazy เป็น default · ฟอร์มเปิดงาน opt-in `keepMounted` · detail mount เมื่อเข้าแล้ว preserve panel ที่เคยเปิด · รอ permission ก่อนวาด action และบอกเหตุผลเมื่อ deep-link ไปแท็บที่ไม่มีสิทธิ์
- [x] **R3 interaction truth + accessibility** — DatePicker/DateRange ไม่มี button ซ้อน · clear/close target ≥44px บนมือถือ · เปลี่ยน delivery placeholder ที่ handler ว่างเป็น read-only state · แก้ป้ายทางลัด dashboard ไม่ให้ตัดคำจนเดาไม่ได้
- [x] **R4 multi-user freshness** — `/production` และ `/production/print-runs` poll 30 วินาที + refetch เมื่อกลับแท็บ/ต่อเน็ตใหม่ โดยคง optimistic/mutation invalidation เดิม · ไม่เพิ่ม realtime infrastructure
- [x] **R5 draft ครบใบ** — ใช้ header state ก้อนเดียวที่มีอยู่แล้ว · กู้ channel/deadline/notes/priority/payment terms/PO/discount/platform fee/tax รวมถึง fees/shipping และ metadata ไฟล์ที่อัปโหลดแล้ว · draft แยกผู้ใช้/หมดอายุ 7 วัน · ไม่เก็บ base64 preview ลง localStorage · cancel timer + revision guard กัน draft ฟื้นหลัง success/reset
- [x] **R6 verify เหมือนงานจริง** — desktop 1280 + mobile 390 บน dashboard/orders/new/detail/production/print-runs · overflow/target/query/error/console · keyboard/Escape/focus return · typecheck/lint/unit/verify:ui/Impeccable detector/build ผ่าน · finish review GO ไม่มี P0/P1 · อัปเดต DESIGN/PROGRESS

#### ใบงานคืน visual contract แบบ minimal ไร้กรอบ (เบสส่งภาพ `/orders` 2026-08-13)

> Regression จากก้อนสีล่าสุด: `.card-surface`, status rail และ field/control กลับมามีเส้นรอบ ทั้งที่มติระดับ 3 (`dc40932`, `dbbba71`) ให้ใช้สีพื้น+ระยะ+เงาแทนเส้น · เบสย้ำจากภาพจริงรอบสองว่า minimal ไม่ใช่ทุกอย่างแบน: status ต้องเป็น card สีขาว และ toolbar control ต้องยกขึ้นจากผืนหน้า

- [x] **M1 พื้นผิวหลักไร้เส้นรอบ** — card/table/list/status rail ใช้ surface สีขาว+เงา · overlay ใช้เงาแยกชั้น · ห้าม ring ปลอมเป็น border กลับมา
- [x] **M2 control อ่านออกโดยไม่ตีกรอบ** — field ในฟอร์มใช้พื้นจม · search/sort/date/filter ที่ยืนบนผืนหน้าใช้ raised control สีขาว+เงา · คงขอบเฉพาะ focus/error, ขอบประพื้นที่เพิ่มไฟล์/รายการ และเส้นคั่นที่จำเป็นต่อการอ่าน
- [x] **M3 ล็อก regression + ตรวจจอจริง** — `verify:ui` กัน visible border กลับใน primitive/caller · `/orders` desktop/mobile Light/Dark และหน้า detail/production/form ผ่านบน production build · typecheck/lint 0 error/unit 693/build ผ่าน

#### ใบงานแยก Surface ของโครงสร้างออกจาก Control (เบสชี้ซ้ำจากจอจริง 2026-08-14)

> มติล่าสุดทับเฉพาะ M2 ด้านบน: การตีความ minimal = “control ไร้ขอบและใช้พื้นจม” ทำให้ช่องกรอกบน card ขาวกับปุ่มรองบนผืนหน้าอ่านไม่ออกซ้ำหลายรอบ · แก้ที่ semantic primitive กลาง ไม่ patch รายหน้า และไม่เปลี่ยนผืน page/card ที่ผ่านแล้ว

- [x] **S1 แยกบทบาทพื้นผิวถาวร** — page = ผืนงาน, card = ขาว+เงาไร้เส้น, field = ขาว+boundary บาง, secondary control = ขาว+boundary/เงาเบา, `surface-muted` = กลุ่มโครงสร้างหรือ disabled เท่านั้น · Light/Dark มีทิศ depth เดียวกัน
- [x] **S2 ย้าย primitive และ caller ที่หลุด** — Input/Textarea/Select/DatePicker/ProductPicker ใช้ field contract เดียว · Button outline/secondary/subtle ใช้ secondary-control contract · ตรวจ search/filter/date/status/chip/drop target/disabled/error/focus โดยไม่ทำ ghost/card/overlay ให้มีกรอบเกินหน้าที่
- [x] **S3 ล็อก regression + ตรวจจอจริง** — `verify:ui` render primitive และกัน field/button กลับไปใช้ `surface-muted` หรือ border โปร่ง · ตรวจ dashboard/orders/new/detail/production/billing/settings ทั้ง Light/Dark ที่ 1280 และ 390 รวม hover/pressed/selected/focus/error/disabled · ไม่มี overflow/hydration/console error · typecheck/lint/unit/verify:ui/Impeccable detector/build ผ่าน

#### ใบงานลดน้ำหนักขอบ Field ให้กลับมา minimal (เบสชี้จากจอจริง 2026-08-14)

> มติล่าสุดทับเฉพาะค่าน้ำหนักขอบ resting ของ S1–S3: field ยังเป็นสีขาวและแยก semantic จากพื้นโครงสร้างเหมือนเดิม แต่การบังคับ resting border ให้ผ่าน 3:1 ทำให้ทุกช่องกลายเป็นกรอบเข้มแข่งกับเนื้อหา · ลด contrast เฉพาะสถานะพัก แล้วสงวนเส้นเข้มไว้ให้ focus/error

- [x] **Q1 resting boundary เบาแต่ไม่หาย** — เทียบ Light/Dark จากจอจริงและเลือกขอบอ่อนที่ยังเห็นรูปทรงบน card/page โดยไม่ทำฟอร์มเป็นตารางเส้น · ไม่เปลี่ยน field fill, page, card หรือ toolbar
- [x] **Q2 state สำคัญยังชัด** — focus/error/invalid/disabled/placeholder และ inline control คง semantics, keyboard และ contrast เดิม · ไม่มี layout shift เมื่อสถานะเปลี่ยน
- [x] **Q3 guard กันแกว่งสองขั้ว + verify** — ล็อกช่วง contrast ของ resting border ให้ไม่จางจนกลืนและไม่เข้มจนรก โดยแยกจากข้อบังคับ contrast ของ focus/error · ตรวจ Light/Dark 1280 + 390, typecheck/lint/unit/verify:ui/Impeccable detector/build ผ่าน

#### ใบงานคืนขอบประพื้นที่เพิ่มเป็นแบบเดิม (เบสชี้จากจอจริง 2026-08-14)

> `DASHED` ถูก semantic color rewrite เปลี่ยนจาก `slate-300/700` เป็น `border-strong` จน CTA ขนาดใหญ่ 5 ใบดูเป็นกรอบเข้มทั้งหน้า · มติล่าสุดให้คืน resting boundary แบบเดิมเฉพาะพื้นที่เพิ่ม/อัปโหลด โดยคง hover, pressed และ keyboard focus ที่ชัดไว้

- [x] **D1 คืน token กลาง ไม่ patch รายใบ** — `DASHED` ใช้ขอบอ่อนเดิมทั้ง Light/Dark และครอบ AddCard/drop target ทุก caller · ไม่แตะ field, card, divider หรือ `border-strong` ที่ยังมีหน้าที่อื่น
- [x] **D2 ล็อก interaction + regression** — resting เบา แต่ hover/pressed/focus ยังบอกพื้นที่กดได้ · guard กัน `DASHED` กลับไปใช้ strong boundary และยืนยัน AddCard 5 ใบยังหน้าตาเดียวกัน
- [x] **D3 ตรวจจอจริงและด่านครบ** — `/orders/new` Light/Dark 1280 + 390 ไม่มี overflow/hydration/console error · typecheck/lint/unit/verify:ui/Impeccable detector/build ผ่าน

#### ใบงานแยกรายการงานเป็นการ์ด + ยกผืนหน้าให้เกือบขาว (เบสชี้จากจอจริง 2026-08-14)

> หน้าเปิดงานและฟอร์มแก้รายการมี card ใหญ่ครอบทุกชุดงาน แล้ววาดกรอบซ้ำรอบ “รายการที่ N” ทำให้หลายรายการอ่านเป็นก้อนเดียวและมีเส้นซ้อน · มติล่าสุดให้หนึ่งรายการยืนเป็น card ขาวไร้ขอบของตัวเอง มีปุ่มเพิ่มรายการอยู่ก่อนรายการเสมอ และให้ Light page ใช้ off-white ที่ต่างจากขาวเพียงเล็กน้อยตามภาพ Vercel โดยไม่เปลี่ยน business flow

- [x] **E1 หนึ่งรายการต่อหนึ่ง card** — `OrderItemCard` เป็น surface ขาวมีเงานุ่ม ไม่มี border/ring ตกแต่ง · รายการที่ 2 เป็นต้นไปเป็น card แยกและมีระยะหายใจเท่ากัน · หน้าเปิดงานกับหน้าแก้ไขใช้ component เดียวกัน
- [x] **E2 ปุ่มเพิ่มรายการอยู่บนและเห็นชัด** — มี CTA “เพิ่มรายการ” เหนือรายการทั้งสองหน้า · mobile กว้างเต็ม/เป้ากด ≥44px · ไม่มีปุ่มเพิ่มรายการซ้ำท้ายรายการ และ count/ลำดับยังตรงหลังเพิ่มหรือลบ
- [x] **E3 ผืนหน้า Light เกือบขาว** — workspace ใน AppShell เป็น neutral off-white ใกล้ `#fff` มากขึ้น โดย card/chrome ยังขาวและอ่าน hierarchy ได้จากสี+เงา · ไม่เปลี่ยน field, hover, status, public/auth/print หรือ Dark ที่ผ่านจอจริงแล้ว
- [x] **E4 ล็อก regression + ตรวจของจริง** — guard ยืนยัน item card ไร้ขอบและ CTA อยู่ก่อน list ทั้ง create/edit · ตรวจ 1/2 รายการ Light/Dark ที่ 1280 + 390, keyboard/focus/target/overflow/hydration/console · typecheck/lint/unit/verify:ui/Impeccable detector/build ผ่าน

#### ใบงานแยกฟอร์มสินค้าตามแหล่ง + จัดเครื่องมือเรียงลำดับใหม่ (เบสชี้จากจอจริง 2026-08-14)

> ภาพจริงยืนยันว่าการใช้ตารางขาย 8 คอลัมน์กับ “ลูกค้าส่งมา” และ “ตัดเย็บใหม่” ทำให้ช่องที่ใช้ไม่ได้กลายเป็นขีดลอย และสินค้าเดียวแตกเป็นแถวหลัก/สเปค/ไซส์คนละก้อน · ปุ่มขึ้นลงยังซ้อนแนวตั้งในคอลัมน์ 44px แล้วเบียดถังขยะ จึงทั้งล้นและสื่อไม่ชัดว่ากำลังย้ายสินค้าทั้งก้อน

- [x] **F1 adaptive product block ตามแหล่งสินค้า** — คงตารางเดิมเมื่อเป็นสินค้าจากสต็อกล้วน · ถ้ามีสินค้ากรอกเอง ให้แต่ละสินค้าเป็นก้อนพื้นจมตามลำดับจริง: ลูกค้าส่งมาเห็นชื่อ/แพค/ไซส์และจำนวนโดยไม่มีราคา–ส่วนลด–รวมปลอม; ตัดเย็บใหม่รวมข้อมูลสินค้า/สเปค/ไซส์ไว้ในขอบเขตเดียว · mixed source ห้ามแยกกลุ่มจนลำดับเปลี่ยน
- [x] **F2 เครื่องมือจัดลำดับมีบ้านเดียว** — header ของก้อนสินค้ามี “สินค้า n/N” + ปุ่มขึ้น/ลงแนวนอนและลบที่แยกชัด · แถวตารางใช้เมนูจัดการหนึ่งปุ่มแทนก้อนลูกศรซ้อนสูง · mobile target ≥44px, keyboard/focus/aria-live ครบ และไม่เพิ่ม drag-and-drop
- [x] **F3 ลำดับและ state ต้องติดสินค้าเดิม** — ใช้ client key คงที่แทน `key={index}` ในรายการสินค้า · ย้ายขึ้น/ลงต้องพา variants, สเปคตัดเย็บ, สี/ไซส์ และ local editor state ไปกับสินค้าเดิม · แก้ id ของ size matrix ไม่ให้ซ้ำระหว่างสินค้า/desktop/mobile พร้อม unit test pure reorder และ draft round-trip
- [x] **F4 ล็อก regression + ตรวจของจริง** — จอจริง create ที่ 1→2 สินค้ากรอกเอง + shared `OrderItemCard` ของ create/edit · pure test ครอบ 2 ชุดงาน, 3 สินค้า, reorder กลาง/ขอบ และ draft round-trip · resize ข้าม layout, Light/Dark ที่ 1440 + 390 · ไม่มีขีดในช่องเงินของลูกค้าส่งมา ไม่มีปุ่ม “ปิดหลายไซส์” ในสองโหมดนี้ ไม่มี overflow/duplicate id/hydration/console error · typecheck/lint/unit/verify:ui/Impeccable detector/build ผ่าน

#### ใบงาน WHITE-CANVAS — เปลี่ยนพื้นผิวทั้งเว็บเป็น minimal บนผืนขาว (เบสสั่ง 2026-08-23)

> เบสไม่ต้องการ UI ที่ใช้สีและกล่องเป็นโครงหลักอีกแล้ว: เนื้อหาต้องวางบนพื้นหลังขาวโดยตรง ใช้ card เฉพาะข้อมูลรองที่เป็นก้อนจริง และ Dark ต้องกลับค่าชั้นผิวแบบเดียวกัน · ขอบเขต presentation/design-system ทั้งเว็บเท่านั้น คง query, mutation, permission, validation, status transition, public/print และ flow งานเดิมทั้งหมด

- [x] **WC1 แกนพื้นผิวเดียวทั้งระบบ** — Light page/workspace/chrome เป็นขาวต่อเนื่อง; Dark เป็น neutral ดำต่อเนื่อง · direct content ไม่มี card/เงาครอบ · secondary object card ใช้พื้นตามธีมพร้อม hairline edge/เงาสั้นเท่าที่ต้องแยกจาก canvas · overlay เป็นชั้นที่ลอยสูงสุด ✅ 2026-08-23
- [x] **WC2 primitive เรียบธรรมดา** — Section/DataTable เป็นเนื้อหาตรงพร้อมเส้นคั่นเท่าที่จำเป็น · Button/Search/toolbar control เป็นทรงเหลี่ยมมนเล็ก ไม่มีเงาตกแต่ง · field ยังคง boundary ที่บอกว่ากรอกได้ และ semantic status/primary/focus คงสีตามหน้าที่ ✅ 2026-08-23
- [x] **WC3 shell และลำดับสายตา** — Sidebar/mobile nav ไม่มีพื้นสีเป็นก้อนตอน active; ใช้น้ำเงินเฉพาะข้อความ/ไอคอน/เส้นบอกตำแหน่ง · ลด marker สีประจำ module ใน PageHeader/Section ให้เป็น neutral โดยไม่เสียชื่อที่เข้าถึงได้หรือ deep-route state ✅ 2026-08-23
- [x] **WC4 ครอบคลุมและไม่เปลี่ยนงาน** — ปรับ caller ที่ยังใช้ card ใหญ่เป็น page scaffold; เก็บ card สำหรับ mobile record, stat, bounded item, alert และ overlay ตาม semantic จริง · ไม่แตะข้อมูล เงิน สิทธิ์ สถานะ หรือ Station handoff ✅ 2026-08-23
- [x] **WC5 verify ของจริงก่อนปิด** — dashboard/orders/production/billing/settings/products/factory representative ที่ 1440×900 + 390×844 ทั้ง Light/Dark · ไม่มี overflow/hydration/console error · keyboard/focus/loading/error/empty/disabled ยังชัด · typecheck/lint/unit/verify:ui/Impeccable detector/build ผ่าน แล้วอัปเดต SPEC/DESIGN/PROGRESS และ commit ก้อนเล็ก ✅ 2026-08-23
- [x] **WC6 เก็บรายละเอียด interaction และ card hierarchy** — หัวตารางต้องอ่านเป็นหัวตารางโดยไม่แตกเป็นช่องสี, row hover ชัดแต่เบา, ปุ่มมี tactile pressed state และ card ใช้เฉพาะ bounded object/กลุ่มงานที่ควรถูกอ่านเป็นหนึ่งก้อนพร้อม depth เบา ๆ · คง direct content บนผืนขาวและกลับชั้นผิวแบบเดียวกันใน Dark · verify desktop/mobile Light/Dark ก่อนปิด ✅ 2026-08-23

#### ใบงาน VERCEL-PANELS — เนื้อหาบนแผงพื้นหลังแบบ Vercel (เบสสั่ง 2026-08-23)

> มติใหม่นี้ทับ White Working Sheet เฉพาะวิธีจัดชั้นผิว: ใช้พื้น workspace off-white/ดำเป็นฉาก และรวมเนื้อหาที่สัมพันธ์กันไว้ใน panel ขาว/ดำมีเส้นบางแบบ Vercel · ไม่ย้อนกลับไปใช้สีหลายก้อน เงาฟู หรือ card ซ้อน card · ตัวกรองข้อความ minimal, ข้อมูล, สิทธิ์, mutation, status transition, public/print และ Station flow เดิมทั้งหมด

- [x] **VP1 surface system แบบ Vercel** — Light workspace `#fafafa`, chrome/card ขาว, Dark workspace ดำและ card ยกขึ้นหนึ่ง neutral step · card/panel ใช้ border 1px, radius 8px, ไม่มี decorative shadow; overlay ลอยด้วยเงาเฉพาะชั้นที่ทับเนื้อหา ✅ 2026-08-23
- [x] **VP2 content grouping** — `Section` ที่เป็นกลุ่มหลักและ `DataTable` กลับมาอยู่ใน panel; หัว panel/หัวตาราง/แถวใช้ divider ต่อเนื่อง · bounded card ลูกใช้เฉพาะ object จริงและห้ามเกิด nested card ที่ไม่ช่วยความหมาย ✅ 2026-08-23
- [x] **VP3 navigation + controls** — Sidebar active ใช้ neutral filled row แบบ reference ไม่ใช้แถบน้ำเงิน; น้ำเงิน Anajak ยังสงวนให้ primary/link/focus · filter แบบข้อความและ field/control contract เดิมไม่ถอย ✅ 2026-08-23
- [x] **VP4 responsive + Dark** — desktop/tablet/mobile ใช้ panel เดียวกันโดยไม่ล้น; mobile record card ยังแตะ ≥44px และ spacing ไม่แน่นเพราะ card ซ้อน · Dark กลับพื้น/card/border/hover ครบ ✅ 2026-08-23
- [x] **VP5 verify + handoff** — ตรวจ `/`, `/orders`, `/production`, `/billing`, `/settings`, `/products`, `/factory/station` ที่ 1440×900 + 390×844 Light/Dark พร้อม hover/focus/loading/error/empty · typecheck/lint/unit/verify:ui/Impeccable detector/build ผ่าน อัปเดต SPEC/DESIGN/PROGRESS/surface brief และ commit ก้อนเล็ก ✅ 2026-08-23

#### ใบงาน VERCEL-CONSISTENCY — เก็บส่วนที่ยังพูดคนละภาษา (เบสสั่งต่อ 2026-08-23)

> audit จอจริงพบว่าหน้าหลักใช้ panel 8px ไร้เงาแล้ว แต่ใบผลิต, Station, mobile record link, alert/context และ loading บางชุดยังค้างมุม 12–16px หรือ `shadow-sm` จาก visual system รุ่นก่อน · งานนี้แก้ presentation contract ที่ component กลางและ surface เฉพาะเท่านั้น ไม่เปลี่ยน flow, query, mutation, permission, status transition, public/print contract หรือ no-money boundary

- [x] **VC1 primitive rhythm เดียว** — `Card`, `Section`, `StatCard`, `ListCard`, `Alert`, `ContextPanel`, `AddCard`, skeleton และ pagination ใช้มุม/spacing/divider ตาม Vercel Panel System; pill/overlay/รูปทรงเฉพาะหน้าที่ยังคงได้เมื่อมี semantic จริง ✅ 2026-08-23
- [x] **VC2 production + Station ไม่เป็น visual island** — panel หลักของ production control/detail และ Station ใช้ 8px + border semantic + ไม่มี decorative shadow โดยคงโครง current/ready/blocked, เป้ากด, sticky action และ no-money เดิม ✅ 2026-08-23
- [x] **VC3 record/list consistency** — ลิงก์ครอบ card, mobile record, alert และ context panel ไม่เผย radius 12–16px รอบ card 8px; table/list divider และ padding ใช้ token กลาง ✅ 2026-08-23
- [x] **VC4 responsive + states** — ตรวจ desktop 1440, tablet 640/1024 และ mobile 390 ทั้ง Light/Dark; loading/empty/error/disabled/hover/focus ไม่กระโดดข้ามภาษา และไม่มี overflow/card ซ้อน ✅ 2026-08-23
- [x] **VC5 verify + handoff** — browser representative ครบ dashboard/orders/order detail/create/production/detail/billing/settings/stock/products/detail/quotations/detail/outsource/Station · typecheck/lint/unit/verify:ui/detector/build ผ่าน อัปเดต SPEC/DESIGN/PROGRESS/surface brief และ commit แยกก้อน ✅ 2026-08-23

#### ใบงาน VERCEL-EDGE-SURFACES — จอโรงงานและ dialog หลังบ้าน (เบสสั่งต่อ 2026-08-23)

> audit รอบสองพบ visual island ที่ยังไม่อยู่ในเส้นทางหน้าหลัก: Factory TV, dialog การเงิน/ส่งของ/ตรวจรับ/ผลิต, picker และ sync state บางชุดยังใช้มุม 12–16px หรือสี slate เขียนตรง · แก้เฉพาะ presentation ของหลังบ้านและจอโรงงาน ไม่เปลี่ยน query, mutation, permission, validation, status transition, payload/no-money หรือ public/print ซึ่งรอเบสตัดสินใจแยก

- [x] **VE1 Factory TV geometry** — stage, rail, loading และ error ใช้มุม 8px/semantic border เดียวกับ Production/Station โดยคง dark-first, 5 work center, 30-second refresh และ read-only/no-money ✅ 2026-08-23
- [x] **VE2 operational dialogs** — create invoice/payment/delivery/production/outsource, goods receipt, order info และ sync state ใช้ `TINT`/surface/text/divider token กลาง; panel ย่อย 8px ส่วน dialog/sheet ที่ลอยจริงคง overlay elevation ✅ 2026-08-23
- [x] **VE3 picker + file interaction** — product picker/add-product/file upload และ attachment hover ใช้ semantic state, 8px และไม่สร้าง decorative shadow; ปุ่มลบวงกลม/overlay control คง elevation ได้เพื่อแยกจากรูป ✅ 2026-08-23
- [x] **VE4 responsive + states** — ตรวจ Factory TV, billing create dialog และ order form picker เป็น representative ของจอแขวน/overlay/form ที่ desktop-mobile ทั้ง Light/Dark พร้อม Escape/focus return และไม่มี overflow; dialog ผลิตชุดเดียวกันตรวจ source/guard ครบ ✅ 2026-08-23
- [x] **VE5 verify + handoff** — typecheck/lint/unit/verify:ui/Impeccable detector/build ผ่าน อัปเดต DESIGN/PROGRESS และ commit แยกก้อน โดยไม่รวม public/print หรือไฟล์ค้างของเบส ✅ 2026-08-23

#### ใบงาน VERCEL-PUBLIC-PRINT — หน้าลูกค้าและเอกสารจริง (เบสสั่งต่อ 2026-08-23)

> audit จอจริงแยกหน้าที่ชัด: public token เป็นหน้าใช้งานของลูกค้า/ร้านนอก จึงต้องรับ panel 8px, neutral masthead และ semantic state ชุดเดียวกับระบบ; เอกสารพิมพ์เป็นกระดาษ A4 ทางธุรกิจ ไม่ใช่ app card จึงคง grayscale, document hierarchy, ความกว้างกระดาษ และกฎหมายเดิม โดยยอมให้มีเงาเฉพาะ preview บนจอและต้องถอดออกตอนพิมพ์

- [x] **VPB1 public masthead** — ถอดแถบสี, icon tile สี, มุม 14px และ shadow ออกจาก `PublicPageShell`/error recovery; คง title/subtitle/footer/blind-ship/noindex/token boundary เดิม ✅ 2026-08-23
- [x] **VPB2 public content rhythm** — quote/status/upload/approve/job ใช้ semantic text/divider/surface, panel ย่อย 8px และ neutral hover; สีเหลือเฉพาะ primary action, focus และสถานะจริง ✅ 2026-08-23
- [x] **VPB3 print contract** — ตรวจ quotation/invoice/billing note/packing list/job ticket ว่ายังคง A4, light-only, grayscale-safe, print CSS, ยอด/ภาษี/กฎหมาย/ลำดับหน้า และ document marker เดิม; ไม่ restyle เป็น dashboard card ✅ 2026-08-23
- [x] **VPB4 responsive + states** — ตรวจ public representative ด้วยข้อมูลจริง/error/action controls ที่ desktop/mobile พร้อม overflow และ touch target; print desktop ครบเอกสารที่มีข้อมูลจริง และ production build ที่ 390px กักการเลื่อน A4 ไว้ใน preview; blind-ship ตรวจจาก source/guard โดยไม่สร้าง token ใหม่ ✅ 2026-08-23
- [x] **VPB5 verify + handoff** — typecheck/lint/unit/verify:ui/Impeccable detector/build ผ่าน อัปเดต SPEC/DESIGN/PROGRESS และ commit แยกก้อนโดยไม่รวมไฟล์ค้างของเบส ✅ 2026-08-23

#### ใบงานต้นแบบ ERP Command Center — ออกแบบใหม่ทั้งระบบแบบไม่ตีกรอบ (เบสสั่ง 2026-08-14)

> พิสูจน์ “ภาษาใหม่ทั้งระบบ” จากของจริงก่อนรื้อหน้าหลัก: แนว **สายการผลิตบนกริด** (Swiss industrial manual + ใบสั่งงานโรงงาน) และน้ำเงิน Anajak `#3973b2` · เว็บอนาคตเป็นอีกช่องทางรับออเดอร์เข้าสู่วงจรเดียว ไม่สร้างหลังบ้าน POD แยก
>
> ขอบเขต: route ทดลอง `/redesign` + shell/navigation + owner command center + drill-through ไป flow จริง · ใช้ auth/query/permission/navigation registry/service เดิม · ไม่แตะ schema, migration, dependency, config, business logic, public/print/factory หรือ URL หลัก

- [x] **world + composition มีหลักฐาน** — เก็บ direction sketch + 3 comps ใน `.impeccable/` · เลือก Flow Matrix พร้อมเหตุผล/ความเสี่ยงและ design contract ที่ตรวจย้อนหลังได้
- [x] **prototype แยกและปลอดภัย** — `/redesign` มี auth ชั้นเดียวกับหลังบ้าน แต่ใช้ shell/presentation แยก · มีทางกลับระบบหลัก · ไม่มีลิงก์หรือ action ปลอม
- [x] **exception-first ด้วยข้อมูลจริง** — งานเสี่ยง/งานค้างและ action หลักมาก่อนสถิติ · flow matrix ใช้ข้อมูลจริงจาก analytics/permission เดิม · เงิน/เมนู fail closed ตามสิทธิ์
- [x] **navigation พร้อมขยายช่องทาง** — เมนูจัดตาม วันนี้/งานขาย/ปฏิบัติการ/การเงิน/ทรัพยากร/ระบบ · ค้นหาและ drill-through กลับ route จริง · ช่องทางเว็บในอนาคตเป็น source ของออเดอร์เดิมเท่านั้น
- [x] **responsive + accessibility** — 390px และ 1440px ไม่มี horizontal overflow · mobile เป็น bottom nav + priority queue ไม่ย่อตาราง desktop ลงจอ · เป้ากด ≥44px · focus/reduced-motion/semantic landmarks/ภาษาไทยครบ
- [x] **verify ก่อนให้เคาะทิศ** — typecheck, lint, unit, `verify:ui`, build และ browser inspection desktop/mobile ผ่าน · loading/error/retry/empty แยกชัด · อัปเดต `docs/DESIGN.md`/surface brief/`PROGRESS.md` และ commit ก้อนเล็ก

#### ใบงานต่อยอดต้นแบบ — Order Workbench ที่บอกงานถัดไปก่อนข้อมูลประกอบ (เบสเคาะให้ทำต่อ 2026-08-14)

> พิสูจน์ว่า visual world ของ Command Center ใช้กับ record ที่ซับซ้อนได้จริง โดยเพิ่ม `/redesign/orders/[id]` ใต้ shell เดิมและเชื่อมจากแถวออเดอร์ในต้นแบบ · route หลัก `/orders/[id]` ยังเป็น canonical สำหรับ mutation/เอกสาร/ฟอร์มลึกทั้งหมด เพื่อลดการสร้าง controller ธุรกิจซ้ำระหว่างช่วงทดลอง
>
> ขอบเขต: presentation + pure view model จาก `order.getById`, `user.me`, permission/status/next-step helper เดิม · ไม่เพิ่ม schema, endpoint, mutation, dependency, config หรือ business rule · ทุก CTA ต้องเปิด canonical record/แท็บที่ทำงานจริง ไม่มี action จำลอง

- [x] **O1 route จริงและ fail closed** — auth ผ่าน layout เดิม · โหลด record/สิทธิ์จริง · not-found/loading/error/retry แยกกัน · เงินและข้อมูลทุนไม่ปรากฏเมื่อไม่มีสิทธิ์
- [x] **O2 งานถัดไปเป็นพระเอก** — first viewport บอกสถานะปัจจุบัน เหตุที่ต้องสนใจ และขั้นถัดไปหนึ่งเรื่องจาก `getOrderNextStep` เดิม · แสดง lifecycle 7 ช่วงโดย derive เลน DTF/ร้านนอกจากรายการจริง ไม่เดาสถานะหรือแต่งข้อมูล
- [x] **O3 อ่านออเดอร์โดยไม่ไล่หลายแท็บ** — desktop จัด work brief + สเปก/จำนวน + production/delivery/finance snapshot ตามสิทธิ์ · mobile เรียง action → คำเตือน → สรุปที่จำเป็น และส่งงานลึกไป canonical tab ที่ตรงเรื่อง · เป้ากด ≥44px ไม่มี horizontal overflow
- [x] **O4 เชื่อมประสบการณ์และมีทางกลับ** — แถวออเดอร์ใน Command Center เปิด workbench ใหม่ · breadcrumb/ปุ่มเปิดรายละเอียดเต็มกลับ canonical URL จริง · query string ของแท็บใช้ contract `order-tabs` เดิม
- [x] **O5 ด่านก่อนส่งให้เคาะ** — pure tests ครอบ lifecycle/route mapping/permission-safe model · typecheck, targeted lint, unit, `verify:ui`, Impeccable detector และ build ผ่าน · browser 1440/390 ทั้ง Light/Dark ไม่มี hydration/console/overflow · finish review + อัปเดต `docs/DESIGN.md`, surface brief, `PROGRESS.md` และ commit ก้อนเล็ก

#### ใบงานต่อยอดต้นแบบ — Connected Operations: ทะเบียนออเดอร์ → ศูนย์ควบคุมการผลิต (เบสสั่ง “เอาให้สุด มีข้อมูล กดได้จริง” 2026-08-14)

> ทำให้ต้นแบบไม่ใช่ภาพหน้าเดียว แต่เดินงานหลักได้เป็นสายจาก Command Center → Orders Registry → Order Workbench → Production Control/flow หลัก · ใช้ query, permission, status/readiness และ URL จริงชุดเดิม โดยคง canonical `/orders*` และ `/production*` เป็นเจ้าของ mutation, dialog และกฎธุรกิจระหว่างช่วงทดลอง
>
> ขอบเขต: เพิ่ม `/redesign/orders` และ `/redesign/production` ใต้ auth/shell เดิม + pure presentation model/test + เชื่อม navigation/drill-through · ไม่เพิ่ม schema, endpoint, mutation, dependency, config หรือ business rule · เว็บ self-serve อนาคตยังเป็นแหล่งออเดอร์เดียวกัน ไม่สร้างหลังบ้านคู่ขนาน

- [x] **C1 Orders Registry ใช้ข้อมูลจริงครบวงจร** — `order.list` เดิมรองรับค้นหา, สถานะ, ความเร่งด่วน, ช่องทาง, ประเภท, เรียง, pagination และ status count · เงิน fail closed ตาม `see_order_money` · URL filter แชร์/ย้อนกลับได้ · ทุกแถวเปิด `/redesign/orders/[id]` และปุ่มเปิดงานใหม่ไป canonical form จริง
- [x] **C2 Production Control เห็นข้อยกเว้นก่อนกำลังงาน** — `production.kanban` + `user.me` เดิมสรุปงานติดด่าน/เลยกำหนด/มีปัญหา/งานของฉัน/คิวรอเปิดใบผลิต/เลน DTF-ร้านนอก-QC-แพ็ค-ส่ง โดยใช้ readiness ที่ sanitize แล้ว · ไม่คำนวณหรือเปิดเผยเงินใหม่ · HEAT_PRESS ใช้ `evaluateHeatPressGate` ชุดเดียวกับหน้า canonical และไม่นับงานที่ยังรอเสื้อ/ฟิล์มว่าเริ่มได้
- [x] **C3 ทุก control มีผลจริง** — lane/lens/filter เปลี่ยนรายการจริง, retry/refetch ทำงาน, Orders Registry → Workbench → Production Control ต่อกันในต้นแบบ · action ลึกไป canonical `/production?create=`, `/production/[id]`, `/orders/[id]?tab=` ที่มี dialog/mutation/server guard จริง ไม่มีปุ่มจำลอง
- [x] **C4 responsive เป็นคนละ composition** — desktop ใช้ registry table + factory control board; mobile เรียง exception → lens/flow → work cards ไม่ย่อตารางกว้างลงจอ · เป้ากด ≥44px, keyboard/focus/aria-live, reduced-motion, loading/error/empty และ no-results ครบ
- [x] **C5 shell และ entry points ไม่หลุดโลก** — sidebar/bottom nav/ลิงก์ดูทั้งหมดใน Command Center พาเข้า surface `/redesign` ที่มีแล้ว; เมนูส่วนที่ยังไม่ redesign ไป canonical ตามเดิมและมีทางกลับระบบหลักชัด
- [x] **C6 ด่านก่อนส่งให้เคาะ** — pure tests, typecheck, targeted lint, unit, `verify:ui`, Impeccable detector หนึ่งรอบและ build ผ่าน · browser เดิน click journey จริงที่ 1440×900 Light + 390×844 Dark ไม่มี hydration/console/overflow · finish review HOLD 1 P1 + 1 P2 แล้วแก้ครบและยืนยัน SHIP · screenshots + `docs/DESIGN.md`, surface brief, `PROGRESS.md` และ commit ก้อนเล็ก

#### ใบงาน PROD-C — ยกโมดูลผลิตทั้งชุดเป็น "ทิศ C ผสม" (เบสเคาะจาก mockup 2026-08-15 "เอา C")

> เบสสั่ง "ทำ UXUI หน้าการผลิตใหม่" แล้วเคาะทิศจาก mockup กดเล่นได้ 3 ทิศ (artifact `0ac53352-9706-4963-b522-149ad8d1c3cf`) · ทิศ C = แถบสายงานของทิศ A ยุบเหลือแถวเดียวทำหน้าที่ **ตัวนำทาง** (ตอบ "โรงงานตันตรงไหน") + รายการเรียงตามวันส่งของทิศ B ทำหน้าที่ **เนื้องาน** (ตอบ "อะไรจะไม่ทัน") · เลือกสายใดสายหนึ่ง → รายการเปลี่ยนเป็นการ์ดหน้างานแบบ A เพราะคำถามเปลี่ยนเป็น "ตรงหน้าฉันต้องทำอะไร"
>
> **จอจริงที่ต้องคุม (เบสยืนยัน 2026-08-15):** คอมของเบส/หัวหน้า + จอทัชตั้งในโรงงาน — **ไม่ใช่มือถือช่าง** (ทับสมมติฐาน mobile-first เดิมของหน้า ops) · มือถือยังต้องใช้ได้ที่ 390px ไม่ล้มแนวนอนตาม SPEC แต่ไม่ใช่ตัวตั้ง
>
> **ห้ามผสมสองแบบนี้ (เคาะแล้วว่าพัง):** ① บอร์ดบน + รายการล่างในหน้าเดียว = งานชุดเดียวโผล่สองที่ ซ้ำปัญหาหน้าปัจจุบัน ② ปุ่มสลับ "บอร์ด/รายการ" = บังคับตัดสินใจก่อนได้เห็นงาน และหน้าเดิมเพิ่งถอดสวิตช์แบบนี้ออกไป
>
> ขอบเขต: `/production` · `/production/[id]` · `/production/print-runs` · **ทำทีละหน้า คนละก้อน คนละคอมมิต ห้ามรวบ** · ไม่แตะ schema, endpoint, mutation, permission หรือกฎสถานะ — `isValidTransition`, guard QC, ใบตรวจรับร้านนอก และ `evaluateHeatPressGate` ใช้ของเดิมทั้งหมด · `/production/films` อยู่คนละเมนู ไม่อยู่ในก้อนนี้

- [x] **PC1 หน้ารวม `/production`** ✅ 2026-08-15 (⏳ เหลือเบสเปิดจอจริงยืนยัน — ฉัน login ไม่ได้) — pure model `lib/production-board.ts` + 16 unit test · component `production-board-view.tsx` · หน้า page.tsx ใช้ `useListPageState` (`?lane=` + `?q=`) · แก้ครบ 3 บั๊ก: (ก) รายการงานแสดงทุกสถานการณ์ ไม่ถูกตัดเมื่อมีงานเลยกำหนด (ข) สาย+คำค้นอยู่ใน URL รีเฟรช/Back/ส่งลิงก์/ตั้งจอค้างได้ (ค) ปุ่มลงมือ gate ด้วย `manage_production` รายจุด ช่างเห็นและกดได้ · mutation/permission/กฎสถานะชุดเดิมทั้งหมด ไม่แตะ server · **ค้าง**: `production-command-center.tsx` และ `lib/production-overview.ts` ไม่มี caller แล้ว รอเบสอนุญาตให้ลบ
  - เกณฑ์ที่ยึด: ต้องแก้ก่อน → แถบสายงานเรียงตามทางเดินงาน → รายการตามวันส่ง (เลยกำหนด/วันนี้/พรุ่งนี้/ใน 7 วัน/หลังจากนั้น/ยังไม่กำหนด) · แถบสายบอกจำนวน + จำนวนเลยกำหนดต่อสาย และบอกตรง ๆ ว่างานผสมนับหลายสาย · แถวมี rail ไล่สาย 6 จุด + จุดงานทุกจุด + ปุ่มลงมือตามสิทธิ์จริง
  - ด่านที่ผ่านแล้ว: typecheck · lint 0 error · unit **840/840** (เพิ่ม 16) · `verify:ui` ผ่านครบ · production build ผ่าน · smoke render ผ่าน `npx tsx` 3 บทบาท ยืนยันช่างเห็นปุ่มลงมือ ช่างไม่เห็นงานติดด่าน ธง blind ship ขึ้นตอนถึงแพ็ค และไม่มีสัญลักษณ์เงินบนหน้าผลิต
  - ⏳ ยังไม่ผ่าน: เปิดจอจริง 1440/390 Light+Dark — หน้าอยู่หลัง login และฉันกรอกรหัสผ่านไม่ได้ ต้องให้เบสเปิดดูเอง
- [x] **PC1.1 ใช้ตัวกรองชุดเดียวกับหน้าออเดอร์ + ภาพรวมเป็นตาราง** ✅ 2026-08-15 (เบสทัก "filter ใช้อันเดียวกันกับหน้าออเดอร์ได้มั้ย · แสดงแบบตารางจะดูง่ายกว่ามั้ย") — ยกหน้าตาแถบสถานะออกจาก `orders/order-status-flow-bar.tsx` มาเป็น primitive กลาง `ui/flow-filter-bar.tsx` แล้วให้ทั้งหน้าออเดอร์และหน้าผลิตใช้ตัวเดียวกัน · หน้าผลิตส่ง "สาย" เข้าไปแทน "สถานะ" พร้อมหัวช่วง รอเปิดใบผลิต/สายผลิต/หลังผลิต และป้ายจำนวนเลยกำหนดต่อสาย · **ภาพรวมทั้งโรงงานเปลี่ยนเป็น `ResponsiveList` + `DataTable`** (desktop ตาราง 7 คอลัมน์ กดทั้งแถวได้ · mobile การ์ด) ชุดเดียวกับอีก 9 หน้าในระบบ ส่วนโหมดเลือกสายยังเป็นการ์ดหน้างานสำหรับจอทัช เพราะกรองแล้วเหลือจุดงานเดียวต่อออเดอร์
  - **พิสูจน์ว่าหน้าออเดอร์ไม่ขยับ:** render `OrderStatusFilter` เป็น HTML ก่อน/หลัง refactor 3 สถานะ (ไม่เลือก · PRODUCING · ON_HOLD) แล้ว diff — **ตรงกันทุกไบต์** · ระหว่างทางจับได้ 3 จุดที่เผลอเปลี่ยน (ลำดับ class, aria-label ของจอแคบที่เคยต่างจากจอกว้าง, โครง DOM ที่ป้ายเตือนแทรกเข้ามา) แก้จนเหมือนเดิมหมด
  - `scripts/verify-ui-tokens.tsx` guard 2 ข้อชี้ไฟล์เดิมอยู่ จึงตามไปตรวจที่ primitive กลาง — เจตนาการตรวจเท่าเดิม ไม่ได้ผ่อนเกณฑ์
  - ด่าน: typecheck · lint 0 error · unit **840/840** · `verify:ui` ผ่านครบ · build ผ่าน · smoke render ยืนยันโหมดทั้งโรงงานเป็นตารางครบคอลัมน์และปุ่มลงมืออยู่ในแถว ส่วนโหมดเลือกสายไม่มี `<table>`
- [x] **PC1.2 เปลี่ยนเป็นบอร์ดโรงงาน (ทิศ A)** ✅ 2026-08-15 (เบสเปลี่ยนใจจากทิศ C — "เอาแบบ A บอร์ดโรงงานดีกว่า" · เคาะเพิ่ม: **บอร์ดล้วนทั้งหน้า ไม่มีโหมดอื่น**) — คอลัมน์ = สถานีจริงเรียงตามทางเดินงาน · การ์ดในคอลัมน์เรียงตามกำหนดส่ง · เปิดจอเดียวเห็นทั้งโรงงานว่ากองอยู่ตรงไหน
  - **ถอดออกตามมติ:** ตาราง `ResponsiveList`/`DataTable` และโหมดสลับการ์ด (ของ PC1.1) · แถบสายงานกลางบนหน้าผลิต (ซ้ำกับหัวคอลัมน์แล้ว) — **`ui/flow-filter-bar.tsx` ยังอยู่และหน้าออเดอร์ยังใช้อยู่** งานรวม filter จึงไม่สูญ แค่หน้าผลิตไม่ได้ใช้
  - **กดหัวคอลัมน์ = โฟกัสสายนั้นสายเดียว** เก็บใน URL `?lane=` — ยังเป็นบอร์ดเหมือนเดิม แค่เหลือคอลัมน์เดียวและการ์ดขยายเป็น grid · จอทัชที่โต๊ะรีดตั้งค้างที่สายตัวเองได้
  - จอแคบไม่ยัดบอร์ดแนวนอน — คอลัมน์ซ้อนลงล่างเป็นสถานีละช่วง ไม่ต้องปัดสองแกน · บอร์ดเลื่อนแนวนอนในกรอบของตัวเอง หน้าไม่เลื่อนซ้ายขวาตาม
  - การ์ดมี: เลขออเดอร์ · ป้ายด่วน/เลยกำหนด/ส่งวันนี้ · ลูกค้าและงาน · กำหนดส่ง + จุดไล่สาย 6 จุด · ธง blind ship (เฉพาะแพ็ค/หลังผลิต) · ขั้นปัจจุบัน + จำนวน · ป้าย "เดินอีก n สาย" สำหรับงานผสม · ปุ่มลงมือเต็มความกว้าง
  - `buildBoardColumns` เป็น pure function มี test 3 เคส (งานผสมโผล่ทุกคอลัมน์ · การ์ดเรียงตามกำหนดส่ง · คอลัมน์เรียงตามทางเดินงาน) · ลบ `BOARD_BUCKETS`/`groupJobsByBucket` ที่ไม่มีใครใช้แล้วออกพร้อม test ของมัน ไม่ทิ้ง dead code
  - ด่าน: typecheck · lint 0 error · unit **842/842** · `verify:ui` ผ่านครบ · build ผ่าน · smoke render 3 บทบาทยืนยันคอลัมน์ 4/1/3 ใบตามสิทธิ์และตัวกรอง ปุ่มลงมือครบทุกชนิดงาน ธง blind ship ขึ้น กล่อง "รอของ" แทนปุ่มเมื่อลงมือไม่ได้ ไม่มี `<table>` หลงเหลือ และไม่มีสัญลักษณ์เงิน
  - ⏳ ยังไม่ผ่าน: เปิดจอจริง — ติด login เหมือนเดิม ต้องให้เบสเปิดดูเอง

- [x] **PC2 ใบผลิต `/production/[id]`** ✅ 2026-08-15 — เพิ่มกล่อง **“ตอนนี้ต้องทำ”** บนสุด ปุ่มใหญ่เต็มความกว้างบนจอแคบ/จอทัช · pure `selectNowSteps()` ใน `lib/production-step-actions.ts` + test 9 เคส — งานผสมคืนได้หลายขั้น (เลนละไม่เกินหนึ่ง) · ขั้นที่ลงมือไม่ได้บอกเหตุผลแทนปุ่ม (รอฟิล์ม/รอเสื้อ · อยู่ในรอบพิมพ์ · งานคนอื่น · อยู่ที่ร้านนอก · QC ไม่ผ่าน · มีปัญหา) · หัวใบเหลือสิ่งที่ช่างใช้จริง (กำหนดส่ง/ด่วน/จำนวน/ความคืบหน้า) — ถอด “สถานะออเดอร์” และ % ที่ซ้ำกับ n/N ออก · `ProductionStepsList` เดิมไม่ถูกแตะ (mutation/dialog/gate/สิทธิ์ ชุดเดิมทั้งหมด) · ด่าน: typecheck · lint 0 error · unit **853/853** · `verify:ui` ผ่านครบ · build ผ่าน · smoke render 5 เคสยืนยันปุ่มถูกชนิดทุกเคส · PC4 ตรวจ browser จริง desktop/mobile Light/Dark แล้ว
- [x] **PC3 รอบพิมพ์ฟิล์ม `/production/print-runs`** ✅ 2026-08-15 — รอบที่เครื่องกำลังพิมพ์อยู่บนสุดเห็นทันที → รอตัดแยก+ติดป้าย → คิวพิมพ์เรียงตามกำหนดส่งพร้อมเลือกเข้ารอบ · desktop วางคิวกับรอบให้เห็นพร้อมกัน เพราะช่างจัดม้วนต้องดูสองอย่างคู่กัน
  - ลำดับข้อมูล/DOM ต้องเป็น **กำลังพิมพ์ → รอตัดแยกและติดป้าย → คิวพิมพ์ → ประวัติ** · ห้ามรวม `PRINTING` กับ `PRINTED` เป็นก้อนเดียวจนรอบใหม่กว่าดันงานบนเครื่องลงล่าง
  - จอหัวหน้า/จอทัช desktop วางสองช่วงรอบไว้ฝั่งหลักและคิวไว้ข้างกันใน first workspace · จอแคบเรียงลงคอลัมน์เดียวตามลำดับเดิม ไม่มี scroll สองแกน
  - คิวคงลำดับกำหนดส่งจาก service, thumbnail/เวอร์ชันลาย, เลือกจำนวนเข้ารอบ และ selection action เดิม · loading/error/empty/read-only แยกจริง
  - รูปลายเป็นลิงก์เปิดไฟล์แยกจากปุ่มเลือกแถว ห้ามมี interactive element ซ้อนกัน · เป้ากดจอทัช ≥44px ทั้งคู่
  - presentation-only: คง polling/refetch, query, mutation, permission และสถานะ `PRINTING → PRINTED → COMPLETED` ชุดเดิมทั้งหมด · ประวัติอยู่นอก workspace หลักเต็มความกว้าง
  - **ปิดงาน:** แยก `PRINTING`/`PRINTED` ด้วย pure model + test รักษาลำดับในแต่ละช่วง · desktop/laptop เป็นสองฝั่ง 4:6 และขยายเป็น 5:7 บนจอกว้าง ให้รอบกับคิวอยู่ first viewport ส่วน mobile ใช้ DOM ลำดับเดียวและประวัติเต็มแถวชั้นรอง · selection bar อยู่ในบริบทคิวและตามเห็นเมื่อเลือก พร้อมทาง keyboard จากแถวที่เลือก · permission รอโหลด/พัง/ดูอย่างเดียวแยกจริง · รูปลายเป็น sibling ของปุ่มเลือกและ control ที่แตะใน flow นี้สูงอย่างน้อย 44px
  - **ด่าน:** typecheck · lint 0 error (28 warning เดิมนอกก้อน) · unit **855/855** · `verify:ui` ผ่านและเพิ่ม source guard กันลำดับ/layout/a11y ถอย · Impeccable layout detector `[]` · production build ผ่าน · browser จริง 1440×900 + 1180×800 + 1024×800 + 390×844 Light/Dark และ 320px ไม่มี horizontal overflow, app console error หรือ hydration บน origin สด · laptop/desktop เห็นรอบกับคิวพร้อมกันจริง · ฐาน dev ไม่มีคิว/รอบ จึงตรวจ browser เฉพาะ empty state และไม่รัน `verify:printrun` ที่เปลี่ยนข้อมูลบนฐานแชร์; transition/integration เดิมคงครอบด้วย test ฝั่ง service
- [x] **PC4 ด่านก่อนเคลมเสร็จ (ทุกก้อน)** ✅ 2026-08-15 — typecheck, lint, unit, `verify:ui`, Impeccable detector และ build ผ่าน · browser จริง 1440×900 + 390×844 ทั้ง Light/Dark: ไม่มี horizontal overflow, console error, hydration หรือ action ทับกัน · เป้ากดจอทัช ≥44px · keyboard focus/aria/reduced-motion ครบ · loading/error/empty/ไม่มีสิทธิ์ แยกกันจริง · อัปเดต `docs/DESIGN.md`, surface brief, `PROGRESS.md` และ commit ก้อนเล็ก
  - **หลักฐาน:** cold origin เปิด `/production` (ข้อมูลจริง 20 ออเดอร์), `/production/cmq9xj6c40001vchwhuxdeded` (`ORD-2606-0001`) และ `/production/print-runs`; console ไม่มี app error, API ทุกก้อนตอบ 200, `scrollWidth=clientWidth` และ control ที่มองเห็นบน 390px ไม่ต่ำกว่า 44×44px
  - **state ที่ฐานแชร์ไม่มีข้อมูลจริง:** print-run populated/read-only ใช้ SSR fixture ไม่ต่อ DB ครบ manager + คิว 24 งาน + selection + `PRINTING`/`PRINTED`/history และ read-only ที่ไม่มี mutation control · ตั้งใจไม่รัน `verify:printrun` และไม่คลิก action ที่เขียนข้อมูล
  - **ด่านโค้ด:** typecheck ผ่าน · lint 0 error (28 warning เดิม) · unit **855/855** · `verify:ui` ผ่าน · detector `[]` · production build ผ่าน

#### ใบงาน MOCK-PROD — ต้นแบบ ERP ฝ่ายผลิตทั้งระบบ (เบสสั่ง 2026-08-15)

> เป้าหมาย: ให้เบสลอง UX ทั้งสายก่อนแตะ canonical `/production*` จริง · ผู้ใช้หลักคือหัวหน้าบนคอมและพนักงานบนจอทัช ไม่ใช่มือถือ · ทางเข้าหลักคือเลขออเดอร์ และ QR เป็นทางลัดเปิดใบผลิตเดียวกันโดยไม่เปลี่ยนสถานะทันที
>
> ขอบเขต: standalone clickable mockup ที่ `docs/mockups/production-erp-system-2026-08-15.html` ใช้ข้อมูลตัวอย่าง/local state เท่านั้น · **ไม่ใช้หรือแตะ `/redesign`** · ไม่ต่อ tRPC/ฐานข้อมูล ไม่เพิ่ม route/schema/API/dependency/config และไม่แก้หน้า production จริงจนกว่าเบสจะเคาะ
>
> ลำดับจริงที่ mockup ต้องยึด: **ผลิต → QC → แพ็กเพื่อจัดส่งครั้งเดียว**; ห้ามวาด PACK ก่อน QC ซ้ำจากความกำกวมใน state เดิม

- [x] **MP1 ศูนย์ควบคุม + คิวสถานี** — ค้นเลขออเดอร์/จำลอง QR, งานต้องแก้ก่อน, สายงาน/สถานี, คิวพร้อมทำ และโหมดโฟกัสจอทัชโดยไม่ทำบอร์ดกับรายการซ้ำกัน
- [x] **MP2 ใบผลิต + work center ครบสาย** — งาน DTF, ร้านนอก, เบิกเสื้อ/วัตถุดิบ, รอบพิมพ์/ตัดติดป้าย, QC/rework และแพ็กส่ง; หน้าหนึ่งมี action หลักเดียวและบอกเหตุผลเมื่อยังทำไม่ได้
- [x] **MP3 scenario ที่ต้องกดพิสูจน์** — DTF ปกติจนแพ็ก, งานผสม DTF+ร้านนอก, งานติด readiness, จำนวนบางส่วน, QC fail→แก้/hold และ read-only; ทุก action ติดป้ายชัดว่าไม่บันทึกจริง
- [x] **MP4 ด่านก่อนส่งให้เคาะ** — เปิดจริง 1440×900 + 1024×768 (จอทัช) Light/Dark, keyboard/QR Enter, เป้ากด ≥44px บน coarse pointer, ไม่มี horizontal overflow/console error และ Impeccable detectorผ่าน; อัปเดต `PROGRESS.md` แล้ว commit mockup แยกก้อน
  - **หลักฐาน:** browser เดิน DTF → QC บางส่วน/ครบ → แพ็ก, QC fail → งานแก้ → ตรวจซ้ำ, งานร้านนอกส่ง/รับกลับ, role read-only และ QR/search จริง; 10 หน้าที่ 1440px + หน้าหลักที่ 1024px ไม่ล้น, 390px regression ผ่าน, console warning/error `[]`, contrast ปุ่มหลักโหมดมืด 4.92:1
  - **ขอบเขตคงเดิม:** ไฟล์ UI standalone + ภาพ review เท่านั้น ไม่มี `/redesign`, API/tRPC/DB/schema/config/dependency หรือ canonical production diff
- [x] **MP5 แก้ shell หลังเบสทักว่าเหมือนระบบแยก** — ใช้กรอบ `AppShell` เดิมจริง: topbar 64px + sidebar ERP 240px (`แดชบอร์ด / งานของฉัน / ออเดอร์ / การผลิต / ลูกค้า / เมนูทั้งหมด`) และเลือก “การผลิต” ตลอด · ย้าย `ภาพรวม / คิวสถานี / DTF / ร้านนอก / QC / แพ็ก / ผลงาน` เป็นแถบภายในเนื้อหา · ใบผลิต/เบิกวัตถุดิบ/งานแก้เปิดตามบริบท ไม่เป็นเมนูถาวร · ที่ 1024 ยังใช้ sidebar 240px และไม่มี sidebar/bottom-nav ฝ่ายผลิตชุดใหม่
  - **หลักฐาน:** browser 1440×900 วัด topbar 64px, sidebar 240px, workspace 1200px; ที่ 1024×768 ยังเป็น sidebar 240px + workspace 784px · ทุกพื้นที่ 7 หน้าใช้ global “การผลิต” เดียว, ใบผลิต/วัตถุดิบไม่มี local tab ปลอมถูกเลือก, QR เปิด `ORD-2608-0041`, read-only ไม่มี field QC ที่แก้ได้, horizontal overflow = 0 และ visible enabled target ≥44px
  - **ด่าน:** inline script syntaxผ่าน · ไม่พบ `/redesign`, tRPC, `fetch()` หรือ `/api/` · `verify:ui` ผ่าน · final Impeccable detector `[]` · อัปเดตภาพ review 1440/1024 ทั้ง 9 ภาพให้ตรง shell ใหม่

#### ใบงาน FACTORY-STATION — โหมดจอประจำสถานีจาก mockup ที่เคาะ (เบสสั่งทำจริง 2026-08-16)

> เป้าหมาย: ใช้ ERP เดิมสำหรับหัวหน้า/เจ้าของ และเพิ่ม `/factory/station` เป็นจอปฏิบัติงานเต็มจอสำหรับพนักงานบนคอม/จอทัช · **ไม่เพิ่ม sidebar ใหม่** · คง `/factory` เป็นทีวีภาพรวม read-only · ทางเข้าอยู่บน `/production`
>
> ความจริงของ flow: **ผลิต → QC → แพ็กสุดท้าย → พร้อมส่ง** · สแกนเลขออเดอร์หรือ QR มีหน้าที่เปิดงานเท่านั้น ห้ามเปลี่ยนสถานะทันที · action ทุกตัวใช้ข้อมูล/permission/transaction จริงและต้องไม่มีข้อมูลเงินบน Station Mode
>
> ขอบเขตความปลอดภัย: ไม่เพิ่ม schema/dependency/config · ไม่เก็บ checklist/รูป/จำนวนกล่องแบบ local แล้วอ้างว่าบันทึกจริง · จอ interactive ต้องใช้บัญชีพนักงานจริงที่มี `manage_production`; บัญชีทีวีและผู้ไม่มีสิทธิ์เป็น read-only

- [x] **FS0 แก้กติกากลางก่อนเปิดจอจริง** — หยุดสร้าง `PACKAGING` ในใบผลิตใหม่และปิดขั้น legacy อย่างเข้ากันได้เมื่อขั้นผลิตอื่นเสร็จ · ห้ามแสดง/กดแพ็กก่อน QC · `PACKING → READY_TO_SHIP` ต้องมีหลักฐานแพ็กจาก Delivery และจำนวนครบเมื่อมีรายการไซส์ · mutation ผลิต/DTF/QC/เบิกเสื้อใช้ lock order เดียวกันและอ่านสถานะสด; regression ครอบการกดพร้อมกัน, retry และสถานะพัก/ยกเลิก
- [x] **FS1 Station shell + Scan-first** — `/factory/station` ไม่มี AppShell/sidebar แต่มีชื่อสถานี/ผู้ใช้/กลับ ERP · เลือกสถานี เตรียมเสื้อ / พิมพ์ DTF / รีดร้อน / QC / แพ็กสุดท้าย · ช่องสแกน autofocus รองรับ `ORD-...`, QR เก่า `/production/:id`, QR ใหม่ของ Station และ URL ออเดอร์ที่อนุญาต; ปฏิเสธ URL อื่น · ถ้ามีหลายใบผลิตต้องให้เลือก ไม่หยิบใบแรกเงียบ ๆ
- [x] **FS2 งานจริงร่วมกับ ERP เดิม** — แยก presentation/controller ใบผลิตให้ `/production/[id]` และ Station ใช้ mutation/permission/invalidation ชุดเดียวกัน · DTF พาเข้ารอบพิมพ์และ fail closed เมื่อออเดอร์พ้น `PRODUCING` · QC นับดี/เสียสะสมพร้อม idempotent retry · แพ็กใช้ `delivery.packContext` + Delivery จริงโดยไม่ส่งค่าจัดส่ง/เงิน แล้วจึงเปิด “พร้อมส่ง” · loading/error/empty/read-only/background stale แยกชัด
- [x] **FS3 ด่านก่อนเคลมใช้จริง** — typecheck · lint 0 error · unit 989/989 · `verify:ui` · production build ผ่าน · browser อ่านอย่างเดียว 1440×900 และ 1024×768 ใน Dark Station ไม่มี horizontal overflow/console/hydration error; ทางเข้าบน `/production` คง Light/Dark ของ ERP · coarse-pointer ≥44px และ keyboard/scan/focus ถูก pin ด้วย source/SSR/pure guards · role, scan เก่า+ใหม่+หลายใบ, QC partial/full/rework, pack ไม่ครบ/ครบ และ concurrency ครอบด้วย router/service tests โดยไม่สร้างข้อมูลบนฐานแชร์ · อัปเดต `SPEC.md`, `docs/DESIGN.md`, surface brief และ `PROGRESS.md` แล้ว

#### ใบงาน PRODUCTION-UX2 — รื้อ UX/UI การผลิตทั้ง family เป็นมาตรฐาน ERP (เบสสั่งทำจริง 2026-08-16)

> เหตุผลที่รื้อ: จอปัจจุบันปลอดภัยทาง business แล้วแต่ mental model ยังซ้ำ — Station มีตัวเลือกสถานี 2 ชุดและยก scan ใหญ่กว่างานจริง, บอร์ดหัวหน้าให้ออเดอร์ผสมโผล่หลายคอลัมน์, ใบผลิตมี action เดียวกันหลายตำแหน่ง และ TV ไม่แสดง QC แยกจากแพ็ก · งานนี้เปลี่ยน presentation/IA เต็มที่ แต่คงข้อมูล, mutation, permission, lock, idempotency และ flow **production → QC → final pack → ready** ชุดเดิม
>
> จอเป้าหมาย: คอม 1440×900 และจอทัช 1024×768 เป็นตัวตั้ง; 390px เป็น regression guard ไม่ใช่ composition หลัก · ไม่มี mockup/fixture route และห้ามเพิ่ม schema/dependency/config

- [x] **UX2.0 โครงโมดูลเดียว** — เพิ่ม local production navigation ชุดเดียวให้ `/production`, `/production/print-runs`, `/production/films`, `/outsource` และ entry ไป Station/TV โดยไม่เพิ่ม sidebar ใหม่ · route/active state/permission อ่านจากของจริงและทางย้อนกลับไม่หลุดโมดูล
- [x] **UX2.1 Production Control `/production`** — เปลี่ยน board ซ้ำเป็น exception-first ERP worklist หนึ่งออเดอร์ต่อหนึ่งแถว · filter `ต้องจัดการ / กำลังผลิต / รอ QC / รอแพ็ก / ทั้งหมด` พร้อม count · search/sort อยู่ URL · แถวเห็นเลขงาน ลูกค้า/ชื่องาน จำนวน จุดปัจจุบัน กำหนดส่ง ความคืบหน้า blocker และเปิด detail ถูก route · reconnect `?create=` กับ `CreateProductionDialog` และแก้ post-pack link ไป delivery
- [x] **UX2.2 Job traveler + supervisor subpages** — รื้อ `/production/[id]` ให้มี next action เดียวและสองชั้นข้อมูล “ต้องใช้ตอนนี้ / ตรวจย้อนหลัง” · action ไม่ซ้ำกับ step history, DTF เข้า print-run, outsource permission ตรง server, garment/material state ยังครบ · จัด `/production/print-runs`, `/production/films`, `/outsource` ให้ใช้ module header/worklist grammar เดียวกันโดยไม่เปลี่ยน controller
- [x] **UX2.3 Station work center `/factory/station`** — control เลือกสถานีมีจุดเดียวและคงใน URL · เมื่อเลือกแล้ว first viewport เป็น active job → ready queue → compact scan · ใช้ station-specific workspace ไม่ mount ERP page ทั้งก้อน · หนึ่งสถานะมี primary action เดียวและ wrong-station/read-only/blocked/stale บอกตรงจังหวะ
- [x] **UX2.4 Factory TV `/factory`** — แสดง 5 สถานีตาม flowจริงในหนึ่ง viewport พร้อม active/queue/next และ exception rail · QC กับ final packแยกกัน · read-only 100%, poll/stale behavior และ factory-safe no-money DTO เดิม
- [x] **UX2.5 ด่านก่อนส่ง** — pure/view tests ครอบ worklist grouping/filter/count/route, Station selected/unselected/active/empty/read-only และ TV 5 stage order · `verify:ui` guard โครงหลัก · browser read-only 1440×900 + 1024×768 Light/Dark ที่เกี่ยวข้อง ไม่มี overflow/hydration/console error และ coarse targets ≥44px · typecheck/lint/unit/build ผ่าน · Impeccable detector + fresh finish review · อัปเดต `docs/DESIGN.md`, surface brief, `PROGRESS.md` และ commit ก้อนเล็ก
- [x] **UX2.6 เก็บส่วนหัว Production Control ตามภาพใช้งานจริง** — ให้เส้น active ของ local module nav ทับเส้นฐานพอดีบน desktop/จอทัช · ตัวกรองสถานะ 5 มุมใช้ช่องไอคอน semantic เดียวและสลับเป็น Check เมื่อเลือกแทนช่องว่างเดิม โดย count/URL/keyboard/touch contract ไม่เปลี่ยน · ตรวจ 1440 desktop + 1024 touch layout, Light/Dark, overflow, console, `verify:ui` และ Impeccable detectorก่อน commit
- [x] **UX2.7 หัวตาราง Production Control เรียงได้เหมือนหน้ารวมออเดอร์** — ใช้ `DataTable.SortableTh` กลางกับออเดอร์/ความคืบหน้า/จำนวน/กำหนดส่ง กดซ้ำสลับทิศและประกาศ `aria-sort` เพียงคอลัมน์เดียว · default “ต้องจัดการก่อน” คง exception-first โดยไม่มีหัวใดอ้างว่าเรียงอยู่ ส่วน explicit column sort ต้องเรียงทั้งตารางจริง · หัวที่กดไม่ได้ห้ามเปลี่ยนพื้นตอนชี้ · URL/back/search/lens และ Select สำหรับ preset/จอการ์ดต้องคง state · ตรวจ mouse/keyboard/1024 touch/Light-Dark/overflow/console พร้อม pure tests และ `verify:ui`
- [x] **UX2.8 เปิดหมวด Sidebar และตัด breadcrumb ซ้ำบนรอบพิมพ์** — desktop sidebar แสดงเมนูที่มีสิทธิ์ครบทุกหมวดตลอดโดยไม่ซ่อนใน “เมนูทั้งหมด” และยังเลื่อนหา active item ได้ · mobile bottom nav/“เพิ่มเติม”, route, longest-match และ permission registry เดิมไม่เปลี่ยน · `/production/print-runs` คงชื่อหน้าและ `ProductionModuleNav` แต่ไม่มี breadcrumb ซ้ำ · ตรวจ 1440×900 + 1024×768 Light/Dark, keyboard/focus, sidebar overflow, console, unit/typecheck/lint/`verify:ui`/build และ Impeccable detectorก่อน commit
- [x] **UX2.9 ทำใบผลิตให้เป็น job traveler จริง** — `/production/[id]` ต้องไม่มี breadcrumb และ local module tabs ที่พาคนออกจากงานใบปัจจุบัน · ส่วนบนตอบตามลำดับ **งานอะไร → ต้องทำอะไรตอนนี้ → ติดอะไร** ด้วย summary แถวเดียวและ action region เด่นเต็มแถว · แบบ/จำนวน, เสื้อ, วัตถุดิบ และเส้นทางย้อนหลังเป็นข้อมูลชั้นรองที่อ่านต่อด้านล่างโดยไม่ซ่อนใน tab/accordion · คง query, mutation, permission, stale guard, legacy PACKAGING recovery, Station surface และ flow DTF/garment/outsource เดิมทุกจุด · ตรวจ 1440×900 + 1024×768 Light/Dark, keyboard/touch/overflow/console, state actionable/blocked/read-only/stale/not-found, unit/typecheck/lint/`verify:ui`/build และ Impeccable detectorก่อน commit
- [x] **UX2.10 แบ่งข้อมูลในใบผลิตตามเจตนางาน** — มติล่าสุดทับข้อ UX2.9 เฉพาะการซ่อนข้อมูลชั้นรอง: `/production/[id]` คงหัวใบและ summary ไว้ตลอด แล้วแบ่งภายในใบเป็น `ทำงาน / เบิกของ / ขั้นตอนทั้งหมด` โดยค่าเริ่มต้นต้องเห็น action + blocker + แบบ/จำนวนที่ใช้ทำงานพร้อมกัน · งานเบิกเสื้อที่เป็น current action ต้องลงมือได้ในแท็บทำงานโดยไม่ซ้ำ mutation · เสื้อ/วัตถุดิบที่เหลืออยู่แท็บเบิกของ และประวัติขั้นเป็น read-only ในแท็บขั้นตอนทั้งหมด · state แท็บอยู่ใน URL และคงหลัง refresh/Back โดยไม่ทำข้อมูลฟอร์มหาย · `/factory/station` คงหน้าเส้นตรงไม่มีแท็บ · ไม่คืน local module nav ที่พาคนออกจากใบงาน และไม่แตะ query/mutation/permission/business flow เดิม · ตรวจ 1440×900 + 1024×768 Light/Dark, keyboard/touch/overflow/console, actionable/blocked/garment/read-only/empty/not-found, unit/typecheck/lint/`verify:ui`/build และ Impeccable detectorก่อน commit
- [x] **UX2.11 ใบผลิตเป็น single-task workspace สำหรับคนหน้างานจริง** — มติล่าสุดทับ presentation ของ UX2.10: ถอด content tabs ที่แยก action/ยอดเสื้อ/ลำดับขั้นออกจากกัน · ย่อ summary ให้เป็นบริบทในหัวใบ แล้วให้ first viewport ตอบพร้อมกันว่า **ตอนนี้ทำอะไร → พร้อมหรือยัง/ติดอะไร → ใช้แบบและจำนวนไหน → กดปุ่มใด** ใน workspace เดียว · เสื้อที่เป็น current หรือเป็น legacy readiness unknown ต้องอยู่ติด action ไม่ส่งคนไปหาอีกแท็บ · เสื้อ/วัตถุดิบที่ไม่ใช่งานปัจจุบันและเส้นทางย้อนหลังเป็น disclosure ชั้นรองที่มีชื่อ/จำนวนชัด เปิดเมื่อจำเป็นและไม่ทำ mutation ซ้ำ · `/factory/station` คง linear และไม่มีเงิน · query/mutation/permission/action policy/transition เดิมห้ามเปลี่ยน · ตรวจ 1440×900 + 1024×768 Light/Dark, keyboard/touch/disclosure/focus/overflow/console, actionable/blocked/garment/legacy/read-only/stale/not-found, unit/typecheck/lint/`verify:ui`/build และ Impeccable detectorก่อน commit
- [x] **UX2.12 จัดใบผลิตใหม่ให้ action อยู่ติดข้อมูลที่ใช้ตัดสินใจ** — เบสส่งภาพจากของจริง 2026-08-18: ขั้นปัจจุบันกับปุ่มเบิกเสื้ออยู่คนละระดับ, คำสั่งงานกินพื้นที่มากกว่างานหลัก และรายละเอียดรองยังเป็น card ซ้อนจน empty state ใหญ่เกินความสำคัญ · รื้อเฉพาะ composition ของ `/production/[id]` ให้ workspace ก้อนเดียวเรียง **งานปัจจุบัน + blocker + เสื้อ/จำนวน + primary action → แบบและสเปก → ขั้นถัดไป** โดย GARMENT_PICK ห้ามพูดซ้ำกับการ์ดเบิกจริง · desktop ให้งานปัจจุบันกว้างกว่าสเปก, mobile ใช้ DOM เดียวเรียง action ก่อน · รวม disclosure รองไว้ surface เดียว, MaterialUsage/เส้นทางงานเป็น compact embedded content และคง `?tab=inventory|history` compatibility · คง query/mutation/permission/action policy/transition, legacy readiness confirmation, mixed-print truth และ `/factory/station` linear เดิม · ตรวจ actionable/blocked/garment/legacy/read-only/stale/not-found ที่ 1440×900 + 1024×768 + 390×844 Light/Dark, keyboard/touch/disclosure/focus/long Thai/overflow/console, targeted+full unit/typecheck/lint/`verify:ui`/build, Impeccable detector และ fresh finish reviewก่อน commit
- [x] **UX2.13 ใบผลิตใช้ process bar โฟกัสทีละขั้น** — เบสส่งภาพจริง 2026-08-19 และเคาะให้รื้อ `/production/[id]` จาก workspace ที่ยังเห็นหลายเรื่องพร้อมกันเป็น process bar ด้านบน ซึ่งแสดง **ข้อมูล + blocker + action ของขั้นที่เลือกเพียงขั้นเดียว** · ราง final ใช้เส้น neutral ต่อเนื่องเต็มความกว้าง, ขั้นเสร็จเป็น check, ขั้นอื่นเป็นเลข และชื่อขั้นอยู่ใต้ node โดยไม่มี lane/badge ซ้ำ; สี node คือ workflow จริง ส่วน selection ring คือขั้นที่กำลังเปิดดู · navigator ต้องมาจาก `productionWorkflowSteps` จริง แสดงเสร็จแล้ว/กำลังทำ/พร้อมทำ/รอโดยไม่ทำให้งานหลายเลนดูเป็นลำดับบังคับปลอม และการกดดูขั้นก่อนหน้า/ถัดไปห้ามเปลี่ยนสถานะ · ค่าเริ่มต้นเลือกงานที่ลงมือได้ก่อนตาม `selectNowSteps`; GARMENT_PICK ใช้ยอดเบิกจริง, ขั้นพิมพ์แสดงเฉพาะแบบ/วิธีพิมพ์ที่เกี่ยวข้อง, ขั้นอนาคต/เสร็จแล้วเป็น read-only และ primary action มีจุดเดียว · เก็บวัตถุดิบระดับทั้งใบไว้เป็น disclosure รองที่ปิดอยู่และคง legacy `?tab=inventory|history` ให้ไม่พาเข้าจอว่าง · `/factory/station` คง linear/no-money และ query/mutation/permission/action policy/transition, mixed-lane, legacy readiness/PACKAGING recovery เดิมทั้งหมด · ตรวจ actionable/multiple-current/blocked/garment/DTF/outsource/completed/all-done/legacy/read-only/stale/not-found ที่ 1440×900 + 1024×768 + 390×844 Light/Dark, mouse/keyboard/touch/focus/long Thai/overflow/console, targeted+full unit/typecheck/lint/`verify:ui`/build, Impeccable detector และ fresh finish reviewก่อน commit
- [x] **UX2.14 รื้อใบผลิตเป็น Job Jacket ใหม่ทั้งหน้า** — เบสทักจากหน้าจริง 2026-08-19 ว่า UX2.13 ยังเป็น UI เดิมที่เอา process bar ไปแปะเหนือ summary/card/disclosure เก่า · ทิ้ง composition ของ ERP `/production/[id]` แล้วสร้าง **แถบตัวตนงานแบบกระชับ → route ribbon ที่เป็นโครงหน้า → selected operation workspace เดียว → primary action จุดเดียว** โดยขั้นที่เลือกต้อง “กลายเป็นหน้า” ไม่ใช่การ์ดใบเดิมใต้ rail · ถอด gray summary bubble, progress ซ้ำ, card stack, เงา/มุมโค้งซ้อน และหัว `ข้อมูลทั้งใบ` ที่ลาก scroll ยาว; ข้อมูลระดับทั้งใบย้ายเข้า inspector ที่เปิดตามต้องการและ legacy `?tab=inventory|history` ต้องเปิด inspector ได้ · desktop ใช้ task canvas + reference sidecar ใน surface เดียว, 1024/มือถือเรียงหนึ่งคอลัมน์และ rail เลื่อนแนวนอนโดยไม่ทำให้ทั้งหน้า overflow · process selection เป็น view state เท่านั้น, workflow รองรับหลาย current/หลาย lane, query/mutation/payload/permission/stale/action policy/lock/transition/legacy recovery เดิมห้ามเปลี่ยน · `/factory/station` คง presentation linear/no-money เดิม · ตรวจ actionable/multiple-current/blocked/garment/DTF/outsource/completed/all-done/legacy/read-only/stale/not-found ที่ 1440×900 + 1024×768 + 390×844 Light/Dark, mouse/keyboard/touch/focus/Back/Escape/long Thai/overflow/hydration/console, targeted+full unit/typecheck/lint/`verify:ui`/build, Impeccable detector และ fresh finish reviewก่อน commit
- [x] **UX2.15 รื้อใบผลิตหลังตรวจรับ Job Jacket บนจอจริง** — เบสส่งภาพ 3024px แล้วไม่รับ UX/UI ของ UX2.14 เพราะหัว graphite กับ route ribbon กินพื้นที่และแย่งลำดับจากงานที่ต้องทำ ขณะที่ GARMENT_PICK เหลือ content กว้างโล่งและ action ไม่เกาะกับจำนวนที่ขาด · เปลี่ยน ERP `/production/[id]` เป็น **ใบงานหน้าเครื่อง**: หัวใบสว่างกระชับหนึ่งก้อน, desktop ใช้ rail ขั้นตอนแนวตั้งขนาดเล็ก + work canvas ที่จำกัดความกว้างอ่านจริง, mobile ใช้ step selector แนวนอนชุด DOM เดียว, และ active operation ต้องรวมชื่อขั้น/สถานะ/blocker/จำนวน/primary actionไว้ใน first viewport · GARMENT_PICK แสดงรายการเสื้อเป็นแถวตัดสินใจที่เห็น “ต้องใช้/เบิกแล้ว/ขาด” ชัดโดยไม่สร้าง mutation ซ้ำ; ข้อมูลใบงาน/ใบสั่งงาน/ออเดอร์เป็น secondary utility และ inspector/deep link เดิมยังอยู่ · สี/ฟอนต์/component token ใช้ P1.0 เดิม, ไม่เปลี่ยน query/mutation/payload/permission/action policy/workflow/legacy recovery และไม่แตะ `/factory/station` · ตรวจ 1440×900 + 1024×768 + 390×844 Light/Dark, mouse/keyboard/touch/focus/Back/Escape/long Thai/overflow/hydration/console, actionable/blocked/garment/multiple-current/read-only/stale/not-found, targeted+full unit/typecheck/lint/`verify:ui`/build, Impeccable detector และ fresh finish reviewก่อน commit
- [ ] **UX2.16 แยก Production Control ออกจาก Station Execution ตามงานจริง** — เบสตรวจ UX2.15 จากจอจริงแล้วชี้ว่ารื้อภาพหลายรอบยังผิดเพราะไม่ได้ออกแบบจากมาตรฐาน ERP และ Station Mode · มติใหม่ทับ framing “ERP detail = ใบงานหน้าเครื่อง”: `/production` และ `/production/[id]` เป็นพื้นผิวหัวหน้า/ออฟฟิศสำหรับจัดลำดับ plan เทียบ actual, owner, blocker, exception, handoff และ audit; `/factory/station` เป็นเจ้าของ routine execution ได้แก่รับ/เริ่มงาน บันทึกจำนวน เบิกของ แจ้งปัญหา และ complete ตาม work center · ทั้งสองใช้ record/readiness/service/transition ชุดเดียวกัน แต่ normal mutation ต้องไม่ซ้ำและต้องบังคับ boundary ฝั่ง server ไม่ใช่แค่ซ่อนปุ่ม · ทำ standalone mockup 3 ทิศแบบคู่จาก state เดียวกันแล้ว และ **เบสเลือก Direction A — Exception Control Record + Current-job-first Station วันที่ 2026-08-20** เป็น implementation target · ปิด Station parity เป็นราย operation ก่อนถอด ERP fallback: QC rework ต้องมี target work center, GARMENT_RECEIVE ต้อง derive จาก receipt evidence, current task ต้อง report problem ได้, DTF ต้องบันทึก partial/waste/reprint/defect, blocked ต้องไม่หาย, QC/Pack ต้องมี ownership/WIP และ stock/material path ต้อง no-money · คง multi-lane, garment/film/outsource heat gate, row lock, own-work, stale fail-closed, idempotency, legacy recovery, permission เดิม และ production→QC→final pack; ปรับ SPEC/DESIGN/verifier/PROGRESS ให้ตรง contract ใหม่พร้อม implementation
  - [x] **Phase 1 — Direction A บนระบบจริง**: ERP default เป็น attention-first control record ที่มีเฉพาะ manager semantic action; Station เป็น current-job-first พร้อม active/ready/blocked rail, scan, one primary action, problem reporting และ operation writer ที่ผูก exact current step · ไม่มีเงินหรือ routine execution บน ERP default surface
  - [x] **Phase 1 — server truth และหลักฐาน**: GARMENT_RECEIVE derive จาก receipt ledger ต่อ variant, report/resolve/assign แยก command, generic writer ปิด future/FAILED/service bypass, Goods/Garment/DTF/Outsource ใช้ ownership + idempotency fingerprint + atomic audit + global lock order · full unit 1219/1219, boundary review 202/202 verdict SHIP, typecheck/lint/verify-ui/build และ browser 1440/1024/390 ผ่านแบบไม่ mutate ข้อมูล
  - [x] **Phase 1 — local demo สำหรับทดลอง flow จริง**: แยกฐาน `127.0.0.1:5433/anajak_erp_demo` ออกจาก Supabase, เก็บเฉพาะ auth mapping/master ที่จำเป็น, ไม่คัดลอก Stock credentials และมี guarded reset + one-command dev · สร้างภาพวันทำงาน 15 ออเดอร์ ครบ Production/Station/DTF/Outsource/QC/Packing/Delivery/Finance · สต๊อกเสื้อใช้สินค้า `DEMO-*` local ที่จอง/เบิก/คืน/กัน shortage และบันทึก ledger ได้จริงโดยไม่มี outbound API; flag+ฐานต้องตรงสองชั้นและ reset คืนยอดเดิม · มี assertions, unit/typecheck/lint/verify-ui และ browser mutation QA บนฐาน demo เท่านั้น
  - [x] **Phase 1 follow-up — ส่งต่องานเดิมข้ามสถานีสำหรับทีมคนเดียว**: เมื่อบริบทที่เปิดอยู่จบขั้นและหลุดจากสถานีเดิม ห้ามปล่อยหน้าเป็น “ไม่พบขั้นที่ตรงกับสถานีนี้” ทั้งที่ออเดอร์ยังมีงานต่อ · derive งานถัดไปจาก `factory.stationQueue` + exact no-money `stationQueueContext` โดยเลือก production เดิมก่อน แล้วค่อย order เดิม; current/ready มาก่อน blocked, หลาย lane มีตัวเลือกรอง และลำดับ deterministic · handoff เป็น navigation เท่านั้น ห้ามเริ่ม/claim/complete อัตโนมัติ · DTF ready เปิด batch พร้อมโฟกัส exact queue row ครั้งเดียว, active คง production context, QC/Pack ใช้ order context และ unmapped/owner อื่น fail-safe ส่ง ERP · Goods Receipt refresh คิวทันที · pure/router/UI tests, full unit 1255/1255, typecheck/lint 0 error/verify-ui และ browser desktop/390/focus 30s ผ่านโดยไม่ reset ความคืบหน้าฐาน demo
  - [x] **Phase 1 follow-up — ลดหนี้ระบบบน Control Record และทำ exception ให้ซื่อสัตย์**: รื้อหัวใบให้เหลือ status/progress/quantity/deadline ที่จำเป็น, ถอดกรอบเส้นปะ developer data-gap, เปลี่ยน operation ledger เป็น responsive list ที่ไม่เลื่อนแนวนอน และใช้คำมอบหมายให้ตรง owner ปัจจุบัน · งานร้านนอก active ที่เลยวันรับกลับยกเป็น warning attention จาก `expectedBackAt` ทุกใบที่ยังรอรับกลับ ขณะที่ `RECEIVED_BACK` แสดงรอตรวจรับตามจริง โดยไม่แตะ mutation/permission/status transition · `npm test`, typecheck, lint 0 error, `verify:ui`, build, Impeccable detector และ browser route demo ที่ 390/1024/1380/1440/1500 Light/Dark ผ่าน; independent re-review verdict `SHIP`
  - [x] **Phase 1 follow-up — Station แบบเห็นเสื้อแล้วทำตามสำหรับแรงงาน low-tech**: เบสเลือกโครง One Task Kiosk ที่อ่านน้อย แต่ให้รูปลายที่ผูกกับสินค้า/ด้าน/ขนาด/หมายเหตุจาก Picture Work Sheet เป็นแกน · current job ต้องเต็มพื้นที่, คิวและสแกนยุบเป็นเครื่องมือรอง, primary action ใหญ่และอยู่กับงานที่เห็น; schema ปัจจุบันยังไม่ระบุว่าไฟล์อนุมัติเป็น mockup เต็มหรือลายแยก จึงต้องวางเป็น “ไฟล์อ้างอิง” รอง ห้ามใช้เป็นหลักฐานตำแหน่งจนกว่าจะมีชนิด asset ที่เชื่อถือได้ · แผนภาพเสื้อบอกได้เพียงด้านหน้า/หลัง/แขน/ปก ห้ามเดาตำแหน่งย่อยหรือสเกล, ต้องไม่วาดหมวก/ถุงผ้า/สินค้าอื่นเป็นเสื้อ และต้องจัดลายแต่ละจุดอยู่กับสินค้า/สี/ไซส์/จำนวนของ item เดียวกัน · Prep/DTF/Heat/QC/Pack คง work object, ownership, stale, multi-lane, handoff, QC correctness และ no-money truth เดิม; ไม่แต่งสี/ด้าน/ขนาด, ไม่ทำ DTF batch เป็นใบเดียว และไม่เปลี่ยน QC mutation ในก้อน presentation นี้ · browser จริง 1024×768/390×844 ยืนยันลำดับภาพ→ด้าน→ขนาด/จำนวน→action, รูปโหลดครบ, horizontal overflow 0, คิว/สแกนเปิดแล้วพัก CTA เดิมและปิดแล้ว CTA กลับมา; full unit 1278/1278, typecheck, lint 0 error, `verify:ui`, build 44 หน้า, `git diff --check` และ independent P0/P1 review ผ่าน ✅ 2026-08-22
  - [x] **Phase 1 follow-up — ม็อกอัพออเดอร์เป็นของจริง และมีบ้านเดียวทั้งระบบ**: เบสชี้ 2026-08-22 ว่าระบบ "ขาด mockup ของออเดอร์" ทั้งที่ต้องใช้ทั้งโชว์ลูกค้าและให้ฝ่ายผลิตดู · ความจริงคือชั้น 2 (`DesignVersion` ตาม `file-layers.ts`) มีอยู่แล้วแต่ไม่เคยถูกเรียกว่าม็อกอัพบนจอ และติดเพดาน **1 เวอร์ชัน = 1 รูป** ทำให้เสื้อที่พิมพ์หลายจุดต้องยัดทุกด้านลงรูปเดียวหรือแตกหลายเวอร์ชันจนประวัติอนุมัติอ่านไม่รู้เรื่อง — ปิดหนี้ที่ Picture Work Sheet เคยบันทึกไว้ว่า "schema ยังไม่ระบุว่าไฟล์อนุมัติเป็น mockup เต็มหรือลายแยก" · เพิ่ม `DesignVersionFile` (migration `20260822052000`, additive ล้วน ไม่ backfill — `file_url` เดิมยังเป็นรูปปก ของเก่าจึงทำงานเหมือนเดิมทุกจอ) พร้อมตำแหน่งพิมพ์ต่อรูป, `design.upload` รับหลายรูปในเวอร์ชันเดียวโดยคง status gate/การฆ่า token ค้าง/`transitionOrder`/audit เดิม และ `/api/files` ติด approval token ให้ครบทั้งชุด โดย allowlist กว้างเท่าที่หน้าโชว์เท่านั้น (ลิงก์สถานะ/ใบงานร้านนอกยังโชว์รูปปกจึงไม่ขยาย) · รวมม็อกอัพเหลือชุดเดียว `src/components/mockup/` + สูตรอ่านเดียว `src/lib/mockup.ts`: หน้าออเดอร์ยุบสองที่ที่ยิง query ซ้ำเหลือแท็บ **ม็อกอัพ & ไฟล์** เรียงตามชั้นไฟล์ (แท็บงานผลิตเหลือแถบสรุปพาไป), `/production` ใส่รูปม็อกอัพทุกแถว + สรุปวันนี้ 3 ตัวเลขที่นับจากทั้งกระดาน, `/production/[id]` เพิ่มแท็บม็อกอัพอ่านอย่างเดียวพร้อมสเปกรีดจากคลังลาย, Station และใบสั่งผลิตแสดงครบทุกด้าน และหน้าอนุมัติลูกค้ากางรูปใหญ่เรียงลงมาให้ตัดสินทั้งชุดครั้งเดียว · ล้าง UI ขนาน 7,178 บรรทัดที่ทำให้ทุกการแก้หน้าจอต้องเดาว่าแก้ชุดไหน (`(redesign)` ทั้งชุด + `production-board-view`/`command-center` ที่กำพร้าตั้งแต่ revert) · คง no-money contract: แท็บม็อกอัพเป็น ERP-only และ `MockupPanel readOnly` ตัดก้อนค่าแก้แบบออกทั้งหมด · full unit 1288/1288, typecheck, lint 0 error, `verify:ui` ผ่านครบ และยืนยัน relation ใหม่ผ่าน tRPC จริงบน dev server ✅ 2026-08-22
  - [x] **Phase 1 follow-up — เก็บ Production Control ให้ตัดสินใจจาก exception ได้ในหนึ่งแถว**: จากการตรวจ `/production` บนจอจริง 2026-08-23 ให้แต่ละออเดอร์บอก **เหตุที่ต้องจัดการ + เจ้าของถัดไป** โดยไม่เปลี่ยน query/status/transition และยังคงงานผลิตครบแต่ส่งช้าไว้เป็น handoff exception ของฝ่ายจัดส่ง · แยกตัวกรอง `ทั้งหมด/ต้องจัดการ` ออกจากแกนขั้นงาน `กำลังผลิต/QC/แพ็ก-พร้อมส่ง`, ย่อสรุปวันนี้และทางเข้าพื้นที่หน้างานเพื่อให้งานแถวแรกขึ้นเร็ว, desktop ใช้หัวตารางเรียงคอลัมน์และเหลือเฉพาะ preset ข้ามคอลัมน์ ส่วน mobile คง Select เต็ม, และคืน focus ไปยังออเดอร์เดิมเมื่อ Back · ตรวจ 1440×900 + 390×844 Light/Dark: desktop แถวแรก y≈460, mobile y≈486, control 44px, overflow 0, URL/filter/sort/menu/focus restoration ผ่าน · full unit 1645/1645, targeted lint/typecheck/`verify:ui`/build, `git diff --check` และ Impeccable detector `[]` ผ่าน ✅ 2026-08-23
  - [x] **Phase 1 follow-up — ตัดข้อมูลและ navigation ซ้ำเหนือคิว Production Control**: เบสตรวจจอจริง 2026-08-23 แล้วชี้ว่าส่วนบนยังรกเพราะจำนวนงานเดียวกันซ้ำใน meta ใต้หัวหน้า, summary วันนี้ และ filter counts จากนั้นสั่งเอาแถบ `คิวผลิต/รอบพิมพ์ DTF/คลังฟิล์ม/งานร้านนอก` ออกทั้งหมด · ให้หัวหน้าเหลือชื่อหน้า+คำอธิบาย+เมนูงานผลิตจุดเดียว และพาคนเข้าตัวกรอง/รายการทันที; เมนูรวมทางเข้าเดิมไว้หลังการกดเพื่อไม่ตัดฟังก์ชัน แต่ห้ามคืน visible tab row · ตัด meta จำนวนรวม/ต้องจัดการและ summary `เลยกำหนด/ครบวันนี้/กำลังลงมือ` ออกจากผิวหน้าและลบ read-model/UI dead codeที่ไม่มีผู้ใช้ โดยคง filter counts, URL/search/sort, exception owner/reason, query/mutation/permission/status/business flow เดิม · ใช้ pattern เดียวกันบนคิว/รอบพิมพ์/คลังฟิล์ม/ร้านนอก · browser 1440×900 + 1024×768 + 390×844 Light/Dark: visible tab row = 0, desktop แถวแรก y≈330, mobile card แรก y≈405, control 44px, overflow 0, เมนู 6 ทางเข้าปิดด้วย Escape/คืน focus · full unit 1643/1643, targeted lint/typecheck/`verify:ui`/build, `git diff --check` และ Impeccable detector `[]` ผ่าน ✅ 2026-08-23
  - [x] **Phase 1 follow-up — ใช้ตัวกรอง Production แบบเดียวกับทั้งระบบ**: เบสตรวจ `/production` แล้วไม่รับชุดตัวกรองเฉพาะกิจที่ใส่ไอคอนทุกตัว, เส้นคั่น และคำว่า `ขั้นงาน` จนดูเป็น navigation ซ้ำ · เปลี่ยนเป็นแถว `FilterChip` มาตรฐานเดียวกับ Products/Notifications/WHT โดยแสดงเฉพาะชื่อ+จำนวน, ใช้ Anajak Blue เฉพาะตัวเลือกที่ active และคง 5 ค่า `view`, count, URL, keyboard/touch, search/sort/query/permission/status/business flow เดิม · browser 1440×900 + 390×844 Light/Dark: ตัวกรอง 5 ปุ่มไม่มี SVG/divider/label ซ้ำ, active เป็น Anajak Blue, desktop สูง 36px, mobile สูง 44px, page overflow 0 และกด `ต้องจัดการ` แล้วได้ `?view=attention` กับ 5 แถวจริง · full unit 1643/1643, targeted lint/typecheck/`verify:ui`/build, `git diff --check` และ Impeccable detector `[]` ผ่าน ✅ 2026-08-23
  - [x] **Phase 1 follow-up — ใช้แถบสถานะแบบหน้าออเดอร์บน Production จริง**: เบสตรวจผลตัวกรองข้อความแถวเดียวแล้วขอให้ `/production` ใช้หน้าตาเดียวกับแถบสถานะ `/orders` ทั้งชุด · เปลี่ยน caller ไปใช้ `FlowFilterBar` กลางโดยตรงใน panel ชั้นเดียว: ตัวเลขอยู่เหนือชื่อ, มีจุดสี semantic, hairline แบ่ง `ภาพรวม/สถานะงาน`, active เป็น Anajak Blue และ mobile ใช้ปุ่ม 44px จาก primitive เดียวกัน · กดตัวที่เลือกซ้ำกลับ `ทั้งหมด` เหมือนหน้าออเดอร์ และคง 5 ค่า `view`, count, search/sort/query/permission/status/business flow เดิม · browser `/production` 1440×900 + 390×844 Light/Dark: panel สูง 108px, mobile 2 คอลัมน์/44px, overflow 0, active/focus/toggle URL ผ่าน; `/orders` desktop ยังมี 14 สถานะใน panel 116px และไม่มี error/overflow · full unit 1644/1644, targeted lint/typecheck/`verify:ui`/build, `git diff --check` และ Impeccable detector `[]` ผ่าน ✅ 2026-08-23
  - [x] **Phase 1 follow-up — ยุบตัวกรอง Production เป็นการ์ดธรรมดา**: เบสตรวจ `FlowFilterBar` บนจอจริงแล้วไม่รับเพราะหัวกลุ่ม, hairline, จุดสี และเส้น selected ทำให้ส่วนนี้ดูแต่งเกินไป · เปลี่ยนเฉพาะ `/production` เป็นการ์ดกดได้ 5 ใบวางตรงบน workspace ไม่มีกรอบครอบซ้ำ: แต่ละใบมีชื่อ+จำนวน, resting neutral, hover/pressed กลาง และ active ใช้ Anajak Blue แบบ tint อ่อนทั้งการ์ด; mobile 2 คอลัมน์และใบสุดท้ายเต็มแถว, tablet/desktop 5 คอลัมน์ · กดตัวเดิมซ้ำกลับทั้งหมด, คง `view`/count/search/sort/query/permission/status/business flow เดิม และห้ามเปลี่ยน `FlowFilterBar` ของ `/orders` · browser 1440×900 + 390×844 Light/Dark: desktop 5 ใบสูง ≈87px, mobile 2+2+1 และใบสุดท้ายเต็มแถว, overflow 0, selected tint/keyboard focus/กดซ้ำล้าง URL ผ่าน; `/orders` ยังมี 14 สถานะใน panel เดิม · full unit 1644/1644, targeted 13/13, typecheck/lint/`verify:ui`/build, `git diff --check` และ Impeccable detector `[]` ผ่าน ✅ 2026-08-24
  - [x] **Phase 1 follow-up — เติมไอคอนและสีให้การ์ดตัวกรอง Production**: เบสตรวจการ์ดธรรมดาแล้วขอให้มีไอคอนและสีเพิ่ม · คงการ์ด 5 ใบ/spacing/responsive/behavior เดิม แต่เพิ่ม Lucide icon และ semantic accent ที่อ่านความหมายได้โดยไม่พึ่งสี: `ทั้งหมด` brand blue, `ต้องจัดการ` red, `กำลังผลิต` production teal, `รอ QC` amber, `แพ็ก / พร้อมส่ง` green; ใช้สีเฉพาะ icon tile กับตัวเลขตอนพัก ส่วน selected ยังคง Anajak Blue ทั้งระบบเพื่อแยกสถานะการกรองจากสถานะงาน · browser 1440×900 + 390×844 Light/Dark: desktop 5 ใบและ mobile 2+2+1 สูง 83px, overflow 0, semantic/selected color, focus และกดซ้ำล้าง URL ผ่าน; console ไม่มี app warning/error · full unit 1644/1644, targeted 13/13, typecheck/lint/`verify:ui`/build, `git diff --check` และ Impeccable detector `[]` ผ่าน ✅ 2026-08-24
  - [x] **Phase 1 follow-up — ลด selected state ของการ์ด Production ให้ minimal**: เบสตรวจตอนกดเลือกแล้วไม่รับ full-card blue tint กับ icon tile สีน้ำเงินทึบเพราะหนักเกินไป · คงพื้นการ์ด neutral ทั้งตอนพักและเลือก, ใช้ Anajak Blue เฉพาะขอบ 1px, ชื่อ/ตัวเลข และ icon tile แบบ tint อ่อน; semantic red/teal/amber/green ยังใช้เฉพาะการ์ดที่ไม่ได้เลือก และ focus ring ยังแยกจาก selected state · browser 1440×900 + 390×844 Light/Dark: selected คงพื้น `#fff`/`#0a0a0a`, ขอบ/ข้อความ/icon tint น้ำเงิน, การ์ดสูง 83px, overflow 0, focus และกดซ้ำล้าง URL ผ่าน; console ไม่มี app warning/error · full unit 1644/1644, targeted 13/13, typecheck/lint/`verify:ui`/build, `git diff --check` และ Impeccable detector `[]` ผ่าน ✅ 2026-08-24
  - [x] **Phase 1 follow-up — ถอดพื้นหลังไอคอนบนการ์ด Production**: เบสไม่รับ icon tile ที่ยังมีพื้น tint แม้ลด selected state แล้ว · เอาพื้น/กรอบ/มุมของ icon tile ออกทั้งหมดทั้ง resting และ selected เหลือ Lucide icon เส้นสีล้วนบนพื้นการ์ด; คงสี semantic red/teal/amber/green ตอนพักและ Anajak Blue ตอนเลือกผ่านสี glyph/ข้อความ/ขอบการ์ดเท่านั้น · browser 1440×900 Light + 390×844 Dark: icon ทุกใบ background transparent/border 0px/glyph 20px, การ์ดสูง 83px, alignment/contrast/focus/toggle URL/overflow ผ่าน และ console ไม่มี app warning/error · full unit 1644/1644, targeted 13/13, typecheck/lint/`verify:ui`/build, `git diff --check` และ Impeccable detector `[]` ผ่าน ✅ 2026-08-24
  - [x] **Phase 1 follow-up — รายการ Production ต้องไม่เด้งข้ามไปหน้าออเดอร์**: ทุกแถวใน `/production` เปิดใบผลิตในบริบท `/production/[id]` โดยเลือกใบของจุดงานปัจจุบันก่อน แล้วค่อยใบผลิตล่าสุดของออเดอร์สำหรับ QC/แพ็ก/พร้อมส่ง · งานคิวที่มีสิทธิ์ยังเปิด dialog ด้วย `?create=` เดิม ส่วนผู้ไม่มีสิทธิ์หรือข้อมูลเก่าที่ไม่มีใบผลิตคงอยู่ในหน้า Production และไม่มี branch ใดเดา route `/orders` · แก้ focus restoration ที่เคยสร้าง `id` ซ้ำระหว่าง desktop/mobile ให้เลือกแถวที่มองเห็นจริง · คง mutation/permission/status/business flow เดิม; query เพิ่มเพียงลำดับ `createdAt desc` เพื่อเลือกใบล่าสุดอย่าง deterministic · browser 1440×900 + 390×844: ลิงก์ที่เห็น 11/11 อยู่ใต้ `/production`, เปิด QC/พร้อมส่งได้ detail จริง, Back คืน focus แถวเดิม, overflow 0 และ console 0 warning/error · targeted 8/8, full unit 1644/1644, typecheck, lint 0 error, `verify:ui`, build 41 หน้า และ `git diff --check` ผ่าน ✅ 2026-08-24
  - [x] **Phase 1 follow-up — selected filter card คงสีประจำสถานะ**: การ์ดตัวกรอง `/production` เมื่อเลือกใช้สี semantic เดียวกับตอนพัก (`ทั้งหมด` brand blue, `ต้องจัดการ` red, `กำลังผลิต` teal, `รอ QC` amber, `แพ็ก / พร้อมส่ง` green) ผ่านขอบ 1px, ไอคอน, ชื่อ และตัวเลข โดยคงพื้น neutral ไม่มี icon background/เงา/แถบสีหนัก · keyboard focus ยังเป็นวง Anajak Blue แยกจาก selected state · browser Light/Dark desktop + 390×844: สี computed ครบทั้ง 5 ค่า, text contrast ต่ำสุด 4.83:1, border ต่ำสุด 3.62:1, mobile 2+2+1 สูง 83px, overflow 0, กดซ้ำล้าง `view` และ console 0 warning/error · count, URL, search/sort/query/permission/status/business flow เดิม · targeted 13/13, full unit 1644/1644, typecheck, lint 0 error, `verify:ui`, build 41 หน้า, `git diff --check` และ Impeccable detector `[]` ผ่าน ✅ 2026-08-24
  - [ ] **Phase 2 — operation parity ที่ยังไม่ควรเดา**: QC rework target work center, DTF partial/waste/reprint/defect event, production owner/plan/SLA/audit actor read model, supervisor material recovery และ WIP ownership ของ QC/final pack · ปิดแล้วจึงติ๊ก UX2.16 หลัก

#### ใบงาน NEXT16-UPGRADE — อัปเกรด framework เป็น Next.js 16.3 (เบสสั่ง 2026-08-16)

> เป้าหมาย: เปลี่ยน runtime จริงจาก Next 15.5.15 เป็น patch ล่าสุดในสาย 16.3 โดยยึด official upgrade guide/codemod · ห้ามใช้การแก้ banner หลอกเวอร์ชัน และห้ามพ่วง Cache Components/React Compiler หรือ refactor ที่ไม่จำเป็น

- [x] **N16.1 dependency และ runtime ตรงกัน** — `package.json`, lockfile, local binary และ build ใช้ Next.js 16.3.x + `eslint-config-next` รุ่นเดียวกัน · React/React DOM/types อยู่ใน peer range · Node local/CI ผ่านขั้นต่ำ 20.9
- [x] **N16.2 breaking changes ถูกย้ายครบ** — ใช้ official codemod เป็นฐาน · async Request APIs ไม่มี synchronous fallback · ย้าย middleware convention เป็น `proxy` โดยคง Supabase session refresh, public-token routes และ API behavior เดิม · Turbopack ใช้ default ของ Next 16 โดยไม่เปิด feature ใหม่
- [x] **N16.3 regression gate** — typecheck, lint, unit, `verify:ui`, production build และ `next --version` ผ่าน · cold browser ตรวจ auth redirect/login, `/production`, `/production/[id]`, `/factory/station`, `/factory` และ public-token boundary โดยไม่มี hydration/console/server error ใหม่
- [x] **N16.4 ปิด React 19.2 client-script warning ของ theme bootstrap** — `next-themes` bootstrap ต้องยัง execute ก่อน hydration เมื่อ SSR เพื่อกันธีมกระพริบ แต่ Fast Refresh/Suspense client remount ต้อง render เป็น inert data block จึงไม่สร้าง `<script>` ที่ React เตือนว่าไม่ execute · คง Light/Dark/System, public/print forced-light และ provider topology เดิม · เพิ่ม guard แล้วตรวจ hard load + soft navigation + Fast Refresh/แท็บเดิมโดยไม่มี hydration/console error
- [x] **N16.5 ส่งมอบ** — อัปเดต `SPEC.md`/`PROGRESS.md`, commit ก้อน upgrade แยกบน branch งาน และไม่ push

**เพิ่มเข้า P1 (เบสเคาะ 2026-06-12 — รื้อโมดูลผลิตตามความจริงโรงงาน):** ✅ ทำแล้ว 2026-06-12 — เบสชี้ "การผลิตเอาไปใช้จริงไม่ได้" + ให้ความจริงใหม่: **ทำเองมีแค่ DTF** (พิมพ์ฟิล์ม→รีดร้อน 2 ขั้น) · DTG/สกรีน/ปัก/Sublimation/ตัดเย็บใหม่/ป้ายคอเย็บติด = **outsource ทั้งหมด กด "ผ่านรวด" ปิดขั้นได้โดยไม่ต้องเปิดใบส่งร้าน/ไม่กรอกเงิน** → ใบผลิตเป็น "สายงานต่อเลน": เตรียมเสื้อ (เบิกสต๊อค/ตรวจรับเสื้อลูกค้า) · ตัดเย็บ (เลนแยก outsource) · เลนต่อเทคนิคพิมพ์ · ป้ายคอ (งอกเองจาก add-on) · แพ็ค (โผล่เมื่อสายอื่นเสร็จ) — ตัวแนะนำอ่านครบ 3 อย่าง: วิธีพิมพ์+แหล่งเสื้อ+add-on · หน้า /production = แท็บต่อเทคนิค + บอร์ดเลน (เบสเคาะ "เอาทั้งสองแบบ") · **มติใหญ่: เลิกคิดต้นทุนต่องานทั้งระบบ** (ดูกติกา 4)
**เพิ่มเข้า P1 (เบสเคาะ 2026-06-11 — redesign การเปิดงาน):** ✅ **ฟอร์มเปิดงานโหมดเดียว + ไม่ถามชนิดออเดอร์** (ทำแล้ว 2026-06-11) — เบสชี้: 2 โหมด (สอบถาม/ระบุครบ) + คำถาม สำเร็จรูป/custom ทำให้ใช้ยาก ไม่มีจุดโฟกัส → ยุบเหลือฟอร์มเดียว เปิดงานได้ด้วยลูกค้า+ชื่องาน · ระบบ **derive ชนิดออเดอร์จากเนื้อรายการเอง** (มีรายการ+ไม่มีลายพิมพ์=สำเร็จรูป · นอกนั้น=custom · re-derive ตอนแก้รายการเฉพาะช่วง DRAFT/INQUIRY) + **ภาษีต่อรายการ** (มีลาย=จ้างทำของ · ไม่มี=ขายสินค้า — ออเดอร์ผสมถูกกฎหมายกว่าเหมาทั้งใบ) · ด่านชดเชยฝั่ง server: ยืนยันออเดอร์ต้องมีรายการ · การ์ด "ขั้นถัดไป" บนหน้าออเดอร์ (`src/lib/order-next-step.ts`) บอกจุดโฟกัสเดียวต่อสถานะ · **เบสเคาะเพิ่มรอบสอง: บังคับแค่ลูกค้าช่องเดียว** — ชื่องาน/รายละเอียดไม่บังคับ (ชื่อว่าง = server ตั้งให้จากรายการแรกหรือชื่อลูกค้า+วันที่) + ถอดจำนวนโดยประมาณออกจากฟอร์ม

## 🏭 MFG — ตัวชี้วัดฝ่ายผลิตตามมาตรฐานสากล (เบสสั่ง 2026-08-15 "ร่างใบงาน 3 ข้อนี้เข้า ROADMAP เลย")

> **ที่มา:** เบสถามว่าอะไรสำคัญที่สุดสำหรับฝ่ายผลิตแบบมาตรฐานสากล · วงการวัดด้วย QCD (คุณภาพ/ต้นทุน/กำหนดส่ง) แต่ Anajak ตัด **ต้นทุนต่องาน** ทิ้งแล้วโดยเจตนา (กติกา 4) จึงเหลือของจริง 2 ตัวคือ **ส่งตรงเวลา** กับ **ทำถูกตั้งแต่ครั้งแรก**
>
> **ข้อค้นพบจาก audit schema 2026-08-15:** ฐานข้อมูลเก็บวัตถุดิบของตัวชี้วัดครบแล้วทั้งหมด — `ProductionStep.startedAt/completedAt` (cycle time ต่อสถานี) · `qtyDone/qtyTotal` (คืบหน้าเป็นชิ้น) · `QcRecord.qtyGood/qtyDefect` + `QcDefect.reason/size/color/printLabel/photoUrls` (มี `@@index([reason])` รอรวมยอดอยู่แล้ว) · `Order.deadline` + `shippedAt/deliveredAt` · `OrderRevision` · **แต่ `analytics.ts` มี 4 procedure และทั้งหมดเป็นเรื่องขาย/เงิน ไม่มีตัวชี้วัดฝ่ายผลิตสักตัว** — ข้อมูลไหลเข้าอย่างเดียว ไม่มีทางออก
>
> ขอบเขตรวม: **อ่านอย่างเดียว ไม่เพิ่ม schema ไม่เก็บ field ใหม่** คำนวณจากข้อมูลที่มีอยู่แล้วทั้งหมด · ไม่แตะ flow ผลิต ไม่เพิ่มช่องเงินตามกติกา 4 · gate สิทธิ์เหมือนหน้าผลิตเดิม (ไม่มีเงินบนจอเหล่านี้)

- [x] **MFG0 ปิดหนี้เอกสาร QC gate** ✅ 2026-08-15 — `SPEC.md:46` เคยเขียนว่าปุ่ม "ผ่าน→แพ็ค" ยังข้ามด่านนับได้ทุก role ทั้งที่ Gate B4 ปิดไปตั้งแต่ 2026-07-02 · audit โค้ดจริงยืนยันทางเข้า PACKING มีสองทางและปิดครบ (`order.ts:1291-1301` เช็ค `qcRecord.count === 0` ใต้ `$transaction` · `services/qc.ts:218-228` advance หลังนับครบ) → แก้ SPEC เป็น `[x]` พร้อม file:line · เอกสารที่โกหกอันตรายกว่าไม่มีเอกสาร เพราะทำให้คนวางแผนงานซ้ำ · **หนี้จริงที่ยังเหลือจาก B4**: ส่งของผ่านใบส่งขณะ QUALITY_CHECK ยังทำได้ทาง API ตรง (UI ทำไม่ได้) — ต้องเคาะ semantics ใบส่งกับเบสก่อนปิด ยังไม่ใช่งานในก้อนนี้
- [ ] **MFG1 สองตัวเลขที่ฝ่ายผลิตต้องเห็น** — หน้าเดียวโชว์ **ส่งตรงเวลากี่ %** (นับจากออเดอร์ที่ `shippedAt` แล้วในงวด เทียบ `deadline` · ไม่มี deadline = ไม่นับ ไม่ใช่นับว่าตรง) และ **ทำถูกตั้งแต่ครั้งแรกกี่ %** (จาก `QcRecord` งวดนั้น: `Σ qtyGood / (Σ qtyGood + Σ qtyDefect)`) · เลือกงวดรายเดือนแบบเดียวกับ `/billing/tax` (ขอบงวด UTC+7) และเทียบกับเดือนก่อนได้ · แต่ละตัวเลขกดลงไปดูรายการออเดอร์ที่ทำให้ตัวเลขเป็นแบบนั้นได้จริง — ตัวเลขที่กดดูของจริงไม่ได้คือตัวเลขที่ไม่มีใครเชื่อ · **ต้องซื่อสัตย์เรื่องความครบ**: งวดที่ยังมีงานค้างส่งต้องบอกว่ายังไม่จบงวด ห้ามโชว์ % ที่ดูดีเพราะงานสายยังไม่ถูกนับ
- [ ] **MFG2 สรุปของเสียตามสาเหตุ** — รวมยอด `QcDefect` ต่อ `reason` ในงวด เรียงมากไปน้อย พร้อมแยกย่อยตาม ไซส์/สี/ลาย ที่เก็บไว้แล้ว และเปิดรูปที่ช่างถ่ายไว้ได้ · ตอบให้ได้ว่าควรไปแก้ที่ร้านไหน เครื่องไหน หรือขั้นไหน · ป้ายสาเหตุใช้ชุดเดิมจาก `src/lib/qc.ts` ห้ามประกาศ map ใหม่ · เชื่อมกลับออเดอร์ต้นทางได้ทุกแถว
- [ ] **MFG3 ด่านก่อนเคลมเสร็จ** — pure function คำนวณตัวชี้วัดแยกจาก router พร้อม unit test ครอบเคสขอบ (งวดไม่มีข้อมูล · ออเดอร์ไม่มี deadline · หารศูนย์ · งานที่ยังไม่ส่ง · ออเดอร์ยกเลิก) · typecheck/lint/unit/`verify:ui`/build ผ่าน · browser จริง 1440 + 390 ทั้ง Light/Dark · loading/error/empty แยกกัน · อัปเดต `SPEC.md` เพิ่มเกณฑ์ "เสร็จ" ของสองตัวเลขนี้ + `PROGRESS.md`

## P2 — ผลิต + สต๊อค
per-item tracking + นับชิ้น + ของเสีย/reprint log (รหัสสาเหตุ) · ~~outsource UI ครบ~~ (ทำแล้ว 2026-06-11 — ดึงขึ้น P1) + **AP vendor + WHT ขาจ่าย 3% + 50ทวิ + ภงด.53 export** (ภาษี/จ่ายร้านนอก — คนละเรื่องกับต้นทุนต่องาน แต่ทบทวนขอบเขตกับเบสก่อนเริ่ม) · ~~film usage log DTF → job costing~~ (**ตัดทิ้ง — เบสเคาะ 2026-06-12 เลิกคิดต้นทุนต่องาน** ดูกติกา 4) · จอง/ตัดสต๊อค READY_MADE + receiveFinished UI + sync cron (verify ท่อ Stock API ก่อน — key ปัจจุบัน placeholder) · ใบแพ็ค+ป้ายกล่อง+นับยืนยัน + **blind shipping** (เบสยืนยันมี reseller) · รูปก่อนส่ง (gate แรก) · เคลม/งานแก้เวอร์ชันเล็ก · **รับเข้าเสื้อลูกค้า (เบสยืนยัน "ส่งมาบ่อย" — ความสำคัญสูง)** · ปุ่ม clone สั่งซ้ำ

## P3 — ฝั่งลูกค้า
Customer portal (สถานะ/ประวัติ/เอกสาร/อนุมัติ/สั่งซ้ำ) · LINE OA notify+ทวงหนี้+WIP photo · CRM เต็ม (แก้ลูกค้า/comm log/follow-up/RFM) · revision quota + คิดเงินเกินโควตา + lock หลังอนุมัติ · strike-off gate (ตัวอย่างจริง opt-in) · preflight ไฟล์ (DPI/พื้นโปร่ง) · analytics ลึก

## P4 — เชื่อมโลก
Order-intake API → เว็บสกรีน 5 นาที · MCP server (เคารพ RBAC) · ฟอร์มเก็บไซซ์ลูกค้าองค์กร (เวอร์ชันเบา) · e-Tax provider / FlowAccount API / courier API เมื่อ volume ถึง

## จงใจไม่ทำ (อย่าหยิบกลับมา — เหตุผลใน plan.md)
GL/งบการเงิน · **job costing/ต้นทุนต่อออเดอร์ (เบสเคาะ 2026-06-12 — ต้นทุนเหมา คิดกำไรขาดทุนรายเดือนในระบบบัญชี · "มันสร้างความยุ่งยาก อันนั้นเป็นหน้าที่ของระบบบัญชี")** · DTF auto-nesting (RIP ทำแล้ว) · in-app chat (ลูกค้าอยู่ LINE) · online designer · ใบกำกับอย่างย่อ · time-clock/payroll (hr-platform-v2) · WMS/PR-PO-GRN (Anajak Stock) · mockup generator · CMMS เต็ม · QR scan-to-update เต็ม · courier API booking
