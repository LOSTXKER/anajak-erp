# Anajak ERP — Roadmap & ใบงาน
> เฉพาะใบงานที่ **ยังเปิดอยู่** + สิ่งที่ตัดสินแล้ว · แผนเต็ม + เหตุผล: bestos `records/projects/anajak-erp/plan.md` · ของที่เสร็จแล้วอยู่ดัชนีท้ายไฟล์ 1 บรรทัด — รายละเอียดใบงานเก่าทุกรอบใน git: `git show 61575af:ROADMAP.md` (420 KB) + `git log`
> กรอบที่เบสเคาะ 2026-06-10: ซ่อมต่อบนโครงเดิม · ผู้ใช้ = พนักงาน 5 คน + เบส + ลูกค้า · ภาษีเต็มรูป + หัก ณ ที่จ่าย 2 ขา · เว็บสกรีน = P4
> เป้าหมายที่เบสย้ำ 2026-09-02 → `AGENTS.md` §เป้าหมาย (ครอบคลุมโรงงาน · ส่งร้านนอกมีประสิทธิภาพ · UX สวยใช้ง่าย · รองรับออเดอร์เว็บธรรมดา + custom)

## กติกา build (ผูกทุก phase — ห้ามข้าม)
1. เพดานจริง ~1-2 โมดูลเล็ก/เดือน — อย่ารับปากเกิน · ทุกไตรมาส prune backlog ที่ยังไม่เริ่มทิ้งครึ่ง
2. เปิด "gate บังคับ" ทีละตัว — เปิดพร้อมกันพนักงาน bypass ข้อมูลกลายเป็นขยะ
3. manual field/CSV ก่อน API เสมอ (courier/บัญชี/stock-link)
4. **ไม่คิดต้นทุนต่องาน** (เบสเคาะ 06-12 — ต้นทุนเหมา คิดกำไรขาดทุนรายเดือนในระบบบัญชี) · ห้ามเพิ่มช่องเงิน/ต้นทุนใน flow ผลิต-outsource
5. **ไม่ build GL** — ERP ออกเอกสารขาย/ใบกำกับ/50ทวิ + export (CSV มาตรฐาน + template PEAK) ให้นักบัญชี
6. surgical: แตะเฉพาะที่ใบงานสั่ง · โครงเดิมดี (order module/status machine/token approval/V2 manufacturing) ต่อยอด อย่ารื้อ
7. refactor = targeted + test ก่อน ห้าม big-bang · boy-scout rule (แตะไฟล์ไหน เก็บกวาดไฟล์นั้น)
8. UI ใหม่ผ่าน design system (`docs/DESIGN.md` — component/token/ความกว้าง/สถานะ) ไม่ไล่ทาสีทีละหน้า · ห้าม redesign หน้าที่ยังไม่มีงาน functional ไปแตะ
9. UI ที่มีหลายทาง → หน้าลอง `/proto` เทียบ 2-4 ทางให้เบสเลือกก่อนแตะของจริง · เคาะแล้วค่อยลง presentation (ไม่แตะ query/mutation/permission/status/schema ถ้าใบงานไม่สั่ง) · เสนอทางเดียว + รื้อก่อนตกลงทิศ = ถูกปฏิเสธมาแล้ว 2 ครั้ง
10. schema เพิ่มแบบ additive · ห้าม apply/reset ฐาน shared/remote โดยไม่ระบุ target + backup แยก

## 🔥 เปิดอยู่ตอนนี้ (เรียงตามลำดับที่ควรทำ)

### A. หน้าใบสั่งผลิต `/production/[id]` — ถอดออกแล้ว เริ่มใหม่จากศูนย์ (เบสสั่ง 2026-09-02)
- [x] ถอด route + UI ใบผลิตทั้ง legacy และ V2 (09-02 ดึก · 32 ไฟล์: detail-screen · control-record · route-rail · now-card · steps-list · step-navigator · mockup-tab · design-card · garment-pick · step dialogs · v2-control-record/actions · presenter · lib production-control/detail-tabs · material-usage · station-garment-preview + test) · ด่าน verify ของหน้านั้นถอดตาม · **ลิงก์ที่ชี้ `/production/[id]` (แถวในหน้าการผลิต · My Tasks · ออเดอร์ · แจ้งเตือน) ยังชี้ที่เดิม = ไม่พบหน้า จนกว่าหน้าใหม่จะมา** (เบสยอมพังชั่วคราวเหมือน §A2/§A3)
- เก็บไว้ให้หน้าใหม่ต่อ: server ทั้งหมด (`production.*` `manufacturing.*` · `lib/production-step-actions` `production-problem` ที่ router ใช้) · `create-production-dialog` (หน้าการผลิตใช้เปิดใบ) · `goods-receipt-dialog` (ออเดอร์ใช้)
- [x] เบสตั้งโจทย์ (09-02): **"เห็นภาพรวมการผลิตออเดอร์นี้ทั้งหมด + ทำงานได้อย่างมีประสิทธิภาพ มีมาตรฐาน"**
- [x] หน้าลอง `/proto/work-order` — **เบสเคาะ D “แท็บ + 2 คอลัมน์” (09-03 “เอาแบบนี้เลย”)**
- [x] **ลงของจริง `/production/[id]` แบบ D (09-03)** — `work-order-page.tsx`: หัวใบตัวเลข 4 ช่อง + การ์ดปัญหา (ทุกแท็บ) → แท็บ ขั้นงาน (รายการขั้นซ้าย / ขั้นที่เลือก + โซนลงมือขวา) · ทำอะไร (สินค้า/ไซซ์ / ลาย+ม็อกอัพ) · ข้อมูลใบ (ออเดอร์ / เสื้อ-วัตถุดิบ + ร้านนอก) · ประวัติ (เวลาจริงต่อขั้น / ม็อกอัพทุกเวอร์ชัน) · server + สิทธิ์ + ปุ่มจาก `selectNowSteps`/`evaluateHeatPressGate` ชุดเดิม · dialog เดิม (update/outsource/qty/garment-pick/goods-receipt/material) คืนจาก git
- [ ] หนี้จากแบบ D: **ข้อกำหนดมาตรฐานต่อขั้น** ตอนนี้เป็นตารางนิ่ง `lib/work-order-standards.ts` อ่านอย่างเดียว — จะให้ติ๊กได้/แก้ในสูตรขั้นงาน ต้องเพิ่ม schema (checklist ใน RoutingTemplate + ผลติ๊กต่อขั้น) ⚠️ ถามเบสก่อน · ประวัติละเอียด (ใครกดอะไร) ยังชี้ไปหน้าประวัติระบบ ไม่ได้ดึงมาแสดงในใบ · V2 `manufacturing.workOrder` ยังไม่ต่อ (ใช้ getById ชุดเดียว) · ทางเข้ารอบพิมพ์ DTF จากขั้นพิมพ์ยังไม่มี (บอกเลขรอบอย่างเดียว)
- [ ] ได้โจทย์ → หน้าลอง `/proto` 2-4 ทาง → เบสเลือก → ลงของจริง (presentation เท่านั้น)

### A2. จอสถานี `/factory/station` — ถอดออกแล้ว เริ่มใหม่จากศูนย์ (เบสสั่ง 2026-09-02)
- [x] ถอด route + UI ทั้งชุด (legacy + V2) + ด่าน verify ของจอเดิม (branch `remove/factory-station-2026-09-02`) · ทางเข้าจากเมนูข้าง/เมนูผลิต/My Tasks/ใบผลิต/แจ้งเตือน ชี้ไปใบผลิต `/production/[id]` แทน · จอโรงงาน `/factory` (TV) ยังอยู่
- [x] หน้าลอง `/proto/station` (09-03 · branch `proto/station-2026-09-03` · โจทย์เบส: พนักงานไม่เก่งคอม ต้องดูแล้วรู้ว่าทำอะไร · หัวหน้าแก้ให้ได้ทุกสถานีตอนช่างกดมั่ว · ต่อเติมได้) — 3 ทางต่างกันที่ "ใครตัดสินว่าช่างทำใบไหนต่อ": **A หยิบงานเอง** (จอ `/station` 3 ชั้น เลือกสถานี → คิว 3 กลุ่ม → หน้าลงมือ · หัวหน้ามีแผงสถานี + ปุ่ม "แก้ให้" ทุกการ์ด) · **B หัวหน้าจ่ายงาน** (ช่างเห็นใบเดียว + ถัดไป · หัวหน้ามีกระดานจ่ายงานทุกสถานี) · **C ใบผลิตเป็นศูนย์กลาง** (ไม่มีจอใหม่ ใช้หน้าการผลิต A + ใบผลิต D บนจอทัช) · ทุกทางใช้หน้าลงมือเดียวกัน (ข้อกำหนดติ๊กปุ่มใหญ่ → ปุ่มเดียว · แจ้งปัญหากดเลือกเหตุ · แป้นยอด +10/+50/ครบ · ย้อนกลับเอง 5 นาที) + "หัวหน้าแก้ให้" 7 อย่าง (6 มี server แล้ว · ย้ายสถานี/ส่งร้านนอกแทนต้องทำเพิ่ม) · สถานี = work center ในหน้าตั้งค่า · สวิตช์ พนักงาน/หัวหน้า · คิวว่าง · ธีม — **รอเบสเคาะ**
- [x] **เบสเคาะ A (09-03) → ลงของจริง `/station`** (branch `proto/station-2026-09-03`): `src/app/station` (layout หลัง auth ไม่มี shell) · `lib/station-desk.ts` (+test 9: สถานี/การ์ด/คิว/สิทธิ์เห็นงาน) ต่อจาก `buildProductionBoard` · `factory.stationQueue` เพิ่ม mockupCover/ร้านนอก/รอบพิมพ์ (ยัง no-money) · `components/station/` (shell · ป้ายสถานี · การ์ดคิว · หน้าลงมือ + แจ้งปัญหากดเลือกเหตุ + dialog แก้ให้) · ใบผลิตแบบ D แยกเครื่องยนต์เป็น `work-order-controller` + `work-order-pieces` ให้จอสถานีใช้ปุ่ม/dialog ชุดเดียวกัน · ปุ่ม “จอสถานี” ที่หัวหน้าการผลิต · URL `?st= &s=job &job= &step= &fix=1` · จำสถานีต่อเครื่อง
- [ ] ค้างจากจอสถานี: **หน้าลงมือของ QC / แพ็กสุดท้าย** (ตอนนี้การ์ดพาไปหน้าออเดอร์ — controller QC/pack ของจอเดิมถอดไปแล้ว ต้องออกแบบใหม่บนโครงหน้าลงมือเดียวกัน) · **ย้อนขั้นที่ปิดแล้ว** (server ปฏิเสธ `COMPLETED` → ต้องมีคำสั่ง reopen ที่มี audit — ถามก่อน) · ข้อกำหนดติ๊กแล้วไม่บันทึก (schema §A) · DTF_PRINT ไม่มีปุ่ม (รอบพิมพ์ยังไม่มีทางเข้า §A3) · PIN ต่อคนบนจอร่วม (auth — ถามก่อน) · สถานีจาก work center ในหน้าตั้งค่าเมื่อ V2 cutover (§B) · เปิดกับข้อมูลจริงยังไม่ได้เห็น (ต้องล็อกอิน) — เบสซ้อม B1 บนจอนี้
- เก็บไว้ให้จอใหม่ต่อ (ยังไม่ลบ): server `factory.station*` / `manufacturing.station*` + read model · กติกาใน `docs/DESIGN.md` §Station work center · โหมด `surface="station"` ที่ฝังในหน้าใบผลิต/รอบพิมพ์ (ไม่มีใครเรียกแล้ว — ถอดตอนจอใหม่เคาะ) · `station-garment-preview` ยังใช้ในการ์ดลายของใบผลิต

### A4. หนึ่งโมดูล สองสายตา — หัวหน้าทำครบจากใบผลิต · ช่างเห็นโหมดหน้างาน (เบสเคาะ "เอาเลย" 2026-09-03)
- ที่มา: เบสถาม (09-03) "ใบผลิตกับจอสถานีซ้ำกันไหม หัวหน้าจะงงไหม" → รอบแรกเสนอ "แบ่งตามที่ยืน" (ถอดปุ่มลงมือจากใบผลิต) เบสท้วง **"หัวหน้าต้องดูจอ 2 ที่ ยุ่งยาก — รื้อโครงใหม่ อย่าขี้เกียจ"** → เสนอโครงใหม่ **แบ่งตามบทบาท ไม่ใช่สถานที่** (ตรงกับแนว Odoo Shop Floor / SAP POD: หัวหน้าทำครบจากใบสั่งผลิต · ช่างมีจอหน้างานของตน · ข้อมูลชุดเดียว) เบสตอบ "เอาเลย" · หน้าลอง `/proto/desk-station` (แบ่งตามที่ยืน) พับ
- โครง (ลง `SPEC.md` §UI แล้ว): **หัวหน้า/เบส** รู้จักแค่ "การผลิต": `/production` โต๊ะงาน → `/production/[id]` ใบผลิต D **ทำได้ครบในหน้าเดียว** (ดู · วางแผน · ลงมือ · "แก้ให้" · แจ้งปัญหาแบบกดเลือกเหตุ) + ทุกขั้นบอกว่าอยู่สถานีไหน · **ช่าง** รู้จักแค่ "งานของฉัน": ล็อกอิน/เปิดการผลิตแล้วเห็น **โหมดหน้างาน** `/production/floor` (จอสถานีเดิมย้ายมาเป็นโหมดของโมดูล) → หน้าลงมือของขั้นตน ปุ่มใหญ่ ไม่มีเงิน · เดินโรงงาน: หัวหน้าเปิดจอทัชเดียวกันเป็นแผงสถานี · `/station` เดิม redirect ไปที่ใหม่
- [x] ลงของจริง (09-03 · branch `proto/desk-station-2026-09-03`): dialog แก้ให้/แจ้งปัญหา ย้ายเป็น `components/production/step-command-dialogs.tsx` ใช้ร่วมใบผลิต+หน้างาน · ใบผลิต D ได้ปุ่ม "แก้ให้" + แจ้งปัญหากดเลือกเหตุ (แทนพิมพ์เอง) + ชิป "สถานี" ต่อขั้น (`lib/station-desk.stationForStep`) · route `/production/floor` (layout เต็มจอเดิม) · `/station` redirect พก query · ช่าง (`PRODUCTION_STAFF` ไม่มีสิทธิ์หัวหน้า · `lib/production-surface.isFloorWorker`) เปิด `/home` `/production` `/production/[id]` แล้วถูกพาไปโหมดหน้างาน/หน้าลงมือของใบนั้น · หน้างานเปิดใบจาก URL ได้โดยไม่ต้องรู้สถานี (หาให้จากคิว) · ปุ่มบนโต๊ะงาน "โหมดหน้างาน"
- [x] เบสดูแล้วถาม "ทำไมดูไม่ค่อยเปลี่ยน · ถ้าลองจัดใหม่ให้ดีกว่านี้ได้มั้ย" (09-03) → หน้าลอง `/proto/work-desk` (branch เดียวกัน): ปัจจุบัน (ใบผลิต D) / **A ใบผลิตเป็นตารางขั้นงาน กดแถวกางโซนลงมือ+แก้ให้ใต้แถว** (แท็บอ้างอิงใต้ตาราง) / **B โต๊ะงาน + แผงข้าง** (กดแถวโต๊ะงานเปิดแผงขวา: หัวใบย่อ → ปัญหา → ตารางขั้นงานย่อ กางแล้วทำได้ · ไม่ต้องออกจากหน้าการผลิต) · สวิตช์หัวหน้า/ช่าง · ธีม — **เบสตอบ "แบบเดิมดีละ" (09-03) = คงใบผลิต D** · แล้วชี้ต่อ "UI แจ้งเตือนดูโง่ไป และเป็นทั้งเว็บ ปัญหาเดิม" → ดู §E2 ข้อ Alert
- [ ] ค้าง: เบสซ้อมจริงทั้ง 2 บทบาท (บัญชีหัวหน้า → ใบผลิต กด แก้ให้/แจ้งปัญหา · บัญชีช่าง → ล็อกอินตกหน้างาน) · หนี้จอหน้างานเดิมยังอยู่ §A2 (QC/แพ็ก · reopen · ติ๊กไม่บันทึก · DTF ไม่มีปุ่ม · PIN ต่อคนบนจอร่วม) · ทางกลับจากหน้างานไป My Tasks/แจ้งเตือนของช่างยังไม่มี (ช่างอยู่ในหน้างานอย่างเดียว — ถามเบสว่าพอไหม)

### A5. กระดาษเป็นหลัก — ระบบจดแค่ 3 จุด (เบสเคาะ "ก" 2026-09-05 · รอเคาะหน้าลอง)
- ที่มา: เบสถาม (09-05) **"ระบบหน้าจอกดติ๊กทีละขั้นดูยุ่งยาก จำเป็นจริงไหม ปกติใช้กระดาษใบงาน · Printify/Printful ทำยังไง"** → ค้นคว้า (bestos `records/projects/anajak-erp/research-paper-first-vs-step-ticks-2026-09-05.md`): POD ใหญ่มีสถานะ 4-5 อัน สแกนเฉพาะจุดที่ "การจด" ทำงานอย่างอื่นให้ · ร้านสกรีน B2B ใช้กระดาษ+QR สถานะระดับใบงาน 7-10 อัน · ของเราบังคับกด ~14-16 ครั้งต่อใบ และใบสั่งงานกระดาษ `/print/job-ticket` มีอยู่แล้ว (ช่างเซ็นกระดาษ **และ** กดจอ = จด 2 ที่)
- ข้อเสนอ ก (เบสเคาะ): กระดาษเป็นหลัก · ระบบจดบังคับแค่ **(1) พิมพ์ DTF เสร็จ = ผ่านเองจากรอบพิมพ์ (2) ผล QC ดี/เสีย (3) แพ็กเสร็จ** + ร้านนอก ส่งไป/รับกลับ · เตรียมงาน/รีดร้อน ไม่บังคับ ระบบถือว่าผ่านเมื่อจุดถัดไปจด · ใบกระดาษพิมพ์จากใบผลิต (QR เปิดใบผลิต · ช่องติ๊กข้อกำหนด · ยอดดี/เสีย · ลงชื่อ)
- [x] หน้าลอง `/proto/paper-first`: ปัจจุบัน / **A จอจด 3 จุด** / B หัวหน้ากรอกจากกระดาษ · สวิตช์ มีร้านนอก/ไม่มี · หัวหน้า/ช่าง · ธีม — **เบสเคาะ A (09-05) + สั่งตรวจ UX อีกรอบ "ให้เข้ากับการใช้กระดาษ"**
- [x] รีวิวรอบ 2 (ux-review + impeccable critique 2 sub-agent · detector 0 finding) → ปรับหน้าลอง A 8 จุด: **P1** ปัญหา/สถานะติด/คนทำ โชว์ทุกขั้น (โหมดจดคุมแค่ยอด/เวลา) · **P2** แถวที่จดในระบบบนกระดาษไม่มีช่องยอด/เสร็จ/ลงชื่อ (ไม่เขียน 2 ที่ — ยอด QC อยู่ที่จอที่เดียว) · **P3** แถบ "ตอนนี้: หลังพิมพ์ DTF → ก่อน QC" (ช่วงงานอนุมาน) + สถานะ "ถือว่าผ่าน" สีเทาแยกจาก "ผ่านแล้ว" · **P4** ฉบับกระดาษ + วันพิมพ์ + ม็อกอัพท้ายใบ · QR ผูกฉบับ สแกนใบเก่าเตือน · **P5** ปุ่มพิมพ์/พิมพ์ใหม่อยู่ในใบผลิต (แถบบนรายการขั้น) · **P6** ถอดปุ่ม "จดเวลาเสร็จ (ไม่บังคับ)" → รายการในเมนูหัวหน้า · **P7** กระดาษแยกตาราง "ทำในโรงงาน (ตามลำดับ)" / "ของจากร้านนอก (เดินคู่ขนาน)" + แถว QC บอกเริ่มได้เมื่อของกลับครบ · **P8** ชิปโหมดไม่ strong · ช่องติ๊ก 14px · **แก้ใหญ่: "เบิกเสื้อจากสต๊อก" กลับมาเป็นจดในระบบ** เพราะ `services/garment-pick.ts` ตัดยอด Anajak Stock ให้ = การกดที่ทำงานให้ → A = 4 จุด (เบิกเสื้อ · DTF อัตโนมัติ · QC · แพ็ก) + ร้านนอก = 7 ครั้ง/ใบ (ทำเองทั้งใบ 3)
- [x] **เบส "ทำเลย" (09-06) → ลงของจริงแล้วโดยตั้งสมมติฐานกับ 3 คำถาม (แก้ทีหลังได้)**: (1) กระดาษ 1 ใบต่อใบผลิต (ปุ่มพิมพ์อยู่ในใบผลิต · ออเดอร์หลายใบผลิต QR ชี้หน้าออเดอร์) (2) QC เจอเสีย → ใช้กลไก QC/งานแก้ที่มีอยู่ ไม่เพิ่มแถวแก้บนกระดาษ (พิมพ์ใบใหม่เมื่อจำเป็น) (3) รีดร้อน **ไม่จด** — ถือว่าผ่านตอนส่งเข้า QC · หัวหน้าจดให้เองได้จากเมนู "เพิ่มเติม" (ไม่บังคับ)
- [ ] **คำถามที่ต้องเคาะก่อนลงของจริง** (จากรีวิว · ตอนนี้ใช้สมมติฐานข้างบนไปก่อน): (1) กระดาษ 1 ใบต่อใบผลิต หรือ 1 ใบต่อกอง (ออเดอร์ 3 สี ปักแขนไปทั้ง 240 — กองสีเทาที่ไม่ปักเดินด้วยใบไหน) (2) QC เจอเสีย 8 ตัวต้องรีดใหม่ — เขียนบนใบเดิม (ระบบไม่รู้) หรือระบบเปิดขั้นแก้แล้วพิมพ์ใบใหม่ (3) รีดร้อน "จด" หรือ "ไม่จด" ให้ขาด — ไม่เอา "ไม่บังคับ" เพราะข้อมูลครึ่งเดียวทำให้ TV/ตัวชี้วัดเชื่อไม่ได้
- [x] **ลงของจริงรอบ 1 (09-06 · branch `proto/paper-first-2026-09-05` · ไม่แตะ schema)**: `lib/work-order-record-mode.ts` (+test 12) กฎโหมดจด screen/paper/auto จากชนิดขั้น (เบิกเสื้อ/ตรวจรับ/ร้านนอก = จดในระบบ · DTF = ผ่านเองจากรอบพิมพ์ · ที่เหลือ = จดบนกระดาษ) · **server `production.sendToQc`** ปิดขั้นกระดาษเป็น "ถือว่าผ่าน" (marker `[ถือว่าผ่าน]` ใน notes · `production-problem` ไม่นับเป็นเหตุปัญหา) แล้ว finalize ใบทางเดิม · ขั้นที่จดในระบบ/ติดปัญหายังกั้นอยู่ · ใบผลิต: ชิปโหมด · ขั้นกระดาษไม่มีปุ่มหลัก (ประโยคบอกว่าจดบนใบสั่งงาน) · เมนูหัวหน้า "จดว่าเสร็จแล้ว (จากกระดาษ)" · แถบ "ใบสั่งงานกระดาษ" + ม็อกอัพอนุมัติ + ปุ่มพิมพ์ + "ตอนนี้: หลัง X → ก่อน Y" · Alert "ส่งเข้า QC" เมื่อขั้นที่จดในระบบครบ · Alert แดงเมื่อสแกน QR ใบเก่า (`?mockup=n` < อนุมัติล่าสุด) · หน้างาน: ขั้นกระดาษไม่มีปุ่มหลัก ชิปโหมด/ถือว่าผ่าน · **ใบสั่งงานกระดาษ** `/print/job-ticket/[orderId]?production=` : ตารางทำในโรงงาน (ช่องติ๊กข้อกำหนดทุกขั้น · แถวจดในระบบไม่มีช่องยอด · DTF ไม่มีช่อง · แถว QC) + ตารางร้านนอก "เดินคู่ขนาน" (ส่ง/นัดรับ) · ท้ายใบ วันพิมพ์ + ม็อกอัพ · QR ชี้ใบผลิต + `?mockup=` · ด่าน verify-work-order-ui เพิ่มเป็น 26 ข้อ
- [ ] ค้างจากรอบ 1: **ระบบยังไม่จำว่าพิมพ์ใบไปเมื่อไร/ฉบับไหน** (ต้องมีตาราง/ฟิลด์ใหม่ = schema ⚠️ ถาม) · สแกน QR ที่สถานี QC ยังไปหน้าลงมือของสถานี ไม่ใช่หน้า QC ตรง ๆ · คิวสถานี "รีดร้อน" ยังโผล่ในโหมดหน้างาน (ดูได้ กดไม่ได้ — ถ้าเบสไม่อยากเห็นค่อยถอด) · MFG §C เวลาต่อขั้นของขั้นกระดาษจะเป็นเวลาที่ส่งเข้า QC ไม่ใช่เวลาจริง (ข้อแลกที่รับแล้ว) · verify:print (integration กับ DB) ต้องรันบนฐาน demo ก่อน merge
- ⚠️ ถามก่อน (schema): เก็บ "ฉบับกระดาษที่พิมพ์" ต่อใบผลิต (เลขฉบับ · เวลา · ม็อกอัพเวอร์ชัน) · สถานะ "ถือว่าผ่าน" (inferred) แยกจาก COMPLETED · "จุดที่ต้องจด" ควรเป็น flag ต่อขั้นใน `RoutingOperation` (สูตรขั้นงาน) ไม่ใช่ hardcode = งาน schema · กฎ "ขั้นที่ไม่จดถือว่าผ่านเมื่อจุดถัดไปจด" ต้องเข้า `transitionOrder`/step service ฝั่ง server · ตัวชี้วัด §C MFG เหลือระดับใบ (ข้อแลกที่เบสรับแล้ว)

### A3. หน้ารายการผลิต `/production` + รอบพิมพ์ + คลังฟิล์ม + คิวร้านนอก — ถอดออกแล้ว เริ่มใหม่จากศูนย์ (เบสสั่ง 2026-09-02)
- [x] ถอด route + UI ทั้งโมดูลรายการ (legacy + V2): `/production` · `/production/print-runs` · `/production/films` · `/outsource` + dialog เปิดใบผลิต + lib worklist/board + หน้าลอง proto ของหน้ารายการ 5 ชุด (production-list/row/groups/filter/canvas-filter) · เหลือใบผลิต `/production/[id]` กับจอโรงงาน `/factory`
- [x] เบสยอมพังชั่วคราว: เมนู "การผลิต" หายจากเมนูข้าง/แดชบอร์ด · **เปิดใบผลิตจากออเดอร์ไม่ได้** จนกว่าหน้าใหม่จะมา · ลิงก์ที่เคยชี้มา (My Tasks · ปุ่มกลับในใบผลิต · การ์ดผลิตในออเดอร์ · แจ้งเตือนร้านนอก) ชี้ไปหน้าออเดอร์/My Tasks แทน
- [x] หน้าลอง `/proto/production-module` — **เบสเคาะ A “โต๊ะงานหัวหน้า” (09-02)**: ตัวเลขใหญ่ 4 ช่องเป็นตัวกรอง · รายการเป็น `DataTable` 8 คอลัมน์ (ใบงาน · จำนวน · กำหนดส่ง · เส้นทางงาน · ตอนนี้อยู่ที่ · ร้านนอก · ผู้รับผิดชอบ · ลูกศรเปิดดู) · กองตามความรีบเป็นหัวกลุ่มในตารางเดียว · **ไม่มีปุ่มในแถว** แถวกดเปิดใบผลิตทั้งแถว · โหมดหน้างาน = โต๊ะเดียวกันย่อเหลือสถานีของฉัน การ์ดใหญ่ปุ่ม 56px
- [x] **ลงของจริง `/production` แบบ A (09-02)** — `production-desk-page` (ข้อมูล: `production.kanban` + `user.me` → `buildProductionBoard` สูตรเดิมที่คืนจาก git → `lib/production-desk` ใหม่ + test 7 ข้อ) · `production-desk-view` (ตัววาด props ล้วน: ตัวเลข 4 ช่อง · ชิปขั้นงาน · ตาราง 8 คอลัมน์ กองตามความรีบ · แถวกดเปิดใบผลิต) · ปุ่ม "เปิดใบผลิต" กรองไปกองรอเปิดใบ → แถวเปิด dialog สร้างใบ (ของเดิมคืนจาก git) · คืนเมนู/แดชบอร์ด/My Tasks/การ์ดออเดอร์/ปุ่มกลับในใบผลิต · URL: `?view=` `?station=` `?q=` `?create=`
- [ ] ค้างจากแบบ A: **โหมดหน้างาน** (จอทัช · การ์ดใหญ่ · ปุ่ม 56px — §A2) · มือถือยังเป็นตารางเลื่อนแนวนอน (ของเดิมมีการ์ดมือถือ — ทำเมื่อเบสยืนยันว่ามีคนใช้บนมือถือ) · รอบพิมพ์/คลังฟิล์มยังไม่มีทางเข้า (ยุบเป็นปุ่มที่สถานีพิมพ์ตามแบบ — รอโหมดหน้างาน) · V2 `controlList` ยังไม่ต่อ (ใช้ kanban ชุดเดียว)
- เก็บไว้ให้หน้าใหม่ต่อ (ยังไม่ลบ): server `production.kanban` · `manufacturing.controlList/workCenterLoad` · router print-run / film-stock / outsource · `lib/outsource-ui` · กติกาเดิมใน `SPEC.md` §UI และ `docs/DESIGN.md`

### B. Cutover Production V2 (PV2.8 / P5.6) — เสี่ยงสูง ทำเป็นขั้น เบสอนุมัติทุกขั้น
> โค้ด + migration + seed สูตรมาตรฐาน + หน้าตั้งค่า `/settings/routings` ครบแล้ว (`SPEC.md` §Production V2) · ของจริงบน Vercel ยังเป็น **legacy** (`PRODUCTION_V2_ENABLED=0`) · ฐาน demo (`npm run dev:demo`) เปิด V2 ไว้ซ้อม · ฐานจริงยังไม่ seed routing/work center
- [ ] B1 เบสซ้อมบนฐาน demo: เปิดใบจากสูตรมาตรฐาน → กดทีละขั้นในจอสถานี (**รอจอสถานีใหม่ §A2** — ของเดิมถอดแล้ว 09-02) (รวมส่งร้านนอก · QC fail/rework · แพ็กแยกไซซ์) → บอกว่าอะไรไม่ตรงหน้างาน
- [ ] B2 แก้ตามที่ซ้อมเจอ (เขียนใบงานย่อยใต้ข้อนี้)
- [ ] B3 seed routing + work center ลงฐานจริง (additive) → backup → เปิด flag บน production → ลบ legacy UI/writer เก่าตามสัญญา rollout window
- [ ] B4 หนี้ที่ V2 ยังขาด (Phase 2 parity — ปิดหลัง cutover): QC rework target work center · DTF partial/waste/reprint event · owner/plan/SLA/audit actor read model · supervisor material recovery · WIP ownership ของ QC/final pack

### C. MFG — ตัวชี้วัดฝ่ายผลิต (เบสสั่ง 2026-08-15) · อ่านอย่างเดียว · ไม่เพิ่ม schema · ไม่มีเงิน
> วัตถุดิบมีครบใน DB แล้ว: `ProductionStep.startedAt/completedAt` · `QcRecord.qtyGood/qtyDefect` · `QcDefect.reason/size/color/printLabel/photoUrls` · `Order.deadline/shippedAt`
- [ ] MFG1 หน้าเดียว 2 ตัวเลข: **ส่งตรงเวลา %** (ออเดอร์ที่ `shippedAt` ในงวด เทียบ `deadline` · ไม่มี deadline = ไม่นับ) + **ทำถูกครั้งแรก %** (`Σ qtyGood / (Σ qtyGood + Σ qtyDefect)`) · เลือกงวดเดือนไทยแบบ `/billing/tax`
- [ ] MFG2 สรุปของเสียตาม `QcDefect.reason` ในงวด แยกย่อย ไซส์/สี/ลาย + เปิดรูปที่ช่างถ่าย · ป้ายสาเหตุจาก `src/lib/qc.ts` ห้ามประกาศ map ใหม่
- [ ] MFG3 ด่าน: pure function แยกจาก router + unit test เคสขอบ (งวดว่าง / ไม่มี deadline / หารศูนย์ / ยังไม่ส่ง / ยกเลิก) · จอจริง 1440 + 390 ทั้งสองธีม · เพิ่มเกณฑ์เสร็จใน SPEC

### D. Gate ก่อนใช้จริง
- [ ] B6 นักบัญชีรีวิว template ใบกำกับ/CN/DN + เลขรัน จากเอกสารพิมพ์จริง
- [ ] B16 walkthrough ของจริงกับทีม + นักบัญชีดูเอกสารเงินพิมพ์จริง 1 รอบ
- [ ] console checklist Supabase/Vercel → `docs/deploy-checklist.md`

### E. Gate C — หลังใช้จริง ~1 เดือน (calibrate จากข้อมูลจริงก่อน build)
- [ ] C1 pricing engine ใบเสนอ (qty break × เทคนิค × ตำแหน่ง + ราคาต่อลูกค้า) — ต้องเป็น service เรียกซ้ำได้ เพราะเป็นฐานราคาออเดอร์เว็บใน P4 ด้วย
- [ ] C2 stale sweep (ใบเสนอ SENT / แบบรอลูกค้า / INQUIRY / outsource เลยกำหนด ค้างเกิน N วัน → กระดิ่ง · โครง cron + notification พร้อมแล้ว)
- [ ] C3 global search ⌘K ค้นเลขออเดอร์/ลูกค้าจริง (server search มีแล้ว เหลือต่อท่อ palette)
- [ ] C4 Owner Pulse drill-down (`/orders` รับ URL filter) + คอลัมน์/sort กำหนดส่ง
- [ ] C5 UX ops เก็บตก (ส่วนใหญ่ถูกทับด้วย Station V2 — ตรวจอีกทีหลัง cutover ว่าเหลืออะไร)
- [ ] C6 LINE OA notify (ระหว่างนี้ template ก๊อปส่ง)

### E2. ลำดับความสำคัญทางสายตา
- [ ] **โซนลงมือ (ActionZone) ปุ่มเยอะ อัดกัน** — เบสส่งรูป 09-03 ("UI CTA ดูเยอะ อัดกัน": ขั้นที่เลือกในใบผลิตมี 4 ปุ่มเท่ากัน + ปุ่มเทาที่กดไม่ได้ + คำอธิบายบีบซ้าย) → หน้าลอง `/proto/action-zone` (branch `proto/action-zone-2026-09-03`): ปัจจุบัน / **A หนึ่งปุ่มหลัก + เมนูรวม** (ประโยคสถานะบน · ปุ่มหลัก 1 · แจ้งปัญหาเบา · "เพิ่มเติม" = บันทึกรายละเอียด + แก้ให้ทั้งชุด · ไม่มีปุ่มที่กดไม่ได้) / **B ปุ่มหลักขึ้นหัวการ์ด** (ปุ่มน้ำเงินมุมขวาบน · ล่างเป็นแถบสถานะ + ลิงก์รองเบา) · 4 สถานะ (รอ/ทำ/ติด/ผ่าน) · หัวหน้า/ช่าง · ธีม — **เบสเคาะ A (09-03) → ลงของจริงแล้ว** (branch `feat/action-zone-a-2026-09-03`): `ui/action-zone.tsx` note บน + icon/tone + ช่อง `menu` · ชิ้นส่วนกลางใหม่ `ui/more-menu.tsx` · `fixCommands()` แยกจาก FixDialog ใช้ทั้งเมนู (ใบผลิต) และ dialog (หน้างาน) · ใบผลิต: ปุ่มหลัก 1 / ปลดปัญหา (แดง) · แจ้งปัญหา ghost · เพิ่มเติม = บันทึกรายละเอียด + แก้ให้ 6 รายการ · หน้างาน: ถอดปุ่มเทา "ลงมือไม่ได้ตอนนี้" + ประโยคสถานะมีไอคอน
- [x] **กล่องแจ้งเตือน (Alert) ทั้งเว็บ** — เบสทัก 09-03 (รูปการ์ดปัญหาในใบผลิตธีมมืด) "ดูโง่ไป และเป็นทั้งเว็บ ปัญหาเดิม": 49 ไฟล์ 72 จุด มีไอคอนแค่ 12 · meta เป็นบรรทัดจุด · ปุ่มยัดในข้อความ → หน้าลอง `/proto/alert` (ปัจจุบัน / **A ตราไอคอน + ชั้นข้อความ** บนพื้นสี / **B แถบสีข้าง พื้นเรียบ** · ทั้งคู่ไอคอนอัตโนมัติ + ช่อง meta เป็นชิป + ช่องปุ่ม · ตัวอย่าง 6 จุดจริง) — **เบสเคาะ B (09-03) → ลงของจริงแล้ว**: `ui/alert.tsx` แบบ B + prop `meta`/`action` · ย้าย 7 จุด (ProblemCard · ส่งเข้า QC · ข้อมูลค้าง โต๊ะงาน/หน้างาน · เครดิต 2 หน้า · แค็ตตาล็อก) · กติกาใน `docs/DESIGN.md` · ค้าง: จุดที่ยังใส่ `text-xs`/สีเองใน children (settings/stock · order-delivery blind ship · qc 7 จุด) ทำงานได้แต่ควรถอด class ทิ้งตอนแตะไฟล์นั้น — refactor ของเก่าให้ผ่านกฎ 3 ชั้น (เบสสั่ง 2026-09-02 "แก้ราก + refactor ด้วย")
> ราก: กฎมีแต่ "ห้าม" ไม่มี "ต้องเด่น" + ไม่มีชิ้นส่วนนำเสนอข้อมูล → ทั้งเว็บเป็นตัวเทาต่อจุด · แก้รากแล้ว: `docs/DESIGN.md` §ลำดับความสำคัญทางสายตา · primitive 5 ชิ้น (`Fact` `Metric` `InfoChip` `DueTag` `ActionZone`) · ด่าน `scripts/ui-hierarchy-ratchet.ts` ใน `verify:ui` (baseline ห้ามเพิ่ม) · AGENTS §วงจร 3 บังคับผ่าน impeccable
- [x] รอบ 1 (09-02): หน้าลอง `/proto/production-module` ทั้ง 3 ทาง · `production-steps-list` (ร้านนอก/กำหนดรับเป็นชิป) · แถวสินค้าตอนเปิดออเดอร์ (`product-table-row` `product-card-mobile`) · รายการสินค้าในใบงาน (`order-items-display` สเปกเสื้อเป็น FactList) · ใบผลิต `production-control-record` + `station-garment-preview` (ไซส์/สีเป็นชิป)
- [ ] รอบ 2 — ไฟล์ที่ยังมี `text-xs text-muted` เยอะสุด (ratchet baseline): `production-v2-control-record` 16 · `order-qc-section` 13 · `material-usage` 11 · `order-files-card` 8 · `billing/tax` 7 · `billing/page` 7 · `job-share-view` 7 · `order-billing-section` 6 · `manufacturing-factory-board` 6 — แตะไฟล์ไหนให้แปลง "ป้าย: ค่า" เป็น `Fact` · ตัวเลขนำเป็น `Metric` · กำหนดส่ง/กำหนดรับเป็น `DueTag` แล้ว `npx tsx scripts/ui-hierarchy-ratchet.ts --update`
- [ ] รอบ 3 — หน้าออเดอร์ `/orders/[id]` แท็บภาพรวม + แถบสถานะ (เบสเห็นบ่อยสุด) · ทำเป็นหน้าลองก่อนเพราะเคาะแบบ B ไปแล้ว 08-31
- ห้าม: โฟกัสด้วยสี (เบสตีกลับ 09-02) · primitive ใหม่ซ้ำหน้าที่ · ลด baseline ด้วยการลบข้อมูลออก

### F. หนี้ที่จดไว้ (ทำตอนแตะไฟล์นั้น หรือเปิดใบงานเมื่อถึงคิว — **อย่าแก้เงียบ**)
- **UI**: `/factory` สี alpha ดิบ 13 จุด (อ่านถูกบนจอจริง ไม่รีบ) · hairline alpha ค่าเดียว · ยังไม่ตรวจจอทัชโรงงานจริง (แสงสะท้อน เงาจางกว่า) · แถบ "ยังไม่อ่าน"/ราง `SegmentedControl` · ธีมมืดบันไดแคบกว่าสว่าง
- **pattern เก่า**: `/quotations/new` ยัง Card/form row รุ่นเก่า → migrate เป็น `Section`/`Field` โดยคง conversion logic · settings company/cost-rates/patterns/services/users ยังมี raw label · dashboard home ซ่อน `max-w-6xl` ใน className → เคาะ width role
- **โค้ด**: `OrderInfoEditDialog` / `OrderItemsEditor` เดิมไม่มี caller — รอเบสอนุญาตลบ · lint warning เก่า (react-hooks/no-img/unused) เก็บตอนแตะไฟล์ ห้ามเพิ่มใหม่
- **ข้อมูล/ธุรกิจ** (ต้องมีใบงาน + test): dropdown ลูกค้าใน orders/new + quotations/new ตัดที่ 100 ไม่มีค้นหา · inquiry ↔ ใบเสนอ ↔ ออเดอร์ยังไม่ผูกกัน + `convertToOrder` ได้ items โครงเปล่า (เคาะ data model — เกี่ยวกับออเดอร์เว็บ P4 โดยตรง) · `attachment.create` มี API ไม่มีปุ่ม (สลิปโอน `evidenceUrl`) · Delivery ไม่มี line items (แบ่งส่งหลายรอบบอกไม่ได้ว่ากล่องไหนมีอะไร — เคาะ schema ก่อน) · token อนุมัติหมดอายุ regenerate ไม่ได้ · `recordPayment` ไม่เตือนว่าใบอยู่บนใบวางบิลแล้ว

## P2 — ผลิต + สต๊อค (หลัง cutover V2)
per-item tracking + นับชิ้น + ของเสีย/reprint log (V2 มี quantity line/event แล้ว — ต่อยอด ไม่สร้างใหม่) · AP vendor + WHT ขาจ่าย 3% + 50ทวิ + ภงด.53 export (**ทบทวนขอบเขตกับเบสก่อนเริ่ม** — คนละเรื่องกับต้นทุนต่องาน) · จองสต๊อกลึกขึ้นกับ Anajak Stock · ใบแพ็ค + Delivery line items

## P3 — ฝั่งลูกค้า
Customer portal เต็ม (สถานะ/ประวัติ/เอกสาร/อนุมัติ/สั่งซ้ำ) · LINE OA notify + ทวงหนี้ + รูป WIP · CRM เต็ม (follow-up/RFM) · revision quota + คิดเงินเกินโควตา + lock หลังอนุมัติ · strike-off gate (ตัวอย่างจริง opt-in) · preflight ไฟล์ (DPI/พื้นโปร่ง) · analytics ลึก

## P4 — เชื่อมโลก + เว็บสกรีนเสื้อ (เบสย้ำ 2026-09-02)
- **Order-intake API** ให้เว็บสกรีน (project `anajak-print-web` ใน bestos) ส่งออเดอร์เข้ามาเป็น **ออเดอร์เดียวกับหน้าร้าน** — ทั้ง **ธรรมดา** (เสื้อสำเร็จ + ลาย/ตำแหน่งที่เลือก) และ **custom** (ไฟล์ลูกค้า · ตำแหน่ง/เทคนิค/ไซซ์เอง · ต้องผ่านขั้นอนุมัติแบบ) → ใช้ order type / garment source / design version / approval token / payment terms ที่มีอยู่
- **สิ่งที่ต้องเผื่อตั้งแต่ตอนนี้**: pricing engine (C1) เป็น service เรียกซ้ำได้ · ไฟล์ลูกค้าเข้า pipeline เดิม (`/upload` + design version) · **ไม่สร้างสถานะ/flow แยกสำหรับออเดอร์เว็บ** · MCP/API เคารพสิทธิ์ชุดเดียวกัน
- MCP server (มีแล้ว · ขยายตามสิทธิ์) · ฟอร์มเก็บไซซ์ลูกค้าองค์กร (เวอร์ชันเบา) · e-Tax provider / PEAK API / courier API เมื่อ volume ถึง

## จงใจไม่ทำ (อย่าหยิบกลับมา — เหตุผลใน plan.md)
GL/งบการเงิน · job costing/ต้นทุนต่อออเดอร์ · DTF auto-nesting (RIP ทำแล้ว) · in-app chat (ลูกค้าอยู่ LINE) · online designer เต็มรูป (เว็บสกรีน = เลือก/อัปโหลด + ดีไซเนอร์ช่วย) · ใบกำกับอย่างย่อ · time-clock/payroll (hr-platform-v2) · WMS/PR-PO-GRN (Anajak Stock) · mockup generator · CMMS เต็ม · anomaly detection · capacity planning เต็มรูป (ใช้ปฏิทินภาระงานเบา) · Block reuse/BOM เต็มรูป (ไม่มีบล็อกในบ้าน)

## ลองแล้วไม่เอา (อย่าเสนอซ้ำ)
- หน้าทำงานหลักรวมออเดอร์ + ผลิต (`/proto/work-board` A/B/C) → เบสเลือก "แบบปัจจุบันยังดีกว่า" (08-30) — คงสองหน้า `/orders` + `/production` (ต่อมา `/production` ถูกถอดทั้งหน้า 09-02 §A3)
- ผังโรงงานเป็นตัวกรอง `/production` (`production-canvas-filter`) → "เอาแบบเดิมก่อน" (09-02) · จอ canvas ทั้งโรงงาน (`factory-canvas`) เก็บเป็นวัตถุดิบอนาคต · หน้าลองของหน้ารายการผลิตทั้ง 5 ชุด (list/row/groups/filter/canvas-filter) ลบไปพร้อมหน้า 09-02 — ดูได้ใน git ก่อน commit นี้
- ใบสั่งผลิตแบบผังสายพาน/จอสถานี/แผงลงมือ 20+ แบบ (08-30 → 09-02) → ลบทั้งหมด เริ่มใหม่
- เมนูซ้ายมีสีหมวด → ถอยกลับ (08-31) · ตารางไม่มีกล่องครอบ → กลับคำ (08-26) · ราคาและเงื่อนไขแบ่ง 2 ฝั่ง · CTA ต่อท้ายหัวข้อ (08-04)
- Order Workbench / Command Center redesign ทั้งระบบทีเดียว (08-14, 08-29) → ปฏิเสธเพราะเสนอทางเดียว + รื้อก่อนตกลงทิศ

## ✅ เสร็จแล้ว (ดัชนี — ไม่ทำซ้ำ · รายละเอียด `git log` / `git show 61575af:ROADMAP.md`)
- 06-10 → 06-19 **P0** ฐานราก: auth + RBAC จริง · Float → Decimal · เลขเอกสารรันต่อเนื่อง · migrations · service layer · test แกน · `docs/ARCHITECTURE.md`
- 06-11 audit วงจรออเดอร์ 31 ข้อแก้ครบ · 06-12 **FLOW-REDESIGN** ผังใหม่ทั้งระบบ · แยกโมดูลผลิต · ตัด job costing
- 07-02 → 07-03 **Gate A** เงินห้ามผิด · **Gate B1-B15**: CN/DN ม.86/10 · VAT 7% · tax point งวดรับเงิน · QC bypass ปิด · รายงานภาษีขาย CSV · CRM · ถอดฟอร์มปลอม · เพดานสองขา · ON_HOLD · soft-delete · sidebar role · delivery state machine · ใบส่งร้านนอก LINE · CI + security headers + backup export
- 07-06 → 07-07 **PERM** สิทธิ์รายคน · 07-07 → 08-05 **UX0-UX3** ทางเดินงาน + design system (PageShell/DataTable/Field/ResponsiveList/verify:ui) + orders/new หลายรอบ
- 08-12 → 08-14 **UI V2 → UI หลัก** · ระบบสีใหม่ · Impeccable integrity refactor · ฟอร์มออเดอร์เดียวสร้าง + แก้ · แยกที่อยู่ผู้ติดต่อ/จัดส่ง
- 08-15 → 08-21 **Next.js 16.3** · PRODUCTION-UX2 family · MFG0 · ใบผลิต → Direction A (Control vs Station) · Station low-tech · ฐาน demo local
- 08-22 → 08-23 ม็อกอัพหลายรูป/บ้านเดียว · **PRODUCTION-V2 PV2.1-2.7** · Visual identity + Vercel panel system · Anajak Blue selection
- 08-25 → 08-28 **UI-2026** เฟส 1-11 (token ฐาน · ธีม · หัวตาราง · sidebar toggle) · production step flow / live signal
- 08-30 **NO-JOB-TITLE** (ลบคอลัมน์ถาวร) · หน้าใบงานหน้าตาใหม่ · 08-31 สีบอกหมวดแบบ B · `/production` แบบ C · แถบกรอง A · ภาพรวมออเดอร์ B
- 09-01 → 09-02 แถบกรองเส้นทางงาน D · คอลัมน์เส้นทางงาน C · **P5.0 สูตรขั้นงานมาตรฐาน (seed)** · **P5.5 หน้าตั้งค่า `/settings/routings`** · V2 เปิดบนฐาน demo · เก็บกวาด repo
